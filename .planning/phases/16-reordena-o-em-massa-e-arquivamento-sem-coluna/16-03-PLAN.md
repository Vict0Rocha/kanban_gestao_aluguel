---
phase: 16-reordena-o-em-massa-e-arquivamento-sem-coluna
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - supabase/verificacao_arquivamento_sem_coluna.sql
autonomous: false
requirements: [ARQCOL-01, ARQCOL-03]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Parte A do runbook foi executada contra o banco de produção real de um jeito que garante continuidade de sessão — Parte A inteira num único clique de Run, ou psql/Supabase CLI (D-19). Rodar bloco a bloco no SQL Editor do Studio não conta como ensaio"
    - "O BLOCO 3 pós-`rollback` confirma, via `information_schema.columns`, que `cards.column_id` voltou a `is_nullable = 'NO'` — se não voltou, o ensaio virou push e o plano para para reportar antes de seguir para o 16-04"
    - "As três provas centrais foram observadas de verdade contra o schema real, na mesma transação: a constraint aceitou `column_id = null` (Prova 2.1), o backfill zerou só o card arquivado sem tocar o ativo (Prova 2.2), e um card arquivado com `column_id` nulo sobreviveu à exclusão da coluna que ele apontava antes (Prova 2.3) — com o contraste (Prova 2.4) confirmando que, sem o fix, o mesmo cenário apaga o card"
    - "`cards_total`, `columns_total`, `cards_arquivados_total` e `cards_updated_at_max` depois do `rollback` são idênticos aos do BLOCO 1"
    - "O bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_arquivamento_sem_coluna.sql` está preenchido com os números reais observados, as mensagens literais das provas, e o caminho de execução usado (único Run vs. psql/CLI)"
  artifacts:
    - supabase/verificacao_arquivamento_sem_coluna.sql
  key_links:
    - "ensaio -> `checkpoint:decision` do plano 16-04: sem o resultado registrado aqui, aquele portão não tem base para decidir"
  prohibitions:
    - "MUST NOT seguir para o plano 16-04 se a Prova 2.3 (o fechamento real do risco de cascade, ARQCOL-03) não passou — uma migração que roda sem erro mas não fecha o risco de D-02 é pior do que não ter mexido em nada, porque cria falsa sensação de segurança"
---

<objective>
Ensaiar a migração `20260827000000_arquivamento_sem_coluna.sql` contra o banco de produção dentro de uma
transação revertida, e registrar o resultado real — as três metades da mudança observadas de verdade, não
presumidas — no próprio runbook.

Segue a mesma disciplina que a Fase 6.1 estabeleceu depois de um acidente real (D-19): rodar um ensaio bloco
a bloco, na mesma aba do SQL Editor, já transformou um ensaio em push real de produção numa fase anterior
deste projeto, só percebido comparando `updated_at_max` antes e depois.

Purpose: o `checkpoint:decision` do plano 16-04 só tem base se este ensaio existir e estiver registrado — e,
mais importante nesta fase específica, se a Prova 2.3 (o fechamento real do risco de cascade que motiva
D-01/D-02) passou de verdade contra o schema real, não só a sintaxe da DDL.
Output: `RESULTADO DO ENSAIO` preenchido em `supabase/verificacao_arquivamento_sem_coluna.sql`.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `16-01-PLAN.md`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-reordena-o-em-massa-e-arquivamento-sem-coluna/16-CONTEXT.md
@supabase/verificacao_arquivamento_sem_coluna.sql
@supabase/migrations/20260827000000_arquivamento_sem_coluna.sql
@supabase/verificacao_relaxar_exclusao_destrava.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rodar a Parte A contra produção e observar as três metades da mudança</name>
  <precondition>`supabase/migrations/20260827000000_arquivamento_sem_coluna.sql` e
  `supabase/verificacao_arquivamento_sem_coluna.sql` existem no repositório (plano 16-01), e
  `cards.column_id` ainda é `not null` no banco de produção.</precondition>
  <read_first>
    - `supabase/verificacao_arquivamento_sem_coluna.sql` — Parte A inteira, principalmente o aviso de
      pooling no cabeçalho e as Provas 2.1 a 2.4
    - `supabase/verificacao_relaxar_exclusao_destrava.sql` — seção `RESULTADO DO ENSAIO`, para ver o
      formato do registro que a Task 2 vai reproduzir
  </read_first>
  <what-built>
    O runbook de ensaio (`supabase/verificacao_arquivamento_sem_coluna.sql`, Parte A) e a migração que ele
    ensaia (`20260827000000_arquivamento_sem_coluna.sql`): `cards.column_id` relaxado para nullable, mais o
    backfill dos cards já arquivados.
  </what-built>
  <how-to-verify>
    **Antes de qualquer coisa — escolha o caminho de execução (D-19). Só existem dois aceitos:**

    - **(a)** Supabase Studio → SQL Editor, colando a **Parte A INTEIRA** (BLOCO 2 do `begin;` até o
      `rollback;`) num **único** clique de "Run". Nunca bloco a bloco.
    - **(b)** `psql` ou Supabase CLI apontando para o banco de produção.

    **Passo 1 — baseline (BLOCO 1, fora de transação).** Anotar `cards_total`, `columns_total`,
    `cards_arquivados_total`, `cards_arquivados_com_column_id_nao_nulo`, `cards_updated_at_max`.

    **Passo 2 — rodar o BLOCO 2 inteiro de uma vez.**

    **Passo 3 — ler cada prova e anotar o resultado real:**
    - 2.1 — a constraint aceitou `column_id = null` num card de teste depois do `alter column`. Se a
      exceção de falha apareceu em vez da mensagem `OK relaxado: ...`, **pare aqui e reporte**.
    - 2.2 — o backfill zerou `column_id` só do card de teste arquivado, nunca do card de teste ativo. Se
      qualquer um dos dois resultados errados apareceu, **pare aqui e reporte** — um backfill que zera card
      ativo é um bug de dado mais sério do que não ter relaxado a constraint.
    - 2.3 — **a prova mais importante deste ensaio:** a coluna de teste foi excluída e o card arquivado de
      teste (com `column_id` já nulo) sobreviveu — mensagem `OK fechado: ...`. Se o card sumiu, **pare aqui
      e reporte** — ARQCOL-03 não vai funcionar em produção, e a migração cria falsa sensação de segurança.
    - 2.4 — o bloco de contraste confirmou que, SEM `column_id` nulo, o mesmo cenário apaga o card em
      cascata (mensagem `OK contraste: ...`) — isso documenta que o risco de D-02 era real, não é uma
      regressão desta migração.
    - 2.5 — a contagem de `pg_policies` não mudou.

    **Passo 4 — rodar o BLOCO 3 (pós-`rollback`) e conferir a coisa mais importante do ensaio inteiro:**
    `information_schema.columns` confirma `is_nullable = 'NO'` para `cards.column_id` (voltou ao estado
    anterior), e os números do Passo 1 batem. Se `is_nullable` já estiver `'YES'` fora da transação, o
    `rollback` não pegou — o ensaio virou push. Nesse caso **não** siga para a Task 2 como se tivesse dado
    certo: registre o acidente com honestidade e reporte antes de continuar.
  </how-to-verify>
  <acceptance_criteria>
    - O caminho de execução usado está declarado: "(a) único Run" ou "(b) psql/CLI"
    - Os cinco números do baseline estão anotados
    - As Provas 2.1, 2.2, 2.3 e 2.4 foram observadas com sucesso — as mensagens `OK ...`, não nenhuma
      exceção de falha
    - Depois do `rollback`, `information_schema.columns` confirma `is_nullable = 'NO'` e os números batem
      com o baseline
  </acceptance_criteria>
  <reversibility rating="reversible">O ensaio inteiro vive dentro de `begin; ... rollback;` e não deixa
  rastro no banco — desde que o caminho de execução do D-19 seja respeitado. É exatamente essa condição que
  o Passo 4 verifica.</reversibility>
  <resume-signal>Responda com os resultados dos Passos 1 a 4, ou com o que travou.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar o resultado do ensaio dentro do próprio runbook</name>
  <precondition>A Task 1 foi executada e os resultados de todas as provas estão disponíveis na
  conversa.</precondition>
  <files>supabase/verificacao_arquivamento_sem_coluna.sql</files>
  <read_first>
    - `supabase/verificacao_relaxar_exclusao_destrava.sql` — a seção `RESULTADO DO ENSAIO`: contexto
      primeiro, depois os números, depois as lições. É esse o formato a repetir
    - `supabase/verificacao_arquivamento_sem_coluna.sql` — o cabeçalho `RESULTADO DO ENSAIO` vazio deixado
      pela Task 2 do plano 16-01
  </read_first>
  <action>
Preencher o bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_arquivamento_sem_coluna.sql` com o que a
Task 1 realmente observou. Escrever em comentário SQL, em português, contexto primeiro, números depois,
lições no fim.

Conteúdo obrigatório:
- a data do ensaio e qual dos dois caminhos de D-19 foi usado
- os cinco números do baseline e os mesmos depois do `rollback`, lado a lado
- o resultado literal das Provas 2.1, 2.2, 2.3 e 2.4 — as mensagens exatas observadas, não um resumo
  genérico "funcionou"
- a confirmação explícita de que `information_schema.columns` mostrou `is_nullable = 'NO'` depois do
  `rollback` — ou, se não mostrou, o registro honesto do que aconteceu
- qualquer divergência entre o que o runbook previa e o que o banco respondeu, inclusive correção feita na
  hora

Se alguma correção foi feita em `20260827000000_arquivamento_sem_coluna.sql` durante o ensaio, o ensaio
precisa ser refeito por inteiro com o arquivo corrigido antes de este bloco ser dado como fechado —
registrar quantas rodadas foram necessárias.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f=supabase/verificacao_arquivamento_sem_coluna.sql && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "cards_total" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qiE "psql|CLI|único Run|unico Run" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "relaxad" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "fechad" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "is_nullable" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | wc -l | awk '$1 > 12 {print "ENSAIO_REGISTRADO"; exit} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - A seção `RESULTADO DO ENSAIO` tem mais de 12 linhas, cita `cards_total`, e cita explicitamente o
      resultado das provas de constraint relaxada, backfill seletivo e fechamento do risco de cascade
    - O caminho de execução (único Run ou psql/CLI) está nomeado por escrito
    - Está escrito, numa frase direta, se `is_nullable` voltou a `'NO'` depois do `rollback`
    - Nenhum arquivo além de `supabase/verificacao_arquivamento_sem_coluna.sql` foi modificado por esta
      task
  </acceptance_criteria>
  <done>O runbook carrega o registro do ensaio real, com números, os resultados observados das quatro
  provas, caminho de execução e o veredito sobre o `rollback`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| operador → banco de produção | Este plano executa SQL contra o banco real. A única coisa que separa "ensaio" de "push" é a continuidade da sessão que envolve o `begin;`/`rollback;` |
| runbook → operador | O texto do runbook é a única instrução que o operador segue; uma instrução errada aqui já causou perda de controle uma vez (Fase 6.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-10 | Tampering | ensaio que commita DDL em produção (pool de conexões do Studio) | high | mitigate | D-19 aplicado literalmente: só dois caminhos aceitos; o Passo 4 checa `information_schema.columns` depois do `rollback` e manda parar se a constraint não voltou |
| T-16-11 | Tampering | cards/colunas de teste das Provas 2.1-2.4 sobrevivendo fora da transação | medium | mitigate | Toda gravação de teste vive dentro de `begin;...rollback;`; o Passo 4 confirma os cinco números idênticos ao baseline |
| T-16-12 | Repudiation | ensaio não registrado, decisão do 16-04 sem base | medium | mitigate | Task 2 grava números, mensagens e caminho de execução dentro do próprio runbook, versionado no git |
| T-16-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala nenhum pacote |
</threat_model>

<verification>
- Observação direta do resultado de cada prova contra o banco de produção, dentro de transação revertida
- Comparação numérica antes/depois do `rollback`
- Checagem de `information_schema.columns` depois do `rollback` — a prova de que o ensaio foi ensaio
- `grep`/`sed` sobre o bloco `RESULTADO DO ENSAIO` confirmando o registro completo
</verification>

<success_criteria>
- O ensaio rodou por um caminho que garante continuidade de sessão, e isso está escrito
- As três metades da mudança (constraint relaxada / backfill seletivo / risco de cascade fechado, com
  contraste) foram observadas funcionando
- Nada persistiu no banco: constraint e cinco números idênticos depois do `rollback`
- `supabase/verificacao_arquivamento_sem_coluna.sql` carrega o registro completo, commitado no git
</success_criteria>

<output>
Create `.planning/phases/16-reordena-o-em-massa-e-arquivamento-sem-coluna/16-03-SUMMARY.md` when done
</output>
