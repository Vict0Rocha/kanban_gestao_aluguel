---
phase: 19-filtro-suspenso-e-exporta-o-em-pdf-no-relat-rio-da-imobili-r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/kanban/actions.ts
  - web/src/lib/kanban/reconciliacao.ts
  - web/src/components/reports/filtro-reconciliacao.tsx
  - web/src/components/reports/dinheiro-imobiliaria-view.tsx
  - web/src/components/reports/reconciliacao-pdf.ts
autonomous: true
requirements: [FILTIMOB-01, FILTIMOB-02, FILTIMOB-03, FILTIMOB-04, PDFIMOB-01, PDFIMOB-02]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Abrir o painel 'Filtrar' em /relatorios/imobiliaria mostra 5 campos (Imóvel/Proprietário/Inquilino/ID do contrato/Período, D-03); digitar em qualquer um filtra a lista e os StatTiles imediatamente, sem botão de submit (D-01/D-02, FILTIMOB-01)"
    - "Filtrar por Inquilino de verdade estreita a lista — campo que hoje não é nem buscado pela Server Action (D-04, FILTIMOB-02)"
    - "Digitar um 'ID do contrato' que não bate exatamente com nenhum contrato (ex.: '12' quando só existe #120) não retorna nenhuma linha — comparação exata de inteiro, nunca substring, mesmo padrão de financeiro/page.tsx (FILTIMOB-03)"
    - "Mudar qualquer um dos 5 campos do filtro volta a paginação para a página 1; a paginação nunca reseta por causa de um render não relacionado ao filtro (FILTIMOB-04)"
    - "Clicar em 'Exportar PDF' baixa um PDF cujo cabeçalho lista os 5 filtros ativos ('Todos' quando vazio), um bloco com os mesmos 6 totais mostrados em tela pelos StatTile, e a lista completa (taxas+caução) na mesma ordem DESC já usada na tela (D-05, PDFIMOB-01)"
    - "O botão 'Exportar PDF' mostra 'Exportando...' e fica desabilitado durante a geração, mesmo padrão do Relatório Financeiro dedicado (D-06, PDFIMOB-02)"
    - "Nenhuma migração de banco foi necessária — cards.inquilino já existia, só passou a ser buscado por esta Server Action"
  artifacts:
    - web/src/lib/kanban/actions.ts
    - web/src/lib/kanban/reconciliacao.ts
    - web/src/components/reports/filtro-reconciliacao.tsx
    - web/src/components/reports/dinheiro-imobiliaria-view.tsx
    - web/src/components/reports/reconciliacao-pdf.ts
  key_links:
    - "buscarReconciliacaoAction's widened .select() -> TaxaImobiliariaRelatorio/CaucaoEventoRelatorio.cards.inquilino -> passaFiltroCardsReconciliacao -> linhas useMemo -> linhas filtradas em tela e no PDF"
    - "FiltroReconciliacao (campos/onChange) -> DinheiroImobiliariaView's filtro state -> resetKey pipe-joined -> usePagination -> itensDaPagina"
    - "handleExportarPDF -> exportarReconciliacaoPDF(linhas, totais, filtro, hojeISO) -> reconciliacao-pdf.ts -> doc.save(`dinheiro-imobiliaria-${hojeISO}.pdf`)"
  prohibitions:
    - "MUST NOT criar nenhum arquivo em supabase/migrations/ — fase zero-schema, cards.inquilino já existe (20260728000000_init_schema.sql:68)"
    - "MUST NOT reordenar `linhas` dentro de reconciliacao-pdf.ts — consumir exatamente a ordem DESC já produzida pela view (divergência deliberada de relatorio-financeiro-lista.tsx, documentada em dinheiro-imobiliaria-view.tsx:91-98)"
    - "MUST NOT importar TaxaOrigemBadge/CaucaoEventoLabel (ou qualquer componente de components/*) dentro de reconciliacao-pdf.ts — usar só o `tipoLabel` de string já computado na view"
    - "MUST NOT usar um `import` estático de topo de módulo de jspdf/jspdf-autotable em reconciliacao-pdf.ts — só `import()` dinâmico dentro da função exportada"
    - "MUST NOT derivar o `resetKey` de usePagination de taxas/caucaoEventos/linhas — só dos 5 campos de `filtro`"
---

<objective>
Fecha FILTIMOB-01..04 e PDFIMOB-01..02 (19-CONTEXT.md D-01..D-06): `/relatorios/imobiliaria` ("Dinheiro da
imobiliária") ganha um painel suspenso (Collapsible) com 5 campos ao vivo — Imóvel/Proprietário/Inquilino/ID
do contrato/Período, o Período relocado do campo solto que existe hoje — e um botão "Exportar PDF" espelhando
a estrutura do PDF já existente no Relatório Financeiro dedicado (Phase 10).

D-01 identifica esta composição (casca `Collapsible`/`CollapsibleTrigger`/`CollapsiblePanel` + comportamento
ao vivo sem botão "Consultar") como já shipada em produção em `relatorio-financeiro-dedicado.tsx`
(RELDED-05) — este plano é uma cópia mecânica dessa composição, não um desenho novo. D-04 amplia
`buscarReconciliacaoAction` para incluir `inquilino` no embed `cards(...)` de ambas as consultas (campo que já
existe em `cards`, só não era buscado por esta tela) — aditivo, sem migração.

Purpose: o usuário pediu explicitamente o mesmo filtro suspenso já usado em outras telas de relatório, e o
mesmo botão de exportação em PDF já usado no Relatório Financeiro, agora na tela de dinheiro da imobiliária.
Output: `buscarReconciliacaoAction`/`reconciliacao.ts` ampliados (D-04), `filtro-reconciliacao.tsx` novo (5
campos ao vivo), `dinheiro-imobiliaria-view.tsx` com o Collapsible + Exportar PDF, `reconciliacao-pdf.ts` novo
(mirror de `relatorio-financeiro-pdf.ts`).
</objective>

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-filtro-suspenso-e-exporta-o-em-pdf-no-relat-rio-da-imobili-r/19-CONTEXT.md
@.planning/phases/19-filtro-suspenso-e-exporta-o-em-pdf-no-relat-rio-da-imobili-r/19-RESEARCH.md
@.planning/phases/19-filtro-suspenso-e-exporta-o-em-pdf-no-relat-rio-da-imobili-r/19-PATTERNS.md
@web/src/lib/kanban/actions.ts
@web/src/lib/kanban/reconciliacao.ts
@web/src/components/reports/dinheiro-imobiliaria-view.tsx
@web/src/components/reports/relatorio-financeiro-dedicado.tsx
@web/src/components/reports/relatorio-financeiro-pdf.ts
@web/src/components/reports/filtro-relatorio-financeiro-live.tsx
@web/src/components/financeiro/filtro-parcelas.tsx
</context>

## Artifacts this phase produces

- **`buscarReconciliacaoAction`** (widened, `web/src/lib/kanban/actions.ts`) — `inquilino` adicionado aos dois
  embeds `cards(...)` (D-04). Auth, error handling e forma de retorno inalterados.
- **`TaxaImobiliariaRelatorio`/`CaucaoEventoRelatorio`** (widened) + **`FiltroReconciliacaoValores`**,
  **`filtroReconciliacaoVazio`**, **`passaFiltroTextoReconciliacao`**, **`passaFiltroIdReconciliacao`**,
  **`passaFiltroCardsReconciliacao`** (novos, `web/src/lib/kanban/reconciliacao.ts`).
- **`FiltroReconciliacao`** (novo, `web/src/components/reports/filtro-reconciliacao.tsx`) — 5 campos ao vivo,
  espelha `filtro-relatorio-financeiro-live.tsx`.
- **`DinheiroImobiliariaView`** (modificado) — ganha Collapsible shell, estado `filtro`/`aberto`/`exportando`/
  `erroExportacao`, `resetKey` composto, `LinhaLista.tipoLabel`, botão "Exportar PDF".
- **`exportarReconciliacaoPDF`** (novo, `web/src/components/reports/reconciliacao-pdf.ts`) — espelha
  `relatorio-financeiro-pdf.ts` bloco a bloco.

<planner_assumptions>
- **A-01 — prefixos de requisito `FILTIMOB`/`PDFIMOB`** (RESEARCH.md não propõe um prefixo): esta fase tem
  duas capacidades distintas (filtro + exportação PDF), mesmo formato de `CANDEST`+`PAGIN` na Phase 15 — dois
  prefixos curtos ligados a cada capacidade, em vez de um único prefixo genérico.
- **A-02 — 2 tasks (tracer + expansão), sem checkpoint dedicado**: Task 1 é o slice vertical do filtro (o
  usuário já consegue filtrar de ponta a ponta depois dela); Task 2 é o slice do PDF, que depende do
  Collapsible shell já existir (criado na Task 1) para adicionar o botão ao lado do trigger. `human_verify_mode:
  end-of-phase` (config.json) — os dois `<human-check>` ficam dentro do `<verify>` de cada task, nunca como
  `type="checkpoint:human-verify"` dedicado.
- **A-03 — nenhuma atualização em `docs/data-model.md`**: esta fase não introduz nenhuma decisão de schema
  nem de arquitetura nova — D-04 é uma ampliação aditiva de `.select()` sobre uma coluna já existente e
  documentada, e a composição Collapsible+filtro-ao-vivo já está documentada como padrão em produção desde a
  Phase 10.
- **A-04 — `erroExportacao` incluído mesmo sem citação explícita em D-06**: D-06 diz "mesmo padrão" do botão
  já existente em `relatorio-financeiro-dedicado.tsx`, cujo `handleExportarPDF` real inclui um estado de erro
  dispensável (`erroExportacao`) além de `exportando` — replicado aqui por completude/qualidade de produção,
  não é escopo novo além do que "mesmo padrão" já implica.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Filtro suspenso ao vivo ponta a ponta — Server Action, matchers, componente e Collapsible shell</name>

  <files>web/src/lib/kanban/actions.ts, web/src/lib/kanban/reconciliacao.ts, web/src/components/reports/filtro-reconciliacao.tsx, web/src/components/reports/dinheiro-imobiliaria-view.tsx</files>

  <read_first>
    - `web/src/lib/kanban/actions.ts` linhas 1966-2007 (`buscarReconciliacaoAction` inteira) — os dois
      `.select("id, data, valor, ..., cards(endereco, proprietario, numero)")` a ampliar (linhas 1986-1994),
      auth/error handling a preservar intocados
    - `web/src/lib/kanban/reconciliacao.ts` (arquivo inteiro, 99 linhas) — `TaxaImobiliariaRelatorio`/
      `CaucaoEventoRelatorio` (linhas 16-32) a ampliar, `passaFiltroPeriodoReconciliacao` (linhas 39-45,
      permanece intocada, ainda chamada separadamente sobre `taxa.data`/`evento.data`), comentário de módulo
      (linhas 3-14, "um arquivo por relatório") — as novas funções entram no mesmo arquivo, mesma convenção
    - `web/src/components/reports/filtro-relatorio-financeiro-live.tsx` (arquivo inteiro, 120 linhas) — o
      contrato `campos`/`onChange` + `atualizarCampo` a espelhar 1:1 (3 campos → 5), sem `Collapsible` próprio
    - `web/src/components/financeiro/filtro-parcelas.tsx` linhas 148-169 — o campo "ID do contrato"
      (`type="text" inputMode="numeric"`, nunca `type="number"`)
    - `web/src/components/reports/dinheiro-imobiliaria-view.tsx` (arquivo inteiro, 204 linhas) — `LinhaLista`
      (linhas 36-43), `periodo`/`totais`/`linhas`/`usePagination` (linhas 57-103) e o JSX retornado (linhas
      105-204), tudo a reestruturar nesta task
    - `web/src/components/reports/relatorio-financeiro-dedicado.tsx` linhas 126-185 — a composição
      `Collapsible`/`CollapsibleTrigger`/`CollapsiblePanel` já shipada (D-01) a copiar verbatim (só o botão
      "Exportar PDF" fica para a Task 2)
    - `web/src/components/financeiro/taxa-origem-label.tsx` (32 linhas) e
      `web/src/components/financeiro/caucao-evento-label.tsx` (41 linhas) — `TAXA_ORIGEM`/`CAUCAO_TIPO`, já
      exportados ao lado de `TaxaOrigemBadge`/`CaucaoEventoLabel`
    - `web/src/lib/kanban/search.ts` linhas 1-13 (`normalizeText`, único export reusado, nunca modificado)
    - `web/src/components/reports/reports-view.tsx` linha 133 — `contractsResetKey`, o precedente de
      `resetKey` pipe-joined (sem `Set`, sem `JSON.stringify`) a espelhar
  </read_first>

  <action>
1. Em `web/src/lib/kanban/actions.ts`, dentro de `buscarReconciliacaoAction`, ampliar os dois `.select()`
(linhas ~1986-1994) adicionando `, inquilino` ao embed `cards(endereco, proprietario, numero)` de ambas as
consultas (`taxas_imobiliaria` e `caucao_eventos`), virando `cards(endereco, proprietario, numero, inquilino)`
nos dois lugares. Nada mais na função muda — auth (`requireUser()`), tratamento de erro
(`erroDoBanco(...)`) e forma do retorno ficam byte a byte como estão (D-04).

2. Em `web/src/lib/kanban/reconciliacao.ts`: (a) adicionar `import { normalizeText } from "./search"` no topo;
(b) ampliar o embed `cards` de `TaxaImobiliariaRelatorio` e `CaucaoEventoRelatorio` (linhas 16-32) com
`inquilino: string | null` (nullable — `cards.inquilino` é `text`, sem `NOT NULL`,
`supabase/migrations/20260728000000_init_schema.sql:68`); (c) logo depois de
`passaFiltroPeriodoReconciliacao` (depois da linha 45), acrescentar: `export type FiltroReconciliacaoValores`
(5 campos string — `imovel`/`proprietario`/`inquilino`/`id`/`periodo`, D-03); `export function
filtroReconciliacaoVazio()` (os 5 campos vazios); `export function passaFiltroTextoReconciliacao(valor:
string, filtro: string): boolean` (via `normalizeText`, filtro vazio sempre passa, accent-insensitive); `export
function passaFiltroIdReconciliacao(numero: number, filtro: string): boolean` (`filtro.trim()` vazio sempre
passa; senão `Number.isInteger(Number(digitos))` vira o alvo, comparação exata `numero === alvo` — nunca
substring, mesmo padrão server-side de `financeiro/page.tsx:64-65,135-136`); `export function
passaFiltroCardsReconciliacao(cards, filtro): boolean` compondo os 4 checks derivados de `cards` — endereco/
proprietario/inquilino via `passaFiltroTextoReconciliacao` (cada um lido com `cards?.campo ?? ""`), numero via
`passaFiltroIdReconciliacao(cards?.numero ?? -1, filtro.id)`.

3. Criar `web/src/components/reports/filtro-reconciliacao.tsx` (novo, `"use client"`), espelhando
`filtro-relatorio-financeiro-live.tsx` 1:1: props `{ campos: FiltroReconciliacaoValores, onChange: (updater)
=> void }`, função interna `atualizarCampo(campo, valor)` chamando `onChange((atual) => ({ ...atual, [campo]:
valor }))`. Grid `grid-cols-[repeat(5,1fr)] gap-3` com 5 `<Input>` (ids `filtro-reconc-imovel`/`-proprietario`/
`-inquilino`/`-id`/`-periodo`): Imóvel/Proprietário/Inquilino como `type="text"` com placeholder descritivo, ID
do contrato como `type="text" inputMode="numeric"` placeholder "Ex: 12" (D-03, nunca `type="number"`), Período
como `type="month"` (comportamento idêntico ao campo solto de hoje). Botão "Limpar filtros" (`variant="ghost"`)
visível só quando `temFiltroPreenchido` (`Boolean` de qualquer um dos 5 campos `.trim()`), chamando
`onChange(() => filtroReconciliacaoVazio())`.

4. Em `web/src/components/reports/dinheiro-imobiliaria-view.tsx`:
   a. Imports: adicionar `Collapsible`/`CollapsiblePanel`/`CollapsibleTrigger` de `@/components/ui/collapsible`,
   `Button` de `@/components/ui/button`, `Filter`/`X` ao bloco `lucide-react` já existente, `FiltroReconciliacao`
   de `./filtro-reconciliacao`, e estender o import já existente de `@/lib/kanban/reconciliacao` com
   `filtroReconciliacaoVazio`, `passaFiltroCardsReconciliacao`, `type FiltroReconciliacaoValores`. Estender os
   imports já existentes de `taxa-origem-label`/`caucao-evento-label` com `TAXA_ORIGEM`/`CAUCAO_TIPO` (ao lado
   de `TaxaOrigemBadge`/`CaucaoEventoLabel`). Remover os imports agora não usados de `Input`/`Label` de
   `@/components/ui/input`/`@/components/ui/label` — o campo solto de Período sai desta view e nenhuma outra
   parte do arquivo usa esses dois componentes.
   b. `LinhaLista` (linhas 36-43): acrescentar `tipoLabel: string` ao lado de `tipo: React.ReactNode`, e
   `inquilino: string | null` ao embed `cards`.
   c. Trocar `const [periodo, setPeriodo] = React.useState(() => hojeISO.slice(0, 7))` por `const [filtro,
   setFiltro] = React.useState<FiltroReconciliacaoValores>(() => ({ ...filtroReconciliacaoVazio(), periodo:
   hojeISO.slice(0, 7) }))` (preserva o comportamento atual de Período pré-preenchido com o mês corrente) e
   acrescentar `const [aberto, setAberto] = React.useState(false)`.
   d. `totais` memo: trocar `periodo` por `filtro.periodo` na chamada de `calcularReconciliacao` e no array de
   dependências.
   e. `linhas` memo: encadear `.filter((taxa) => passaFiltroCardsReconciliacao(taxa.cards, filtro))` /
   `.filter((evento) => passaFiltroCardsReconciliacao(evento.cards, filtro))` logo depois dos filtros de
   período já existentes (`passaFiltroPeriodoReconciliacao(taxa.data, filtro.periodo)` — a chamada também troca
   `periodo` por `filtro.periodo`), acrescentar `tipoLabel: TAXA_ORIGEM[taxa.origem].label` /
   `tipoLabel: CAUCAO_TIPO[evento.tipo].label` a cada `LinhaLista` mapeada, e trocar o array de dependências do
   memo de `[taxas, caucaoEventos, periodo]` para `[taxas, caucaoEventos, filtro]`. O `.sort()` DESC existente
   (linhas 96-98) e o comentário que o justifica ficam exatamente como estão — nunca reordenar.
   f. Trocar `usePagination(linhas, periodo)` por `usePagination(linhas, resetKey)`, com `const resetKey =
   \`${filtro.imovel}|${filtro.proprietario}|${filtro.inquilino}|${filtro.id}|${filtro.periodo}\`` declarado
   logo antes (pipe-joined, mesmo formato de `reports-view.tsx:133` — nunca derivado de `taxas`/
   `caucaoEventos`/`linhas`).
   g. JSX retornado: envolver tudo que já é retornado num `<Collapsible open={aberto} onOpenChange={setAberto}>`.
   No header row existente (`flex flex-wrap items-center justify-between gap-4`), manter o `<div />` da
   esquerda como está e trocar o bloco da direita (Label+Input de Período) por um `<div className="flex items-
   center gap-2">` contendo só um `CollapsibleTrigger` (`Button variant="outline" size="sm"`, ícone `X`/`Filter`
   + texto "Fechar filtros"/"Filtrar" conforme `aberto` — cópia exata de
   `relatorio-financeiro-dedicado.tsx:134-145`). Logo depois do header row, acrescentar
   `<CollapsiblePanel><FiltroReconciliacao campos={filtro} onChange={setFiltro} /></CollapsiblePanel>`. O grid
   de `StatTile` e a seção da tabela (usando `itensDaPagina`/`pagina`/`totalPaginas`/`setPagina`) continuam
   estruturalmente como estão, só passam a ser filhos do `<Collapsible>` em vez do `<div>` externo antigo.
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && a=src/lib/kanban/actions.ts && r=src/lib/kanban/reconciliacao.ts && f=src/components/reports/filtro-reconciliacao.tsx && v=src/components/reports/dinheiro-imobiliaria-view.tsx && test "$(grep -c 'cards(endereco, proprietario, numero, inquilino)' "$a")" = "2" && grep -q "inquilino: string | null" "$r" && grep -q "export type FiltroReconciliacaoValores" "$r" && grep -q "export function filtroReconciliacaoVazio" "$r" && grep -q "export function passaFiltroTextoReconciliacao" "$r" && grep -q "export function passaFiltroIdReconciliacao" "$r" && grep -q "export function passaFiltroCardsReconciliacao" "$r" && grep -q "normalizeText" "$r" && test -f "$f" && grep -q "export function FiltroReconciliacao" "$f" && grep -q "filtro-reconc-inquilino" "$f" && grep -q "filtro-reconc-id" "$f" && grep -q 'inputMode="numeric"' "$f" && grep -q "FiltroReconciliacao" "$v" && grep -q "Collapsible" "$v" && grep -q "CollapsibleTrigger" "$v" && grep -q "CollapsiblePanel" "$v" && grep -q "React.useState<FiltroReconciliacaoValores>" "$v" && grep -q "const \[aberto, setAberto\] = React.useState(false)" "$v" && grep -q "passaFiltroCardsReconciliacao" "$v" && grep -q "tipoLabel" "$v" && grep -q "TAXA_ORIGEM\[taxa.origem\].label" "$v" && grep -q "CAUCAO_TIPO\[evento.tipo\].label" "$v" && grep -q "resetKey" "$v" && ! grep -q "dinheiro-imobiliaria-periodo" "$v" && ! grep -q '@/components/ui/input"' "$v" && ! grep -q '@/components/ui/label"' "$v" && test -z "$(git status --porcelain -- ../supabase/migrations 2>/dev/null)" && npx tsc --noEmit && npm run lint && npm run build && echo TASK1_TRACER_OK</automated>
    <human-check>
      <test>Em `/relatorios/imobiliaria` (local ou produção), abrir o painel "Filtrar" e testar os 5 campos:
      digitar um Imóvel/Proprietário/Inquilino parcial (com e sem acento) e confirmar que a lista e os
      StatTiles atualizam na hora; digitar um ID de contrato que exista de verdade (ex.: "12") e confirmar que
      só as linhas desse contrato aparecem; digitar um ID que não corresponda a nenhum contrato exato (ex.:
      "999" ou um prefixo de outro ID, como "1" quando só existe "#12") e confirmar que a lista fica vazia, não
      que ela ainda mostra "#12"/"#120"/etc. Trocar o Período e confirmar que continua funcionando como antes
      (dentro do painel agora). Clicar "Limpar filtros" e confirmar que os 5 campos voltam a vazio (Período
      volta ao mês corrente). Por fim, com mais de 12 resultados filtrados, ir para a página 2 e mudar um dos
      outros 4 campos — confirmar que a paginação volta para a página 1.</test>
      <expected>Os 5 campos filtram ao vivo sem nenhum botão de submit; ID do contrato é comparação exata
      (nunca substring); "Limpar filtros" restaura os 5 campos; mudar qualquer campo do filtro volta a
      paginação para a página 1.</expected>
      <why_human>O comportamento renderizado do filtro ao vivo e o reset de paginação só são observáveis
      interagindo com a tela real — grep/tsc confirmam a forma do código (resetKey composto, matcher exato),
      não o comportamento depois de uma interação real do usuário.</why_human>
    </human-check>
  </verify>

  <acceptance_criteria>
    - Os dois `.select()` de `buscarReconciliacaoAction` incluem `inquilino` no embed `cards(...)`; nada mais
      na função muda
    - `TaxaImobiliariaRelatorio`/`CaucaoEventoRelatorio` têm `cards.inquilino: string | null`
    - `FiltroReconciliacaoValores`/`filtroReconciliacaoVazio`/`passaFiltroTextoReconciliacao`/
      `passaFiltroIdReconciliacao`/`passaFiltroCardsReconciliacao` existem em `reconciliacao.ts`
    - `FiltroReconciliacao` (novo componente) tem 5 campos, ID do contrato com `inputMode="numeric"`
    - `dinheiro-imobiliaria-view.tsx` envolve o conteúdo num `Collapsible`, filtra `linhas` pelos 5 campos,
      compõe `resetKey` dos 5 campos de `filtro` (nunca de `taxas`/`caucaoEventos`/`linhas`), e `LinhaLista`
      ganha `tipoLabel`
    - Zero arquivo novo em `supabase/migrations/`
    - `npx tsc --noEmit`, `npm run lint` e `npm run build` (de dentro de `web/`) não reportam erro novo
  </acceptance_criteria>

  <reversibility rating="reversible">Ampliação aditiva de `.select()` sobre uma coluna já existente
  (`cards.inquilino`) + composição client-side de peças já em produção (Collapsible, filtro ao vivo). Reverter
  é restaurar os 4 arquivos tocados — nenhuma migração para desfazer, nenhum dado gravado.</reversibility>

  <done>Painel "Filtrar" com 5 campos (Imóvel/Proprietário/Inquilino/ID do contrato/Período) filtra a lista e
  os StatTiles ao vivo, sem botão de submit; ID do contrato usa comparação exata; mudar o filtro reseta a
  paginação para a página 1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Exportar PDF ponta a ponta — novo módulo espelhando o Relatório Financeiro</name>

  <files>web/src/components/reports/reconciliacao-pdf.ts, web/src/components/reports/dinheiro-imobiliaria-view.tsx</files>

  <read_first>
    - `web/src/components/reports/relatorio-financeiro-pdf.ts` (arquivo inteiro, 251 linhas) — módulo a
      espelhar bloco a bloco: imports (linhas 1-9), disciplina de import dinâmico (linhas 11-17, 66-68), cores
      (linhas 79-84), cabeçalho + tabela de filtros (linhas 95-137), tabela de resumo (linhas 139-160), lista
      (linhas 164-221, especialmente o comentário sobre `showHead: "everyPage"` vs. `headerRows`, linhas
      203-215), rodapé (linhas 223-246, especialmente `doc.getNumberOfPages()` sem `.internal`, linhas
      224-229), `doc.save(...)` (linha 250)
    - `web/src/components/reports/relatorio-financeiro-dedicado.tsx` linhas 59-124 (`handleExportarPDF`,
      estados `exportando`/`erroExportacao`, try/catch/finally) e linhas 146-181 (botão "Exportar PDF" +
      alerta dispensável de erro) — padrão completo a copiar (D-06/A-04)
    - `web/src/lib/kanban/reconciliacao.ts` linhas 47-99 (`ReconciliacaoTotais`, os 6 campos que o bloco de
      resumo do PDF precisa)
    - `web/src/components/reports/dinheiro-imobiliaria-view.tsx` (já modificado pela Task 1) — o
      `<CollapsibleTrigger>` dentro de `flex items-center gap-2` onde o botão "Exportar PDF" entra ao lado
  </read_first>

  <action>
1. Criar `web/src/components/reports/reconciliacao-pdf.ts` (novo, módulo puro — sem `"use client"`, zero
`import` estático de `jspdf`/`jspdf-autotable` no topo do arquivo), espelhando `relatorio-financeiro-pdf.ts`
bloco a bloco: import de `formatCurrency`/`formatDate`/`formatInstantDateTime` de `@/lib/kanban/format` e dos
tipos `FiltroReconciliacaoValores`/`ReconciliacaoTotais` de `@/lib/kanban/reconciliacao`; `mesFormatter`/
`periodoLabel` idênticos; `export async function exportarReconciliacaoPDF(linhas, totais, filtro, hojeISO):
Promise<void>` que só dentro da função faz `const { jsPDF } = await import("jspdf")` e `const { autoTable } =
await import("jspdf-autotable")`; mesmas constantes de cor (`foreground`/`muted`/`border`/`rowShade`, RGB
idêntico ao contrato de layout já em uso); cabeçalho "Dinheiro da imobiliária" + timestamp; tabela de filtros
com 5 linhas (Imóvel/Proprietário/Inquilino/ID do contrato/Período, cada um `filtro.campo.trim() || "Todos"`,
Período via `periodoLabel`/regex igual ao existente); tabela de totais com 6 linhas (Administração/Comissão 1º
aluguel/Total recebido/Caução recebida/devolvida/usada, de `totais`); tabela de lista com `head: ["Data",
"Contrato", "Tipo", "Valor", "Observação"]`, corpo de `linhas.map(...)` lendo `l.tipoLabel` (nunca `l.tipo`,
que é JSX) e `` `#${l.cards?.numero ?? 0} ${l.cards?.endereco ?? ""}` `` para Contrato, consumindo `linhas`
exatamente na ordem em que chega (nunca reordenar), `showHead: "everyPage"` explícito (nunca `headerRows`,
que não existe em `jspdf-autotable@5.0.8`); rodapé em todas as páginas via `doc.getNumberOfPages()` (nunca
`.internal.getNumberOfPages()`); `doc.save(\`dinheiro-imobiliaria-${hojeISO}.pdf\`)`. Nunca importar
`TaxaOrigemBadge`/`CaucaoEventoLabel` nem nenhum outro componente de `components/*` neste módulo.

2. Em `web/src/components/reports/dinheiro-imobiliaria-view.tsx`: adicionar `FileDown`/`AlertCircle` ao bloco
`lucide-react` já existente e `exportarReconciliacaoPDF` de `./reconciliacao-pdf`. Acrescentar `const
[exportando, setExportando] = React.useState(false)` e `const [erroExportacao, setErroExportacao] =
React.useState<string | null>(null)`. Acrescentar uma função `async function handleExportarPDF()` (espelha
`relatorio-financeiro-dedicado.tsx:109-124`): `setExportando(true)`, `setErroExportacao(null)`, `try { await
exportarReconciliacaoPDF(linhas, totais, filtro, hojeISO) } catch { setErroExportacao("Não foi possível
exportar o PDF. Tente novamente.") } finally { setExportando(false) }`. No JSX, acrescentar um `<Button
variant="default" onClick={handleExportarPDF} disabled={exportando}>` (ícone `FileDown` + texto `{exportando ?
"Exportando..." : "Exportar PDF"}`) dentro do mesmo `<div className="flex items-center gap-2">` que já contém o
`CollapsibleTrigger` (criado na Task 1), logo depois dele. Renderizar o alerta dispensável de erro (`role=
"alert" aria-live="assertive"`, ícone `AlertCircle`, mensagem, botão `X` de fechar chamando
`setErroExportacao(null)` — cópia exata de `relatorio-financeiro-dedicado.tsx:157-181`) logo depois do header
row, guardado por `erroExportacao ? (...) : null`.
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && p=src/components/reports/reconciliacao-pdf.ts && v=src/components/reports/dinheiro-imobiliaria-view.tsx && test -f "$p" && grep -q "export async function exportarReconciliacaoPDF" "$p" && grep -q 'await import("jspdf")' "$p" && grep -q 'await import("jspdf-autotable")' "$p" && ! grep -qE '^import .*"jspdf' "$p" && grep -q "doc.getNumberOfPages()" "$p" && grep -q 'showHead: "everyPage"' "$p" && grep -q 'dinheiro-imobiliaria-\${hojeISO}.pdf' "$p" && grep -q "tipoLabel" "$p" && ! grep -q "TaxaOrigemBadge" "$p" && ! grep -q "CaucaoEventoLabel" "$p" && grep -q "FileDown" "$v" && grep -q "handleExportarPDF" "$v" && grep -q "exportarReconciliacaoPDF" "$v" && grep -q "Exportando" "$v" && grep -q "Exportar PDF" "$v" && grep -q "erroExportacao" "$v" && npx tsc --noEmit && npm run lint && npm run build && echo TASK2_PDF_OK</automated>
    <human-check>
      <test>Em `/relatorios/imobiliaria` (local ou produção), aplicar uma combinação de filtros (ex.: um
      Imóvel + um Período específico) e clicar "Exportar PDF". Abrir o arquivo baixado
      (`dinheiro-imobiliaria-AAAA-MM-DD.pdf`) e conferir: cabeçalho "Dinheiro da imobiliária" com data/hora de
      geração; tabela de 5 filtros mostrando os valores aplicados ("Todos" nos campos vazios); tabela de 6
      totais batendo com os `StatTile` visíveis em tela no momento da exportação; lista com as mesmas linhas
      da tela, na mesma ordem (mais recente primeiro), coluna "Tipo" mostrando texto ("Administração"/
      "Comissão 1º aluguel"/"Recebida"/"Devolvida"/"Usada"), nunca `[object Object]` ou célula em branco.
      Repetir com o filtro totalmente vazio (todas as linhas) e depois com um filtro que não bate com nenhuma
      linha, conferindo a mensagem "Nenhuma taxa ou movimento de caução encontrado para os filtros
      aplicados." em vez de uma tabela vazia. Durante a geração, confirmar que o botão mostra "Exportando..."
      e fica desabilitado.</test>
      <expected>PDF gerado com a estrutura completa (filtros/totais/lista/rodapé), coluna "Tipo" sempre
      texto, nunca reordenado em relação à tela, "Exportando..." visível durante a geração.</expected>
      <why_human>A estrutura real do arquivo PDF gerado (layout, quebra de página, texto renderizado) só é
      verificável abrindo o arquivo — grep/tsc confirmam a forma do código-fonte (funções/imports corretos),
      não o conteúdo visual do PDF produzido.</why_human>
    </human-check>
  </verify>

  <acceptance_criteria>
    - `reconciliacao-pdf.ts` existe, exporta `exportarReconciliacaoPDF`, nunca tem `import` estático de
      `jspdf`/`jspdf-autotable` no topo do arquivo — só `import()` dinâmico dentro da função
    - PDF usa `doc.getNumberOfPages()` (nunca `.internal.getNumberOfPages()`) e `showHead: "everyPage"`
      (nunca `headerRows`)
    - Lista do PDF lê `tipoLabel` (string), nunca `tipo` (JSX); nunca importa `TaxaOrigemBadge`/
      `CaucaoEventoLabel`
    - Botão "Exportar PDF" chama `handleExportarPDF`, mostra "Exportando..." e fica `disabled` durante a
      geração; erro de geração mostra alerta dispensável, não quebra a tela
    - `npx tsc --noEmit`, `npm run lint` e `npm run build` (de dentro de `web/`) não reportam erro novo
  </acceptance_criteria>

  <reversibility rating="reversible">Módulo novo, client-side, sem escrita no banco e sem chamada de rede —
  reverter é apagar `reconciliacao-pdf.ts` e restaurar `dinheiro-imobiliaria-view.tsx` ao estado da Task
  1.</reversibility>

  <done>Botão "Exportar PDF" baixa um PDF com cabeçalho de filtros (5 linhas), totais (6 linhas) e a lista
  completa na mesma ordem da tela; estado "Exportando..." visível durante a geração.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Server Action (`buscarReconciliacaoAction`, `.select()` ampliado) → `DinheiroImobiliariaView` (client) | `inquilino` cruza essa fronteira pela primeira vez nesta tela — RLS de `cards`/`taxas_imobiliaria`/`caucao_eventos` é só row-level (`is_team_member()`), sem policy coluna-a-coluna |
| 5 campos do filtro (`<Input>` do usuário) → matchers em memória (`reconciliacao.ts`) | nenhum campo sai do navegador — nunca vira parte de uma query SQL, sempre comparado em memória contra dado já carregado pela sessão |
| `linhas`/`totais`/`filtro` (já em memória, client-side) → `reconciliacao-pdf.ts` (jsPDF, client-side) | o PDF só usa dado que a sessão já recebeu do servidor e já está renderizado na tela — gerado inteiramente no navegador, nunca reenviado ao servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-19-01 | Information Disclosure | `.select()` ampliado de `buscarReconciliacaoAction` (adiciona `cards.inquilino`) | low | accept | RLS em `cards`/`taxas_imobiliaria`/`caucao_eventos` é só row-level (`is_team_member()`), sem policy coluna-a-coluna em nenhum dos 8 arquivos de migração — `inquilino` já era legível por qualquer sessão que já lê a linha inteira (mesma linha de `endereco`/`proprietario`/`numero`, já buscados hoje). Confirmado contra `supabase/migrations/20260728000000_init_schema.sql:68` (`inquilino text`, nullable) e `20260811000000_security_hardening.sql:100-114` (CHECK de tamanho, não RLS) |
| T-19-02 | Denial of Service (client-side) | campo "ID do contrato" (`passaFiltroIdReconciliacao`) | low | mitigate | `Number.isInteger(Number(...))` antes de qualquer comparação numérica — entrada não-numérica/malformada é ignorada (nunca filtra, nunca lança exceção), mesmo padrão do precedente server-side em `financeiro/page.tsx:64-65` |
| T-19-03 | Information Disclosure | leitura de `cards.inquilino` (nullable) sem guarda | low | mitigate | toda leitura passa por `cards?.inquilino ?? ""` antes de normalizar/comparar/renderizar — um contrato sem `inquilino` nunca lança exceção nem deixa `undefined` vazar para o matcher ou para o PDF |
| T-19-04 | Tampering | `linha.tipo` (`React.ReactNode`) passado para `jsPDF`/`autoTable` | low | mitigate | `LinhaLista` ganha um campo paralelo `tipoLabel: string` (`TAXA_ORIGEM[...].label`/`CAUCAO_TIPO[...].label`); `reconciliacao-pdf.ts` só lê `tipoLabel`, nunca `tipo` |
| T-19-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | zero pacote novo instalado — `jspdf`/`jspdf-autotable` já são dependências de produção desde a Phase 10, reusadas sem alteração; Package Legitimacy Audit não aplicável (confirmado em 19-RESEARCH.md) |
</threat_model>

<verification>
1. `grep`/`npx tsc --noEmit`/`npm run lint`/`npm run build` (ambas as tasks) confirmam a forma do código: os
   dois `.select()` ampliados, os 5 tipos/funções novos em `reconciliacao.ts`, o componente `FiltroReconciliacao`
   com 5 campos, o Collapsible shell + `resetKey` composto em `dinheiro-imobiliaria-view.tsx`, e o módulo
   `reconciliacao-pdf.ts` com a disciplina de import dinâmico/`getNumberOfPages()`/`showHead` corretas — além
   de confirmar zero arquivo novo em `supabase/migrations/`
2. `<human-check>` da Task 1 cobre o comportamento renderizado do filtro ao vivo (5 campos, ID exato) e o
   reset de paginação; `<human-check>` da Task 2 cobre a estrutura real do PDF gerado (cabeçalho/totais/lista/
   rodapé) e o estado "Exportando..." — nenhum dos dois é verificável só por grep/tsc
</verification>

<success_criteria>
- Painel suspenso com 5 campos (Imóvel/Proprietário/Inquilino/ID do contrato/Período) filtra
  `/relatorios/imobiliaria` ao vivo, sem botão de submit (D-01/D-02/D-03, FILTIMOB-01)
- `buscarReconciliacaoAction` busca `inquilino`, tornando o campo filtrável de verdade (D-04, FILTIMOB-02)
- ID do contrato usa comparação exata de inteiro, nunca substring (FILTIMOB-03)
- Mudar qualquer campo do filtro reseta a paginação para a página 1; ações não relacionadas não resetam
  (FILTIMOB-04)
- Botão "Exportar PDF" gera um PDF espelhando a estrutura do Relatório Financeiro — filtros/totais/lista/
  rodapé — com o estado "Exportando..." durante a geração (D-05/D-06, PDFIMOB-01, PDFIMOB-02)
- Zero migração de banco — `cards.inquilino` já existia, só passou a ser buscado por esta Server Action
</success_criteria>

<output>
Create `.planning/phases/19-filtro-suspenso-e-exporta-o-em-pdf-no-relat-rio-da-imobili-r/19-01-SUMMARY.md` when done
</output>