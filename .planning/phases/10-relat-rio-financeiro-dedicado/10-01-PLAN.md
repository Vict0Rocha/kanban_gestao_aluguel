---
phase: 10-relat-rio-financeiro-dedicado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/kanban/relatorio-financeiro.ts
  - web/src/lib/kanban/actions.ts
  - web/src/components/reports/reports-view.tsx
  - web/src/components/financeiro/parcela-situacao-badge.tsx
  - web/src/app/(app)/relatorios/financeiro/page.tsx
  - web/src/components/reports/relatorio-financeiro-dedicado.tsx
  - web/src/components/reports/filtro-relatorio-financeiro-live.tsx
  - web/src/components/reports/relatorio-financeiro-lista.tsx
  - web/src/components/reports/filtro-relatorio-financeiro.tsx
autonomous: true
requirements: [RELDED-01, RELDED-02, RELDED-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "RELDED-01: a rota `/relatorios/financeiro` existe (`web/src/app/(app)/relatorios/financeiro/page.tsx`) e carrega dados chamando `buscarParcelasRelatorioAction()` diretamente do Server Component (RESEARCH.md Pitfall #7) — nenhuma query nova/duplicada é escrita nesta página, `grep -c 'from(\"parcelas\")' page.tsx` = 0"
    - "RELDED-02/D-01: qualquer mudança em Imóvel/Proprietário/Período/Situação recalcula os 4 tiles E a lista imediatamente — `filtro` vive em `RelatorioFinanceiroDedicado`, atualizado a cada `onChange` de `FiltroRelatorioFinanceiroLive`, sem nenhum botão de submit/`onGerar` (diferente deliberadamente do padrão botão-só-dispara da Phase 8/D-04 de `08-CONTEXT.md`, que continua intocado em `/relatorios`)"
    - "RELDED-03/D-03: a lista abaixo dos tiles mostra uma linha por parcela (não por contrato), sempre em sincronia com `filtro` via o mesmo `useMemo` — reusando `passaFiltroTexto`/`passaFiltroPeriodo` recém-exportados de `relatorio-financeiro.ts` e `situacaoDaParcela` (`parcelas.ts`), nunca uma segunda implementação do casamento de filtro"
    - "D-05 (herdado da Phase 8, reconfirmado aqui): contrato arquivado/inativo continua contando nos 4 tiles e na lista — a página nova chama `buscarParcelasRelatorioAction()` verbatim, que já não filtra `arquivado_em`/`ativo`; nenhuma filtragem nova é introduzida por esta fase"
    - "Duas mudanças aditivas em componentes já em produção não alteram nenhum ponto de uso existente: `FilterChip` (`reports-view.tsx`) e `ParcelaSituacaoBadge` (`parcela-situacao-badge.tsx`) ganham `className?: string` opcional, mesclado por último via `cn()` — todo call site das Phases 5-8 (que não passa esse prop) renderiza exatamente como hoje"
    - "`ParcelaSituacaoBadge` renomeia a variável local desestruturada de `className` para `toneClassName` antes de aceitar o prop `className` (RESEARCH.md Pitfall #2) — sem essa renomeação o TypeScript recusaria a declaração duplicada"
    - "Cada parcela na lista tem uma chave estável em React: `ParcelaRelatorio` ganha o campo `id` (aditivo — a query de `buscarParcelasRelatorioAction` passa a selecionar `id` também), consumido só pela lista nova, sem afetar o consumidor existente (`RelatorioFinanceiro`, Phase 8, que não lê `id`)"
    - "Coluna Competência mostra 'Mês por extenso de Ano' capitalizado (ex.: 'Agosto de 2026') — não o formato 'mês/ano' em minúsculas usado nos diálogos (`registrar-pagamento-dialog.tsx` e afins), porque o UI-SPEC especifica esse formato para uma célula de tabela isolada, não para uma frase; `Intl.DateTimeFormat('pt-BR', {month:'long'})` devolve o mês em minúsculas por padrão em pt-BR, então a primeira letra é capitalizada manualmente antes de compor com o ano"
    - "Coluna Valor segue a mesma regra D-07 já usada pelos 4 tiles: pagas/conciliadas mostram `valorPago`; a_vencer/vencidas mostram `Math.max(valorDevido - valorPago, 0)` — nunca `valor_original` bruto, para nunca discordar visualmente dos totais dos tiles logo acima"
    - "Lista ordenada por `vencimento` ascendente (cronológico) — a mesma ordenação será reaplicada de forma idêntica na exportação PDF do plano 10-02, para as duas nunca discordarem de ordem"
    - "empty (zero-resultado, on-screen): filtro sem nenhuma parcela correspondente mostra 'Nenhuma parcela encontrada para os filtros aplicados.' no lugar da tabela — mesma string de `VAZIO_LABEL['sem-resultado-filtro']` (`parcelas-table.tsx`), não uma frase nova"
    - "empty (zero-resultado): a grade de 4 tiles nunca tem uma variante vazia própria — um filtro que não bate com nada mostra as 4 categorias legitimamente em '0'/'R$ 0,00', os mesmos números com que a lista vazia corresponde"
    - statement: "loading: não há estado de carregamento visível no primeiro carregamento da página — o fetch roda inteiramente no Server Component antes do primeiro paint (rota sem `searchParams`/segmento dinâmico), então o caminho comum não expõe nenhum estado intermediário no cliente"
      verification: backstop
    - "error: falha no fetch inicial do Server Component mostra 'Não foi possível carregar o relatório agora. Tente novamente.' no lugar de todo o conteúdo de dado (tiles+lista), mas o cabeçalho (link Relatórios, h1, subtítulo) continua renderizando normalmente"
    - statement: "long-text: valores de Imóvel/Proprietário na lista quebram linha livremente, sem classe `truncate`/reticências — mesma convenção já usada em `ParcelasTable`, não uma introduzida por esta fase"
      verification: backstop
    - "zero-one-many: chip 'Todas' de Situação corresponde a conjunto vazio (nenhum filtro) — mesma semântica `toggle()`/`STATUS_OPTIONS` já usada pela Phase 8"
    - "zero-one-many (on-screen): 0 linhas mostra o estado vazio; 1 linha renderiza normalmente sem variação gramatical especial (o contador '{filtradas} de {total} parcelas' já cobre a diferença); muitas linhas rolam numa tabela simples, sem paginação nova"
    - "overflow (on-screen): resultado filtrado com muitas parcelas é uma tabela simples com rolagem, sem paginação inventada — mesmo precedente de não-paginação já usado pela aba Financeiro na escala atual (~357 parcelas)"
    - statement: "overflow: a grade de 3 campos + linha de chips do painel de filtro em telas estreitas segue o mesmo comportamento responsivo já usado por `FiltroRelatorioFinanceiro` (Phase 8) — nenhum breakpoint novo introduzido"
      verification: backstop
    - "RELDED-01 completo: o botão 'Relatório financeiro' (ícone `ArrowUpRight`, `Button variant='outline' size='sm'`) aparece na página `/relatorios` atual, dentro da linha de cabeçalho de `FiltroRelatorioFinanceiro`, entre o bloco de heading e o próprio trigger 'Filtrar' — navega via `next/link` para `/relatorios/financeiro` na mesma aba (D-05); nenhuma linha pré-existente de `filtro-relatorio-financeiro.tsx` é removida (`git diff --unified=0` mostra 0 linhas `-`)"
  artifacts:
    - web/src/app/(app)/relatorios/financeiro/page.tsx
    - web/src/components/reports/relatorio-financeiro-dedicado.tsx
    - web/src/components/reports/filtro-relatorio-financeiro-live.tsx
    - web/src/components/reports/relatorio-financeiro-lista.tsx
  key_links:
    - "relatorios/financeiro/page.tsx (buscarParcelasRelatorioAction, reusada verbatim) -> RelatorioFinanceiroDedicado (props parcelas/hojeISO)"
    - "RelatorioFinanceiroDedicado (useMemo -> calcularRelatorioFinanceiro) -> grade de 4 StatTile, recalculada a cada onChange de filtro (D-01)"
    - "RelatorioFinanceiroDedicado (useMemo -> passaFiltroTexto/passaFiltroPeriodo/situacaoDaParcela) -> linhasFiltradas -> RelatorioFinanceiroLista (D-03)"
    - "FiltroRelatorioFinanceiroLive (onChange) -> RelatorioFinanceiroDedicado (setFiltro) -> os dois useMemo acima recalculam juntos, nunca um sem o outro"
    - "filtro-relatorio-financeiro.tsx (botão novo) -> next/link -> /relatorios/financeiro (RELDED-01)"
---

<objective>
Entregar a rota nova `/relatorios/financeiro` ponta a ponta: filtro ao vivo (D-01) recalculando os mesmos 4 tiles de categoria da Phase 8 e uma lista nova de parcelas (uma linha por parcela, D-03) — sem nenhum botão "aplicar", e com o botão de entrada em `/relatorios` levando até lá. A exportação em PDF (RELDED-04/05) fica para o plano 10-02, que expande esta mesma tela sem reescrever nada dela.

Este é o slice mais fino que atravessa toda a camada que a fase toca (rota nova -> componente cliente com estado de filtro -> agregação pura reusada -> painel de filtro sem submit -> lista nova): a Task 1 prova a arquitetura ponta a ponta com dado real (tiles + lista, ambos ao vivo); a Task 2 acrescenta o botão de entrada em `/relatorios`, sem tocar em nenhuma outra linha da página existente.

Purpose: RELDED-01, RELDED-02, RELDED-03 — o gestor filtra os contratos ao vivo numa página própria, sem abrir o painel suspenso da Phase 8 pra cada consulta.
Output: rota `web/src/app/(app)/relatorios/financeiro/page.tsx`, `RelatorioFinanceiroDedicado`/`FiltroRelatorioFinanceiroLive`/`RelatorioFinanceiroLista` (componentes novos), `passaFiltroTexto`/`passaFiltroPeriodo` exportados, `ParcelaRelatorio.id`, `className` aditivo em `FilterChip`/`ParcelaSituacaoBadge`, e o botão "Relatório financeiro" em `/relatorios`.

## Artifacts this phase produces

Lista canônica de todo símbolo criado ou alterado pela Phase 10 inteira. O plano 10-02 se refere a esta lista em vez de repeti-la.

**Arquivos novos**
- `web/src/app/(app)/relatorios/financeiro/page.tsx` (Task 1) — Server Component da rota nova
- `web/src/components/reports/relatorio-financeiro-dedicado.tsx` (Task 1, expandido no plano 10-02 Task 2) — orquestra `filtro`, os dois `useMemo` (tiles + lista), o `Collapsible` do painel e a linha de ações
- `web/src/components/reports/filtro-relatorio-financeiro-live.tsx` (Task 1) — painel de filtro sem botão de submit
- `web/src/components/reports/relatorio-financeiro-lista.tsx` (Task 1) — tabela de 6 colunas, uma linha por parcela
- `web/src/components/reports/relatorio-financeiro-pdf.ts` (plano 10-02 Task 1) — construtor de PDF, sem JSX, importa `jspdf`/`jspdf-autotable` dinamicamente

**Arquivos alterados**
- `web/src/lib/kanban/relatorio-financeiro.ts` (Task 1 — `export` em `passaFiltroTexto`/`passaFiltroPeriodo`; campo `id: string` novo em `ParcelaRelatorio`)
- `web/src/lib/kanban/actions.ts` (Task 1 — `buscarParcelasRelatorioAction` passa a selecionar `id` também; nenhuma outra mudança na função)
- `web/src/components/reports/reports-view.tsx` (Task 1 — `FilterChip` ganha `className?: string` opcional)
- `web/src/components/financeiro/parcela-situacao-badge.tsx` (Task 1 — `ParcelaSituacaoBadge` ganha `className?: string` opcional; variável local renomeada para `toneClassName`)
- `web/src/components/reports/filtro-relatorio-financeiro.tsx` (Task 2 — botão de entrada "Relatório financeiro" na linha de cabeçalho, nenhuma outra linha muda)
- `web/src/lib/kanban/format.ts` (plano 10-02 Task 1 — `formatInstantDateTime` novo, aditivo)

**Tipos/funções novas ou alteradas (`relatorio-financeiro.ts`)**
- `ParcelaRelatorio` ganha `id: string` (aditivo, primeiro campo)
- `passaFiltroTexto(valor, filtro): boolean` e `passaFiltroPeriodo(competencia, periodo): boolean` — mesmo corpo de hoje, só ganham `export`

**Componentes novos**
- `RelatorioFinanceiroDedicado({ parcelas, hojeISO })` — estado `filtro`/`aberto`; `useMemo` de `categorias` (via `calcularRelatorioFinanceiro`) e de `linhasFiltradas` (via `passaFiltroTexto`/`passaFiltroPeriodo`/`situacaoDaParcela`, ordenado por `vencimento`); renderiza a linha de ações (contador + trigger "Filtrar"), o painel de filtro, a grade de 4 `StatTile` e `RelatorioFinanceiroLista`
- `FiltroRelatorioFinanceiroLive({ campos, onChange })` — os mesmos 3 campos + chips de situação da Phase 8, cada `onChange` chama `onChange((atual) => ({...}))` diretamente, sem botão "Gerar relatório"
- `RelatorioFinanceiroLista({ linhas, hojeISO })` — tabela de 6 colunas (Imóvel/Proprietário/Competência/Vencimento/Situação/Valor), sem coluna de ID nem Ações

Nenhuma migração de banco é necessária: fase somente leitura sobre `parcelas`/`parcela_lancamentos`, ambas vivas desde a Phase 4. Nenhuma Server Action nova nesta fase — `buscarParcelasRelatorioAction` (Phase 8) é reusada verbatim, só ganha um campo a mais no `.select()`.
</objective>

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-relat-rio-financeiro-dedicado/10-CONTEXT.md
@.planning/phases/10-relat-rio-financeiro-dedicado/10-RESEARCH.md
@.planning/phases/10-relat-rio-financeiro-dedicado/10-PATTERNS.md
@.planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Rota nova, filtro ao vivo, tiles + lista ponta a ponta (RELDED-01 rota, RELDED-02, RELDED-03)</name>
  <files>web/src/lib/kanban/relatorio-financeiro.ts, web/src/lib/kanban/actions.ts, web/src/components/reports/reports-view.tsx, web/src/components/financeiro/parcela-situacao-badge.tsx, web/src/app/(app)/relatorios/financeiro/page.tsx, web/src/components/reports/relatorio-financeiro-dedicado.tsx, web/src/components/reports/filtro-relatorio-financeiro-live.tsx, web/src/components/reports/relatorio-financeiro-lista.tsx</files>
  <read_first>
    - `web/src/lib/kanban/relatorio-financeiro.ts` — inteiro (136 linhas): `ParcelaRelatorio`, `FiltroRelatorioValores`, `CategoriaRelatorio`, `SITUACAO_RELATORIO_ORDEM`, `filtroRelatorioVazio`, `passaFiltroTexto`/`passaFiltroPeriodo` (linhas 59/71, hoje sem `export`), `calcularRelatorioFinanceiro` — nada disso é reimplementado, só consumido/estendido
    - `web/src/lib/kanban/actions.ts:1297-1325` — `buscarParcelasRelatorioAction` completa: `requireUser()`, o `.select(...)` atual (sem `id`), o cast `(data ?? []) as unknown as ParcelaRelatorio[]`, `erroDoBanco`
    - `web/src/lib/kanban/parcelas.ts:380-413` — `situacaoDaParcela`, `somarLancamentos` (a lista nova reusa as duas para cada linha, mesma regra D-06/D-07 dos tiles)
    - `web/src/components/reports/relatorio-financeiro.tsx` — componente inteiro da Phase 8: o `useMemo` de `categorias` (linhas 43-49) é o padrão a adaptar removendo o gate de `aplicado`/clique; o mapa `ICONE_E_ROTULO` (linhas 17-23) e o grid de `StatTile` (linhas 93-111) são reusados quase verbatim
    - `web/src/components/reports/filtro-relatorio-financeiro.tsx` — componente inteiro da Phase 8: os 3 campos + chips de situação (linhas 92-158) são o ponto de partida visual/estrutural para `FiltroRelatorioFinanceiroLive`, mas SEM a linha de ação "Gerar relatório"/"Limpar filtros" (linhas 160-175) — essa versão nova não tem botão de submit
    - `web/src/components/reports/reports-view.tsx:37-61` — `FilterChip` e `toggle<T>` atuais (já exportados desde a Phase 8)
    - `web/src/components/financeiro/parcela-situacao-badge.tsx` — arquivo inteiro (56 linhas): note que `SITUACAO[situacao]` desestrutura uma chave literalmente chamada `className` (linha 48) — é essa colisão que exige renomear a variável local antes de aceitar o prop novo
    - `web/src/app/(app)/relatorios/page.tsx` — arquivo inteiro (36 linhas): padrão de Server Component que a rota nova espelha (fetch -> passa como prop, `hojeEmCuiaba()` para "hoje")
    - `web/src/components/financeiro/parcelas-table.tsx` — cabeçalho da tabela (linhas 195-207) e célula de Situação (linha 230) como referência de estrutura `Table`/`TableRow`/`TableCell`; `VAZIO_LABEL["sem-resultado-filtro"]` (linha 33) é a string exata a reusar no estado vazio
    - `web/src/components/financeiro/registrar-pagamento-dialog.tsx:20-32` — `mesFormatter`/o padrão de extrair `mesPorExtenso` de uma string "YYYY-MM-01" sem passar por `new Date(competencia)` direto (bug de fuso já documentado) — a lista nova reusa esse padrão, mas capitaliza o resultado (ver `<action>` item 8)
    - `web/src/lib/kanban/format.ts` — `formatCurrency`, `formatDate` (usados tal como estão, sem nova instância de `Intl`)
    - `web/src/components/ui/collapsible.tsx` — `Collapsible`/`CollapsibleTrigger`/`CollapsiblePanel`, API `open`/`onOpenChange`
    - `web/src/components/ui/button.tsx` — `Button` aceita `render` (base-ui), é assim que `CollapsibleTrigger` compõe com `Button` hoje
    - `.planning/phases/10-relat-rio-financeiro-dedicado/10-CONTEXT.md` — D-01 a D-05, "Claude's Discretion" (estratégia de dado ao vivo, painel fechado por padrão)
    - `.planning/phases/10-relat-rio-financeiro-dedicado/10-RESEARCH.md` — "Live-filter-in-memory pattern", Pitfall #1 (export das duas funções), Pitfall #2 (colisão `className`), Pitfall #7 (chamar a Server Action direto do Server Component), "Code Examples" (filtro ao vivo)
    - `.planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md` — "Layout & Component Contract" §1 a §6 e §8, "Copywriting Contract" (header/subtítulo/painel/lista/erro), "Typography" (h1 `font-semibold`, não `font-extrabold`), "UI Considerations"
  </read_first>
  <action>
**1. `relatorio-financeiro.ts` — export + campo `id`.**
Adicione `id: string` como primeiro campo de `ParcelaRelatorio` (antes de `competencia`) — puramente aditivo, o único consumidor existente (`RelatorioFinanceiro`, Phase 8) nunca lê `id`, então seu comportamento não muda. Troque `function passaFiltroTexto(...)` e `function passaFiltroPeriodo(...)` para `export function ...` — mesmo corpo, mesma assinatura, sem nenhuma outra mudança nas duas funções.

**2. `actions.ts` — `buscarParcelasRelatorioAction` seleciona `id` também.**
No `.select(...)` de `buscarParcelasRelatorioAction` (linha ~1305-1307), acrescente `id` como primeiro campo da string: `"id, competencia, vencimento, valor_original, status, cards(endereco, proprietario), parcela_lancamentos(tipo, valor)"`. Nenhuma outra linha da função muda — `requireUser()`, o tratamento de erro e o cast final continuam exatamente como estão.

**3. `reports-view.tsx` — `FilterChip` ganha `className` aditivo.**
Estenda a assinatura de `FilterChip` com `className?: string` (junto de `active`/`children`/`onClick`) e passe `className` como último argumento de `cn(...)`, depois da string de classes condicionais `active ? ... : ...` — `tailwind-merge` resolve o conflito a favor do valor passado por último. Nenhum call site existente (Phase 5-8) passa esse prop, então renderiza idêntico a hoje.

**4. `parcela-situacao-badge.tsx` — `className` aditivo + renomeação da colisão.**
Estenda a assinatura de `ParcelaSituacaoBadge` com `className?: string` (junto de `situacao`). Renomeie a desestruturação `const { icon: Icon, label, className } = SITUACAO[situacao]` para `const { icon: Icon, label, className: toneClassName } = SITUACAO[situacao]` (RESEARCH.md Pitfall #2 — sem essa renomeação o TypeScript recusa a declaração duplicada). No `cn(...)` do `span`, passe `toneClassName` seguido do `className` recebido por prop, nessa ordem. Nenhum call site existente passa esse prop.

**5. `page.tsx` (`relatorios/financeiro/page.tsx`) — Server Component novo.**
`export default async function RelatorioFinanceiroPage()`: chame `const resultado = await buscarParcelasRelatorioAction()` diretamente (é uma função `"use server"`, chamável como qualquer async function do lado do servidor — RESEARCH.md Pitfall #7; NÃO reimplemente a query aqui). Renderize um wrapper `<div className="flex flex-col gap-4 p-6">` contendo: (a) um bloco de cabeçalho `<div className="flex flex-col gap-2">` com, primeiro, um `<Link href="/relatorios" className="inline-flex w-fit items-center gap-1 text-sm text-muted-foreground transition-colors hover:text-foreground">` contendo `<ChevronLeft className="size-3.5" />` + "Relatórios" (verbatim), depois um `<div>` com `<h1 className="font-heading text-2xl font-semibold text-foreground">Relatório Financeiro</h1>` e `<p className="text-sm text-muted-foreground">Os filtros abaixo atualizam os totais e a lista na hora — sem precisar clicar em nada.</p>` (h1 em `font-semibold`, NÃO `font-extrabold` como o h1 de `/relatorios` — UI-SPEC "Typography", peso 600 deliberado, distinto do 800 da página irmã); (b) condicionalmente, `resultado.ok ? <RelatorioFinanceiroDedicado parcelas={resultado.data.parcelas} hojeISO={resultado.data.hojeISO} /> : <p className="text-sm text-muted-foreground">Não foi possível carregar o relatório agora. Tente novamente.</p>` (verbatim). Importe `Link` de `next/link`, `ChevronLeft` de `lucide-react`, `buscarParcelasRelatorioAction` de `@/lib/kanban/actions`, `RelatorioFinanceiroDedicado` de `@/components/reports/relatorio-financeiro-dedicado`.

**6. `relatorio-financeiro-dedicado.tsx` (novo, `"use client"`).**
Exporte `RelatorioFinanceiroDedicado({ parcelas, hojeISO }: { parcelas: ParcelaRelatorio[]; hojeISO: string })`. Estado: `const [aberto, setAberto] = React.useState(false)` (painel fechado por padrão, 10-CONTEXT.md "Claude's Discretion") e `const [filtro, setFiltro] = React.useState<FiltroRelatorioValores>(filtroRelatorioVazio())` — **D-01: `filtro` É o estado aplicado, não existe distinção rascunho/aplicado, então não há `aplicado`/`onGerar` como na Phase 8**. Dois `useMemo`: `categorias = calcularRelatorioFinanceiro(parcelas, filtro, hojeISO)` (deps `[parcelas, filtro, hojeISO]`) e `linhasFiltradas` — filtra `parcelas` reusando `passaFiltroTexto(p.cards?.endereco ?? "", filtro.imovel)`, `passaFiltroTexto(p.cards?.proprietario ?? "", filtro.proprietario)`, `passaFiltroPeriodo(p.competencia, filtro.periodo)`, e a checagem de situação via `situacaoDaParcela(p.status, p.vencimento, hojeISO)` contra `filtro.situacoes` (mesma lógica de `calcularRelatorioFinanceiro`, RESEARCH.md "Code Examples"), depois `.sort((a, b) => (a.vencimento < b.vencimento ? -1 : a.vencimento > b.vencimento ? 1 : 0))` (ordem cronológica ascendente, mesmas deps). Ícone/rótulo por situação: mesmo `ICONE_E_ROTULO` de `relatorio-financeiro.tsx` (import de `lucide-react`: `CheckCircle2`/`Clock`/`AlertCircle`/`Lock`). Retorne, envolvendo tudo num único `<Collapsible open={aberto} onOpenChange={setAberto}>` (o trigger fica na linha de ações, o painel mais abaixo — os dois precisam do mesmo `Collapsible` ancestral):
  - `<div className="flex flex-col gap-4">` contendo, nessa ordem:
    1. Linha de ações: `<div className="flex flex-wrap items-center justify-between gap-4">` com, à esquerda, `<p className="text-sm text-muted-foreground">{linhasFiltradas.length} de {parcelas.length} parcelas</p>` (contador ao vivo, mesma convenção de `resultSummary` de `SearchField`), e à direita `<div className="flex items-center gap-2">` contendo `<CollapsibleTrigger render={<Button variant="outline" size="sm">{aberto ? <X className="size-3.5" /> : <Filter className="size-3.5" />}{aberto ? "Fechar filtros" : "Filtrar"}</Button>} />` — **o botão "Exportar PDF" entra aqui, como próximo irmão dentro do mesmo `div`, no plano 10-02 Task 2; esta task não o renderiza ainda**.
    2. `<CollapsiblePanel><FiltroRelatorioFinanceiroLive campos={filtro} onChange={setFiltro} /></CollapsiblePanel>`.
    3. Grade de tiles: `<div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4">` mapeando `categorias`, um `StatTile` por categoria — mesmo padrão de `relatorio-financeiro.tsx:93-111` (ícone/rótulo, `value={String(categoria.quantidade)}`, `hint={formatCurrency(categoria.total)}`, `tone={categoria.situacao === "vencida" && categoria.quantidade > 0 ? "alert" : "default"}`), sempre renderizada (sem estado "pré-geração" — esta página não tem um).
    4. `<RelatorioFinanceiroLista linhas={linhasFiltradas} hojeISO={hojeISO} />`.

**7. `filtro-relatorio-financeiro-live.tsx` (novo, `"use client"`).**
Exporte `FiltroRelatorioFinanceiroLive({ campos, onChange }: { campos: FiltroRelatorioValores; onChange: (updater: (atual: FiltroRelatorioValores) => FiltroRelatorioValores) => void })`. Declare `atualizarCampo(campo: "imovel" | "proprietario" | "periodo", valor: string)` chamando `onChange((atual) => ({ ...atual, [campo]: valor }))` (mesmo molde de `filtro-parcelas.tsx`/`filtro-relatorio-financeiro.tsx`, mas escrevendo no estado do pai em vez de um `useState` local). Retorne `<div className="mt-4 rounded-2xl border border-border bg-card px-6 py-4">` contendo: (a) `<div className="grid grid-cols-[repeat(3,1fr)] gap-3">` com três campos `flex flex-col gap-2` + `Label` + `Input` — Imóvel (`id="filtro-rel-live-imovel"`, `type="text"`, placeholder "Endereço do imóvel", `value={campos.imovel}`), Proprietário (`id="filtro-rel-live-proprietario"`, `type="text"`, placeholder "Nome do proprietário", `value={campos.proprietario}`), Período (`id="filtro-rel-live-periodo"`, `type="month"`, sem placeholder, `value={campos.periodo}`) — verbatim reuso das labels/placeholders da Phase 8, mas `gap-2` (não `gap-1.5`) nos wrappers, on-grid per UI-SPEC §4; (b) logo abaixo, `<div className="mt-3 flex flex-wrap items-center gap-2">` com `<span className="shrink-0 text-xs font-semibold text-muted-foreground uppercase">Situação</span>`, um `FilterChip` "Todas" (`active={campos.situacoes.size === 0}`, `onClick` chama `onChange(() => ({ ...campos, situacoes: new Set() }))`, `className="font-semibold"`) e um `FilterChip` por `SITUACAO_OPTIONS` (mesmas 4 entradas de `filtro-relatorio-financeiro.tsx`: paga/"Pagas", a_vencer/"A vencer", vencida/"Vencidas", conciliada/"Conciliadas"; `active={campos.situacoes.has(option.value)}`, `onClick` chama `onChange((atual) => ({ ...atual, situacoes: toggle(atual.situacoes, option.value) }))`, `className="font-semibold"` em todos — UI-SPEC "Typography", peso 600, não o `font-medium` interno padrão do componente); (c) `const temFiltroPreenchido = Boolean(campos.imovel.trim() || campos.proprietario.trim() || campos.periodo.trim() || campos.situacoes.size > 0)`; quando verdadeiro, `<div className="mt-3 flex justify-end"><Button variant="ghost" onClick={() => onChange(() => filtroRelatorioVazio())}>Limpar filtros</Button></div>`. **Sem nenhum botão de submit/`onGerar` neste arquivo, nenhum rótulo de "aplicar filtro" em lugar nenhum** — é essa ausência que implementa D-01. Importe `FilterChip`/`toggle` de `@/components/reports/reports-view`, `filtroRelatorioVazio`/`type FiltroRelatorioValores`/`type SituacaoRelatorio` de `@/lib/kanban/relatorio-financeiro`, `Input`/`Label`/`Button` dos respectivos `@/components/ui/*`.

**8. `relatorio-financeiro-lista.tsx` (novo).**
Exporte `RelatorioFinanceiroLista({ linhas, hojeISO }: { linhas: ParcelaRelatorio[]; hojeISO: string })`. Declare `const mesFormatter = new Intl.DateTimeFormat("pt-BR", { month: "long" })` no escopo do módulo (não recriar por render) e `function competenciaLabel(competencia: string): string` que faz `const [ano, mes] = competencia.split("-").map(Number)`, `const mesPorExtenso = mesFormatter.format(new Date(ano, mes - 1, 1))`, capitaliza a primeira letra (`mesPorExtenso.charAt(0).toUpperCase() + mesPorExtenso.slice(1)` — `Intl` devolve minúsculo em pt-BR, e o UI-SPEC pede "Agosto de 2026" capitalizado para esta célula de tabela, diferente do formato "mês/ano" minúsculo usado nos diálogos) e devolve `` `${mesCapitalizado} de ${ano}` ``. Retorne `<div className="rounded-2xl border border-border bg-card p-6">`: se `linhas.length === 0`, `<p className="text-sm text-muted-foreground">Nenhuma parcela encontrada para os filtros aplicados.</p>` (string idêntica a `VAZIO_LABEL["sem-resultado-filtro"]` de `parcelas-table.tsx`); senão, `<Table>` com `<TableHeader><TableRow>` de 6 `<TableHead>` — "Imóvel", "Proprietário", "Competência", "Vencimento", "Situação", `<TableHead className="text-right">Valor</TableHead>` — e `<TableBody>` mapeando `linhas` (key: `parcela.id`, já disponível após o item 1/2 desta task): `<TableCell className="text-sm font-semibold text-foreground">{parcela.cards?.endereco ?? ""}</TableCell>`, `<TableCell className="text-sm text-muted-foreground">{parcela.cards?.proprietario ?? ""}</TableCell>`, `<TableCell className="text-sm text-muted-foreground">{competenciaLabel(parcela.competencia)}</TableCell>`, `<TableCell className="tabular-nums text-muted-foreground">{formatDate(parcela.vencimento)}</TableCell>`, `<TableCell><ParcelaSituacaoBadge situacao={situacao} className="gap-2" /></TableCell>` (onde `situacao = situacaoDaParcela(parcela.status, parcela.vencimento, hojeISO)`, calculado por linha, antes das células), `<TableCell className="text-right tabular-nums">{formatCurrency(valor)}</TableCell>` (onde `valor` vem de `somarLancamentos(parcela.valor_original, parcela.parcela_lancamentos)` — `situacao === "paga" || situacao === "conciliada" ? valorPago : Math.max(valorDevido - valorPago, 0)`, D-07, mesma regra de `calcularRelatorioFinanceiro`). Importe `formatCurrency`/`formatDate` de `@/lib/kanban/format`, `situacaoDaParcela`/`somarLancamentos` de `@/lib/kanban/parcelas`, `ParcelaSituacaoBadge` de `@/components/financeiro/parcela-situacao-badge`, `type ParcelaRelatorio` de `@/lib/kanban/relatorio-financeiro`, `Table`/`TableBody`/`TableCell`/`TableHead`/`TableHeader`/`TableRow` de `@/components/ui/table`.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && f1="web/src/lib/kanban/relatorio-financeiro.ts" && f2="web/src/lib/kanban/actions.ts" && f3="web/src/components/reports/reports-view.tsx" && f4="web/src/components/financeiro/parcela-situacao-badge.tsx" && f5="web/src/app/(app)/relatorios/financeiro/page.tsx" && f6="web/src/components/reports/relatorio-financeiro-dedicado.tsx" && f7="web/src/components/reports/filtro-relatorio-financeiro-live.tsx" && f8="web/src/components/reports/relatorio-financeiro-lista.tsx" && grep -q "export function passaFiltroTexto" "$f1" && grep -q "export function passaFiltroPeriodo" "$f1" && grep -A6 "export type ParcelaRelatorio" "$f1" | grep -q "id: string" && grep -q '"id, competencia, vencimento, valor_original, status' "$f2" && grep -q "className" "$f3" && grep -q "toneClassName" "$f4" && test -f "$f5" && grep -q "buscarParcelasRelatorioAction" "$f5" && grep -q "RelatorioFinanceiroDedicado" "$f5" && [ "$(grep -c 'from("parcelas")' "$f5")" = "0" ] && test -f "$f6" && grep -q "calcularRelatorioFinanceiro" "$f6" && grep -q "passaFiltroTexto" "$f6" && grep -q "passaFiltroPeriodo" "$f6" && grep -q "useMemo" "$f6" && test -f "$f7" && [ "$(grep -c "Gerar relatório" "$f7")" = "0" ] && grep -q "onChange" "$f7" && test -f "$f8" && grep -q "ParcelaSituacaoBadge" "$f8" && grep -q "Nenhuma parcela encontrada para os filtros aplicados." "$f8" && grep -q 'className="gap-2"' "$f8" && echo RELATORIO_DEDICADO_TRACER_OK</automated>
    <human-check>
      <test>Abrir `/relatorios/financeiro` em produção. Sem preencher nada, conferir os 4 tiles e a lista contra uma consulta no SQL Editor (mesma consulta usada na Phase 8). Digitar no campo Imóvel (sem clicar em nada) e observar os 4 tiles e a lista mudarem imediatamente, a cada tecla. Alternar um chip de Situação e confirmar que tiles e lista mudam juntos, na hora. Testar Período (`type="month"`) e Proprietário da mesma forma. Clicar "Limpar filtros" e confirmar que os 4 campos voltam a vazio e tiles/lista voltam ao total geral. Aplicar um filtro que não bate com nenhuma parcela e confirmar que os 4 tiles mostram "0"/"R$ 0,00" e a lista mostra "Nenhuma parcela encontrada para os filtros aplicados." Conferir visualmente que a coluna Competência mostra "Agosto de 2026" (mês capitalizado), não "agosto/2026". Voltar para `/relatorios` e conferir que a página original (painel suspenso, botão "Gerar relatório", 4 tiles antigos, ContractsTable) está pixel-idêntica a antes desta fase.</test>
      <expected>Nenhum campo/chip precisa de clique — tiles e lista sempre refletem o filtro atual, na mesma tecla. Os números batem com o SQL Editor, incluindo contrato arquivado/inativo se houver algum em produção (D-05). `/relatorios` continua exatamente como estava.</expected>
      <why_human>Confirmar que a tela recalcula "ao vivo" a cada tecla (não só no clique) e que os números batem com o banco exige interação real no navegador — grep confirma que o código existe, não que o comportamento ao vivo se sustenta na prática.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` e `npm run build` terminam com código 0
    - `/relatorios/financeiro` existe e chama `buscarParcelasRelatorioAction()` diretamente, sem duplicar a query (`grep -c 'from("parcelas")' page.tsx` = 0)
    - `passaFiltroTexto`/`passaFiltroPeriodo` são exportadas de `relatorio-financeiro.ts` e reusadas por `relatorio-financeiro-dedicado.tsx`, nunca reimplementadas
    - `ParcelaRelatorio` tem `id: string`; `buscarParcelasRelatorioAction` seleciona `id`
    - `FilterChip`/`ParcelaSituacaoBadge` têm `className?: string` opcional aditivo; `ParcelaSituacaoBadge` renomeou a variável local colidente para `toneClassName`
    - `FiltroRelatorioFinanceiroLive` não tem nenhuma ocorrência do texto "Gerar relatório" — nenhum botão de submit existe nesse arquivo
    - `RelatorioFinanceiroLista` usa a string exata "Nenhuma parcela encontrada para os filtros aplicados." no estado vazio e passa `className="gap-2"` para `ParcelaSituacaoBadge`
  </acceptance_criteria>
  <reversibility rating="reversible">Leitura pura, nenhuma escrita nova, nenhuma migração — a única mudança de schema-adjacent é selecionar mais um campo já existente (`id`) numa query de leitura. Reverter este commit remove a rota nova e os componentes novos sem afetar `parcelas`/`parcela_lancamentos` nem a página `/relatorios` existente (que só ganha o botão de entrada na Task 2).</reversibility>
  <done>`/relatorios/financeiro` mostra 4 tiles e uma lista de parcelas que recalculam a cada mudança de filtro, sem nenhum clique de "aplicar" — dado real, batendo com o banco, incluindo contrato arquivado/inativo (D-05).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Botão de entrada "Relatório financeiro" em /relatorios (RELDED-01 completo)</name>
  <precondition>Task 1 aplicada e o projeto compila; a rota `/relatorios/financeiro` já existe e responde.</precondition>
  <files>web/src/components/reports/filtro-relatorio-financeiro.tsx</files>
  <read_first>
    - `web/src/components/reports/filtro-relatorio-financeiro.tsx:66-90` — a linha de cabeçalho atual (`<div className="flex items-center justify-between gap-3">` com o bloco de heading à esquerda e `CollapsibleTrigger` à direita) — o botão novo entra como irmão do `CollapsibleTrigger`, dentro de um wrapper novo, sem remover nem reordenar nada existente
    - `.planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md` — "Copywriting Contract" (Entry-point button: copy "Relatório financeiro" verbatim, ícone `ArrowUpRight`, `Button variant="outline" size="sm"`, navega via `next/link` na mesma aba — D-05)
  </read_first>
  <action>
Em `filtro-relatorio-financeiro.tsx`, importe `Link` de `next/link` e `ArrowUpRight` de `lucide-react` (junto dos ícones já importados `Filter`/`X`). Na linha de cabeçalho existente (`<div className="flex items-center justify-between gap-3">`), envolva o `CollapsibleTrigger` atual num novo `<div className="flex items-center gap-2">` e insira, ANTES dele (como primeiro filho desse novo wrapper), `<Button variant="outline" size="sm" render={<Link href="/relatorios/financeiro" />}><ArrowUpRight className="size-3.5" />Relatório financeiro</Button>` (mesmo padrão `render` de `Button`+`Link` já usado por `CollapsibleTrigger`+`Button` neste mesmo arquivo — evita aninhar `<button>` dentro de `<a>`, que seria HTML inválido). O resultado: `<div className="flex items-center justify-between gap-3">{blocoDeHeading}<div className="flex items-center gap-2">{botãoNovo}{CollapsibleTrigger}</div></div>` — nenhuma linha do bloco de heading (`h2`/`p`) nem do `CollapsibleTrigger` original muda de conteúdo, só ganham um wrapper e um vizinho novo. Nenhuma outra linha do arquivo é tocada.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && f="web/src/components/reports/filtro-relatorio-financeiro.tsx" && grep -q "Relatório financeiro" "$f" && grep -q "ArrowUpRight" "$f" && grep -q 'href="/relatorios/financeiro"' "$f" && grep -q "next/link" "$f" && [ "$(git diff --unified=0 -- "$f" | grep -c '^-')" = "0" ] && echo RELATORIO_ENTRY_BUTTON_OK</automated>
    <human-check>
      <test>Abrir `/relatorios`. Confirmar que o botão "Relatório financeiro" aparece na linha de cabeçalho do painel "Relatório financeiro" existente, imediatamente à esquerda do botão "Filtrar". Clicar nele e confirmar que navega para `/relatorios/financeiro` na mesma aba (sem abrir nova aba/janela). Voltar (botão do navegador ou o link "Relatórios" da página nova) e confirmar que `/relatorios` está exatamente como estava, sem nenhuma mudança visual fora do botão novo.</test>
      <expected>O botão navega direto para `/relatorios/financeiro`, na mesma aba (D-05). Todo o resto de `/relatorios` — painel suspenso da Phase 8, 4 tiles antigos, gráfico, tabela de contratos — permanece pixel-idêntico.</expected>
      <why_human>Confirmar navegação na mesma aba e paridade visual do resto da página exige clicar e olhar — grep confirma que o `href`/import existem, não que o clique de fato navega sem abrir aba nova.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` e `npm run build` terminam com código 0
    - O botão "Relatório financeiro" existe em `filtro-relatorio-financeiro.tsx`, com ícone `ArrowUpRight`, `Button variant="outline" size="sm"`, `Link href="/relatorios/financeiro"` (sem `target="_blank"`)
    - `git diff --unified=0` desse arquivo mostra 0 linhas removidas — mudança puramente aditiva
  </acceptance_criteria>
  <reversibility rating="reversible">Um botão de navegação a mais numa página existente, sem nenhuma escrita nem migração. Reverter o commit apenas remove o botão — `/relatorios/financeiro` continua existindo, só fica sem um ponto de entrada dentro do app (ainda acessível por URL direta).</reversibility>
  <done>`/relatorios` tem um botão "Relatório financeiro" que leva a `/relatorios/financeiro` na mesma aba, sem alterar nenhuma outra linha da página existente (RELDED-01 completo).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| navegador -> Server Component | A rota nova busca `parcelas` uma única vez, no primeiro carregamento; todo filtro subsequente roda em memória no cliente sobre o mesmo array — nenhum campo/chip dispara nova consulta |
| Server Component -> banco | `relatorios/financeiro/page.tsx` chama `buscarParcelasRelatorioAction()` (sessão do usuário via `requireUser()`, RLS `is_team_member()`), nunca `service_role` — mesma Server Action já em produção desde a Phase 8, sem query nova |
| sessão -> allowlist | Usuário fora de `allowed_members` recebe lista vazia de `parcelas` (RLS Phase 4) — o relatório dedicado mostra 4 tiles zerados e lista vazia, nunca dado de outro cliente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-01 | Information Disclosure | novo Server Component `relatorios/financeiro/page.tsx` chamando `buscarParcelasRelatorioAction()` direto | medium | mitigate | reusa a Server Action existente verbatim (nenhuma query nova é escrita); a página branca explicitamente em `resultado.ok` antes de renderizar qualquer dado, para que uma falha de auth/RLS nunca renderize dado parcial/obsoleto silenciosamente |
| T-10-02 | Tampering | estado de filtro no cliente (`FiltroRelatorioValores`, ao vivo) | low | accept | o filtro roda em memória sobre linhas já escopadas pelo RLS na consulta do servidor — um filtro manipulado só pode reduzir/reordenar o que a query já trouxe, nunca alcançar uma linha que o RLS não devolveu. Nenhum SQL/string é montado a partir do input do usuário para esta feature |
| T-10-03 | Tampering (regressão futura) | invariante D-05 ("sem filtro de arquivado/ativo"), herdada de `buscarParcelasRelatorioAction` | low | mitigate | esta fase não reescreve a query — chama a Server Action já documentada com o comentário D-05 na origem (`actions.ts:1297-1325`); nenhum novo `.is("cards.arquivado_em", null)`/`.eq("ativo", true)` é introduzido em nenhum ponto novo desta fase |
| T-10-04 | Tampering | merge aditivo de `className` em `FilterChip`/`ParcelaSituacaoBadge` via `cn()` | low | accept | `cn()` só recebe strings de classe literais, escritas pelo desenvolvedor em cada call site deste próprio código-fonte — nunca um valor vindo de input do usuário — então não existe superfície de injeção de classe apesar do ponto de merge ser novo |
| T-10-SC | Tampering | instalação de pacote npm | n/a | accept | zero pacotes novos neste plano — `Collapsible`, `Button`, `Input`, `Label`, `Table`, ícones lucide já instalados desde as Phases 5-8; nenhum `npm install`/`npx shadcn add` é executado (isso fica só no plano 10-02, com seu próprio T-10-SC) |

</threat_model>

<verification>
- `npm run lint` e `npm run build` em `web/`
- Asserção de fonte: `relatorios/financeiro/page.tsx` não encadeia `.from("parcelas")` própria — só chama `buscarParcelasRelatorioAction()`
- Asserção de fonte: `passaFiltroTexto`/`passaFiltroPeriodo` exportadas e reusadas, `id` presente em `ParcelaRelatorio` e no `.select()`
- Asserção de fonte: `filtro-relatorio-financeiro-live.tsx` não contém o texto "Gerar relatório" em lugar nenhum
- Asserção de fonte: `git diff --unified=0` de `filtro-relatorio-financeiro.tsx` (Task 2) mostra 0 linhas removidas
- Teste manual: digitar/alternar cada um dos 4 campos de filtro e confirmar que tiles + lista mudam imediatamente, sem clique; números batendo com o SQL Editor
</verification>

<success_criteria>
- Existe uma rota `/relatorios/financeiro`, alcançada por um botão "Relatório financeiro" em `/relatorios` (RELDED-01)
- O painel de filtro é dinâmico — qualquer mudança recalcula os 4 tiles e a lista imediatamente, sem clicar em nada (RELDED-02)
- Abaixo dos tiles, uma lista mostra uma linha por parcela filtrada (endereço, proprietário, competência, vencimento, situação, valor), sempre em sincronia com o filtro atual (RELDED-03)
- `/relatorios` (Phase 8) permanece byte a byte inalterada fora do botão de entrada novo
</success_criteria>

<output>
Create `.planning/phases/10-relat-rio-financeiro-dedicado/10-01-SUMMARY.md` when done
</output>
</content>
