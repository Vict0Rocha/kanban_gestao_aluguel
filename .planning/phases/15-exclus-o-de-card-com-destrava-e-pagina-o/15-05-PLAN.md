---
phase: 15-exclus-o-de-card-com-destrava-e-pagina-o
plan: 05
type: execute
wave: 2
depends_on: ["15-03"]
files_modified:
  - web/src/components/reports/contracts-table.tsx
  - web/src/components/reports/reports-view.tsx
  - web/src/components/reports/relatorio-financeiro-lista.tsx
  - web/src/components/reports/relatorio-financeiro-dedicado.tsx
  - web/src/components/reports/dinheiro-imobiliaria-view.tsx
  - web/src/components/financeiro/configuracao-financeira-view.tsx
  - web/src/components/arquivados/arquivados-view.tsx
autonomous: true
requirements: [PAGIN-01, PAGIN-02, PAGIN-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As cinco listagens restantes (Situação dos contratos, Relatório Financeiro dedicado, Relatório da imobiliária, Configuração financeira, Arquivados) mostram no máximo 10 itens por página, com navegação numerada — mesmo componente `Pagination`/`usePagination` do plano 15-03, reutilizado sem reimplementação (D-06)"
    - "Nas três listagens com filtro (Situação dos contratos, Relatório Financeiro dedicado, Relatório da imobiliária), mudar o filtro volta a página para 1; nas duas sem filtro (Configuração financeira, Arquivados), uma `resetKey` constante garante que a página nunca reseta sozinha por causa de um `router.refresh()` de mutação (editar percentuais, desarquivar)"
    - "Nenhuma das cinco listagens ganhou paginação de servidor — todas continuam recebendo o array completo já filtrado como prop e fatiando em memória, exatamente como a lista de parcelas do Financeiro (plano 15-03)"
  artifacts:
    - web/src/components/reports/contracts-table.tsx
    - web/src/components/reports/relatorio-financeiro-lista.tsx
    - web/src/components/reports/dinheiro-imobiliaria-view.tsx
    - web/src/components/financeiro/configuracao-financeira-view.tsx
    - web/src/components/arquivados/arquivados-view.tsx
  key_links:
    - "`usePagination`/`Pagination` (plano 15-03, `web/src/components/pagination.tsx`) -> as cinco listagens deste plano, importados sem nenhuma mudança no componente compartilhado"
  prohibitions:
    - "MUST NOT criar uma segunda implementação de paginação — importar `usePagination`/`Pagination` de `@/components/pagination` em todos os cinco call sites, igual ao plano 15-03"
    - "MUST NOT usar `useEffect(() => setPagina(1), [itens])` em nenhum dos cinco sites — mesma armadilha do Pitfall 3, mesma solução de `resetKey` comparado durante a renderização"
    - "MUST NOT tocar o Board — fora de escopo (D-04 de 15-CONTEXT.md, confirmado pelo usuário: 'lá está tudo certo')"
---

<objective>
Replicar `usePagination`/`Pagination` (construídos no plano 15-03) nas cinco listagens restantes do escopo
confirmado em D-04 (15-CONTEXT.md): Situação dos contratos (`/relatorios`), Relatório Financeiro dedicado
(`/relatorios/financeiro`), Relatório da imobiliária (`/relatorios/imobiliaria`), Configuração financeira
(`/financeiro/configuracao`) e Arquivados (`/arquivados`) — a sexta listagem do escopo (Financeiro) já foi
paginada no plano 15-03.

Três das cinco (contratos, relatório dedicado, imobiliária) têm filtro ativo — o `resetKey` amarra à
identidade do filtro de cada tela. As outras duas (configuração, arquivados) não têm filtro nenhum — a
`resetKey` é uma constante, exatamente para o `router.refresh()` de uma mutação (editar percentuais,
desarquivar) nunca resetar a posição do usuário, mesma armadilha documentada no Pitfall 3.

Purpose: fecha PAGIN-01/02/03 nas seis listagens do escopo — Board fica de fora por decisão explícita do
usuário.
Output: as cinco listagens restantes paginadas em blocos de 10, com navegação numerada e reset correto.
</objective>

Este plano não cria símbolo de banco — ver `## Artifacts this phase produces` em `15-01-PLAN.md`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-CONTEXT.md
@.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-RESEARCH.md
@.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-PATTERNS.md
@web/src/components/pagination.tsx
@web/src/components/financeiro/parcelas-table.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Paginar "Situação dos contratos" (`/relatorios`)</name>

  <files>web/src/components/reports/contracts-table.tsx, web/src/components/reports/reports-view.tsx</files>

  <read_first>
    - `web/src/components/reports/contracts-table.tsx` (arquivo inteiro) — `rows.map(...)` linha 42,
      dentro de `<TableBody>`; assinatura hoje é só `{ rows }: { rows: ContractRow[] }`
    - `web/src/components/reports/reports-view.tsx` (arquivo inteiro) — `statusFilters`/`columnFilters`
      (`Set<T>`, `useState`) e `query` (`string`, `useState`) são os três estados de filtro; `<ContractsTable
      rows={report.contratos} />` é o call site a estender, linha ~247
    - `web/src/components/pagination.tsx` (plano 15-03) — `usePagination`/`Pagination`, já prontos, mesma
      forma de import usada em `parcelas-table.tsx`
    - `.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-RESEARCH.md` "Per-call-site wiring"
      linha 2 da tabela — `resetKey` recomendado: computado em `ReportsView` a partir de
      `query`/`statusFilters` (ordenado)/`columnFilters` (ordenado)
  </read_first>

  <action>
Em `web/src/components/reports/contracts-table.tsx`: importar `{ usePagination, Pagination }` de
`@/components/pagination`. Adicionar `resetKey: unknown` à assinatura de `ContractsTable` (ao lado de
`rows`). Chamar `usePagination(rows, resetKey)` e usar `itensDaPagina` no lugar de `rows` no `.map()` da
linha 42 — o resto do JSX fica igual. Renderizar `<Pagination pagina={pagina} totalPaginas={totalPaginas}
onPaginaChange={setPagina} />` logo depois de `</Table>`, dentro do `<div className="mt-4">` que já
envolve a tabela. O texto de estado vazio (`rows.length === 0`) continua usando `rows`, não
`itensDaPagina`.

Em `web/src/components/reports/reports-view.tsx`: calcular uma `resetKey` derivada dos três estados de
filtro — `` `${query}|${[...statusFilters].sort().join(",")}|${[...columnFilters].sort().join(",")}` `` —
e passar como prop `resetKey` para `<ContractsTable rows={report.contratos} resetKey={...} />`. Não criar
um `useState`/`useMemo` novo para essa chave a menos que necessário para evitar recomputar a cada render —
uma expressão inline já é suficientemente barata no volume atual do projeto.
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && c=web/src/components/reports/contracts-table.tsx && r=web/src/components/reports/reports-view.tsx && grep -q "import { usePagination, Pagination } from \"@/components/pagination\"" "$c" && grep -q "itensDaPagina" "$c" && grep -q "resetKey" "$c" && grep -q "resetKey" "$r" && grep -c "useEffect" "$c" | grep -qx 0 && cd web && npx tsc --noEmit && echo TASK1_OK</automated>
  </verify>

  <acceptance_criteria>
    - `ContractsTable` ganha a prop `resetKey`, usa `itensDaPagina` no `.map()`, `rows` continua sendo a
      fonte do estado vazio
    - `ReportsView` calcula e passa uma `resetKey` derivada de `query`/`statusFilters`/`columnFilters`
    - `npx tsc --noEmit` sem erro novo
  </acceptance_criteria>

  <done>"Situação dos contratos" pagina em blocos de 10; trocar qualquer filtro (busca, status, coluna)
  volta para a página 1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Paginar Relatório Financeiro dedicado e Relatório da imobiliária</name>

  <files>web/src/components/reports/relatorio-financeiro-lista.tsx, web/src/components/reports/relatorio-financeiro-dedicado.tsx, web/src/components/reports/dinheiro-imobiliaria-view.tsx</files>

  <read_first>
    - `web/src/components/reports/relatorio-financeiro-lista.tsx` (arquivo inteiro) — `linhas.map(...)`
      linha 58; assinatura hoje `{ linhas, hojeISO }: { linhas: ParcelaRelatorio[]; hojeISO: string }`
    - `web/src/components/reports/relatorio-financeiro-dedicado.tsx` (arquivo inteiro) — `filtro`
      (`FiltroRelatorioValores`, `useState`) e `<RelatorioFinanceiroLista linhas={linhasFiltradas}
      hojeISO={hojeISO} />` (linha ~194), o call site a estender
    - `web/src/components/reports/dinheiro-imobiliaria-view.tsx` (arquivo inteiro) — `linhas.map(...)`
      linha 166, DENTRO do próprio componente (sem componente filho separado); `periodo` (`useState`,
      linha 56) é o único filtro desta tela
    - `web/src/components/pagination.tsx` (plano 15-03)
    - `.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-RESEARCH.md` "Per-call-site wiring"
      linhas 3 e 4 da tabela — `resetKey` recomendado para cada uma: `JSON.stringify(filtro)` para o
      relatório dedicado, `periodo` (direto, sem prop nova) para o de imobiliária
  </read_first>

  <action>
Em `web/src/components/reports/relatorio-financeiro-lista.tsx`: importar `{ usePagination, Pagination }`.
Adicionar `resetKey: unknown` à assinatura. Chamar `usePagination(linhas, resetKey)`, usar `itensDaPagina`
no `.map()` da linha 58, renderizar `<Pagination .../>` logo depois de `</Table>`, dentro do mesmo `<div
className="rounded-2xl border border-border bg-card p-6">` que já envolve a tabela. Estado vazio
(`linhas.length === 0`) continua usando `linhas`.

Em `web/src/components/reports/relatorio-financeiro-dedicado.tsx`: passar
`resetKey={JSON.stringify(filtro)}` para `<RelatorioFinanceiroLista>` — `filtro` já é o `useState`
existente, `JSON.stringify` é suficiente porque `FiltroRelatorioValores` é um objeto plano de strings/Set
(conferir a serialização de qualquer `Set` dentro do tipo antes de assumir — se `filtro` tiver um campo
`Set`, converter para array ordenado antes do `JSON.stringify`, mesma cautela da Task 1).

Em `web/src/components/reports/dinheiro-imobiliaria-view.tsx` (sem componente filho — o `.map()` roda
dentro do próprio arquivo): importar `{ usePagination, Pagination }`. Chamar `usePagination(linhas,
periodo)` diretamente (não precisa de prop nova — `periodo` já é local ao componente), usar
`itensDaPagina` no `.map()` da linha 166, renderizar `<Pagination .../>` logo depois de `</Table>`, dentro
do `<div className="rounded-2xl border border-border bg-card p-6">` que já envolve a tabela. Estado vazio
(`linhas.length === 0`) continua usando `linhas`.
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && l=web/src/components/reports/relatorio-financeiro-lista.tsx && d=web/src/components/reports/relatorio-financeiro-dedicado.tsx && i=web/src/components/reports/dinheiro-imobiliaria-view.tsx && grep -q "import { usePagination, Pagination } from \"@/components/pagination\"" "$l" && grep -q "itensDaPagina" "$l" && grep -q "resetKey" "$l" && grep -q "resetKey={JSON.stringify(filtro)}" "$d" && grep -q "import { usePagination, Pagination } from \"@/components/pagination\"" "$i" && grep -q "itensDaPagina" "$i" && grep -q "usePagination(linhas, periodo)" "$i" && grep -c "useEffect" "$l" | grep -qx 0 && grep -c "useEffect" "$i" | grep -qx 0 && cd web && npx tsc --noEmit && echo TASK2_OK</automated>
  </verify>

  <acceptance_criteria>
    - `RelatorioFinanceiroLista` ganha `resetKey`, usa `itensDaPagina`, `linhas` continua sendo a fonte do
      estado vazio
    - `RelatorioFinanceiroDedicado` passa `resetKey={JSON.stringify(filtro)}`
    - `DinheiroImobiliariaView` chama `usePagination(linhas, periodo)` sem nenhuma prop nova (filtro já é
      local ao componente)
    - `npx tsc --noEmit` sem erro novo
  </acceptance_criteria>

  <done>Relatório Financeiro dedicado e Relatório da imobiliária paginam em blocos de 10; trocar o filtro
  de cada tela volta para a página 1.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Paginar Configuração financeira e Arquivados (telas sem filtro, `resetKey` constante)</name>

  <files>web/src/components/financeiro/configuracao-financeira-view.tsx, web/src/components/arquivados/arquivados-view.tsx</files>

  <read_first>
    - `web/src/components/financeiro/configuracao-financeira-view.tsx` (arquivo inteiro) — `linhas.map(...)`
      linha 167; a assinatura hoje é `{ linhas, todayISO, erro }`, sem nenhum campo de filtro
    - `web/src/components/arquivados/arquivados-view.tsx` (arquivo inteiro) — `contratos.map(...)` linha
      114; a assinatura hoje é `{ contratos, erroCarregamento }`, também sem filtro; `handleDesarquivar`
      chama `router.refresh()` (linha 57) depois de uma mutação — o caso central do Pitfall 3 nesta tela
    - `web/src/components/pagination.tsx` (plano 15-03)
    - `.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-RESEARCH.md` "Per-call-site wiring"
      linhas 5 e 6 da tabela — as duas telas sem filtro usam `resetKey` constante (ex.: `"config"` /
      `"arquivados"`), justamente para o `router.refresh()` de uma mutação nunca resetar a página
  </read_first>

  <action>
Em `web/src/components/financeiro/configuracao-financeira-view.tsx`: importar `{ usePagination, Pagination
}`. Chamar `usePagination(linhas, "config")` — a chave de reset é a string literal constante `"config"`
(nunca muda, porque não existe filtro nesta tela — editar percentuais ou o histórico de caução dispara
`router.refresh()` via `ConfigurarPercentuaisDialog`/`CaucaoHistoricoSheet`, e a paginação não deve reagir
a isso). Usar `itensDaPagina` no `.map()` da linha 167, renderizar `<Pagination .../>` logo depois de
`</Table>`, dentro do `<div className="rounded-2xl border border-border bg-card p-6">` existente. Estado
vazio (`linhas.length === 0`) continua usando `linhas`.

Em `web/src/components/arquivados/arquivados-view.tsx`: importar `{ usePagination, Pagination }`. Chamar
`usePagination(contratos, "arquivados")` — mesma lógica de chave constante, pela mesma razão:
`handleDesarquivar` chama `router.refresh()` (linha 57) depois de desarquivar um contrato, e a página do
usuário não deve pular de volta para a 1 por causa disso. Usar `itensDaPagina` no `.map()` da linha 114,
renderizar `<Pagination .../>` logo depois de `</Table>`, dentro do `<div className="mt-4">` que já
envolve a tabela. Estado vazio (`contratos.length === 0`) continua usando `contratos`.
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && cf=web/src/components/financeiro/configuracao-financeira-view.tsx && av=web/src/components/arquivados/arquivados-view.tsx && grep -q "import { usePagination, Pagination } from \"@/components/pagination\"" "$cf" && grep -q "usePagination(linhas, \"config\")" "$cf" && grep -q "itensDaPagina" "$cf" && grep -q "import { usePagination, Pagination } from \"@/components/pagination\"" "$av" && grep -q "usePagination(contratos, \"arquivados\")" "$av" && grep -q "itensDaPagina" "$av" && grep -c "useEffect" "$cf" | grep -qx 0 && grep -c "useEffect" "$av" | grep -qx 0 && cd web && npx tsc --noEmit && echo TASK3_OK</automated>
    <human-check>Abrir /financeiro/configuracao e /arquivados em produção. Se houver mais de 10 contratos
    em qualquer uma das duas, confirmar navegação numerada. Em Arquivados, com pelo menos 11 contratos
    arquivados (ou testando localmente com dado reduzido), desarquivar um contrato que esteja numa página
    diferente da 1 e confirmar que a tela NÃO volta para a página 1 depois do `router.refresh()`. Repetir
    em Configuração financeira editando percentuais de um contrato fora da página 1.</human-check>
  </verify>

  <acceptance_criteria>
    - `ConfiguracaoFinanceiraView` chama `usePagination(linhas, "config")` (chave literal constante)
    - `ArquivadosView` chama `usePagination(contratos, "arquivados")` (chave literal constante)
    - Nenhuma das duas telas ganhou um estado de filtro novo — a paginação é a única mudança
    - `npx tsc --noEmit` sem erro novo
  </acceptance_criteria>

  <done>Configuração financeira e Arquivados paginam em blocos de 10; uma mutação (editar percentuais,
  desarquivar) fora da página 1 nunca reseta a posição do usuário.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| navegador → `Pagination`/`usePagination` (cinco sites) | Nenhuma fronteira nova — cada array já é o resultado filtrado pelo RLS antes de chegar nestes componentes; paginar em memória não expõe nada que a página já não tivesse recebido |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-13 | Information Disclosure | Paginação client-side vazando linhas que o RLS já deveria ter filtrado | low | accept | Mesma mitigação do plano 15-03 — os cinco arrays já chegam filtrados pelo servidor antes deste componente existir |
| T-15-14 | Tampering | `resetKey` constante mal escolhida faz duas telas sem relação compartilharem estado de página por engano | low | accept | Cada `usePagination` é uma instância de `useState` local ao componente — `"config"`/`"arquivados"` são só o valor comparado dentro daquele componente específico, nunca uma chave global compartilhada entre telas |
| T-15-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala nenhum pacote novo |
</threat_model>

<verification>
1. `grep`/`tsc --noEmit` confirmam o wiring de cada um dos cinco sites, incluindo a ausência de
   `useEffect` para reset de página
2. `<human-check>` (Task 3) cobre o caso interativo de mutação-sem-reset nas duas telas sem filtro
</verification>

<success_criteria>
- As cinco listagens restantes paginam em blocos de 10 com navegação numerada
- Nas três com filtro, trocar o filtro reseta para página 1
- Nas duas sem filtro, uma mutação (`router.refresh()`) nunca reseta a página
- Nenhuma paginação de servidor foi introduzida em nenhum dos cinco sites
</success_criteria>

<output>
Create `.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-05-SUMMARY.md` when done
</output>
