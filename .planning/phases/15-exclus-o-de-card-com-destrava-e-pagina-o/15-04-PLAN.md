---
phase: 15-exclus-o-de-card-com-destrava-e-pagina-o
plan: 04
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - supabase/verificacao_relaxar_exclusao_destrava.sql
autonomous: false
requirements: [CANDEST-01]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Parte A do runbook foi executada contra o banco de produção real de um jeito que garante continuidade de sessão — Parte A inteira num único clique de Run, ou psql/Supabase CLI (D-19). Rodar bloco a bloco no SQL Editor do Studio não conta como ensaio"
    - "O BLOCO 3 pós-`rollback` confirma que o corpo da função voltou ao estado anterior — se não voltou, o ensaio virou push e o plano para para reportar antes de seguir para o 15-06"
    - "As duas metades da mudança foram observadas de verdade contra o schema real, na mesma transação: um card só-destrava foi excluído sem erro (Prova 2.1), e um card com pagamento continuou recusando exclusão (Prova 2.2), assim como um card com taxa (Prova 2.3) — não presumidas, observadas"
    - "`cards_total`, `parcelas_total`, `lancamentos_total` e `cards_updated_at_max` depois do `rollback` são idênticos aos do BLOCO 1"
    - "O bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_relaxar_exclusao_destrava.sql` está preenchido com os números reais observados, as mensagens literais das Provas 2.1/2.2/2.3, e o caminho de execução usado (único Run vs. psql/CLI)"
  artifacts:
    - supabase/verificacao_relaxar_exclusao_destrava.sql
  key_links:
    - "ensaio -> `checkpoint:decision` do plano 15-06: sem o resultado registrado aqui, aquele portão não tem base para decidir"
  prohibitions:
    - "MUST NOT seguir para o plano 15-06 se a Prova 2.2 ou 2.3 (as proteções que precisam continuar) não passou — um relaxamento que também derruba a proteção de dinheiro de verdade é pior do que não relaxar nada"
---

<objective>
Ensaiar a migração `20260826010000_relaxar_exclusao_destrava.sql` contra o banco de produção dentro de uma
transação revertida, e registrar o resultado real — as duas metades da mudança observadas de verdade, não
presumidas — no próprio runbook.

Segue a mesma disciplina que a Fase 6.1 estabeleceu depois de um acidente real (D-19): rodar um ensaio
bloco a bloco, na mesma aba do SQL Editor, já transformou um ensaio em push real de produção numa fase
anterior deste projeto, só percebido comparando `updated_at_max` antes e depois.

Purpose: o `checkpoint:decision` do plano 15-06 só tem base se este ensaio existir e estiver registrado —
e, mais importante nesta fase específica, se as Provas 2.2/2.3 (as proteções que TÊM que continuar)
passaram de verdade. Relaxar demais é o risco real de um `create or replace function` sobre um predicado
de segurança.
Output: `RESULTADO DO ENSAIO` preenchido em `supabase/verificacao_relaxar_exclusao_destrava.sql`.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `15-01-PLAN.md`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-CONTEXT.md
@supabase/verificacao_relaxar_exclusao_destrava.sql
@supabase/migrations/20260826010000_relaxar_exclusao_destrava.sql
@supabase/verificacao_taxas_imobiliaria_lancamento_id.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rodar a Parte A contra produção e observar as duas metades da mudança</name>
  <precondition>`supabase/migrations/20260826010000_relaxar_exclusao_destrava.sql` e
  `supabase/verificacao_relaxar_exclusao_destrava.sql` existem no repositório (plano 15-01), e o predicado
  de `impedir_exclusao_de_card_com_lancamento()` ainda conta `destrava` como impeditivo no banco de
  produção.</precondition>
  <read_first>
    - `supabase/verificacao_relaxar_exclusao_destrava.sql` — Parte A inteira, principalmente o aviso de
      pooling no cabeçalho e as Provas 2.1/2.2/2.3
    - `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` — a seção `RESULTADO DO ENSAIO`, para ver
      o formato do registro que a Task 2 vai reproduzir
  </read_first>
  <what-built>
    O runbook de ensaio (`supabase/verificacao_relaxar_exclusao_destrava.sql`, Parte A) e a migração que
    ele ensaia (`20260826010000_relaxar_exclusao_destrava.sql`): o predicado relaxado de
    `impedir_exclusao_de_card_com_lancamento()`.
  </what-built>
  <how-to-verify>
    **Antes de qualquer coisa — escolha o caminho de execução (D-19). Só existem dois aceitos:**

    - **(a)** Supabase Studio → SQL Editor, colando a **Parte A INTEIRA** (BLOCO 2 do `begin;` até o
      `rollback;`) num **único** clique de "Run". Nunca bloco a bloco.
    - **(b)** `psql` ou Supabase CLI apontando para o banco de produção.

    **Passo 1 — baseline (BLOCO 1, fora de transação).** Anotar `cards_total`, `parcelas_total`,
    `lancamentos_total`, `cards_updated_at_max`.

    **Passo 2 — rodar o BLOCO 2 inteiro de uma vez.**

    **Passo 3 — ler cada prova e anotar o resultado real:**
    - 2.1 — **a prova mais importante deste ensaio, metade "relaxa":** o card criado só com lançamento
      `tipo='destrava'` foi excluído sem erro, e a mensagem `OK relaxado: ...` apareceu (não a exceção de
      falha). Se a exceção apareceu em vez disso, **pare aqui e reporte** — CANDEST-01 não vai funcionar
      em produção
    - 2.2 — **igualmente importante, metade "continua protegendo":** o card com `tipo='pagamento'`
      continuou recusando exclusão, e a mensagem `OK protegido: ...` apareceu. Se a exclusão passou sem
      erro, **pare aqui e reporte** — isso significa que a proteção contra apagar dinheiro de verdade
      quebrou, um problema mais sério do que não ter relaxado nada
    - 2.3 — o card com taxa (sem nenhum lançamento) também continuou recusando exclusão
    - 2.4 — a contagem de `pg_policies` não mudou
    **Passo 4 — rodar o BLOCO 3 (pós-`rollback`) e conferir a coisa mais importante do ensaio inteiro:** o
    corpo da função (via `pg_get_functiondef`) voltou a ser o antigo (sem o filtro de tipo), e os quatro
    números do Passo 1 batem. Se o corpo não voltou, o `rollback` não pegou — o ensaio virou push. Nesse
    caso **não** siga para a Task 2 como se tivesse dado certo: registre o acidente com honestidade e
    reporte antes de continuar.
  </how-to-verify>
  <acceptance_criteria>
    - O caminho de execução usado está declarado: "(a) único Run" ou "(b) psql/CLI"
    - Os quatro números do baseline estão anotados
    - A Prova 2.1 (relaxa) e as Provas 2.2/2.3 (continuam protegendo) foram observadas com sucesso — as
      três mensagens `OK ...`, não nenhuma exceção de falha
    - Depois do `rollback`, o corpo da função voltou ao estado anterior e os quatro números batem com o
      baseline
  </acceptance_criteria>
  <reversibility rating="reversible">O ensaio inteiro vive dentro de `begin; ... rollback;` e não deixa
  rastro no banco — desde que o caminho de execução do D-19 seja respeitado. É exatamente essa condição
  que o Passo 4 verifica.</reversibility>
  <resume-signal>Responda com os resultados dos Passos 1 a 4, ou com o que travou.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar o resultado do ensaio dentro do próprio runbook</name>
  <precondition>A Task 1 foi executada e os resultados de todas as provas estão disponíveis na
  conversa.</precondition>
  <files>supabase/verificacao_relaxar_exclusao_destrava.sql</files>
  <read_first>
    - `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` — a seção `RESULTADO DO ENSAIO`: contexto
      primeiro, depois os números, depois as lições. É esse o formato a repetir
    - `supabase/verificacao_relaxar_exclusao_destrava.sql` — o cabeçalho `RESULTADO DO ENSAIO` vazio
      deixado pela Task 2 do plano 15-01
  </read_first>
  <action>
Preencher o bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_relaxar_exclusao_destrava.sql` com o que
a Task 1 realmente observou. Escrever em comentário SQL, em português, contexto primeiro, números depois,
lições no fim.

Conteúdo obrigatório:
- a data do ensaio e qual dos dois caminhos de D-19 foi usado
- os quatro números do baseline e os quatro depois do `rollback`, lado a lado
- o resultado literal das Provas 2.1, 2.2 e 2.3 — as mensagens exatas observadas, não um resumo genérico
  "funcionou"
- a confirmação explícita de que o corpo da função voltou ao estado anterior depois do `rollback` — ou, se
  não voltou, o registro honesto do que aconteceu
- qualquer divergência entre o que o runbook previa e o que o banco respondeu, inclusive correção feita na
  hora

Se alguma correção foi feita em `20260826010000_relaxar_exclusao_destrava.sql` durante o ensaio, o ensaio
precisa ser refeito por inteiro com o arquivo corrigido antes de este bloco ser dado como fechado —
registrar quantas rodadas foram necessárias.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f=supabase/verificacao_relaxar_exclusao_destrava.sql && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "cards_total" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qiE "psql|CLI|único Run|unico Run" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "relaxad" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "protegid" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | wc -l | awk '$1 > 12 {print "ENSAIO_REGISTRADO"; exit} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - A seção `RESULTADO DO ENSAIO` tem mais de 12 linhas, cita `cards_total`, e cita explicitamente o
      resultado das provas de relaxamento E de proteção
    - O caminho de execução (único Run ou psql/CLI) está nomeado por escrito
    - Está escrito, numa frase direta, se o corpo da função sobreviveu ou não ao `rollback`
    - Nenhum arquivo além de `supabase/verificacao_relaxar_exclusao_destrava.sql` foi modificado por esta
      task
  </acceptance_criteria>
  <done>O runbook carrega o registro do ensaio real, com números, os resultados observados das três
  provas, caminho de execução e o veredito sobre o `rollback`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| operador → banco de produção | Este plano executa SQL contra o banco real. A única coisa que separa "ensaio" de "push" é a continuidade da sessão que envolve o `begin;`/`rollback;` |
| runbook → operador | O texto do runbook é a única instrução que o operador segue; uma instrução errada aqui já causou perda de controle uma vez (Fase 6.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-10 | Tampering | ensaio que commita DDL em produção (pool de conexões do Studio) | high | mitigate | D-19 aplicado literalmente: só dois caminhos aceitos; o Passo 4 checa `pg_get_functiondef` depois do `rollback` e manda parar se o corpo não voltou |
| T-15-11 | Tampering | cards de teste da Prova 2.1/2.2/2.3 sobrevivendo fora da transação | medium | mitigate | Toda gravação de teste vive dentro de `begin;...rollback;`; o Passo 4 confirma `cards_total` idêntico ao baseline |
| T-15-12 | Repudiation | ensaio não registrado, decisão do 15-06 sem base | medium | mitigate | Task 2 grava números, mensagens e caminho de execução dentro do próprio runbook, versionado no git |
| T-15-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala nenhum pacote |
</threat_model>

<verification>
- Observação direta do resultado de cada prova contra o banco de produção, dentro de transação revertida
- Comparação numérica antes/depois do `rollback`
- Checagem de `pg_get_functiondef` depois do `rollback` — a prova de que o ensaio foi ensaio
- `grep` sobre o bloco `RESULTADO DO ENSAIO` confirmando o registro completo
</verification>

<success_criteria>
- O ensaio rodou por um caminho que garante continuidade de sessão, e isso está escrito
- As duas metades da mudança (relaxa destrava / protege dinheiro de verdade) foram observadas funcionando
- Nada persistiu no banco: corpo da função e quatro números idênticos depois do `rollback`
- `supabase/verificacao_relaxar_exclusao_destrava.sql` carrega o registro completo, commitado no git
</success_criteria>

<output>
Create `.planning/phases/15-exclus-o-de-card-com-destrava-e-pagina-o/15-04-SUMMARY.md` when done
</output>
