---
phase: 06.1-consulta-financeira-e-gera-o-por-per-odo
plan: 04
type: execute
wave: 4
depends_on: ["06.1-03"]
files_modified:
  - web/src/lib/kanban/parcelas.ts
  - web/src/lib/kanban/types.ts
  - "web/src/app/(app)/financeiro/page.tsx"
  - web/src/components/financeiro/financeiro-view.tsx
  - web/src/components/financeiro/parcelas-table.tsx
  - web/src/components/financeiro/id-pill.tsx
  - web/src/components/kanban/card-item.tsx
  - web/src/components/financeiro/mes-switcher.tsx
autonomous: false
requirements: [CONTRATO-03, PARCELA-05, CONSULTA-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Abrir /financeiro sem nenhum filtro mostra só as parcelas cujo `vencimento` é a data de hoje (`hojeISO`) — o seletor Mês atual/Próximo mês (`MesSwitcher`) foi removido, não apenas escondido, e o arquivo `mes-switcher.tsx` não existe mais — D-01, D-02, CONSULTA-02"
    - "O dia de vencimento de uma parcela NOVA sem `periodo_inicio` é o dia 20 (não mais o último dia do mês); parcelas com `periodo_inicio` continuam usando o dia do mês de `periodo_inicio`, limitado ao último dia do mês — D-10, D-11, PARCELA-05"
    - "Parcelas geradas ANTES desta mudança para contratos sem `periodo_inicio` continuam com `vencimento` no último dia do mês — nenhum código desta fase faz `UPDATE` em `parcelas.vencimento` de linha existente"
    - "Cada contrato mostra `#{numero}` no card do Board (grupo inferior direito, ao lado do pill Ativo/Inativo) e como a primeira coluna da tabela do Financeiro — D-07, D-08, CONTRATO-03"
    - "As ações Pagamento/Ajustar/Histórico da coluna Ações continuam exatamente como a Phase 6 deixou — nenhum arquivo de diálogo é tocado por este plano — D-06"
    - "Estado vazio do padrão (nenhuma parcela vence hoje): 'Nenhuma parcela vence hoje. Use os filtros para consultar outro período.' — distinto do estado 'sem contrato ativo', que continua reaproveitado da Phase 5"
    - "Mensagem de erro amendada: 'Não foi possível carregar as parcelas. Tente novamente.' (sem referência a um recorte mensal específico, que ficou incorreto com o novo padrão)"
    - "A pílula de ID cresce com o conteúdo (sem `min-width` fixo) para não cortar/apertar a partir de 3 dígitos"
    - "Estado 'nenhum contrato ativo' (nem padrão nem filtrado tem contrato ativo algum) continua reaproveitando o texto da Phase 5 verbatim e continua sendo a primeira checagem, antes de decidir entre padrão/filtrado — UI Consideration já resolvida em Phase 5, não re-litigada aqui"
  artifacts:
    - web/src/components/financeiro/id-pill.tsx
  key_links:
    - "`FinanceiroPage` -> `garantirParcelas(supabase, competencias)` continua com a MESMA assinatura e o MESMO escopo de geração (mês atual + próximo) — este plano só muda a leitura (`vencimento = hojeISO`) e o fallback de dia; o escopo de geração (retroativo) é do plano 06.1-06"
    - "`CardItem` -> `IdPill` -> `card.numero` (precisa da coluna `cards.numero` viva, aplicada no plano 06.1-03)"
    - "`ParcelasTable` -> `IdPill` -> `linha.numero` (precisa do embed `cards(numero)` no `.select()` de `page.tsx`)"
---

<objective>
Fatia vertical ponta a ponta: a aba Financeiro passa a mostrar, por padrão, só as parcelas vencendo hoje (D-01/D-02); o vencimento de parcelas novas sem `periodo_inicio` passa a cair no dia 20 (D-10/D-11); e o identificador sequencial do contrato aparece como pílula no card do Board e como primeira coluna da tabela do Financeiro (D-07/D-08).

Este plano NÃO implementa os filtros (proprietário/inquilino/período/ID — CONSULTA-01, plano 06.1-05) nem a geração retroativa (PARCELA-06, plano 06.1-06). É a fatia que prova a arquitetura: consulta padrão lendo direto de `vencimento`, e o `numero` do contrato circulando do banco até a tela.

Purpose: CONSULTA-02 e PARCELA-05 só existem quando um usuário real abre `/financeiro` e vê exatamente isso; CONTRATO-03 só está completo quando o número aparece nos dois lugares que o usuário pediu (Board e Financeiro), não só no banco.
Output: `/financeiro` com a visão "Vencendo hoje" funcionando ponta a ponta; pílula de ID visível no Board e no Financeiro.
</objective>

Este plano não cria símbolo de banco novo — ver `## Artifacts this phase produces` em `06.1-01-PLAN.md` para a lista canônica da fase inteira.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-CONTEXT.md
@.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-UI-SPEC.md
@.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-01-SUMMARY.md
@web/src/lib/kanban/parcelas.ts
@web/src/components/kanban/card-item.tsx
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: "Vencendo hoje" ponta a ponta + fallback de vencimento no dia 20</name>
  <files>web/src/lib/kanban/parcelas.ts, web/src/app/(app)/financeiro/page.tsx, web/src/components/financeiro/financeiro-view.tsx, web/src/components/financeiro/parcelas-table.tsx, web/src/components/financeiro/mes-switcher.tsx</files>
  <read_first>
    - `web/src/lib/kanban/parcelas.ts` — o arquivo real como está hoje, especialmente `vencimentoDaCompetencia` e seu comentário atual sobre A-02
    - `web/src/app/(app)/financeiro/page.tsx` — a leitura atual em duas competências (`linhasAtual`/`linhasProximo`)
    - `web/src/components/financeiro/financeiro-view.tsx` e `mes-switcher.tsx` — o que está sendo removido
    - `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-UI-SPEC.md` — seção Copywriting Contract (linha "Page subheading", "List heading (h2, default view)", "List subheading (default view)", "Empty state — default view", "Error state") e seção Visual Hierarchy
  </read_first>
  <action>
Em `web/src/lib/kanban/parcelas.ts`, alterar `vencimentoDaCompetencia`: o ramo sem `periodoInicio` passa de `ultimoDia` para o literal `20` — dia 20 existe em todo mês, então não precisa de `Math.min` nem capping (D-11). O ramo COM `periodoInicio` continua idêntico: `Math.min(Number(periodoInicio.split("-")[2]), ultimoDia)`. Reescrever o comentário da função para citar D-10/D-11 em vez de A-02, explicando explicitamente que isto SUBSTITUI a escolha da Phase 5 (última dia do mês, feita para não fazer contrato sem data cadastrada nascer "vencido" no dia seguinte) por um fallback fixo — decisão do usuário, não uma reversão silenciosa. Deixar claro no comentário que esta função só é chamada dentro de `parcelasFaltantes`, ao montar uma parcela NOVA — nunca num `UPDATE` contra `parcelas.vencimento` de uma linha já existente, então parcelas geradas antes desta mudança mantêm o vencimento antigo (último dia do mês) intocado.

Em `web/src/app/(app)/financeiro/page.tsx`: manter a chamada de geração exatamente como está — `const competencias = competenciasAlvo(hojeISO); await garantirParcelas(supabase, competencias)` — o escopo de geração (mês atual + próximo) não muda neste plano. Trocar só a LEITURA: em vez de `.in("competencia", competencias)` seguido da divisão em `parcelasDoMesAtual`/`parcelasDoProximoMes`, usar `.eq("vencimento", hojeISO)` no `.select()` de `parcelas` e produzir uma única lista via `montarLinhas(parcelas, hojeISO)`. Isso funciona porque `vencimentoDaCompetencia` sempre devolve uma data dentro do mesmo mês da `competencia` (só o dia muda) — logo uma parcela com `vencimento = hojeISO` é sempre da competência do mês corrente, que `garantirParcelas` já garante gerada. Atualizar o subtítulo do `h1` para a redação amendada do Copywriting Contract: "Parcelas vencendo hoje, por padrão. Use os filtros para consultar outro período, proprietário, inquilino ou contrato." Passar para `FinanceiroView` uma única prop `linhas` (não mais `linhasAtual`/`linhasProximo`).

Em `web/src/components/financeiro/financeiro-view.tsx`: remover o import e o uso de `MesSwitcher`, remover o `useState` de `competencia`. Receber `linhas: LinhaParcela[]` em vez de `linhasAtual`/`linhasProximo`. Calcular `vazio` como `temContratoAtivo ? "sem-parcela-hoje" : "sem-contrato-ativo"`. Renderizar `<ParcelasTable titulo="Vencendo hoje" subtitulo={formatDate(todayISO)} linhas={linhas} erro={erro} vazio={vazio} todayISO={todayISO} />`, importando `formatDate` de `@/lib/kanban/format`.

Excluir o arquivo `web/src/components/financeiro/mes-switcher.tsx` (D-01: removido, não escondido — sem call site restante).

Em `web/src/components/financeiro/parcelas-table.tsx`: adicionar uma prop opcional `subtitulo?: string`, renderizada como um parágrafo `text-sm text-muted-foreground` logo abaixo do `<h2>{titulo}</h2>` existente. Renomear a chave `"sem-parcela-no-periodo"` de `VAZIO_LABEL` para `"sem-parcela-hoje"`, com o texto "Nenhuma parcela vence hoje. Use os filtros para consultar outro período." Ampliar o tipo da prop `vazio` de `"sem-contrato-ativo" | "sem-parcela-no-periodo"` para `"sem-contrato-ativo" | "sem-parcela-hoje" | "sem-resultado-filtro"`, já incluindo a terceira chave em `VAZIO_LABEL` com o texto final "Nenhuma parcela encontrada para os filtros aplicados." — este terceiro estado só se torna alcançável no plano 06.1-05 (filtros), mas o texto já nasce definitivo, não um placeholder. Atualizar a mensagem de erro para "Não foi possível carregar as parcelas. Tente novamente." — a redação anterior (Phase 5) citava um recorte mensal específico no texto, que ficou impreciso agora que a visão padrão não é mais organizada por mês.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && test ! -f web/src/components/financeiro/mes-switcher.tsx && P="web/src/lib/kanban/parcelas.ts" && grep -A12 "export function vencimentoDaCompetencia" "$P" | grep -qE ':\s*20\s*$|:\s*20;' && F="web/src/app/(app)/financeiro/page.tsx" && grep -q '\.eq("vencimento", hojeISO)' "$F" && test "$(grep -c 'linhasAtual\|linhasProximo\|MesSwitcher' "$F" web/src/components/financeiro/financeiro-view.tsx)" -eq 0 && V="web/src/components/financeiro/parcelas-table.tsx" && grep -q "sem-parcela-hoje" "$V" && grep -q "sem-resultado-filtro" "$V" && grep -q "Não foi possível carregar as parcelas. Tente novamente." "$V" && test "$(grep -c 'deste mês' "$V")" -eq 0 && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` e `npm run build` passam
    - `web/src/components/financeiro/mes-switcher.tsx` não existe mais; nenhum arquivo cita `MesSwitcher`, `linhasAtual` ou `linhasProximo`
    - `vencimentoDaCompetencia` devolve `20` (não `ultimoDia`) quando `periodoInicio` é nulo, e continua com `Math.min(..., ultimoDia)` quando `periodoInicio` existe
    - `page.tsx` lê parcelas via `.eq("vencimento", hojeISO)`, não mais `.in("competencia", competencias)`
    - `parcelas-table.tsx` contém as chaves `sem-parcela-hoje` e `sem-resultado-filtro` em `VAZIO_LABEL`, e a mensagem de erro não cita mais "deste mês"
  </acceptance_criteria>
  <reversibility rating="reversible">Muda leitura e um fallback de data; nenhuma escrita nova em produção além da geração já existente.</reversibility>
  <done>Abrir `/financeiro` sem filtro mostra só as parcelas vencendo hoje, com o cabeçalho/subtítulo corretos; `MesSwitcher` não existe mais; parcelas novas sem `periodo_inicio` vencem no dia 20.</done>
</task>

<task type="auto">
  <name>Task 2: Pílula de ID sequencial — Board e Financeiro (CONTRATO-03)</name>
  <files>web/src/lib/kanban/types.ts, web/src/lib/kanban/parcelas.ts, web/src/app/(app)/financeiro/page.tsx, web/src/components/financeiro/id-pill.tsx, web/src/components/financeiro/parcelas-table.tsx, web/src/components/kanban/card-item.tsx</files>
  <read_first>
    - `web/src/components/financeiro/parcela-situacao-badge.tsx` — molde de componente de apresentação puro (sem "use client"), mesmo padrão que `IdPill` segue
    - `web/src/components/kanban/card-item.tsx` — a estrutura real do card, especialmente a `div.mt-2.flex.items-center.justify-between.gap-2` que já tem `valor` e o pill Ativo/Inativo
    - `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-UI-SPEC.md` — seção "Board card — ID pill placement", com o JSX exato e a justificativa de peso visual (D-08)
    - `web/src/lib/kanban/parcelas.ts` — `ParcelaComCard`, `LinhaParcela`, `montarLinhas` como ficaram depois da Task 1
  </read_first>
  <action>
Em `web/src/lib/kanban/types.ts`: acrescentar `numero: number` ao tipo `Card` (D-07/CONTRATO-03).

Criar `web/src/components/financeiro/id-pill.tsx` (novo, sem `"use client"` — componente de apresentação puro, mesmo molde de `parcela-situacao-badge.tsx`): exporta `IdPill({ numero }: { numero: number })`, devolvendo `<span className="inline-flex h-5 items-center rounded-full bg-secondary/25 px-2 text-xs font-semibold tabular-nums text-secondary-foreground" aria-label={\`Contrato número ${numero}\`}>#{numero}</span>`. Sem `min-width` fixo — a largura cresce com o conteúdo, para não cortar/apertar a partir de 3 dígitos (UI Consideration "long-text").

Em `web/src/components/kanban/card-item.tsx`: importar `IdPill`. Na `div` `mt-2 flex items-center justify-between gap-2` (que hoje tem `valor` à esquerda e o `button` Ativo/Inativo à direita como irmão direto), envolver o `button` existente e o novo `<IdPill numero={card.numero} />` num `div` `flex items-center gap-1.5`, com `IdPill` ANTES do `button`, exatamente na ordem `[ #12 ] [ Ativo ]` — nenhuma outra mudança no card.

Em `web/src/lib/kanban/parcelas.ts`: estender o tipo `ParcelaComCard.cards` de `{ endereco: string; proprietario: string } | null` para `{ endereco: string; proprietario: string; numero: number } | null`; acrescentar `numero: number` a `LinhaParcela`; em `montarLinhas`, popular `numero: parcela.cards?.numero ?? 0` (mesmo padrão defensivo já usado para `endereco`/`proprietario` quando o embed vem nulo).

Em `web/src/app/(app)/financeiro/page.tsx`: estender o embed do `.select()` de `cards(endereco, proprietario)` para `cards(endereco, proprietario, numero)`.

Em `web/src/components/financeiro/parcelas-table.tsx`: importar `IdPill`. Acrescentar `<TableHead>ID</TableHead>` como a PRIMEIRA coluna do `<TableHeader>`, antes de "Imóvel", e `<TableCell><IdPill numero={linha.numero} /></TableCell>` como a primeira célula de cada linha, antes da célula de endereço.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && grep -q "numero: number" web/src/lib/kanban/types.ts && test -f web/src/components/financeiro/id-pill.tsx && grep -q "aria-label" web/src/components/financeiro/id-pill.tsx && grep -q "bg-secondary/25" web/src/components/financeiro/id-pill.tsx && test "$(grep -c 'min-width\|min-w-' web/src/components/financeiro/id-pill.tsx)" -eq 0 && grep -q "IdPill" web/src/components/kanban/card-item.tsx && grep -q "IdPill" web/src/components/financeiro/parcelas-table.tsx && F="web/src/app/(app)/financeiro/page.tsx" && grep -q "cards(endereco, proprietario, numero)" "$F" && echo DISPLAY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` e `npm run build` passam
    - `Card` em `types.ts` tem `numero: number`
    - `id-pill.tsx` existe, usa `aria-label`, usa `bg-secondary/25` e não usa nenhum `min-width`/`min-w-`
    - `card-item.tsx` e `parcelas-table.tsx` importam/usam `IdPill`
    - `page.tsx` estende o embed para `cards(endereco, proprietario, numero)`
  </acceptance_criteria>
  <reversibility rating="reversible">Só leitura/exibição de um campo já existente no banco (aplicado no plano 06.1-03); nenhuma escrita nova.</reversibility>
  <done>O card do Board mostra `#{numero}` ao lado do pill Ativo/Inativo, sem competir com `valor`; a tabela do Financeiro mostra `#{numero}` como primeira coluna.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar em produção — vencendo hoje, dia 20, e a pílula de ID</name>
  <precondition>As Tasks 1 e 2 estão commitadas e `npm run lint && npm run build` passam.</precondition>
  <read_first>
    - `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-UI-SPEC.md` — Copywriting Contract e Visual Hierarchy
  </read_first>
  <what-built>
    A aba Financeiro agora mostra por padrão só as parcelas vencendo hoje (sem o seletor de mês, removido); parcelas novas sem `periodo_inicio` passam a vencer no dia 20; e cada contrato mostra `#{numero}` no Board e no Financeiro.
  </what-built>
  <how-to-verify>
    1. Abra `/financeiro` sem nenhum filtro. Confirme o cabeçalho "Vencendo hoje" com a data de hoje como subtítulo, e que a tabela só lista parcelas cujo vencimento é hoje — compare com `select count(*) from public.parcelas where vencimento = current_date;` no SQL Editor.
    2. Se não houver nenhuma parcela vencendo hoje (situação mais comum), confirme a mensagem "Nenhuma parcela vence hoje. Use os filtros para consultar outro período." — distinta da mensagem de "sem contrato ativo".
    3. Confirme que a primeira coluna da tabela mostra `#N` batendo com o `numero` do card correspondente (comparar com o Board).
    4. Abra o Board e confirme que cada card mostra a pílula `#N` no canto inferior direito, ao lado do pill Ativo/Inativo, sem competir visualmente com o valor do aluguel.
    5. Rode `select vencimento from public.parcelas p join public.cards c on c.id = p.card_id where c.periodo_inicio is null order by p.competencia desc limit 5;` e confirme que as parcelas geradas ANTES desta mudança continuam com vencimento no último dia do mês — não foram reescritas.
    6. Se possível, force a geração de uma parcela nova para um contrato de teste sem `periodo_inicio` (ex.: avançando para o próximo mês de competência ou usando um card de teste) e confirme que o vencimento cai no dia 20.
  </how-to-verify>
  <acceptance_criteria>
    - Passo 1: contagem da tela bate com a consulta SQL
    - Passo 2: mensagem correta quando não há parcela vencendo hoje
    - Passo 3: coluna ID bate com o `numero` do card
    - Passo 4: pílula visível no Board, sem competir com `valor`
    - Passo 5: parcelas antigas mantêm o vencimento antigo (última dia do mês), não reescritas
    - Passo 6: parcela nova sem `periodo_inicio` vence no dia 20
    - Resultado reportado com os números/observações reais, não um "está tudo certo"
  </acceptance_criteria>
  <reversibility rating="reversible">Só leitura e navegação; nenhuma escrita além do que a geração preguiçosa já faz normalmente.</reversibility>
  <resume-signal>Cole os resultados dos 6 passos, ou descreva o que diverge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| navegador -> Server Component | `page.tsx` continua lendo com a sessão do usuário; nenhum parâmetro controlado pelo cliente entra nesta consulta (a query padrão usa só `hojeISO`, calculado no servidor) |
| leitura de `cards.numero` via embed | Mesma policy de `cards` já cobre; nenhuma superfície nova de leitura |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-13 | Information Disclosure | Embed `cards(numero)` em `parcelas.select()` | low | accept | Mesma policy `is_team_member()` de `cards` já cobre; nenhuma coluna sensível nova exposta (número sequencial, não dado pessoal) |
| T-06.1-14 | Tampering | `vencimentoDaCompetencia` reescrever vencimento de parcela já existente | high | mitigate | A função só é chamada dentro de `parcelasFaltantes`, ao montar uma linha NOVA para `insert`; nenhum `.update()` deste plano toca `parcelas.vencimento`. Verificação manual (Task 3, passo 5) confirma contra dado real |
| T-06.1-15 | Tampering | Escrita de geração continuar via cliente de sessão, não `service_role` | high | mitigate | `garantirParcelas` não muda de cliente Supabase neste plano — mesma função, mesmo parâmetro `supabase` recebido de fora |

**Supply chain:** nenhuma instalação de pacote neste plano.
</threat_model>

<verification>
1. `npm run lint && npm run build` passam nas duas tasks
2. `/financeiro` sem filtro mostra só parcelas vencendo hoje, com cabeçalho/subtítulo/estado-vazio corretos
3. Parcelas novas sem `periodo_inicio` vencem no dia 20; parcelas antigas não são reescritas
4. `#{numero}` aparece no Board e como primeira coluna do Financeiro, sem competir com `valor`
5. A coluna Ações (Pagamento/Ajustar/Histórico) continua idêntica — nenhum arquivo de diálogo tocado
</verification>

<success_criteria>
- Critérios 1 e 4 do ROADMAP satisfeitos: visão padrão "vencendo hoje" sem seletor de mês; regra de vencimento com fallback de dia 20
- Metade de exibição do critério 3 do ROADMAP satisfeita: `#{numero}` visível no Board e no Financeiro
- CONSULTA-02 e PARCELA-05 completos; CONTRATO-03 completo (schema + exibição)
</success_criteria>

<output>
Create `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-04-SUMMARY.md` when done
</output>
