---
phase: 06.1-consulta-financeira-e-gera-o-por-per-odo
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified:
  - supabase/verificacao_cards_numero.sql
  - supabase/migrations/20260818000000_cards_numero_sequencial.sql
autonomous: false
requirements: [CONTRATO-03]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A DDL da migração aplica sem erro contra o schema real de produção, dentro de uma transação que termina em `rollback` — nada é gravado"
    - "Rodar a DDL duas vezes seguidas na mesma transação não gera erro nem duplica numeração"
    - "Dentro da transação: `count(distinct numero) = cards_total = numero_max`, `numero_min = 1`, `sem_numero = 0`, `fora_de_ordem = 0`"
    - "A constraint `cards_numero_unico` recusa um `update` que tentasse repetir um `numero` já usado por outro card"
    - "O RLS de `cards` continua barrando o e-mail fora da allowlist e liberando o de dentro, com a coluna `numero` presente"
    - "Depois do `rollback`, `cards_total` e `updated_at_max` continuam idênticos ao BLOCO 1"
    - "O resultado do ensaio fica registrado por escrito no cabeçalho de `supabase/verificacao_cards_numero.sql`, com data"
  artifacts:
    - supabase/verificacao_cards_numero.sql
  key_links:
    - "BLOCO 3 (constraint única) só tem valor se o BLOCO 2 (backfill correto) já garantiu que existem pelo menos dois cards com `numero` distintos para tentar colidir"
---

<objective>
Provar contra o banco de produção real, **sem gravar nada**, que a migração escrita no plano 06.1-01 faz o que promete: aplica limpo, é reexecutável, numera os ~46 cards existentes sem duplicata e sem alterar a ordem de criação, a constraint única recusa colisão, e o RLS de `cards` continua valendo exatamente como antes.

Todo o ensaio acontece dentro de uma transação que termina em `rollback`. É o último ponto em que um erro na migração custa zero — depois dele vem o push, que é porta de mão única.

Purpose: transformar CONTRATO-03 de "está escrito no arquivo" em "foi observado acontecendo", único nível de prova disponível num projeto sem suíte automatizada.
Output: resultado do ensaio registrado no cabeçalho do runbook e, se o ensaio apontar problema, a migração corrigida.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `06.1-01-PLAN.md` para a lista canônica da fase inteira.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-CONTEXT.md
@supabase/verificacao_cards_numero.sql
@supabase/migrations/20260818000000_cards_numero_sequencial.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Ensaio da migração no SQL Editor de produção, com rollback</name>
  <precondition>O operador tem acesso ao SQL Editor do projeto Supabase de produção e `public.cards` tem pelo menos duas linhas (o ensaio compara `numero` entre dois cards no BLOCO 3).</precondition>
  <read_first>
    - `supabase/verificacao_cards_numero.sql` — o runbook completo; a Parte A é o que será rodado
    - `supabase/migrations/20260818000000_cards_numero_sequencial.sql` — para conferir que a DDL colada no BLOCO 2 corresponde ao arquivo de migração
  </read_first>
  <what-built>
    O plano 06.1-01 escreveu a migração aditiva do identificador sequencial de contrato (`public.cards_numero_seq`, `public.cards.numero`, constraint `cards_numero_unico`) e o runbook `supabase/verificacao_cards_numero.sql`, cuja Parte A é um ensaio completo que roda contra o banco real e desfaz tudo no fim.
  </what-built>
  <how-to-verify>
    Abra o SQL Editor do projeto Supabase de produção. Rode **um bloco de cada vez**, na mesma aba, na ordem.

    1. **BLOCO 1 (pré-voo)** — anote `cards_total`, `updated_at_max` e `com_numero` (deve ser 0).
    2. **BLOCO 4** — antes de rodar, troque o marcador `SEU-EMAIL-DA-ALLOWLIST@exemplo.com` pelo seu e-mail real, exatamente como aparece em `auth.users`.
    3. **BLOCOs 2 a 4** — selecione do `begin;` do BLOCO 2 até o fim do BLOCO 4 e rode de uma vez só (precisa ser atômico).
    4. Leia as mensagens/NOTICE e confirme, na ordem:
       - a DDL passou duas vezes sem erro (idempotência)
       - a consulta de integridade do BLOCO 2 deu `cards_total = numeros_distintos = numero_max`, `numero_min = 1`, `sem_numero = 0`
       - a consulta de ordem do BLOCO 2 deu `fora_de_ordem = 0`
       - o BLOCO 3 mostrou `OK recusado: cards_numero_unico` e nenhuma linha `FALHOU:`
       - o BLOCO 4 mostrou `count(*) = 0` sob o e-mail intruso, e um número maior que 0 sob o e-mail da allowlist
    5. **BLOCO 1 de novo**, agora fora da transação — `cards_total` e `updated_at_max` têm que estar idênticos aos do passo 1. Se mudaram, o `rollback` não rodou e é preciso avisar antes de qualquer coisa.
  </how-to-verify>
  <acceptance_criteria>
    - O operador reporta: DDL aplicou nas duas passagens sem erro
    - O operador reporta: `cards_total = numeros_distintos = numero_max`, `numero_min = 1`, `sem_numero = 0`, `fora_de_ordem = 0`
    - O operador reporta: `OK recusado: cards_numero_unico`, nenhuma linha `FALHOU:`
    - O operador reporta: BLOCO 4 devolveu `count(*) = 0` sob o e-mail intruso e leu linhas sob o e-mail da allowlist
    - O operador reporta: `cards_total`/`updated_at_max` idênticos antes e depois da transação
  </acceptance_criteria>
  <reversibility rating="reversible">O ensaio inteiro está dentro de `begin; ... rollback;`. Se algo der errado no meio, o Postgres aborta a transação e nada persiste.</reversibility>
  <resume-signal>Cole o resumo dos NOTICE (idempotência, integridade, ordem, `OK recusado`, os dois `count(*)` do BLOCO 4) e os dois números do BLOCO 1 antes/depois, ou descreva o erro encontrado.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar o resultado do ensaio e aplicar as correções que ele apontou</name>
  <files>supabase/verificacao_cards_numero.sql, supabase/migrations/20260818000000_cards_numero_sequencial.sql</files>
  <read_first>
    - `supabase/verificacao_cards_numero.sql` — o cabeçalho onde o registro entra
    - O relato do operador na Task 1
  </read_first>
  <action>
Acrescentar no topo de `supabase/verificacao_cards_numero.sql`, logo abaixo do cabeçalho existente, um bloco de comentário rotulado `RESULTADO DO ENSAIO` contendo: a data do ensaio; a contagem de cards antes e depois da transação; a confirmação de `cards_total = numeros_distintos = numero_max`, `numero_min = 1`, `sem_numero = 0`, `fora_de_ordem = 0`; a confirmação de `OK recusado: cards_numero_unico`; e o veredito do BLOCO 4 (contagem zero sob e-mail intruso, contagem positiva sob e-mail da allowlist). Escrever em português, no mesmo tom "contexto primeiro, resultado depois" já usado em `verificacao_financeiro.sql`.

Se qualquer mensagem `FALHOU:` apareceu, ou algum número não bateu com o esperado, corrigir a causa no arquivo de migração, atualizar a cópia da DDL no BLOCO 2 do runbook para bater com a migração corrigida, e pedir novo ensaio — não registrar `RESULTADO DO ENSAIO` como aprovado enquanto houver pendência.

Se o ensaio passou inteiro, nenhuma alteração na migração é necessária — registrar isso explicitamente, para que a ausência de mudança não pareça esquecimento.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && R=supabase/verificacao_cards_numero.sql && grep -q "RESULTADO DO ENSAIO" "$R" && grep -qE '20[0-9]{2}-[0-9]{2}-[0-9]{2}' "$R" && test "$(grep -c 'FALHOU: pendente' "$R")" -eq 0 && M=supabase/migrations/20260818000000_cards_numero_sequencial.sql && S=$(grep -v '^[[:space:]]*--' "$M") && test "$(echo "$S" | grep -Ec 'drop (table|column)|alter column .* type|rename')" -eq 0 && test "$(echo "$S" | grep -Ec 'create policy|enable row level security')" -eq 0 && echo ENSAIO_REGISTRADO</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/verificacao_cards_numero.sql` contém um bloco `RESULTADO DO ENSAIO` com data no formato `AAAA-MM-DD`
    - Esse bloco cita as contagens antes/depois, os vereditos de integridade/ordem/constraint/RLS
    - Nenhum `FALHOU:` continua pendente — se houve, a migração foi corrigida e um novo ensaio foi registrado
    - A migração continua sem DDL destrutiva e sem policy nova/alterada, ignorando comentários
    - A cópia da DDL no BLOCO 2 do runbook corresponde ao conteúdo atual do arquivo de migração
  </acceptance_criteria>
  <reversibility rating="reversible">Edita dois arquivos `.sql` no repositório; o banco continua sem nenhuma alteração aplicada.</reversibility>
  <done>O resultado do ensaio está escrito no runbook com data, e a migração está no estado exato que vai ser empurrado no plano 06.1-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| operador -> SQL Editor de produção | Roda como superusuário, ignora RLS por padrão — por isso o BLOCO 4 troca de papel explicitamente antes de medir qualquer coisa |
| transação de ensaio -> dados reais | Um `update` de teste (BLOCO 3) toca em cards reais dentro da transação; só o `rollback` impede persistência |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-06 | Tampering | Linhas/updates de teste do ensaio | high | mitigate | Parte A inteira é uma transação terminando em `rollback`; o passo 5 do roteiro reexecuta o BLOCO 1 fora da transação e exige `cards_total`/`updated_at_max` idênticos |
| T-06.1-07 | Spoofing | Falso negativo de RLS por falta de dado, não por RLS funcionando | medium | mitigate | O BLOCO 4 troca para um e-mail real da allowlist depois do teste negativo, provando que o `count(*) = 0` anterior era RLS e não ausência de linhas |
| T-06.1-08 | Spoofing | E-mail de teste que existe de verdade | medium | mitigate | `intruso-teste@exemplo.invalid` usa o TLD `.invalid`, reservado pela RFC 2606 |

**Supply chain:** nenhuma instalação de pacote neste plano.
</threat_model>

<verification>
1. O operador rodou a Parte A no SQL Editor de produção e reportou idempotência, integridade, ordem, recusa de duplicata e RLS inalterado
2. `cards_total`/`updated_at_max` idênticos antes e depois da transação — o `rollback` funcionou
3. `supabase/verificacao_cards_numero.sql` traz o bloco `RESULTADO DO ENSAIO` com data e os vereditos
4. A migração está no estado final a ser empurrado no plano 06.1-03
</verification>

<success_criteria>
- D-09 foi observado, não só escrito: a migração aplica limpo, numera corretamente, recusa duplicata e não afrouxa RLS
- A migração está congelada e pronta para o push do plano 06.1-03
</success_criteria>

<output>
Create `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-02-SUMMARY.md` when done
</output>
