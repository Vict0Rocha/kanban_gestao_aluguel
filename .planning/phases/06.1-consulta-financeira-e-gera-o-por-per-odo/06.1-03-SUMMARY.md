---
phase: 06.1-consulta-financeira-e-gera-o-por-per-odo
plan: 03
subsystem: database
tags: [postgres, supabase, migration, verification, docs]

# Dependency graph
requires:
  - phase: 06.1-02
    provides: migração `20260818000000_cards_numero_sequencial.sql` já aplicada e commitada de verdade em produção (fora do fluxo planejado, durante um "ensaio" que o pooling de conexão do SQL Editor transformou em push real) — ver 06.1-02-SUMMARY.md
provides:
  - Verificação pós-push completa de `cards.numero` contra o schema real de produção: coluna, default, constraint, sequence-ownership e integridade de dados, mais o teste funcional no navegador (board carrega igual, card novo recebe `numero` automaticamente)
  - `docs/data-model.md` documentando `cards.numero` no diagrama de entidades, na lista de entidades e em "Decisões de design" (sequence + backfill único vs. `uuid` vs. recálculo na leitura)
affects: [06.1-04, 06.1-05, 06.1-06]

actuals:
  tokens: 2200
  tasks: 3
  commits: 1

tech-stack:
  added: []
  patterns:
    - "Migração aplicada fora do fluxo CLI (mesmo padrão do precedente em 04-03-SUMMARY.md): supabase CLI ausente/não linkado nesta máquina — tracking de `supabase migration list` fica manually-applied-only para esta migração, sem risco porque a DDL é idempotente"

key-files:
  created: []
  modified:
    - docs/data-model.md

key-decisions:
  - "Task 1 (checkpoint:decision original do plano) não foi re-perguntada: já havia sido resolvida de fato durante o checkpoint do plano 06.1-02, quando o orquestrador levou ao usuário a decisão de aceitar a migração como aplicada em vez de reverter — o usuário escolheu aceitar. Este plano só registra essa decisão já tomada, não solicita uma nova"
  - "Task 2 não reaplicou a DDL (já viva em produção, reaplicar seria um no-op idempotente mas sem necessidade de arriscar mais um round-trip no SQL Editor). Checado `supabase migration list` — CLI não instalado nesta máquina — e não se tentou login interativo; seguido o mesmo precedente documentado em 04-03-SUMMARY.md de aceitar o tracking de migração como manually-applied-only"
  - "Task 3 reaproveitou os resultados já coletados durante o checkpoint do plano 06.1-02 (BLOCO 5/6-equivalentes: coluna, constraint, integridade) em vez de pedir ao operador para repetir os mesmos comandos; só o que faltava (sequence-ownership e o teste de navegador) foi solicitado neste plano"

patterns-established: []

requirements-completed: [CONTRATO-03]

coverage:
  - id: D1
    description: "cards.numero vivo em produção: coluna integer not null com default nextval, constraint cards_numero_unico, sequence pg_get_serial_sequence apontando para public.cards_numero_seq"
    requirement: CONTRATO-03
    verification:
      - kind: manual_procedural
        ref: "information_schema.columns e pg_constraint verificados no plano 06.1-02 (RESULTADO DO ENSAIO); pg_get_serial_sequence('public.cards','numero') verificado neste plano — retornou public.cards_numero_seq"
        status: pass
    human_judgment: true
    rationale: "Verificação só pode ser feita por um humano com acesso ao SQL Editor de produção; não existe suíte automatizada nem ambiente de staging neste projeto."
  - id: D2
    description: "Integridade de dados pós-push: cards_total = numeros_distintos = numero_max = 47, numero_min = 1, sem_numero = 0"
    requirement: CONTRATO-03
    verification:
      - kind: manual_procedural
        ref: "BLOCO 6-equivalente já rodado e registrado no RESULTADO DO ENSAIO do plano 06.1-02 contra o schema real (não uma transação revertida) — reaproveitado aqui, não re-executado"
        status: pass
    human_judgment: true
    rationale: "Mesma fonte de evidência do plano 06.1-02: dados já batidos contra o schema real de produção, sem necessidade de repetir a mesma query."
  - id: D3
    description: "Board carrega no navegador com os mesmos cards de antes, e um card novo recebe o próximo numero automaticamente sem nenhuma mudança em createCardAction"
    requirement: CONTRATO-03
    verification:
      - kind: manual_procedural
        ref: "Operador logou com conta da allowlist, confirmou board carregando com os mesmos cards de sempre, criou um card de teste pelo app; select numero from public.cards order by numero desc limit 1 retornou 48 — o próximo número depois dos 47 existentes"
        status: pass
    human_judgment: true
    rationale: "Único jeito de provar que createCardAction não precisa de nenhuma mudança de código é criar um card de verdade pelo app e conferir o numero atribuído contra o banco real."
  - id: D4
    description: "docs/data-model.md documenta a entidade cards.numero e o porquê da sequence com backfill, no estilo decisão + porquê das entradas existentes"
    requirement: CONTRATO-03
    verification:
      - kind: other
        ref: "grep automatizado da Task 2 <verify>: numero e sequence presentes em docs/data-model.md"
        status: pass
    human_judgment: false
---

# Phase 6.1 Plan 03: Verificação pós-push e documentação de `cards.numero` Summary

**A migração `cards.numero` — já aplicada de fato em produção durante o "ensaio" do plano 06.1-02 — foi verificada completamente contra o schema real (coluna, constraint, sequence-ownership, integridade de dados) e testada no navegador (board carrega igual, card novo recebe `numero` automaticamente); `docs/data-model.md` documenta a decisão de design.**

## Performance

- **Tasks:** 3 (1 decisão já resolvida fora deste plano + 1 auto + 1 checkpoint humano)
- **Files modified:** 1 (`docs/data-model.md`)
- **Commits:** 1 (docs)

## Accomplishments

- Task 1 (checkpoint:decision original) reconhecida como já resolvida: durante o checkpoint do plano 06.1-02, o orquestrador levou ao usuário a decisão de aceitar a migração como aplicada em produção em vez de reverter, e o usuário escolheu aceitar — nenhuma pergunta nova foi feita neste plano
- Task 2: checado que o Supabase CLI não está instalado/linkado nesta máquina (`supabase migration list` falhou); nenhuma tentativa de login interativo; DDL não reaplicada (já viva, reaplicar seria idempotente mas desnecessário); `docs/data-model.md` atualizado — `int numero UK` no diagrama de entidades, descrição de `cards` estendida, e uma nova entrada em "Decisões de design" explicando por que a numeração vem de uma `sequence` com backfill único em vez de um `uuid` ou de um recálculo na leitura
- Task 3: sequence-ownership confirmada (`pg_get_serial_sequence('public.cards', 'numero')` → `public.cards_numero_seq`) e teste funcional completo no navegador — login com conta da allowlist, board carregando com os mesmos cards de sempre, card de teste criado pelo app recebendo `numero = 48` (o próximo depois dos 47 existentes), provando que `createCardAction` não precisou de nenhuma mudança de código
- Todos os itens de BLOCO 5/6 (coluna, constraint, integridade) já haviam sido verificados durante o checkpoint do plano 06.1-02 contra o schema real — reaproveitados aqui em vez de pedir ao operador para repetir os mesmos comandos

## Task Commits

1. **Task 1: Autorizar a aplicação da migração** — sem commit próprio; decisão já tomada e executada durante o plano 06.1-02 (ver `06.1-02-SUMMARY.md`, `key-decisions[0]`)
2. **Task 2: Aplicar a migração (no-op, já viva) e documentar em `docs/data-model.md`** — `e0eb9a6` (docs)
3. **Task 3: Confirmar em produção que a numeração nasceu correta** — checkpoint humano, sem commit de código (verificação de leitura + teste funcional no app real)

**Plan metadata:** commit deste SUMMARY.md (a seguir)

## Files Created/Modified

- `docs/data-model.md` — `int numero UK` adicionado ao bloco `CARDS` do diagrama mermaid; descrição de `cards` na lista de entidades estendida para citar `numero`; nova entrada em "Decisões de design" ("Identificador sequencial por `sequence` com backfill único, não `uuid` nem recálculo na leitura") explicando o porquê da sequence e por que recalcular na leitura quebraria referências salvas

## Decisions Made

Ver `key-decisions` no frontmatter. Resumo:
- Task 1 não foi re-perguntada — já resolvida no plano 06.1-02
- Task 2 não reaplicou a DDL (já viva); CLI ausente, mesmo precedente da Phase 4 (`04-03-SUMMARY.md`) de tracking manually-applied-only
- Task 3 reaproveitou evidência já coletada no plano 06.1-02 (BLOCO 5/6) e só pediu ao operador o que ainda faltava: sequence-ownership e o teste de navegador com card real

## Deviations from Plan

### O plano original assumia um Task 1 interativo e um push ainda não aplicado — ambos já haviam acontecido de fato antes deste plano começar

**Issue:** O `PLAN.md` original de 06.1-03 foi escrito assumindo que a migração ainda não tinha sido aplicada e que a Task 1 (`checkpoint:decision`) seria uma pergunta nova, gating um `supabase db push` real. Na prática, o plano 06.1-02 já havia aplicado a migração de verdade (via o gotcha de connection pooling do SQL Editor documentado em `06.1-02-SUMMARY.md`), e a decisão de "aceitar como aplicada" já tinha sido tomada pelo usuário antes deste plano começar.

**Ação tomada:** Executado com adaptação explícita, orientada pelo orquestrador (ver prompt de execução deste plano): Task 1 registrada como já resolvida (sem nova pergunta); Task 2 reduzida a checar o tracking do CLI e documentar (sem reaplicar DDL); Task 3 mantida como checkpoint vivo, mas reaproveitando evidência já coletada em 06.1-02 e pedindo ao operador só o que faltava (sequence-ownership + teste de navegador).

**Impacto:** Nenhum no resultado técnico — todos os critérios de aceite originais das três tasks estão satisfeitos, com evidência concreta contra o banco real, só que coletada em duas sessões (06.1-02 + 06.1-03) em vez de uma.

---

**Total deviations:** 1 (plano adaptado à realidade já registrada em 06.1-02-SUMMARY.md; nenhuma mudança de resultado técnico, só de sequenciamento de quando cada verificação foi coletada)
**Impact on plan:** CONTRATO-03 está totalmente verificado contra produção real — coluna, constraint, sequence, integridade de dados e comportamento funcional no app, sem nenhuma pendência.

## Issues Encountered

Nenhum issue novo nesta execução — a migração já estava aplicada e íntegra desde o plano 06.1-02; este plano só completou a verificação (sequence-ownership + navegador) e a documentação que ainda faltavam.

## User Setup Required

None — nenhuma configuração externa adicional necessária.

## Next Phase Readiness

- `cards.numero` está totalmente verificado e documentado em produção: coluna, constraint, sequence-ownership, integridade de dados e comportamento funcional no app (card novo recebe `numero` automaticamente, sem nenhuma mudança de código)
- CONTRATO-03 está pronto para os planos 06.1-04 a 06.1-06 se apoiarem nele sem reimplementar nada — o plano 06.1-04 (exibição do ID no Board e no Financeiro) pode prosseguir sem bloqueio
- Nenhuma pendência aberta relacionada a esta migração

## Self-Check: PASSED

- FOUND: `docs/data-model.md` (modificado, contém `numero` e `sequence`)
- FOUND commit: `e0eb9a6`

---
*Phase: 06.1-consulta-financeira-e-gera-o-por-per-odo*
*Completed: 2026-08-18*
