---
phase: 06.1-consulta-financeira-e-gera-o-por-per-odo
plan: 03
type: execute
wave: 3
depends_on: ["06.1-02"]
files_modified:
  - supabase/migrations/20260818000000_cards_numero_sequencial.sql
  - supabase/verificacao_cards_numero.sql
  - docs/data-model.md
autonomous: false
requirements: [CONTRATO-03]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A migração `20260818000000_cards_numero_sequencial` aplica no banco de produção sem erro (via `supabase db push` ou, se o CLI não estiver disponível, colando a DDL no SQL Editor — mesmo fallback documentado em 04-03-SUMMARY.md)"
    - "Depois do push, `cards_total` e `updated_at_max` permanecem idênticos aos anotados antes, e `sem_numero = 0` — nenhum card apagado, alterado ou sem `numero`"
    - "`information_schema.columns` mostra `numero integer` `not null` com default citando `nextval`; `pg_constraint` lista `cards_numero_unico`; `pg_get_serial_sequence` aponta para `public.cards_numero_seq`"
    - "`docs/data-model.md` documenta a entidade `cards.numero` e o porquê da sequence com backfill, no mesmo estilo 'decisão + porquê' das entradas existentes"
    - "O board carrega no navegador com os mesmos cards de antes, e criar um card novo atribui a ele o próximo `numero` automaticamente, sem nenhuma mudança em `createCardAction`"
  artifacts:
    - docs/data-model.md
  key_links:
    - "migração aplicada -> `public.is_team_member()`, já existente, não redefinida aqui"
    - "`cards.numero` -> `createCardAction` (web/src/lib/kanban/actions.ts), que continua sem passar `numero` no `insert` — o banco preenche via `nextval()`"
---

<objective>
Aplicar a migração `cards.numero` no banco de produção e provar, contra o schema já gravado, que o identificador sequencial nasce correto — e documentar a decisão em `docs/data-model.md`.

Este é o único plano da fase que altera o banco real. `supabase db push` (ou o equivalente via SQL Editor) é **porta de mão única** — mesma régua da Phase 4. Por isso o push está atrás de um `checkpoint:decision` explícito.

Purpose: CONTRATO-03 só protege/serve alguma coisa depois de estar no banco onde os contratos moram — e o plano 06.1-04 (exibição do ID no Board e no Financeiro) depende desta coluna existir de verdade.
Output: `cards.numero` vivo em produção, verificação pós-push registrada, `docs/data-model.md` atualizado.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `06.1-01-PLAN.md`. Depois deste plano, `public.cards_numero_seq`, `public.cards.numero` e a constraint `cards_numero_unico` passam a existir de fato no banco.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-CONTEXT.md
@.planning/phases/04-funda-o-financeira/04-03-SUMMARY.md
@supabase/verificacao_cards_numero.sql
@supabase/migrations/20260818000000_cards_numero_sequencial.sql
@docs/data-model.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Autorizar a aplicação da migração `cards.numero` no banco de produção</name>
  <read_first>
    - `supabase/verificacao_cards_numero.sql` — o bloco `RESULTADO DO ENSAIO` gravado no plano 06.1-02
    - `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-02-SUMMARY.md` — o que o ensaio observou
  </read_first>
  <decision>
    Aplicar `20260818000000_cards_numero_sequencial.sql` agora contra o banco de produção.
  </decision>
  <context>
    O banco tem ~46 imóveis reais e não existe ambiente de staging. A migração é estritamente aditiva — só cria uma sequence, uma coluna e uma constraint — e o ensaio do plano 06.1-02 já observou aplicação limpa, numeração correta e sem duplicata, e RLS inalterado.

    O que **não** é reversível: depois do push, desfazer significa escrever outra migração contra produção. O runbook tem um bloco de rollback de emergência (BLOCO 7), mas ele apaga a coluna inteira — só serve enquanto o identificador sequencial ainda não estiver sendo usado de verdade em nenhum filtro ou card.
  </context>
  <options>
    <option id="aplicar-agora">
      <name>Aplicar agora</name>
      <pros>Desbloqueia o plano 06.1-04 (exibição do ID) imediatamente; o ensaio já cobriu tudo que dá para cobrir sem gravar</pros>
      <cons>É porta de mão única sobre dado real</cons>
    </option>
    <option id="conferir-backup-antes">
      <name>Conferir o backup antes de aplicar</name>
      <pros>Abre uma saída independente do BLOCO 7 — mesmo procedimento já usado em 04-03 (CSV manual da tabela `cards` pelo Table Editor, já que o plano gratuito do Supabase não tem backup automático)</pros>
      <cons>Atrasa a fase pelo tempo da conferência</cons>
    </option>
    <option id="adiar">
      <name>Adiar</name>
      <pros>Nada muda em produção hoje</pros>
      <cons>Os planos 06.1-04 a 06.1-06 ficam bloqueados — nenhum deles pode exibir ou filtrar por um `numero` que não existe</cons>
    </option>
  </options>
  <acceptance_criteria>
    - O usuário escolheu explicitamente uma das três opções
    - Se `conferir-backup-antes`, o backup foi anotado antes de seguir para a Task 2
    - Se `adiar`, as Tasks 2 e 3 não são executadas e o plano encerra com o motivo registrado
  </acceptance_criteria>
  <reversibility rating="one-way">O portão existe porque a task seguinte grava DDL em produção; desfazer exige outra migração sobre dados reais.</reversibility>
  <resume-signal>Responda: `aplicar-agora`, `conferir-backup-antes` (com a data do backup) ou `adiar` (com o motivo).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Aplicar a migração em produção e documentar a decisão em `docs/data-model.md`</name>
  <precondition>A Task 1 foi respondida com `aplicar-agora` ou `conferir-backup-antes`.</precondition>
  <files>supabase/migrations/20260818000000_cards_numero_sequencial.sql, docs/data-model.md</files>
  <read_first>
    - `supabase/migrations/20260818000000_cards_numero_sequencial.sql` — o arquivo a aplicar; conferir que é o estado aprovado no plano 06.1-02
    - `.planning/phases/04-funda-o-financeira/04-03-SUMMARY.md` — como o push foi feito de verdade na Phase 4 (CLI não instalado na máquina do operador; aplicado via SQL Editor colando a DDL diretamente) — repetir o mesmo caminho se o CLI continuar ausente
    - `docs/data-model.md` — diagrama de entidades e seção "Decisões de design", estilo "decisão + porquê" a seguir
  </read_first>
  <action>
Primeiro, checar se o Supabase CLI está instalado e linkado (`supabase migration list`). Se funcionar, rodar `supabase db push` a partir da raiz do repositório e confirmar com `supabase migration list` que `20260818000000` passa a aparecer em Local **e** Remote. Se o CLI não estiver instalado ou pedir autenticação interativa sem TTY disponível, seguir o mesmo caminho documentado em `04-03-SUMMARY.md`: colar a DDL completa do arquivo de migração diretamente no SQL Editor de produção e confirmar "Success. No rows returned" — sem o wrapper `begin/rollback` do ensaio, porque aqui a intenção é gravar de verdade. Registrar qual caminho foi usado.

Se o push falhar, ler o erro, corrigir a causa no arquivo de migração, atualizar `supabase/verificacao_cards_numero.sql` para continuar batendo, voltar ao ensaio do plano 06.1-02 e nunca tentar um segundo push sem reensaiar.

Depois do push bem-sucedido, atualizar `docs/data-model.md`: no diagrama de entidades (bloco `mermaid`), acrescentar `int numero UK` ao bloco `CARDS`; na lista de entidades, estender a descrição de `cards` mencionando `numero` — identificador sequencial (`#1`, `#2`…) exibido no card do Board e usado como filtro exato no Financeiro; e acrescentar um item em "Decisões de design", no mesmo estilo "decisão + porquê" já usado ali, explicando por que um `id uuid` não serve como identificador legível para humano e por que a numeração vem de uma `sequence` com um backfill único (`row_number() over (order by created_at, id)`) em vez de, por exemplo, recalcular a numeração a cada leitura (recalcular mudaria o número de um contrato antigo sempre que outro fosse excluído, quebrando qualquer filtro/link salvo por esse número).
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && grep -q "numero" docs/data-model.md && grep -qi "sequence" docs/data-model.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - O comando de push (CLI ou SQL Editor) terminou sem erro; se terminou com erro, o arquivo foi corrigido e o ensaio do plano 06.1-02 foi refeito antes de nova tentativa
    - `docs/data-model.md` cita `numero` no diagrama de entidades e na lista de entidades, e explica em "Decisões de design" por que a numeração vem de uma sequence com backfill em vez de um `uuid` ou de um recálculo na leitura
    - Nenhum arquivo fora de `supabase/migrations/20260818000000_cards_numero_sequencial.sql` e `docs/data-model.md` foi modificado por esta task
  </acceptance_criteria>
  <reversibility rating="one-way">Grava DDL no banco de produção. Desfazer exige outra migração; o BLOCO 7 do runbook só é saída enquanto o identificador ainda não estiver em uso real.</reversibility>
  <done>A migração `20260818000000_cards_numero_sequencial` está aplicada em produção e `docs/data-model.md` documenta a decisão.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar em produção que a numeração nasceu correta e que nada quebrou</name>
  <precondition>A Task 2 aplicou a migração com sucesso.</precondition>
  <read_first>
    - `supabase/verificacao_cards_numero.sql` — Parte B, BLOCOs 5 e 6
    - O `RESULTADO DO ENSAIO` registrado no plano 06.1-02, para comparar os números
  </read_first>
  <what-built>
    A migração foi aplicada no banco de produção: a sequence `cards_numero_seq`, a coluna `cards.numero` (sequencial, única, `not null`) e a constraint `cards_numero_unico`.
  </what-built>
  <how-to-verify>
    **No SQL Editor de produção**, rodar um bloco de cada vez:

    1. **BLOCO 5 (inventário)** — confirmar `numero` como `integer`, `is_nullable = NO`, `column_default` citando `nextval`; a constraint `cards_numero_unico` presente; `pg_get_serial_sequence` apontando para `public.cards_numero_seq`.
    2. **BLOCO 6 (integridade)** — `cards_total` e `updated_at_max` idênticos aos anotados antes do push; `sem_numero = 0`; `numeros_distintos = cards_total`.

    **No navegador:**

    3. Abra o app em produção, faça login com uma conta da allowlist. O board tem que carregar com os mesmos cards de sempre.
    4. Crie um card novo de teste (ou peça para o operador criar um e depois excluir) e confirme via SQL (`select numero from public.cards order by numero desc limit 1;`) que ele recebeu o próximo número da sequência — prova de que `createCardAction` não precisou de nenhuma mudança de código para o `numero` nascer certo.
  </how-to-verify>
  <acceptance_criteria>
    - BLOCO 5: `numero` é `integer not null` com default citando `nextval`; constraint `cards_numero_unico` presente; sequence dona é `public.cards_numero_seq`
    - BLOCO 6: `cards_total`/`updated_at_max` idênticos aos de antes do push; `sem_numero = 0`; `numeros_distintos = cards_total`
    - Navegador: o board carrega com os mesmos cards; um card novo recebe automaticamente o próximo `numero`
    - O resultado é reportado com os números observados, não com um "está tudo certo"
  </acceptance_criteria>
  <reversibility rating="reversible">Só leitura no SQL Editor e uso normal do app; criar/excluir um card de teste é a mesma operação que qualquer uso legítimo já faz.</reversibility>
  <resume-signal>Cole os resultados dos BLOCOs 5 e 6, confirme que o board carregou, e informe o `numero` atribuído ao card de teste — ou descreva o que deu errado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| repositório -> banco de produção | `supabase db push` (ou SQL Editor) transporta DDL do disco para o banco onde moram os dados reais |
| usuário autenticado -> allowlist | A coluna `numero` passa a existir de fato; o RLS herdado de `cards` continua sendo a única coisa separando um login qualquer do dado |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-09 | Denial of Service | Push contra produção | high | mitigate | `checkpoint:decision` bloqueante (Task 1) com opção de conferir backup; DDL estritamente aditiva já ensaiada no plano 06.1-02; BLOCO 6 compara contagem/`updated_at_max` antes/depois |
| T-06.1-10 | Elevation of Privilege | Policy de `cards` aplicada com predicado errado | critical | mitigate | Esta fase não toca em nenhuma policy; BLOCO 5/6 não inventariam policy porque nenhuma foi criada — a ausência de qualquer `create policy`/`alter policy` no diff é o próprio portão (já verificado nos planos 06.1-01/06.1-02) |
| T-06.1-11 | Tampering | Regra de banco ausente depois do push | high | mitigate | BLOCO 5 confere `not null`, default e constraint por nome contra o schema já aplicado |
| T-06.1-12 | Repudiation | Numeração divergente do backfill ensaiado | medium | mitigate | BLOCO 6 reexecuta a integridade (`sem_numero`, `numeros_distintos`) contra o schema real, não contra um ensaio |

**Supply chain:** nenhuma instalação de pacote neste plano; único binário externo é o Supabase CLI, já em uso desde `20260728000000`.
</threat_model>

<verification>
1. `supabase migration list` mostra `20260818000000` em Local e Remote (ou, se via SQL Editor, o comando terminou com "Success")
2. BLOCO 5: coluna, default, constraint e sequence conferidos contra o schema real
3. BLOCO 6: contagem de cards e `updated_at_max` inalterados; `sem_numero = 0`
4. Navegador: board carrega igual; card novo recebe `numero` automaticamente
5. `docs/data-model.md` documenta a decisão
</verification>

<success_criteria>
- Critério 3 do ROADMAP (identificador sequencial atribuído automaticamente) satisfeito contra o banco aplicado — a metade de exibição fica para o plano 06.1-04
- CONTRATO-03 está vivo em produção, pronto para os planos 06.1-04 a 06.1-06 se apoiarem nele sem reimplementá-lo
</success_criteria>

<output>
Create `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-03-SUMMARY.md` when done
</output>
