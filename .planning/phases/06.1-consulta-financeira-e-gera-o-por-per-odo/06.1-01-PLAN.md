---
phase: 06.1-consulta-financeira-e-gera-o-por-per-odo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260818000000_cards_numero_sequencial.sql
  - supabase/verificacao_cards_numero.sql
autonomous: true
requirements: [CONTRATO-03]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "O arquivo de migração declara `public.cards_numero_seq`, a coluna `public.cards.numero integer` com default `nextval('public.cards_numero_seq')` e `not null`, e nenhuma instrução destrutiva (sem `drop table`, `drop column`, `alter column ... type`, `rename`) — D-09"
    - "O backfill dos ~46 cards existentes usa `row_number() over (order by created_at, id)`, só atinge linhas com `numero is null`, e a segunda passagem da mesma migração não duplica numeração — reexecutável por construção"
    - "A constraint `cards_numero_unico` cobre `unique (numero)` e é derrubada com `if exists` antes de ser recriada, no mesmo padrão de `20260816000000_financeiro_schema.sql`"
    - "A migração não cria, altera nem remove nenhuma policy de RLS — `cards` já está coberta por `is_team_member()` desde `20260811000000_security_hardening.sql`, e a coluna nova herda essa proteção sem predicado novo"
    - "`supabase/verificacao_cards_numero.sql` existe e contém, em blocos numerados no estilo de `verificacao_financeiro.sql`, o ensaio em transação (Parte A, com prova da constraint única e confirmação de RLS) e o esqueleto completo da verificação pós-push (Parte B)"
  artifacts:
    - supabase/migrations/20260818000000_cards_numero_sequencial.sql
    - supabase/verificacao_cards_numero.sql
  key_links:
    - "`public.cards.numero` -> default `nextval('public.cards_numero_seq')`, sequence possuída pela coluna via `alter sequence ... owned by`"
    - "constraint `cards_numero_unico` -> índice único implícito, base do filtro exato por ID de contrato que CONSULTA-01 vai usar (plano 06.1-05)"
    - "coluna `numero` -> policy existente `is_team_member()` de `public.cards`, não uma policy nova"
---

<objective>
Escrever a migração aditiva que dá a cada contrato um identificador sequencial (`#1`, `#2`, `#3`…) — `public.cards.numero`, sustentado por uma sequence e um backfill em ordem de criação para os ~46 cards já existentes — junto com o runbook que prova essa mecânica.

**Nada é aplicado em produção neste plano.** Só nascem dois arquivos `.sql`. O ensaio contra o banco real acontece no plano 06.1-02 e a aplicação no 06.1-03, cada um atrás do seu portão — mesma sequência de três plano da Phase 4 (04-01 → 04-02 → 04-03), escalada para o tamanho real desta mudança: uma coluna, não duas tabelas inteiras.

Purpose: CONTRATO-03 só existe se o identificador sequencial estiver gravado no banco, sobrevivendo a reload e a criação de novos contratos. D-09 exige o mesmo cuidado de migração-como-porta-de-mão-única já usado na Phase 4, porque o alvo é o mesmo banco de produção com os mesmos ~46 imóveis reais.
Output: `supabase/migrations/20260818000000_cards_numero_sequencial.sql` e `supabase/verificacao_cards_numero.sql`.
</objective>

## Artifacts this phase produces — Artefatos que toda a Phase 6.1 produz

Lista canônica de todo símbolo criado ou alterado pela Phase 6.1 inteira (migração + camada de aplicação). Os planos 06.1-02 a 06.1-06 se referem a esta lista em vez de repeti-la.

**Arquivos novos**
- `supabase/migrations/20260818000000_cards_numero_sequencial.sql` (este plano)
- `supabase/verificacao_cards_numero.sql` (este plano)
- `web/src/components/financeiro/id-pill.tsx` (plano 06.1-04)
- `web/src/components/financeiro/filtro-parcelas.tsx` (plano 06.1-05)
- `web/src/components/ui/collapsible.tsx` (plano 06.1-05)

**Arquivo removido**
- `web/src/components/financeiro/mes-switcher.tsx` (plano 06.1-04, D-01 — removido, não escondido)

**Arquivos alterados**
- `docs/data-model.md` (plano 06.1-03)
- `web/src/lib/kanban/types.ts` (plano 06.1-04 — `Card.numero`)
- `web/src/lib/kanban/parcelas.ts` (planos 06.1-04 e 06.1-06)
- `web/src/app/(app)/financeiro/page.tsx` (planos 06.1-04, 06.1-05 e 06.1-06)
- `web/src/components/financeiro/financeiro-view.tsx` (planos 06.1-04 e 06.1-05)
- `web/src/components/financeiro/parcelas-table.tsx` (planos 06.1-04 e 06.1-05)
- `web/src/components/kanban/card-item.tsx` (plano 06.1-04 — pílula de ID no card do Board)

**Coluna nova em tabela existente**
- `public.cards.numero` — `integer not null unique`, default `nextval('public.cards_numero_seq')`, backfill por `created_at` para os cards já existentes

**Sequence nova**
- `public.cards_numero_seq` — possuída por `public.cards.numero`

**Constraint nova**
- `cards_numero_unico` — `unique (numero)` em `public.cards`

**Funções puras novas/alteradas em `parcelas.ts`** (planos 06.1-04/06.1-06)
- `vencimentoDaCompetencia` — alterada (fallback vira dia 20, D-10/D-11)
- `competenciasDoPeriodo` — nova (PARCELA-06)
- `temPeriodoCompleto` — nova (discrimina D-12/D-14/D-15)
- `competenciasAlvoParaCard` — nova (roteia cada card entre período completo e mês atual/próximo)
- `parcelasFaltantes` — assinatura alterada (recebe `hojeISO` em vez de uma lista fixa de competências)
- `garantirParcelas` — assinatura alterada (recebe `hojeISO` em vez de `[string, string]`)

**Componentes novos** (planos 06.1-04/06.1-05)
- `IdPill` — `web/src/components/financeiro/id-pill.tsx`
- `FiltroParcelas` — `web/src/components/financeiro/filtro-parcelas.tsx`
- `Collapsible`/`CollapsibleTrigger`/`CollapsiblePanel` — `web/src/components/ui/collapsible.tsx`

**Nada é removido do banco.** A migração deste plano é 100% aditiva (D-09) — mesma régua da Phase 4.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-CONTEXT.md
@supabase/migrations/20260816000000_financeiro_schema.sql
@supabase/migrations/20260811000000_security_hardening.sql
@supabase/verificacao_financeiro.sql
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Fatia vertical — sequence, coluna, backfill e constraint única, da DDL ao bloco de prova</name>
  <files>supabase/migrations/20260818000000_cards_numero_sequencial.sql</files>
  <read_first>
    - `supabase/migrations/20260816000000_financeiro_schema.sql` — cabeçalho em português, `if not exists`/`drop constraint if exists`, comentários "decisão + porquê"
    - `supabase/migrations/20260728000000_init_schema.sql` — padrão de `create table`/índice deste projeto
    - `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-CONTEXT.md` — D-07 a D-09
  </read_first>
  <action>
Criar o arquivo de migração. Esta é a fatia vertical: `sequence` -> coluna -> backfill -> default -> `not null` -> constraint única, tudo num único arquivo reexecutável.

**Arquivo — `supabase/migrations/20260818000000_cards_numero_sequencial.sql`** (nome exato). Cabeçalho em português no estilo de `20260816000000_financeiro_schema.sql`: explica que esta migração adiciona o identificador sequencial de contrato (D-07/CONTRATO-03), que é estritamente aditiva — só cria uma sequence, uma coluna e uma constraint, nada é apagado, renomeado ou tem tipo alterado — e que é reexecutável.

Seção 1 — a sequence: `create sequence if not exists public.cards_numero_seq;`.

Seção 2 — a coluna, ainda sem `not null` (D-09): `alter table public.cards add column if not exists numero integer;`. Comentar que o `not null` só entra na Seção 4, depois do backfill, porque uma coluna nova não pode nascer `not null` sem default numa tabela que já tem ~46 linhas.

Seção 3 — backfill em ordem de criação, idempotente via `where numero is null`: um `with candidatos as (select id, row_number() over (order by created_at, id) as posicao from public.cards where numero is null) update public.cards as c set numero = (select coalesce(max(numero), 0) from public.cards) + candidatos.posicao from candidatos where c.id = candidatos.id;`. Comentar que o subselect `coalesce(max(numero), 0)` é resolvido uma única vez, contra o estado da tabela ANTES deste `update` (snapshot de início de statement do Postgres — um `update` nunca lê as próprias escritas em andamento), então rodar esta seção duas vezes na mesma migração não duplica numeração: na segunda passagem `candidatos` vem vazio (nenhum `numero is null` sobrando) e o `update` não toca em nada. A ordem `created_at, id` desempata determinismo entre cards criados no mesmo instante — mesmo padrão de desempate por segunda coluna já usado no índice único de `parcelas`.

Seção 4 — trava o default e o `not null`, e prepara a sequence para os próximos cards: `alter table public.cards alter column numero set default nextval('public.cards_numero_seq');`, depois `select setval('public.cards_numero_seq', (select coalesce(max(numero), 0) from public.cards) + 1, false);` (o terceiro argumento `false` faz o próximo `nextval()` devolver exatamente esse número, sem pular um), depois `alter table public.cards alter column numero set not null;`. Comentar que o `setval` é recalculado a cada execução a partir do `max(numero)` atual, então repetir a migração nunca desalinha a sequence — e que é por isso que `createCardAction` (em `web/src/lib/kanban/actions.ts`) não precisa de nenhuma mudança: o `insert` já feito por essa Server Action não passa `numero`, e o banco preenche via `nextval()` no default, exatamente como já acontece hoje com `id uuid default gen_random_uuid()`.

Seção 5 — a constraint única, no padrão `drop constraint if exists` já usado em `parcelas`/`parcela_lancamentos`: `alter table public.cards drop constraint if exists cards_numero_unico; alter table public.cards add constraint cards_numero_unico unique (numero);`.

Seção 6 — amarra a sequence à coluna, só para that `\d cards` no SQL Editor mostre a relação (documentação viva, sem mudar comportamento): `alter sequence public.cards_numero_seq owned by public.cards.numero;`.

Comentário final obrigatório, citando explicitamente T-06.1-01 e T-06.1-02 deste plano: nenhuma policy de RLS é criada, alterada ou removida por esta migração — `public.cards` já está coberta pela policy que usa `public.is_team_member()` desde `20260811000000_security_hardening.sql`, e a coluna nova herda essa mesma proteção sem precisar de predicado novo; e que a sequence é uma numeração de contratos dentro de um único board compartilhado por toda a allowlist — não expõe nada sobre um "outro tenant" porque, no desenho atual de board único, não existe outro tenant a proteger (ver PROJECT.md sobre o caminho de migração para SaaS multi-tenant, que precisaria revisitar esta decisão).
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && F=supabase/migrations/20260818000000_cards_numero_sequencial.sql && test -f "$F" && S=$(grep -v '^[[:space:]]*--' "$F") && echo "$S" | grep -q "create sequence if not exists public.cards_numero_seq" && echo "$S" | grep -q "add column if not exists numero integer" && echo "$S" | grep -q "where numero is null" && echo "$S" | grep -q "set default nextval" && echo "$S" | grep -q "set not null" && echo "$S" | grep -q "add constraint cards_numero_unico unique (numero)" && echo "$S" | grep -q "owned by public.cards.numero" && test "$(echo "$S" | grep -Ec 'drop (table|column)|alter column .* type|rename')" -eq 0 && test "$(echo "$S" | grep -Ec 'create policy|enable row level security')" -eq 0 && echo TRACER_OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260818000000_cards_numero_sequencial.sql` existe com esse nome exato
    - Ignorando comentários, o arquivo contém `create sequence if not exists public.cards_numero_seq`, `add column if not exists numero integer`, o backfill guardado por `where numero is null`, `set default nextval`, `set not null` e `add constraint cards_numero_unico unique (numero)`
    - Ignorando comentários, o arquivo contém `owned by public.cards.numero`
    - Ignorando comentários, `grep -Ec 'drop (table|column)|alter column .* type|rename'` devolve exatamente 0 — a migração é aditiva
    - Ignorando comentários, `grep -Ec 'create policy|enable row level security'` devolve exatamente 0 — nenhuma policy nova ou re-declarada (T-06.1-01)
  </acceptance_criteria>
  <reversibility rating="reversible">Só cria um arquivo `.sql` no repositório; nada toca o banco. Reverter é apagar o arquivo.</reversibility>
  <done>A migração declara a sequence, a coluna `numero` completa (backfill, default, not null, constraint única) e é aditiva e reexecutável — pronta para o ensaio do plano 06.1-02.</done>
</task>

<task type="auto">
  <name>Task 2: Runbook de ensaio e verificação pós-push</name>
  <files>supabase/verificacao_cards_numero.sql</files>
  <read_first>
    - `supabase/verificacao_financeiro.sql` — estilo integral do runbook: cabeçalho, "rode um bloco de cada vez", Parte A (ensaio em `begin;...rollback;`) e Parte B (blocos independentes pós-push), formato `do $$ ... $$` com `raise notice 'OK recusado: ...'`/`raise exception 'FALHOU: ...'`
    - `supabase/migrations/20260811010000_security_advisor_fixes.sql` — BLOCO 3, o padrão exato de `set local role authenticated; set local request.jwt.claims to '...'; ... reset role;`
    - `supabase/migrations/20260818000000_cards_numero_sequencial.sql` — o arquivo criado na Task 1; o BLOCO 2 do runbook cola esta DDL literalmente, duas vezes
  </read_first>
  <action>
Criar `supabase/verificacao_cards_numero.sql`. Runbook no estilo de `verificacao_financeiro.sql`: cabeçalho em português explicando o que o arquivo prova, instrução de rodar um bloco de cada vez na mesma aba do SQL Editor. Duas partes, as duas nascem nesta task porque o escopo (uma coluna, não duas tabelas) cabe inteiro num único plano de escrita.

PARTE A — ENSAIO (roda antes do push, termina em `rollback`, não persiste nada):

- BLOCO 1 — PRÉ-VOO, só leitura: `select count(*) as cards_total, max(updated_at) as updated_at_max, count(*) filter (where numero is not null) as com_numero from public.cards;`. Instruir o operador a anotar os três números — `com_numero` deve ser 0 antes desta migração existir.
- BLOCO 2 — ENSAIO EM TRANSAÇÃO. `begin;`, depois a DDL completa das seis seções do arquivo de migração colada literalmente, e a mesma DDL colada de novo logo em seguida (prova de idempotência — se a segunda passagem der erro ou duplicar numeração, a migração não está pronta). Depois, a consulta de integridade: `select count(*) as cards_total, count(distinct numero) as numeros_distintos, min(numero) as numero_min, max(numero) as numero_max, count(*) filter (where numero is null) as sem_numero from public.cards;` (esperado: `cards_total = numeros_distintos = numero_max`, `numero_min = 1`, `sem_numero = 0`). Depois, a consulta de ordem: `select count(*) as fora_de_ordem from public.cards c where exists (select 1 from public.cards c2 where c2.numero < c.numero and c2.created_at > c.created_at);` (esperado 0 — nenhum card mais novo recebeu número menor que um mais antigo).
- BLOCO 3 — PROVA DA CONSTRAINT ÚNICA (mesma transação, não rodar `begin;` de novo). Um `do $$ ... $$` que lê o `id` do card de `numero = 1` numa variável e o `id` do card de segundo menor `numero` noutra, tenta fazer o primeiro assumir o `numero` do segundo via `update public.cards set numero = ... where id = ...`, e espera `unique_violation`, no mesmo formato `exception when unique_violation then raise notice 'OK recusado: cards_numero_unico'; ... raise exception 'FALHOU: cards_numero_unico deveria ter recusado numero duplicado';` já usado em `verificacao_financeiro.sql`.
- BLOCO 4 — CONFIRMAÇÃO DE QUE O RLS CONTINUA VALENDO (mesma transação; bônus barato, não reprova RLS do zero). `set local role authenticated; set local request.jwt.claims to '{"email":"intruso-teste@exemplo.invalid","role":"authenticated"}';`, depois `select count(*) from public.cards;` (esperado 0 — mesmo resultado que já valia antes desta coluna existir). Depois `reset role;` e `set local request.jwt.claims` com o marcador `SEU-EMAIL-DA-ALLOWLIST@exemplo.com` (instrução de trocar pelo e-mail real, igual ao BLOCO 7 de `verificacao_financeiro.sql`) mais `set local role authenticated;`, confirmando que o mesmo `select count(*)` volta a devolver as linhas. Comentar explicitamente que este bloco não está reprovando o RLS do zero — isso já foi feito em `20260811000000_security_hardening.sql` — está confirmando que a coluna `numero` não abriu nenhuma exceção nova a essa regra (T-06.1-01).
- Fechar com `rollback;` e o mesmo aviso forte de `verificacao_financeiro.sql` sobre nunca trocar por `commit`.

PARTE B — VERIFICAÇÃO PÓS-PUSH (blocos independentes; escritos aqui, rodados só no plano 06.1-03):

- BLOCO 5 — INVENTÁRIO (só leitura). Uma consulta em `information_schema.columns` filtrando `table_name = 'cards' and column_name = 'numero'`, mostrando `data_type`, `is_nullable`, `column_default`; uma consulta em `pg_constraint` filtrando `conrelid = 'public.cards'::regclass and conname = 'cards_numero_unico'`; e `select pg_get_serial_sequence('public.cards', 'numero') as sequence_dona;`. Documentar o esperado: `integer`, `NO`, um default citando `nextval`, a constraint presente, `sequence_dona = 'public.cards_numero_seq'`.
- BLOCO 6 — INTEGRIDADE PÓS-PUSH (só leitura). `select count(*) as cards_total, count(*) filter (where numero is null) as sem_numero, count(distinct numero) as numeros_distintos, max(updated_at) as updated_at_max from public.cards;`. Documentar o esperado: `cards_total` e `updated_at_max` idênticos ao BLOCO 1, `sem_numero = 0`, `numeros_distintos = cards_total`.
- BLOCO 7 — ROLLBACK DE EMERGÊNCIA, inteiro dentro de comentário de bloco SQL, no espírito do BLOCO 11 de `verificacao_financeiro.sql`: `alter table public.cards drop constraint if exists cards_numero_unico; alter table public.cards alter column numero drop default; alter table public.cards alter column numero drop not null; alter table public.cards drop column if exists numero; drop sequence if exists public.cards_numero_seq;`. Aviso forte de que isso apaga o identificador sequencial de todo contrato e só deve ser usado se o push precisar ser revertido no mesmo dia, antes de qualquer filtro por ID ter sido usado de verdade.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && R=supabase/verificacao_cards_numero.sql && test -f "$R" && for b in "BLOCO 1" "BLOCO 2" "BLOCO 3" "BLOCO 4" "BLOCO 5" "BLOCO 6" "BLOCO 7"; do grep -q "$b" "$R" || { echo "FALTA $b"; exit 1; }; done && grep -q "intruso-teste@exemplo.invalid" "$R" && grep -q "unique_violation" "$R" && grep -q "pg_get_serial_sequence" "$R" && grep -q "information_schema.columns" "$R" && test "$(grep -c 'rollback;' "$R")" -ge 1 && test "$(grep -c '^commit;' "$R")" -eq 0 && echo RUNBOOK_OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/verificacao_cards_numero.sql` contém os rótulos `BLOCO 1` a `BLOCO 7`
    - O BLOCO 2 cola a DDL da migração duas vezes seguidas dentro do mesmo `begin;`
    - O BLOCO 3 usa `unique_violation` e traz `OK recusado: cards_numero_unico` e um `FALHOU:` de contraste
    - O BLOCO 4 usa `intruso-teste@exemplo.invalid` e um marcador de e-mail da allowlist a trocar
    - O BLOCO 5 consulta `information_schema.columns` e `pg_get_serial_sequence`
    - `grep -c 'rollback;'` devolve pelo menos 1 e `grep -c '^commit;'` devolve exatamente 0
    - O BLOCO 7 está inteiramente dentro de comentário de bloco SQL e avisa que apaga o identificador sequencial real
  </acceptance_criteria>
  <reversibility rating="reversible">Só cria um arquivo `.sql` de runbook; nada aplicado ao banco.</reversibility>
  <done>O runbook prova, num único ensaio transacional, que a coluna `numero` nasce correta, sequencial, sem duplicata e sem afrouxar o RLS existente — e traz os blocos de conferência pós-push mais o rollback de emergência comentado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| arquivo de migração -> banco de produção | Mesma fronteira da Phase 4: ~46 cards reais, sem staging |
| coluna `numero` -> uso futuro multi-tenant | Hoje inofensiva (board único); precisa ser revisitada se o app um dia ganhar múltiplos tenants |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.1-01 | Elevation of Privilege | RLS de `public.cards` | critical | mitigate | Nenhuma policy nova ou alterada nesta migração; a coluna herda `is_team_member()` já existente. Portão automatizado: `grep -Ec 'create policy\|enable row level security'` igual a 0 no arquivo de migração. BLOCO 4 do runbook confirma comportamento inalterado contra o schema real |
| T-06.1-02 | Information Disclosure | Sequence de `numero` num cenário multi-tenant futuro | low | accept | Hoje single-board: todo mundo na allowlist já vê todos os contratos, não há "outro tenant" a vazar. Comentário explícito na migração para quando a migração multi-tenant (fora de escopo) precisar revisitar isso |
| T-06.1-03 | Tampering | DDL destrutiva por engano | high | mitigate | Portão automatizado recusa `drop table`, `drop column`, `alter column ... type`, `rename` no arquivo |
| T-06.1-04 | Tampering | Numeração duplicada por reexecução da migração | medium | mitigate | Backfill guardado por `where numero is null`; BLOCO 2 do runbook roda a DDL duas vezes e confere `numeros_distintos = cards_total` |
| T-06.1-05 | Tampering | Linhas/estado de teste do ensaio persistidos em produção | high | mitigate | Parte A inteira dentro de `begin; ... rollback;`; BLOCO 1 reexecutado fora da transação (plano 06.1-02) compara contra os números anotados |

**Supply chain:** esta fase não instala nenhum pacote npm, pip ou cargo — só escreve SQL. O portão de legitimidade de pacotes não se aplica e nenhuma linha `T-06.1-SC` é registrada.
</threat_model>

<verification>
1. Os dois arquivos `.sql` existem com os nomes exatos de `## Artifacts this phase produces`
2. Todos os símbolos da coluna `numero` (sequence, coluna, backfill, default, not null, constraint única) aparecem no arquivo de migração, ignorando comentários
3. Zero DDL destrutiva e zero policy nova/alterada no arquivo de migração
4. O runbook cobre BLOCO 1 a BLOCO 7, termina toda transação em `rollback` e não tem nenhum `commit;` em início de linha
5. Nenhum comando foi executado contra o banco — este plano não abre conexão com produção
</verification>

<success_criteria>
- CONTRATO-03 está escrito: `public.cards.numero` nasce sequencial, único, sustentado por sequence, com backfill correto para os ~46 cards existentes
- A migração é aditiva e reexecutável, e não introduz nenhuma mudança de RLS
- O runbook consegue provar, num único ensaio que termina em `rollback`, a integridade do backfill, a recusa de duplicata e a continuidade do RLS
</success_criteria>

<output>
Create `.planning/phases/06.1-consulta-financeira-e-gera-o-por-per-odo/06.1-01-SUMMARY.md` when done
</output>