---
phase: 8
slug: relatorios-financeiros
status: draft
shadcn_initialized: true
preset: base-nova (baseColor neutral, iconLibrary lucide, rsc true)
created: 2026-08-20
---

# Phase 8 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Scope note:** This phase is a strict ADD to the existing `/relatorios` route (`web/src/components/reports/reports-view.tsx`). Every pixel of the current page (h1, subtitle, existing filter card with `SearchField`/`STATUS_OPTIONS` chips/column chips, `StatTile` row, `ColumnBarChart`, `ContractsTable`) is byte-for-byte unchanged. This contract governs only the new pieces: the collapsible financial-report filter panel and the four-category result display. No new design tokens are introduced anywhere in this contract — every value below is a value already live in `web/src/app/globals.css` or an existing component.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (already initialized — `components.json` present, no gate needed) |
| Preset | `base-nova`, baseColor `neutral`, cssVariables true, iconLibrary `lucide` |
| Component library | base-ui (`@base-ui/react`) — this preset does not use Radix; e.g. `Collapsible` in `web/src/components/ui/collapsible.tsx` wraps `@base-ui/react/collapsible` |
| Icon library | lucide-react |
| Font | Body: Plus Jakarta Sans (`--font-body`, mapped to `font-sans`) · Heading: Sora (`--font-heading-family`, mapped to `font-heading`) |

No new shadcn components are required for this phase. `Button`, `Input`, `Label`, `Collapsible`/`CollapsibleTrigger`/`CollapsiblePanel` are already present in `web/src/components/ui/` and already used by `filtro-parcelas.tsx` — reuse them as-is, do not reinstall or fork.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps inside chips/badges (`gap-1.5` ≈ 6px is the one pre-existing exception — see below) |
| sm | 8px | Compact element spacing (`gap-2` between filter fields) |
| md | 16px | Default element spacing (`p-4`, `gap-4` — grid of the 4 category tiles) |
| lg | 24px | Section padding (`p-6` page container, `gap-3`→`gap-5` page-level stacking) |
| xl | 32px | Not used in this phase's new pieces |
| 2xl | 48px | Not used in this phase's new pieces |
| 3xl | 64px | Not used in this phase's new pieces |

Exceptions (pre-existing, not introduced by this phase — do not "fix"):
- `gap-1.5` (6px) is already used throughout the app for icon+label pairs (`FilterChip` spacing equivalents, `ParcelaSituacaoBadge`, `StatTile` icon row) — this phase's new situação chips and the filter panel's trigger button inherit this exact pattern, not a new one.
- `p-5` (20px) is the established card padding for `FiltroParcelas`' inner panel (`rounded-2xl border border-border bg-card px-5 py-4`) and for `ContractsTable`/`StatTile`-adjacent cards. The new financial filter panel reuses this exact class string — do not switch to `p-4`/`p-6` for visual novelty.

---

## Typography

This phase does not introduce new type roles — it reuses the four sizes and two weights already load-bearing on `/relatorios` and `/financeiro`.

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 14px (`text-sm`) | 400 regular | 1.5 (Tailwind default `leading-normal`) |
| Label / chip / uppercase micro-label | 12px (`text-xs`) | 600 semibold (`font-semibold`) — filter-row labels use `font-semibold uppercase`, chips use `font-medium` (500, existing exception, see below) | 1.4 |
| Heading (panel title, e.g. "Relatório financeiro") | 16px (`text-base`), `font-heading font-bold` | 700 bold | 1.2 |
| Display (page h1 "Relatórios" — untouched by this phase) | 24px (`text-2xl`), `font-heading font-extrabold` | 800 extrabold | 1.2 |

Weight exception (pre-existing, inherited not invented): the existing `FilterChip` component (`reports-view.tsx`) uses `font-medium` (500) for chip labels, one step lighter than the `font-semibold` (600) used on the `FilterRow` uppercase label above it. The new situação chips (paga/a vencer/vencida/conciliada) MUST reuse `FilterChip` verbatim, inheriting this exact weight — do not "correct" it to semibold, that would visually diverge the new chips from the contract-status chips sitting in the same page.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `--background` `#f4fcf4` | Page background |
| Secondary (30%) | `--card` `#ffffff` | Filter panel surface, the four category tiles, table/report surfaces — all `rounded-2xl border border-border bg-card` |
| Accent (10%) | `--primary` `#74ac1c` | See "Accent reserved for" below |
| Destructive | `--destructive` (oklch, red family) | Not used in this phase — the report is read-only, no delete/destructive action exists here |

Accent reserved for (explicit list, nothing else):
- The **active** state of a situação filter chip (`FilterChip active` variant — `border-primary bg-primary text-primary-foreground`), reusing the exact same rule already governing the contract-status chips (`vencido`/`vencendo`/`em_dia`/`sem_data`) on this same page. This is not a new use of the accent color, it is the same rule extended to a new, adjacent chip row.
- The **"Gerar relatório" button** (`Button variant="default"`), the phase's one primary call-to-action — same variant already used for "Consultar" in `FiltroParcelas`.
- Input focus rings (`--ring`, which equals `--primary`) on the three text/date filter fields — inherited from the shared `Input` component, not a per-phase decision.

Explicitly NOT accent: the collapsible trigger button ("Filtrar"/"Fechar filtros" equivalent) is `Button variant="outline"`, matching `FiltroParcelas` — the accent color is not spent on the panel's own open/close affordance, only on the action that actually runs the query.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Panel trigger (closed) | "Filtrar" (icon: `Filter`, same as `FiltroParcelas`) |
| Panel trigger (open) | "Fechar filtros" (icon: `X`, same as `FiltroParcelas`) |
| Panel heading | "Relatório financeiro" |
| Panel subheading (optional, only if room) | "Combine imóvel, proprietário, período e situação — depois gere o relatório." |
| Filter field labels | "Imóvel" (placeholder: "Endereço do imóvel"), "Proprietário" (placeholder: "Nome do proprietário"), "Período" (type `month`, no placeholder needed — matches `filtro-periodo` in `FiltroParcelas`) |
| Situação chip row label | "Situação" (uppercase micro-label, matches `FilterRow`'s `label` prop pattern: "Contrato"/"Coluna") |
| Situação chip options | "Todas" (clear-all, matches existing `STATUS_OPTIONS` "Todos"/"Todas" convention) · "Pagas" · "A vencer" · "Vencidas" · "Conciliadas" |
| Primary CTA | **"Gerar relatório"** (verbatim, D-04 — locked, do not rename to "Consultar"/"Filtrar"/"Buscar") |
| Secondary action | "Limpar filtros" (ghost/text button, only shown when at least one filter field or chip is non-empty — same conditional-render rule as the existing `clearFilters`/"Limpar filtros" button already on this page at line ~150 of `reports-view.tsx`) |
| Pre-generation empty state (report not yet run this session) | Heading: none needed (tiles simply don't render yet) · Body: "Ajuste os filtros acima e clique em **Gerar relatório** para ver pagas, a vencer, vencidas e conciliadas." Placed where the four category tiles will appear once generated. |
| Zero-result state (report was generated, but a category has 0 parcelas) | No special copy — the category tile itself renders "0" and "R$ 0,00" via `StatTile`, exactly like any other numeric zero-state elsewhere in the app (e.g. `StatTile` "Sem inquilino" already renders 0 this way). Do not write a sentence implying "nothing to show" — zero pagas/vencidas etc. is a legitimate, expected business state, not an error or missing-data condition. **Critical: never phrase this as if archived/inactive contracts were excluded** — per D-05, archived and inactive contracts count toward these totals; if the count is genuinely 0 it is 0 including them, not "0 among active contracts." |
| Error state (server/query failure on "Gerar relatório") | "Não foi possível gerar o relatório agora. Tente novamente." — mirrors the existing `ConciliarFalhaToast` phrasing pattern (non-optimistic, generic retry, no raw Postgres text — `erroDoBanco()` if the failure originates from a Server Action). |
| Destructive actions | None in this phase — the report is read-only (FINREL-01..05 are all "ver"/"combinar filtros", no delete/write path). No confirmation dialog needed anywhere in this contract. |

---

## Layout & Component Contract (phase-specific, informed by Claude's Discretion in 08-CONTEXT.md)

**1. Filter panel — position and mechanics**
Insert directly after the existing subtitle block (`reports-view.tsx` line 138, "Uma visão geral da carteira...") and before the existing filter card (line 142 `<div className="flex flex-col gap-3 rounded-2xl border...">`). A new, separate component (e.g. `FiltroRelatorioFinanceiro`, sibling file to `filtro-parcelas.tsx` but under `components/reports/`) — never import or move `FiltroParcelas` itself. Mirror its mechanics exactly:
- `Collapsible`/`CollapsibleTrigger`/`CollapsiblePanel` from `@/components/ui/collapsible`.
- Closed by default; opens automatically if the panel's own query params are present on load (same rule as `FiltroParcelas`' `filtrosAtivos` prop) — since D-04 makes this a client-local "Gerar relatório" flow rather than URL-driven per-keystroke, "params present" here means: local form state is non-empty AND a report has already been generated in this session (e.g. via a `?relFiltro=1`-style marker, or simply keep it session/component state — no URL round-trip is required by D-04, this is planner's call, not a design constraint).
- Panel surface: `mt-3 rounded-2xl border border-border bg-card px-5 py-4` (verbatim class string from `FiltroParcelas`).
- Trigger button: `Button variant="outline" size="sm"` with `Filter`/`X` icon swap (verbatim from `FiltroParcelas`).

**2. Filter fields inside the panel**
Three text/date fields + one chip row, in this order: Imóvel (text, `ilike` semantics — same free-text pattern as Proprietário/Inquilino in `FiltroParcelas`) → Proprietário (text, same pattern) → Período (`type="month"`, same as `filtro-periodo`) → Situação (chip row, see below). Use the same `flex flex-col gap-1.5` field wrapper + `Label` + `Input` pattern as `FiltroParcelas`. Layout: a responsive grid (`grid-cols-[repeat(3,1fr)] gap-3` for the three text/date fields, chip row full-width beneath, action buttons row beneath that) — do not force the 4-column-plus-auto grid of `FiltroParcelas` verbatim since this panel has one more filter dimension (situação chips need full width to wrap comfortably); a stacked layout (fields row, then chip row, then action row) is the correct adaptation, not a violation of the reused pattern.

**3. Situação chips**
Reuse `FilterChip` and the `toggle()` helper from `reports-view.tsx` verbatim (import or lift into a shared location — planner's call) for a new `SITUACAO_OPTIONS` array: `paga` → "Pagas", `a_vencer` → "A vencer", `vencida` → "Vencidas", `conciliada` → "Conciliadas". Same multi-select-toggle semantics as `STATUS_OPTIONS` (empty set = "Todas", no filter applied).

**4. "Gerar relatório" button**
`Button variant="default"` (primary), placed at the end of the panel's action row alongside a `Button variant="ghost"` "Limpar filtros" (mirrors `FiltroParcelas`' Consultar/Limpar pair). Per D-04, this button is the only trigger for the query — no field change runs anything live.

**5. Four-category result display**
Use `StatTile` (`web/src/components/reports/stat-tile.tsx`) as-is, unmodified, one per category, in a `grid gap-4 sm:grid-cols-2 xl:grid-cols-4` row (same grid class already used for the page's existing 4 `StatTile`s at line 212) — placed below the new filter panel, above or below the existing `StatTile` row per planner's layout call, but visually as its own labeled group (wrap in a `<div>` with a small heading "Relatório financeiro" so it reads as a distinct block, not a merge into the existing 4-tile row). Each tile: `icon` (suggest `CheckCircle2` pagas, `Clock` a vencer, `AlertCircle` vencidas, `Lock` conciliadas — same icon-to-situação mapping already established in `ParcelaSituacaoBadge`, for visual consistency across the app), `label` (category name), `value` (parcela count as a plain number string, e.g. `"12"`), `hint` (the money total via `formatCurrency`, e.g. `hint="R$ 4.200,00"`). Do **not** invent a new visual pattern (table, custom card) unless `StatTile`'s two-line layout (icon+label, then big number, then optional hint) cannot fit both count and money total legibly — if so, the fallback is a small variant of `StatTile` (new `secondaryValue` prop) rather than a wholly new component, to keep this page visually uniform.
Tone: `vencidas` may use `tone="alert"` (matching `StatTile`'s existing alert-tone convention, reserved for counts needing action) only if `count > 0` — same rule already applied to "Contratos a vencer" at line 229 of `reports-view.tsx`.

**6. Data/behavior notes for the planner (not visual, but shape the copy/UI states above)**
- Archived/inactive contracts count toward all four totals (D-05) — no filter/query on this new panel should silently apply the `arquivado_em is null` filter that `relatorios/page.tsx` uses for the existing contract-status report; that filter must not propagate to the new financial-report query.
- Período filter applies to `competencia`, not `vencimento` (D-08) — matches the Financeiro tab's existing period filter semantics, so users moving between the two screens don't hit a surprising mismatch.
- Vencida/a-vencer split is computed on read against `hojeEmCuiaba()` (already piped into `ReportsView` as `todayISO`), never stored (FINREL "Pilares cruzados").

---

## UI Considerations

Applicable state considerations resolved: 5 covered, 2 backstop, 0 unresolved.

| Category | Element(s) | Status | Resolution / Reason |
|----------|------------|--------|---------------------|
| empty (pre-action) | Four-category result block | ✅ covered | Before "Gerar relatório" is clicked, the block renders the "Ajuste os filtros acima..." copy documented in Copywriting Contract, never an empty grid of zero-value tiles |
| empty (zero-result) | Individual `StatTile` in the four-category row | ✅ covered | Renders "0" / "R$ 0,00" via `StatTile`'s existing number formatting — no special empty-state sentence, per Copywriting Contract row "Zero-result state" |
| error | "Gerar relatório" action | ✅ covered | "Não foi possível gerar o relatório agora. Tente novamente." documented in Copywriting Contract, sourced through `erroDoBanco()` if server-originated |
| loading | "Gerar relatório" action | 🧪 backstop | No copy specified for an in-flight/pending state on the button (e.g. disabled + spinner vs. text swap) — planner should default to the same loading affordance already used elsewhere for Server Action buttons in this codebase (e.g. `ConciliarParcelaAction`'s button), not invent a new one |
| long-text | Imóvel/Proprietário free-text filter results | ✅ covered | Reuses the same `ilike`/free-text matching already proven in `FiltroParcelas` for Proprietário/Inquilino — no truncation concern distinct from that existing pattern |
| zero-one-many | Situação chip multi-select | ✅ covered | Empty set = "Todas" (no filter), one or more chips active = AND/OR semantics inherited verbatim from `STATUS_OPTIONS`' existing `toggle()` behavior (chips within the same row are OR'd, matching D-03's description of "situação" as one filter dimension) |
| overflow | Filter panel field row on narrow viewports | 🧪 backstop | No explicit breakpoint behavior specified for the 3-field grid + chip row on mobile widths — planner should follow the same responsive collapse already used by `FiltroParcelas`' grid (or the page's own `sm:`/`xl:` breakpoints), not introduce a new breakpoint scheme |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Input, Label, Collapsible/CollapsibleTrigger/CollapsiblePanel — all already installed under `web/src/components/ui/`, no new install needed this phase | not required |

No third-party registries declared or needed for this phase.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
