---
phase: 06-baixa-e-ajustes-de-parcela
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - web/src/components/financeiro/lancamento-tipo-label.tsx
  - web/src/components/financeiro/parcela-historico-sheet.tsx
  - web/src/components/financeiro/parcelas-table.tsx
autonomous: false
requirements: [BAIXA-05]

estimate:
  tokens: 27000
  raw_tokens: 27000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Clicar em Histórico abre uma Sheet lateral com todos os lançamentos daquela parcela, sem nenhuma requisição nova ao banco — os dados já vieram na consulta inicial de /financeiro, estendida pelo plano 06-01 (BAIXA-05, ROADMAP SC4)"
    - "Cada lançamento no histórico mostra ícone + rótulo do tipo (Pagamento/Acréscimo/Desconto/Destrava), valor com o prefixo certo (+ para pagamento e acréscimo, − para desconto, sem valor para destrava), quando foi feito e quem fez — nunca o UUID cru de criado_por"
    - "Uma parcela sem nenhum lançamento mostra a frase de estado vazio do Copywriting Contract, nunca uma lista em branco sem explicação"
    - "O componente já nasce pronto para os 4 tipos de lançamento, incluindo destrava — mesmo destrava só ficando alcançável a partir da Phase 7, nenhuma reabertura deste componente será necessária então (mesmo precedente de futuro-proofing de ParcelaSituacaoBadge na Phase 5)"
    - "UI Consideration `loading` (Sheet): abrir o histórico não mostra nenhum spinner nem esqueleto — os dados já estão na página, então a abertura é instantânea"
    - "UI Consideration `zero-one-many` (lista de lançamentos): zero cai no estado vazio; um ou muitos renderizam a mesma lista de cards, sem contador de itens (mesma decisão da própria lista de parcelas na Phase 5)"
    - "UI Consideration `long-text` (observação no histórico): o texto quebra linha naturalmente dentro do card do lançamento, nunca é truncado"
  artifacts:
    - "web/src/components/financeiro/lancamento-tipo-label.tsx — novo"
    - "web/src/components/financeiro/parcela-historico-sheet.tsx — novo"
    - "web/src/components/financeiro/parcelas-table.tsx — botão Histórico na coluna Ações"
  key_links:
    - "linha.lancamentos (já montado por montarLinhas no plano 06-01) → ParcelaHistoricoSheet, direto — sem fetch adicional"
    - "Botão Histórico → estado local de AcoesCell (já existente desde 06-01) → Sheet aberta com side=\"right\", mesmo padrão já usado por AlertsPanel"
---

<objective>
Entregar a última peça do escopo da fase: a Sheet de histórico que mostra todos os lançamentos de
uma parcela — os pagamentos, acréscimos e descontos gravados pelo plano 06-01 (e, mais adiante,
destravas da Phase 7). Fecha visualmente a garantia de append-only: nada some, e o usuário consegue
ver isso, não só confiar nisso.

Purpose: o plano 06-01 já grava e recalcula corretamente, mas nenhuma tela mostra o histórico
completo — só os totais na própria linha. ROADMAP critério de sucesso 4 exige explicitamente que "o
histórico da parcela mostra todos eles", não só o resultado agregado.
Output: o botão Histórico funcionando em toda linha da tabela, mostrando os lançamentos já registrados
durante a verificação do plano 06-01.
</objective>

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-baixa-e-ajustes-de-parcela/06-CONTEXT.md
@.planning/phases/06-baixa-e-ajustes-de-parcela/06-UI-SPEC.md
@.planning/phases/06-baixa-e-ajustes-de-parcela/06-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: LancamentoTipoLabel + Sheet de histórico</name>

  <files>
web/src/components/financeiro/lancamento-tipo-label.tsx
web/src/components/financeiro/parcela-historico-sheet.tsx
  </files>

  <read_first>
- `web/src/components/financeiro/parcela-situacao-badge.tsx` — o molde exato: mapa `{ estado: { icon, label, className } }`, `<span className="inline-flex items-center gap-1.5 text-xs font-semibold">` com o ícone `size-3.5 shrink-0` — `LancamentoTipoLabel` segue esta forma ao pé da letra, só trocando o conjunto de estados
- `web/src/components/alerts/alerts-panel.tsx` — o molde exato de `Sheet`/`SheetContent side="right"`/`SheetHeader`/`SheetTitle`, a lista `<ul className="flex flex-col gap-3">` de `<li className="flex flex-col gap-2 rounded-xl border border-border bg-card p-3">`, e o estado vazio (`count === 0`) como parágrafo simples dentro do body — `ParcelaHistoricoSheet` replica esta estrutura
- `web/src/lib/kanban/parcelas.ts` — o tipo `LancamentoDetalhado` (criado pelo plano 06-01): `id`, `tipo`, `valor`, `data`, `observacao`, `motivo`, `criado_em`, `profiles: { full_name, email } | null`
- `web/src/lib/kanban/format.ts` — `formatCurrency`, `formatDate`
- `.planning/phases/06-baixa-e-ajustes-de-parcela/06-UI-SPEC.md` — a tabela Color com os 4 estados de lançamento (ícone/token/prefixo de valor), a seção Copywriting Contract (título, subtítulo, estado vazio da Sheet), e a nota sobre "quem" nunca ser o UUID cru
  </read_first>

  <precondition>O plano 06-01 está completo: `LancamentoDetalhado` já existe em `web/src/lib/kanban/parcelas.ts` com os campos `id`/`tipo`/`valor`/`data`/`observacao`/`motivo`/`criado_em`/`profiles`. Se `grep -c 'LancamentoDetalhado' web/src/lib/kanban/parcelas.ts` devolver 0, pare: o plano 06-01 precisa estar commitado primeiro.</precondition>

  <action>
**1. `web/src/components/financeiro/lancamento-tipo-label.tsx`** (novo) — sem `"use client"` (componente puro de apresentação, sem estado nem evento). Mapa module-level `TIPO` com os 4 estados de `LancamentoDetalhado["tipo"]`: `pagamento` → ícone `Banknote` (`lucide-react`), rótulo `"Pagamento"`, `className: "text-status-good"`; `acrescimo` → `TrendingUp`, `"Acréscimo"`, `"text-status-warning"`; `desconto` → `TrendingDown`, `"Desconto"`, `"text-muted-foreground"`; `destrava` → `Unlock`, `"Destrava"`, `"text-muted-foreground"`. Componente `export function LancamentoTipoLabel({ tipo }: { tipo: LancamentoDetalhado["tipo"] })` devolvendo `<span className={cn("inline-flex items-center gap-1.5 text-xs font-semibold", className)}><Icon className="size-3.5 shrink-0" />{label}</span>` — mesma estrutura de `parcela-situacao-badge.tsx`, só o mapa de estados muda. Importe `LancamentoDetalhado` como tipo de `@/lib/kanban/parcelas`.

**2. `web/src/components/financeiro/parcela-historico-sheet.tsx`** (novo) — `"use client"`. Props: `{ endereco: string; competencia: string; vencimento: string; lancamentos: LancamentoDetalhado[]; open: boolean; onOpenChange: (open: boolean) => void }`.
- `Sheet open={open} onOpenChange={onOpenChange}`, `SheetContent side="right"` (mesmas classes de largura/animação já vêm do primitivo, nenhuma classe nova precisa ser passada além do que `AlertsPanel` já usa).
- `SheetHeader` com `SheetTitle` = `` `Histórico — ${endereco}` `` e, logo abaixo, um `<p className="text-sm text-muted-foreground">` com o subtítulo `` `Competência ${mesPorExtenso}/${ano} · Vencimento ${formatDate(vencimento)}` `` — `mesPorExtenso`/`ano` calculados com `Intl.DateTimeFormat("pt-BR", { month: "long" })` a partir de `competencia.split("-")`, mesmo padrão descrito no plano 06-01 para os dois diálogos (nunca `new Date(string)` direto).
- Corpo: `<div className="flex-1 overflow-y-auto p-4">`. Se `lancamentos.length === 0`, um parágrafo `text-sm text-muted-foreground`: `"Nenhum lançamento registrado ainda. Dar baixa ou lançar um ajuste aparece aqui."`. Senão, `<ul className="flex flex-col gap-3">` com um `<li className="flex flex-col gap-2 rounded-xl border border-border bg-card p-3">` por lançamento (key = `lancamento.id`):
  - Linha superior `<div className="flex items-start justify-between gap-2">`: `<LancamentoTipoLabel tipo={lancamento.tipo} />` à esquerda; à direita, `<span className="text-sm font-semibold tabular-nums text-foreground">` com o valor formatado: prefixo `"+ "` quando `tipo` é `pagamento` ou `acrescimo`, prefixo `"− "` (sinal de menos U+2212, não hífen) quando `desconto`, e apenas `"—"` (travessão, sem `formatCurrency`) quando `destrava`; nos três primeiros casos o valor é `formatCurrency(lancamento.valor)` depois do prefixo.
  - Linha seguinte `<p className="text-sm text-muted-foreground">`: `` `${formatDate(lancamento.data)} · ${quem}` ``, onde `quem = lancamento.profiles?.full_name ?? lancamento.profiles?.email ?? "—"` — nunca o identificador bruto de quem lançou (esse identificador nem está no tipo `LancamentoDetalhado`, então não há como vazar por acidente).
  - Se `lancamento.observacao` existir, mais um `<p className="text-sm text-muted-foreground">{lancamento.observacao}</p>` deixando o texto quebrar linha naturalmente, sem nenhuma classe de corte ou reticências.
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
- `cd web && npm run lint` e `cd web && npm run build` saem com código 0
- `grep -c 'export function LancamentoTipoLabel' web/src/components/financeiro/lancamento-tipo-label.tsx` devolve 1
- `grep -cE 'pagamento|acrescimo|desconto|destrava' web/src/components/financeiro/lancamento-tipo-label.tsx` devolve 4 ou mais (os 4 estados do mapa)
- `grep -c 'Banknote\|TrendingUp\|TrendingDown\|Unlock' web/src/components/financeiro/lancamento-tipo-label.tsx` devolve 1 ou mais por ícone (confira os 4 nomes aparecem)
- `grep -c 'export function ParcelaHistoricoSheet' web/src/components/financeiro/parcela-historico-sheet.tsx` devolve 1
- `grep -c 'Nenhum lançamento registrado ainda' web/src/components/financeiro/parcela-historico-sheet.tsx` devolve 1
- `grep -c 'full_name ?? .*email ?? "—"' web/src/components/financeiro/parcela-historico-sheet.tsx` devolve 1 ou mais (confira manualmente a expressão exata se o grep não casar por causa de espaçamento)
- `grep -c 'criado_por' web/src/components/financeiro/parcela-historico-sheet.tsx` devolve 0 (o campo nem existe em `LancamentoDetalhado` — confirma que nada tenta ler um UUID cru)
- `grep -c 'truncate\|line-clamp' web/src/components/financeiro/parcela-historico-sheet.tsx` devolve 0
  </acceptance_criteria>

  <done>
`npm run lint` e `npm run build` passam; `LancamentoTipoLabel` cobre os 4 tipos com ícone+rótulo+cor
de status; `ParcelaHistoricoSheet` renderiza a lista completa de lançamentos de uma parcela sem
nenhum fetch próprio, com estado vazio, quem/quando/observação corretos e nunca um UUID cru.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fiar o botão Histórico na coluna Ações</name>

  <files>
web/src/components/financeiro/parcelas-table.tsx
  </files>

  <read_first>
- `web/src/components/financeiro/parcelas-table.tsx` — o resultado dos planos 06-01/06-01-Task2: `AcoesCell`, o `useState<"pagamento" | "ajustar" | null>`, os botões Pagamento (`variant="outline"`) e Ajustar (`variant="ghost"`) já wireados
- `web/src/components/alerts/alerts-panel.tsx` — o `SheetTrigger` como `Button variant="ghost" size="icon"` com `aria-label` dinâmico — o molde do botão Histórico, que é icon-only
- `.planning/phases/06-baixa-e-ajustes-de-parcela/06-UI-SPEC.md` — ordem dos 3 controles (Pagamento → Ajustar → Histórico, peso visual decrescente) e o `aria-label` exato do botão de histórico
  </read_first>

  <precondition>A Task 1 deste plano está commitada: `ParcelaHistoricoSheet` e `LancamentoTipoLabel` já existem. Se `grep -c 'export function ParcelaHistoricoSheet' web/src/components/financeiro/parcela-historico-sheet.tsx` devolver 0, pare: a Task 1 precisa estar commitada primeiro.</precondition>

  <action>
Em `web/src/components/financeiro/parcelas-table.tsx`, dentro de `AcoesCell`:
- Amplie o tipo do estado local para `"pagamento" | "ajustar" | "historico" | null`.
- Adicione, depois do botão Ajustar, um terceiro `Button variant="ghost" size="icon"` só com o ícone `History` (`lucide-react`) — sem texto visível, `aria-label={\`Ver histórico de lançamentos — ${linha.endereco}\`}`, `onClick={() => setDialogoAberto("historico")}`.
- Renderize `<ParcelaHistoricoSheet endereco={linha.endereco} competencia={linha.competencia} vencimento={linha.vencimento} lancamentos={linha.lancamentos} open={dialogoAberto === "historico"} onOpenChange={(open) => setDialogoAberto(open ? "historico" : null)} />` como irmão da célula, no mesmo molde dos dois diálogos existentes. Importe `ParcelaHistoricoSheet` no topo do arquivo.
- Não altere a ordem nem as `variant`s dos botões Pagamento/Ajustar já existentes — Histórico entra depois dos dois, é o de menor peso visual dos três (icon-only, `ghost`).
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
- `cd web && npm run lint` e `cd web && npm run build` saem com código 0
- `grep -c 'ParcelaHistoricoSheet' web/src/components/financeiro/parcelas-table.tsx` devolve 2 ou mais (import + uso)
- `grep -c '"historico"' web/src/components/financeiro/parcelas-table.tsx` devolve 2 ou mais
- `grep -c 'Ver histórico de lançamentos —' web/src/components/financeiro/parcelas-table.tsx` devolve 1
- `grep -c 'lancamentos={linha.lancamentos}' web/src/components/financeiro/parcelas-table.tsx` devolve 1
  </acceptance_criteria>

  <done>
`npm run lint` e `npm run build` passam; a coluna Ações tem os 3 controles na ordem Pagamento →
Ajustar → Histórico; clicar em Histórico abre a Sheet com os lançamentos daquela linha, sem nenhuma
requisição nova.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferir o histórico contra os lançamentos já gravados na verificação do plano 06-01</name>

  <files>(nenhum arquivo é alterado — verificação operacional)</files>

  <read_first>
- `.planning/phases/06-baixa-e-ajustes-de-parcela/06-01-SUMMARY.md` — quais parcelas receberam lançamento durante a Task 3 daquele plano (pagamento total, parcial, acréscimo, desconto) — são elas que este checkpoint reabre para conferir o histórico
  </read_first>

  <what-built>
O botão Histórico, terceiro controle da coluna Ações, abre uma Sheet lateral com todos os
lançamentos já registrados de uma parcela — os mesmos que o plano 06-01 gravou durante sua própria
verificação em produção.
  </what-built>

  <how-to-verify>
Abra `/financeiro` em produção. Nas mesmas parcelas usadas na Task 3 do plano 06-01 (a que recebeu
baixa total, a que recebeu baixa parcial + complemento, a que recebeu acréscimo, a que recebeu
desconto):

1. Clique **Histórico** em cada uma. A Sheet abre imediatamente, sem nenhum estado de carregamento.
2. Confira que **todos** os lançamentos daquela parcela aparecem — a que teve baixa parcial e depois
   complemento deve mostrar **duas** linhas de pagamento distintas, não uma só (prova de que nada foi
   sobrescrito, D-01/BAIXA-05).
3. Cada linha mostra o ícone e o rótulo certos (Pagamento/Acréscimo/Desconto), o valor com o sinal
   certo (+ para pagamento e acréscimo, − para desconto), a data e quem fez — confira que "quem" é um
   nome ou e-mail legível, nunca uma sequência de letras/números (UUID).
4. Abra o histórico de uma parcela sem nenhum lançamento (qualquer uma "A vencer" que você não tenha
   tocado): a Sheet mostra a frase de estado vazio, não uma lista em branco.
5. Feche a Sheet (X, Esc ou clique fora) e reabra: os mesmos lançamentos aparecem, na mesma ordem.
  </how-to-verify>

  <acceptance_criteria>
- A parcela que recebeu baixa parcial + complemento mostra duas linhas de pagamento distintas no histórico, não uma
- Cada lançamento mostra ícone, rótulo, valor com sinal correto, data e um nome/e-mail legível (nunca UUID)
- Uma parcela sem lançamento mostra a frase de estado vazio
- Abrir a Sheet não mostra nenhum spinner nem atraso perceptível
  </acceptance_criteria>

  <resume-signal>Responda "aprovado" ou descreva o que divergiu, item por item</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → dado já embutido na página | a Sheet não faz nenhuma nova requisição — lê `linha.lancamentos`, que já veio da consulta server-side de `page.tsx` (plano 06-01), filtrada por RLS antes de chegar ao componente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-07 | Information Disclosure | `ParcelaHistoricoSheet` — campo "quem" | medium | mitigate | O componente lê só `profiles.full_name`/`profiles.email`, nunca `criado_por` (o UUID nem está no tipo `LancamentoDetalhado`, então não há campo para vazar por engano); fallback `"—"` quando os dois vêm nulos |
| T-06-08 | Information Disclosure | vazamento entre usuários fora da allowlist | high | mitigate | A Sheet não tem nenhuma consulta própria — os dados vêm inteiramente da consulta server-side de `page.tsx`, já sob a mesma policy RLS `is_team_member()` provada na Phase 4/5; um usuário fora da allowlist recebe lista vazia na origem, não uma Sheet com dado filtrado no cliente |
| T-06-SC | Tampering | npm/pip/cargo installs | low | accept | Este plano não instala nenhum pacote — reusa `sheet.tsx` já instalado desde a Phase 5 (`AlertsPanel`) e o ícone `History` de `lucide-react`, já uma dependência existente |
</threat_model>

<verification>
1. `cd web && npm run lint && npm run build` saem com código 0
2. O botão Histórico abre a Sheet com todos os lançamentos da parcela, sem requisição nova
3. Task 3 confirma contra as parcelas testadas no plano 06-01: nenhum lançamento sumiu, nenhum foi sobrescrito, "quem" nunca é um UUID cru
</verification>

<success_criteria>
- O histórico da parcela mostra todos os lançamentos, com quem fez, quando e a observação (BAIXA-05, ROADMAP SC4)
- Nenhum lançamento antigo some, é editado ou aparece apagado quando um novo é lançado por cima
</success_criteria>

<artifacts_this_phase_produces>
Criados/alterados neste plano:

**Componentes**
- `LancamentoTipoLabel({ tipo })` — `web/src/components/financeiro/lancamento-tipo-label.tsx`
- `ParcelaHistoricoSheet({ endereco, competencia, vencimento, lancamentos, open, onOpenChange })` — `web/src/components/financeiro/parcela-historico-sheet.tsx`
- `ParcelasTable` — `AcoesCell` ganha o terceiro controle (Histórico, icon-only) e o import de `ParcelaHistoricoSheet`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/06-baixa-e-ajustes-de-parcela/06-02-SUMMARY.md` when done
</output>
