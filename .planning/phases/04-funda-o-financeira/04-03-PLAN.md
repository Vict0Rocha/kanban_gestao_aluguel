---
phase: 04-funda-o-financeira
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - supabase/migrations/20260816000000_financeiro_schema.sql
autonomous: false
requirements: [FINSEG-01, FINSEG-02]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`supabase db push` aplica a migração `20260816000000_financeiro_schema` no banco de produção sem erro, e `supabase migration list` passa a mostrá-la nas colunas Local e Remote"
    - "Depois do push, `select count(*) from public.cards` devolve o mesmo total anotado antes, e `count(*) filter (where ativo is not true)` devolve 0 — nenhum card apagado, alterado ou com `ativo` nulo (critério 1 do ROADMAP)"
    - "`pg_constraint` lista 10 CHECK constraints entre `public.parcelas` e `public.parcela_lancamentos`, com os nomes exatos declarados em 04-01"
    - "`pg_policies` lista `team full access parcelas` e `team full access parcela_lancamentos`, ambas citando `is_team_member` em `qual` e em `with_check` — FINSEG-01"
    - "As provas de RLS e de CHECK do runbook, reexecutadas contra o schema já aplicado, dão o mesmo resultado do ensaio — FINSEG-01, FINSEG-02"
    - "O board carrega no navegador com os mesmos cards de antes da migração"
  artifacts:
    - supabase/migrations/20260816000000_financeiro_schema.sql
  key_links:
    - "migração aplicada -> `public.is_team_member()`, que já existe desde `20260811000000_security_hardening.sql` e não é redefinida aqui"
    - "`cards.ativo` -> a geração preguiçosa de parcelas da Phase 5, que filtra por essa coluna"
---

<objective>
Aplicar a migração no banco de produção e provar, contra o schema já gravado, que os três primeiros critérios de sucesso do ROADMAP são verdade.

Este é o único plano da fase que altera o banco real. `supabase db push` é **porta de mão única**: desfazer exige outra migração contra dados de produção, e o app roda com ~46 imóveis reais, com nome de proprietário, nome de inquilino, telefone e valor de aluguel de gente de verdade. Não existe staging. Por isso o push está atrás de um `checkpoint:decision` explícito.

Purpose: FINSEG-01 e FINSEG-02 só protegem alguma coisa depois de estarem no banco onde os dados moram.
Output: schema financeiro vivo em produção, com a verificação pós-push registrada.
</objective>

## Artifacts this phase produces — Artefatos que esta fase produz

Este plano **não cria símbolo novo** — ele materializa em produção os símbolos declarados no plano 04-01. A lista canônica está na seção `## Artifacts this phase produces` de `04-01-PLAN.md`. Depois deste plano, os seguintes objetos passam a existir de fato no banco:

- Coluna `public.cards.ativo`
- Tabelas `public.parcelas` e `public.parcela_lancamentos`
- Constraints `parcelas_valor_original_positivo`, `parcelas_status_valido`, `parcelas_competencia_dia_1`, `parcelas_conciliada_rastreada`, `parcela_lancamentos_tipo_valido`, `parcela_lancamentos_valor_nao_negativo`, `parcela_lancamentos_valor_exigido`, `parcela_lancamentos_destrava_exige_motivo`, `parcela_lancamentos_motivo_tamanho`, `parcela_lancamentos_observacao_tamanho`
- Índices `parcelas_unica_por_competencia`, `parcelas_vencimento_idx`, `parcelas_competencia_idx`, `parcela_lancamentos_parcela_id_idx`
- Policies `team full access parcelas` e `team full access parcela_lancamentos`

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-funda-o-financeira/04-CONTEXT.md
@.planning/phases/04-funda-o-financeira/04-02-SUMMARY.md
@supabase/verificacao_financeiro.sql
@supabase/migrations/20260816000000_financeiro_schema.sql
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Autorizar a aplicação da migração no banco de produção</name>
  <read_first>
    - `supabase/verificacao_financeiro.sql` — o bloco `RESULTADO DO ENSAIO` gravado no plano 04-02
    - `.planning/phases/04-funda-o-financeira/04-02-SUMMARY.md` — o que o ensaio observou
  </read_first>
  <decision>
    Rodar `supabase db push` agora contra o banco de produção, aplicando `20260816000000_financeiro_schema.sql`.
  </decision>
  <context>
    O banco tem ~46 imóveis reais com dado pessoal de terceiros e **não existe ambiente de staging**. A migração é estritamente aditiva — nenhuma coluna apagada, renomeada ou com tipo alterado — e a coluna `cards.ativo` nasce com `not null default true`, o que no Postgres 11+ preenche as linhas existentes sem reescrever a tabela e sem disparar o trigger `cards_set_updated_at`.

    O ensaio do plano 04-02 já rodou essa mesma DDL contra o schema real dentro de uma transação desfeita no fim, e observou: aplicação limpa nas duas passagens, contagem de cards inalterada, `ativo` sem nulos, 10 recusas de CHECK/índice único, RLS barrando o e-mail de fora da allowlist e liberando o de dentro.

    O que **não** é reversível: depois do push, desfazer significa escrever outra migração contra produção. O runbook tem um bloco de rollback de emergência (BLOCO 11), mas ele derruba as tabelas — só serve enquanto nenhuma parcela real existir. A partir da Phase 5 esse caminho fecha.
  </context>
  <options>
    <option id="aplicar-agora">
      <name>Aplicar agora</name>
      <pros>Desbloqueia a Phase 5 imediatamente; o ensaio já cobriu tudo que dá para cobrir sem gravar; quanto mais cedo, menos risco de o BLOCO 11 deixar de ser uma saída viável</pros>
      <cons>É porta de mão única sobre dado real</cons>
    </option>
    <option id="conferir-backup-antes">
      <name>Conferir o backup antes de aplicar</name>
      <pros>Abre uma saída independente do BLOCO 11; custa poucos minutos no painel (Dashboard -> Database -> Backups), confirmando que existe um backup recente e anotando a data/hora dele</pros>
      <cons>Atrasa a fase pelo tempo da conferência</cons>
    </option>
    <option id="adiar">
      <name>Adiar</name>
      <pros>Nada muda em produção hoje</pros>
      <cons>A Phase 4 fica sem poder fechar (os critérios 1, 2 e 3 do ROADMAP falam do banco aplicado) e as Phases 5 a 8 ficam todas bloqueadas</cons>
    </option>
  </options>
  <acceptance_criteria>
    - O usuário escolheu explicitamente uma das três opções
    - Se escolheu `conferir-backup-antes`, a data/hora do backup mais recente foi anotada antes de seguir para a Task 2
    - Se escolheu `adiar`, a Task 2 e a Task 3 não são executadas e o plano encerra com o motivo registrado
  </acceptance_criteria>
  <reversibility rating="one-way">O portão existe justamente porque a task seguinte não é reversível: `supabase db push` grava DDL no banco de produção, e desfazer exige outra migração sobre dados reais de terceiros.</reversibility>
  <resume-signal>Responda: `aplicar-agora`, `conferir-backup-antes` (com a data do backup) ou `adiar` (com o motivo).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a migração em produção com `supabase db push`</name>
  <precondition>A Task 1 foi respondida com `aplicar-agora` ou `conferir-backup-antes`, e o Supabase CLI está instalado e linkado ao projeto de produção (`supabase link` já feito, ou `SUPABASE_ACCESS_TOKEN` no ambiente para rodar sem TTY).</precondition>
  <files>supabase/migrations/20260816000000_financeiro_schema.sql</files>
  <read_first>
    - `supabase/migrations/20260816000000_financeiro_schema.sql` — o arquivo que será aplicado; conferir que é exatamente o estado aprovado no plano 04-02
    - `docs/data-model.md` — seção "Como aplicar", que documenta `supabase db push` como o comando do projeto
  </read_first>
  <action>
Esta é a task de push obrigatória da fase. Sem ela a Phase 4 não passa na verificação: `npm run lint` e `npm run build` continuam verdes com ou sem o schema aplicado, porque nada no `web/` lê o banco em tempo de build — passar sem o push seria um falso positivo.

Antes de qualquer comando, conferir com `git status` que `supabase/migrations/20260816000000_financeiro_schema.sql` está no estado aprovado no plano 04-02 e que nenhuma edição não revisada entrou depois do ensaio.

Rodar `supabase migration list` para ver o estado atual: a migração nova deve aparecer só na coluna Local, e as três anteriores (`20260728000000`, `20260811000000`, `20260811010000`) devem aparecer nas duas colunas. Se alguma migração antiga estiver só em Local, **parar** e reportar — significa que o histórico remoto divergiu e um push cegamente aplicaria mais coisa do que esta fase autorizou.

Rodar `supabase db push` a partir da raiz do repositório. Se o CLI pedir confirmação interativa e o ambiente não tiver TTY, exportar `SUPABASE_ACCESS_TOKEN` e tentar de novo; se ainda assim o prompt bloquear, **não forçar** — entregar o comando exato ao operador para ele rodar no próprio terminal e reportar a saída. O plano já é `autonomous: false` justamente para permitir esse handoff.

Depois do push, rodar `supabase migration list` de novo e confirmar que `20260816000000` agora aparece em Local **e** em Remote.

Se o push falhar, ler a mensagem de erro, corrigir a causa no arquivo de migração, atualizar a cópia da DDL no BLOCO 2 do runbook para continuar batendo, e voltar ao ensaio do plano 04-02 antes de tentar de novo. Nunca tentar um segundo push sem reensaiar.

Não editar, remover nem renumerar as três migrations anteriores em nenhuma hipótese.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && supabase migration list 2>&1 | tee /dev/stderr | grep "20260816000000" | grep -qE '20260816000000.*20260816000000' && echo PUSH_APLICADO</automated>
  </verify>
  <acceptance_criteria>
    - `supabase migration list` mostra `20260816000000` nas colunas Local **e** Remote
    - As migrations `20260728000000`, `20260811000000` e `20260811010000` continuam listadas nas duas colunas e nenhum arquivo delas foi alterado (`git status` limpo para `supabase/migrations/20260728*`, `20260811*`)
    - O comando de push terminou sem erro; se terminou com erro, o arquivo foi corrigido e o ensaio do plano 04-02 foi refeito antes de nova tentativa
    - Nenhum arquivo fora de `supabase/migrations/20260816000000_financeiro_schema.sql` foi modificado por esta task
  </acceptance_criteria>
  <reversibility rating="one-way">Grava DDL no banco de produção. Desfazer exige outra migração; o BLOCO 11 do runbook só é uma saída enquanto nenhuma parcela real existir (ou seja, até a Phase 5 rodar).</reversibility>
  <done>A migração `20260816000000_financeiro_schema` consta como aplicada no banco remoto e o histórico das três migrations anteriores está intacto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar em produção que nada quebrou e que as regras valem</name>
  <precondition>A Task 2 aplicou a migração e `supabase migration list` mostra `20260816000000` em Remote.</precondition>
  <read_first>
    - `supabase/verificacao_financeiro.sql` — Parte B, BLOCOs 8, 9 e 10
    - O `RESULTADO DO ENSAIO` registrado no plano 04-02, para comparar os números
  </read_first>
  <what-built>
    A migração foi aplicada no banco de produção: `cards.ativo`, as tabelas `parcelas` e `parcela_lancamentos`, 10 CHECK constraints, o índice único `parcelas_unica_por_competencia`, dois índices de apoio e as duas policies de RLS via `public.is_team_member()`.
  </what-built>
  <how-to-verify>
    **No SQL Editor de produção**, rodar um bloco de cada vez:

    1. **BLOCO 8 (inventário)** — a primeira consulta deve listar exatamente 10 CHECK constraints com os nomes declarados; a segunda deve listar as duas policies, e nas colunas `qual` e `with_check` das duas tem que aparecer `is_team_member`. Se aparecer qualquer outro predicado de autorização, **pare e reporte** — isso é o buraco de segurança que a migration `20260811000000` fechou, reaberto.
    2. **BLOCO 9 (integridade)** — `cards_total` e `updated_at_max` têm que estar idênticos aos anotados antes do push, e `sem_ativo` tem que ser **0**. É o critério 1 do ROADMAP.
    3. **BLOCO 10 (provas contra o schema real)** — antes de rodar, confirme com o olho que o bloco termina em `rollback;`. Ele repete as 10 recusas de CHECK/índice único e as provas de RLS, agora contra as tabelas de verdade. Esperado: 10 linhas `OK recusado:`, as linhas `OK recusado pelo RLS:`, nenhuma `FALHOU:`.

    **No navegador:**

    4. Abra o app em produção e faça login com uma conta da allowlist. O board tem que carregar com os mesmos cards de sempre — mesma quantidade, mesmos valores, mesma ordem das colunas. Arraste um card e confirme que a movimentação ainda salva.
    5. Abra o modal de edição de um card qualquer, salve sem mudar nada e confirme que não aparece erro — a coluna nova não pode ter quebrado o caminho de escrita existente.

    **Bônus, se você tiver como (não bloqueia esta fase):** faça login com uma conta que **não** esteja em `allowed_members` e confirme que aparece a tela de "acesso pendente" em vez de um board vazio. Isso fecharia o `ROBUST-02` que ficou pendente na v1.0 por não ter sido testado com sessão real — está anotado em `STATE.md` como uma oportunidade justamente desta fase.
  </how-to-verify>
  <acceptance_criteria>
    - BLOCO 8: 10 CHECK constraints listadas com os nomes exatos de `## Artifacts this phase produces`; 2 policies listadas, ambas com `is_team_member` em `qual` e em `with_check`
    - BLOCO 8: nenhum outro predicado de autorização aparece nas policies das duas tabelas novas
    - BLOCO 9: `cards_total` e `updated_at_max` idênticos aos de antes do push; `sem_ativo` = 0
    - BLOCO 10: 10 mensagens `OK recusado:`, as mensagens `OK recusado pelo RLS:` das duas tabelas, e nenhuma `FALHOU:`
    - Navegador: o board carrega com os mesmos cards; arrastar um card salva; salvar o modal de edição não dá erro
    - O resultado é reportado com os números observados, não com um "está tudo certo"
  </acceptance_criteria>
  <reversibility rating="reversible">Só leitura e um bloco transacional que termina em `rollback`; a verificação no navegador não grava nada além do que o uso normal do app já faria.</reversibility>
  <resume-signal>Cole os números do BLOCO 9 (antes/depois), a contagem de constraints e policies do BLOCO 8, o resumo dos NOTICE do BLOCO 10, e confirme que o board carregou — ou descreva o que deu errado.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repositório -> banco de produção | `supabase db push` transporta DDL do disco para o banco onde moram os dados reais; é a fronteira mais perigosa da fase inteira |
| histórico de migrations local -> remoto | Um histórico divergente faria o push aplicar mais coisa do que esta fase autorizou |
| usuário autenticado -> allowlist | A partir deste push as tabelas financeiras existem de verdade; o RLS passa a ser a única coisa separando um login qualquer do dado financeiro |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-14 | Denial of Service | `supabase db push` contra produção | high | mitigate | `checkpoint:decision` bloqueante antes do push (Task 1), com a opção de conferir backup; DDL estritamente aditiva já ensaiada no plano 04-02; BLOCO 9 compara contagem e `updated_at_max` antes/depois |
| T-04-15 | Tampering | Histórico de migrations divergente | high | mitigate | `supabase migration list` antes do push; parar e reportar se qualquer migration antiga estiver só em Local, em vez de empurrar às cegas |
| T-04-16 | Elevation of Privilege | Policy aplicada com predicado errado | critical | mitigate | BLOCO 8 lê `pg_policies` no banco já aplicado e exige `is_team_member` em `qual` e em `with_check` das duas tabelas; qualquer outro predicado de autorização é motivo de parar |
| T-04-17 | Tampering | Regra de banco ausente depois do push | high | mitigate | BLOCO 8 conta as 10 CHECK constraints por nome e o BLOCO 10 reexecuta as 10 recusas contra o schema real, não contra um ensaio |
| T-04-18 | Information Disclosure | Dado financeiro legível por login fora da allowlist | critical | mitigate | BLOCO 10 repete as provas de RLS negativa e positiva já contra as tabelas aplicadas; a verificação bônus no navegador com conta fora da allowlist fecharia também o `ROBUST-02` herdado |
| T-04-19 | Tampering | Linhas de teste persistidas em produção | high | mitigate | O BLOCO 10 é uma transação terminando em `rollback` e o roteiro manda conferir isso com o olho antes de rodar; o runbook não tem nenhum `commit;` |

**Supply chain:** nenhuma instalação de pacote neste plano; o único binário externo usado é o Supabase CLI, já em uso no projeto desde `20260728000000`. O portão de legitimidade de pacotes não se aplica.
</threat_model>

<verification>
1. `supabase migration list` mostra `20260816000000` em Local e Remote; as três migrations anteriores continuam intactas
2. BLOCO 8: 10 CHECK constraints e 2 policies, todas com `is_team_member` nas duas expressões
3. BLOCO 9: contagem de cards e `updated_at_max` inalterados; zero cards com `ativo` diferente de true
4. BLOCO 10: 10 `OK recusado:`, as recusas de RLS nas duas tabelas, nenhum `FALHOU:`
5. Navegador: board carrega igual, arrastar salva, editar salva
</verification>

<success_criteria>
- Critério 1 do ROADMAP satisfeito: a migração aplicou limpo, nenhum card foi apagado ou alterado, todos têm `ativo = true`, e o board continua carregando
- Critério 2 do ROADMAP satisfeito contra o banco aplicado: fora da allowlist não lê nem grava; dentro da allowlist, lê e grava
- Critério 3 do ROADMAP satisfeito contra o banco aplicado: as cinco famílias de recusa acontecem com SQL escrito na mão
- FINSEG-01 e FINSEG-02 estão vivos em produção, prontos para as Phases 5 a 8 se apoiarem neles sem reimplementá-los
</success_criteria>

<output>
Create `.planning/phases/04-funda-o-financeira/04-03-SUMMARY.md` when done
</output>
