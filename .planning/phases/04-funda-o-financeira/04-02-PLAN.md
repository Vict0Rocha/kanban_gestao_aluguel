---
phase: 04-funda-o-financeira
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - supabase/verificacao_financeiro.sql
  - supabase/migrations/20260816000000_financeiro_schema.sql
autonomous: false
requirements: [FINSEG-01, FINSEG-02]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A DDL da migração aplica sem erro contra o schema real de produção, dentro de uma transação que termina em `rollback` — nada é gravado"
    - "Rodar a DDL duas vezes seguidas na mesma transação não gera erro (a migração é reexecutável)"
    - "Sob um papel `authenticated` com e-mail fora de `public.allowed_members`, `select count(*)` em `parcelas` e em `parcela_lancamentos` devolve 0 e o `insert` é recusado — FINSEG-01"
    - "Sob um e-mail que está em `public.allowed_members`, os mesmos `select` e `insert` funcionam — o controle positivo que dá sentido ao resultado negativo"
    - "As 10 recusas de CHECK/índice único acontecem de verdade contra o schema real: valor negativo, status inválido, competência fora do dia 1, conciliada sem autor, duplicata de (card_id, competencia), tipo inválido, valor negativo em lançamento, valor zero em pagamento, destrava com motivo nulo, destrava com motivo em branco — FINSEG-02"
    - "Depois do `rollback`, `select count(*) from public.cards` devolve o mesmo número anotado no BLOCO 1"
    - "O resultado do ensaio fica registrado por escrito no cabeçalho de `supabase/verificacao_financeiro.sql`, com data"
  artifacts:
    - supabase/verificacao_financeiro.sql
  key_links:
    - "BLOCO 6 (RLS negativa) só tem valor se o BLOCO 5 (grants) e o BLOCO 7 (RLS positiva) passarem — sem eles, um 0 pode ser falta de permissão em vez de RLS"
---

<objective>
Provar contra o banco de produção real, **sem gravar nada**, que a migração escrita no plano 04-01 faz o que promete: aplica limpo, é reexecutável, o RLS barra quem está fora da allowlist e as CHECK constraints recusam dado financeiro inválido.

Todo o ensaio acontece dentro de uma transação que termina em `rollback`. É o último ponto em que um erro na migração custa zero — depois dele vem o push, que é porta de mão única.

Purpose: transformar FINSEG-01 e FINSEG-02 de "está escrito no arquivo" em "foi observado acontecendo", que é o único nível de prova disponível num projeto sem suíte automatizada.
Output: resultado do ensaio registrado no cabeçalho do runbook e, se o ensaio apontar problema, a migração corrigida.
</objective>

## Artifacts this phase produces — Artefatos que esta fase produz

Este plano **não cria símbolo novo**. Ele exercita e registra os símbolos declarados no plano 04-01 — ver a seção `## Artifacts this phase produces` de `04-01-PLAN.md` para a lista canônica de tabelas, colunas, constraints, índices e policies da fase.

O que este plano produz:
- Um bloco de comentário `-- RESULTADO DO ENSAIO` no topo de `supabase/verificacao_financeiro.sql`, com a data e o veredito de cada bloco
- Se necessário, correções em `supabase/migrations/20260816000000_financeiro_schema.sql` apontadas pelo ensaio (o caso mais provável: linhas de `grant` explícito para o papel `authenticated`)

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-funda-o-financeira/04-CONTEXT.md
@.planning/phases/04-funda-o-financeira/04-01-SUMMARY.md
@supabase/verificacao_financeiro.sql
@supabase/migrations/20260816000000_financeiro_schema.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Ensaio da migração no SQL Editor de produção, com rollback</name>
  <precondition>O operador tem acesso ao SQL Editor do projeto Supabase de produção, sabe pelo menos um e-mail presente em `public.allowed_members`, e `public.cards` tem pelo menos uma linha (o ensaio usa um `card_id` real).</precondition>
  <read_first>
    - `supabase/verificacao_financeiro.sql` — o runbook completo; a Parte A é o que será rodado
    - `supabase/migrations/20260816000000_financeiro_schema.sql` — para conferir que a DDL duplicada no BLOCO 2 corresponde ao arquivo de migração
  </read_first>
  <what-built>
    O plano 04-01 escreveu a migração aditiva do módulo financeiro (`cards.ativo`, `parcelas`, `parcela_lancamentos`, 10 CHECK constraints, índice único, 2 policies de RLS via `is_team_member()`) e o runbook `supabase/verificacao_financeiro.sql`, cuja Parte A é um ensaio completo que roda contra o banco real e desfaz tudo no fim.
  </what-built>
  <how-to-verify>
    Abra o SQL Editor do projeto Supabase de produção (Dashboard -> SQL Editor). Rode **um bloco de cada vez**, selecionando o trecho com o mouse e apertando Run — é assim que os runbooks deste projeto foram feitos para serem usados.

    1. **BLOCO 1 (pré-voo)** — anote os dois números: `cards_total` e `updated_at_max`. Você vai comparar com eles no fim.
    2. **BLOCO 7** — antes de rodar qualquer coisa, troque o marcador `SEU-EMAIL-DA-ALLOWLIST@exemplo.com` pelo seu e-mail real, exatamente como ele aparece em `auth.users`. Copie e cole, não digite de memória.
    3. **Parte A inteira** — selecione do `begin;` do BLOCO 2 até o `rollback;` final (BLOCOs 2 a 7) e rode de uma vez só. Ela precisa ser atômica.
    4. Leia a aba de mensagens/NOTICE do resultado e confirme, na ordem:
       - a DDL passou duas vezes sem erro (idempotência)
       - `cards_total` continua igual ao do BLOCO 1 e `count(*) where ativo is not true` deu **0**
       - o BLOCO 5 listou privilégios do papel `authenticated` para as duas tabelas (se veio **vazio**, anote isso — é o desvio previsto)
       - aparecem **10 linhas** `OK recusado:` — 5 do BLOCO 3 (parcelas) e 5 do BLOCO 4 (lançamentos)
       - aparecem as linhas `OK recusado pelo RLS:` do BLOCO 6, e os dois `count(*)` do BLOCO 6 deram **0**
       - no BLOCO 7 os dois `count(*)` voltaram diferentes de zero e o `insert` completou sem erro
       - **nenhuma** linha começando com `FALHOU:`
    5. **BLOCO 1 de novo**, agora fora da transação — `cards_total` e `updated_at_max` têm que estar idênticos aos do passo 1. Se mudaram, o `rollback` não rodou e é preciso avisar antes de qualquer coisa.

    Se o Supabase CLI estiver linkado e autenticado (`SUPABASE_ACCESS_TOKEN` no ambiente), o mesmo ensaio pode ser rodado por `supabase db execute -f supabase/verificacao_financeiro.sql` — mas nesse caso rode só a Parte A, porque a Parte B pressupõe o schema já aplicado.
  </how-to-verify>
  <acceptance_criteria>
    - O operador reporta: DDL aplicou nas duas passagens sem erro
    - O operador reporta: `count(*) from public.cards where ativo is not true` = 0, e `cards_total` idêntico antes e depois da transação
    - O operador reporta: 10 mensagens `OK recusado:` e nenhuma `FALHOU:`
    - O operador reporta: BLOCO 6 devolveu `count(*)` = 0 nas duas tabelas e recusou os dois `insert`
    - O operador reporta: BLOCO 7 leu linhas e gravou com sucesso sob o e-mail da allowlist
    - O operador reporta o resultado do BLOCO 5 (lista de privilégios de `authenticated`, ou vazio)
  </acceptance_criteria>
  <reversibility rating="reversible">O ensaio inteiro está dentro de `begin; ... rollback;`. Se algo der errado no meio, o Postgres aborta a transação e nada persiste.</reversibility>
  <resume-signal>Cole o resumo dos NOTICE (as linhas `OK recusado`, qualquer `FALHOU`, o resultado do BLOCO 5 e os dois números do BLOCO 1 antes/depois), ou descreva o erro encontrado.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar o resultado do ensaio e aplicar as correções que ele apontou</name>
  <files>supabase/verificacao_financeiro.sql, supabase/migrations/20260816000000_financeiro_schema.sql</files>
  <read_first>
    - `supabase/verificacao_financeiro.sql` — o cabeçalho onde o registro entra
    - `supabase/migrations/20260816000000_financeiro_schema.sql` — alvo de qualquer correção
    - `supabase/hardening_seguranca.sql` — estilo de cabeçalho em português a seguir no registro
    - O relato do operador na Task 1
  </read_first>
  <action>
Gravar a evidência do ensaio e corrigir o que ele apontou. Duas partes, nesta ordem.

**Parte obrigatória — registro.** Acrescentar no topo de `supabase/verificacao_financeiro.sql`, logo abaixo do cabeçalho existente, um bloco de comentário rotulado `RESULTADO DO ENSAIO` contendo: a data do ensaio; a contagem de cards antes e depois da transação; o número de mensagens `OK recusado` observadas; o veredito do BLOCO 5 (privilégios presentes ou ausentes); o veredito do BLOCO 6 (contagem zero e insert recusado) e do BLOCO 7 (leitura e escrita funcionando); e a frase explícita de que nenhuma linha `FALHOU:` apareceu. Escrever em português, no mesmo tom dos cabeçalhos de `hardening_seguranca.sql` — contexto primeiro, resultado depois. O ponto deste registro é que daqui a seis meses ninguém precise refazer o ensaio para saber que ele foi feito.

**Parte condicional — correções.** Se o BLOCO 5 voltou vazio (o papel `authenticated` não tem privilégios nas tabelas novas), acrescentar ao arquivo de migração, depois das seções de RLS, as duas linhas `grant select, insert, update, delete on public.parcelas to authenticated;` e `grant select, insert, update, delete on public.parcela_lancamentos to authenticated;`, com um comentário registrando o desvio: as tabelas existentes dependem do default privileges do Supabase e não declaram grants, e a declaração explícita aqui existe porque `permission denied for table` e violação de RLS compartilham o SQLSTATE `42501` — sem o grant, o resultado do BLOCO 6 seria ambíguo. Depois disso, pedir ao operador para rodar a Parte A de novo e confirmar que o BLOCO 5 agora lista os privilégios.

Se qualquer mensagem `FALHOU:` apareceu, corrigir a causa no arquivo de migração (constraint com expressão errada, policy mal formada, nome divergente), atualizar a cópia da DDL no BLOCO 2 do runbook para bater com a migração, e pedir novo ensaio. Não registrar `RESULTADO DO ENSAIO` como aprovado enquanto houver `FALHOU:` pendente.

Se o ensaio passou inteiro e o BLOCO 5 listou os privilégios, nenhuma alteração na migração é necessária — registrar isso explicitamente no bloco de resultado, para que a ausência de mudança não pareça esquecimento.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && R=supabase/verificacao_financeiro.sql && grep -q "RESULTADO DO ENSAIO" "$R" && grep -qE '20[0-9]{2}-[0-9]{2}-[0-9]{2}' "$R" && test "$(grep -c 'FALHOU: pendente' "$R")" -eq 0 && M=supabase/migrations/20260816000000_financeiro_schema.sql && S=$(grep -v '^[[:space:]]*--' "$M") && test "$(echo "$S" | grep -c 'auth\.role')" -eq 0 && test "$(echo "$S" | grep -Ec 'drop (table|column)|alter column .* type|rename')" -eq 0 && test "$(echo "$S" | grep -c 'is_team_member')" -ge 4 && echo ENSAIO_REGISTRADO</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/verificacao_financeiro.sql` contém um bloco `RESULTADO DO ENSAIO` com uma data no formato `AAAA-MM-DD`
    - Esse bloco cita as contagens de cards antes/depois, o número de `OK recusado` observados, e os vereditos dos BLOCOs 5, 6 e 7
    - Nenhum `FALHOU:` continua pendente — se houve, a migração foi corrigida e um novo ensaio foi rodado e registrado
    - Se o BLOCO 5 voltou vazio, a migração passou a conter `grant select, insert, update, delete on public.parcelas to authenticated` e a linha equivalente para `public.parcela_lancamentos`, com comentário justificando o desvio
    - A migração continua com `grep -c 'auth\.role'` igual a 0, `grep -c 'is_team_member'` maior ou igual a 4, e zero DDL destrutiva, ignorando linhas de comentário
    - A cópia da DDL no BLOCO 2 do runbook corresponde ao conteúdo atual do arquivo de migração
  </acceptance_criteria>
  <reversibility rating="reversible">Edita dois arquivos `.sql` no repositório; o banco continua sem nenhuma alteração aplicada.</reversibility>
  <done>O resultado do ensaio está escrito no runbook com data, e a migração está no estado exato que vai ser empurrado — sem `FALHOU:` pendente e com os grants resolvidos se o ensaio pediu.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador -> SQL Editor de produção | O SQL Editor roda como superusuário e **ignora RLS por padrão**; é por isso que os blocos de RLS trocam de papel com `set local role authenticated` antes de medir qualquer coisa |
| transação de ensaio -> dados reais | Linhas de teste são inseridas em tabelas que ainda não existem no schema real, dentro de uma transação; só o `rollback` impede que virem dado permanente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-10 | Tampering | Linhas de teste do ensaio | high | mitigate | A Parte A inteira é uma única transação terminando em `rollback`; o passo 5 do roteiro reexecuta o BLOCO 1 fora da transação e exige `cards_total` e `updated_at_max` idênticos aos do passo 1. O runbook não contém nenhum `commit;` |
| T-04-11 | Spoofing | Falso negativo de RLS | high | mitigate | Um `count(*)` igual a 0 no BLOCO 6 poderia vir de falta de `grant`, não de RLS. O BLOCO 5 (inventário de privilégios) e o BLOCO 7 (controle positivo com e-mail da allowlist) tornam essa confusão detectável; o critério de aceite exige o resultado dos três |
| T-04-12 | Spoofing | E-mail de teste que existe de verdade | medium | mitigate | O BLOCO 6 usa `intruso-teste@exemplo.invalid` — o TLD `.invalid` é reservado pela RFC 2606 e não pode ser um endereço real presente em `allowed_members` |
| T-04-13 | Information Disclosure | Evidência do ensaio no repositório | low | accept | O registro guarda contagens e vereditos, nunca nome de proprietário, telefone ou valor de aluguel. O e-mail real da allowlist fica só na sessão do SQL Editor do operador; no arquivo permanece o marcador a trocar |

**Supply chain:** nenhuma instalação de pacote neste plano — o portão de legitimidade de pacotes não se aplica.
</threat_model>

<verification>
1. O operador rodou a Parte A no SQL Editor de produção e reportou 10 `OK recusado`, zero `FALHOU:`, BLOCO 6 com contagem zero e insert recusado, BLOCO 7 lendo e gravando
2. `cards_total` e `updated_at_max` são idênticos antes e depois da transação — o `rollback` funcionou
3. `supabase/verificacao_financeiro.sql` traz o bloco `RESULTADO DO ENSAIO` com data e os vereditos
4. A migração está no estado final a ser empurrado: sem `auth\.role`, sem DDL destrutiva, com as duas policies e — se o ensaio pediu — com os grants explícitos
</verification>

<success_criteria>
- O critério 2 do ROADMAP (RLS barra quem está fora da allowlist, libera quem está nela) foi **observado**, não só escrito
- O critério 3 do ROADMAP (o banco recusa sozinho valor negativo, status inválido, tipo inválido, destrava sem motivo e duplicata de competência) foi **observado**, com SQL escrito na mão
- O critério 1 do ROADMAP foi ensaiado: a DDL aplica limpo e nenhum card é apagado ou alterado
- A migração está congelada e pronta para o push do plano 04-03
</success_criteria>

<output>
Create `.planning/phases/04-funda-o-financeira/04-02-SUMMARY.md` when done
</output>
