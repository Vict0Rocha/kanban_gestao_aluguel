---
phase: 05-aba-financeiro-com-parcelas-autom-ticas
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/components/financeiro/parcela-situacao-badge.tsx
  - web/src/components/financeiro/mes-switcher.tsx
  - web/src/components/financeiro/financeiro-view.tsx
  - web/src/components/financeiro/parcelas-table.tsx
  - web/src/app/(app)/financeiro/page.tsx
autonomous: false
requirements: [FINUI-02, FINUI-03]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Dentro da aba Financeiro existe um seletor único entre `Mês atual` e `Próximo mês`, e trocar de segmento troca as parcelas listadas sem recarregar a página nem mudar de rota (FINUI-02, D-12)"
    - "O seletor é um segmented control de seleção única (`bg-muted p-1 rounded-full`, segmento ativo `bg-card text-foreground shadow-sm`), não abas dentro de abas e não duas rotas separadas (D-12)"
    - "A situação de cada parcela aparece como ícone + rótulo em português, nunca cor sozinha, cobrindo os 5 estados `a vencer`/`vencida`/`paga`/`parcial`/`conciliada` — os três últimos ficam prontos agora para que a Phase 6/7 não precise mexer neste componente (FINUI-03, D-13)"
    - "UI Consideration `empty` (sem contrato ativo): a lista mostra `Nenhum contrato ativo no momento. Marque um contrato como ativo no board para ele começar a gerar parcelas automaticamente.`"
    - "UI Consideration `empty` (há contrato ativo mas nenhuma linha nesta competência): a lista mostra `Nenhuma parcela para este período. Contratos ativos sem parcela aqui já passaram do período contratado.`"
    - "UI Consideration `empty` (nenhum board): a página reaproveita literalmente `Nenhum board encontrado.`, a mesma frase já usada por `BoardPage`"
    - "UI Consideration `zero-one-many`: zero linhas cai no estado vazio; uma ou muitas linhas renderizam pelo mesmo primitivo `Table` sem mudança de layout e sem contador de plural"
    - "UI Consideration `partial`: descartada como inaplicável — uma parcela gerada está sempre completa, e `valor pago = R$ 0,00` nesta fase é estado válido, não campo faltando"
    - "UI Consideration `long-text` (endereço longo): a célula de imóvel quebra linha naturalmente como em `ContractsTable`, sem truncar nem reticências"
    - "O seletor de mês não usa a cor de destaque da marca — a UI-SPEC reserva `--primary` ao link ativo da navegação e à pilha ativo do card, e um segundo destaque na mesma tela competiria com ele"
    - "Nenhum componente novo desta fase usa o peso 500 nem introduz valor de espaçamento fora da grade de 4px além dos dois herdados (`gap-1.5`, `p-5`)"
  artifacts:
    - "web/src/components/financeiro/parcela-situacao-badge.tsx — badge de 5 estados"
    - "web/src/components/financeiro/mes-switcher.tsx — segmented control de seleção única"
    - "web/src/components/financeiro/financeiro-view.tsx — client component que guarda a competência selecionada"
  key_links:
    - "`page.tsx` → `FinanceiroView` (recebe as linhas das duas competências já montadas no servidor) → filtro por `competencia` → `ParcelasTable` (é o elo que faz as duas visões existirem sem segunda ida ao banco)"
    - "`situacaoDaParcela()` de `parcelas.ts` → `ParcelaSituacaoBadge` (é o elo que mantém `a vencer`/`vencida` derivadas na leitura até o pixel)"
---

<objective>
Terminar a interface da aba Financeiro: as duas visões separadas (`Mês atual` / `Próximo mês`), o
badge de situação com os 5 estados e ícone, e os três estados vazios distintos que a UI-SPEC fixa.

Purpose: o plano 05-01 provou o caminho de dados e já gera as duas competências, mas só exibe uma.
Este plano é o que entrega FINUI-02 — a separação entre mês atual e próximo mês foi um pedido
explícito e específico do usuário, não um detalhe de layout.
Output: a aba Financeiro no formato final definido pelo contrato de design aprovado.
</objective>

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-CONTEXT.md
@.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-UI-SPEC.md
@.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-01-SUMMARY.md
@web/AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Badge de situação com os 5 estados e estados vazios da tabela</name>

  <files>
web/src/components/financeiro/parcela-situacao-badge.tsx
web/src/components/financeiro/parcelas-table.tsx
  </files>

  <read_first>
- `web/src/components/reports/contract-status-badge.tsx` — o irmão que este componente espelha: mapa de estados com ícone + rótulo + classe de cor, e o comentário sobre por que a cor nunca é o único sinal
- `web/src/components/reports/contracts-table.tsx` — o estilo do parágrafo de estado vazio
- `web/src/components/financeiro/parcelas-table.tsx` — o arquivo a ser alterado (criado no plano 05-01)
- `web/src/lib/kanban/parcelas.ts` — o tipo `Situacao` e o que `situacaoDaParcela()` devolve
- `web/src/app/globals.css` — as variáveis `--status-good`, `--status-warning`, `--status-critical` e a regra que rege o uso delas
- `.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-UI-SPEC.md` — seção Color (a tabela de 5 estados com ícone e token) e Copywriting Contract (os dois textos de estado vazio)
  </read_first>

  <action>
**1. `web/src/components/financeiro/parcela-situacao-badge.tsx`** (novo). Componente de
apresentação `ParcelaSituacaoBadge` com uma prop: `situacao: Situacao` (tipo importado de
`@/lib/kanban/parcelas` com `import type`). Estrutura igual à de `ContractStatusBadge`: um objeto
constante mapeando cada valor para `{ icon, label, className }`, e um `<span>` com
`inline-flex items-center gap-1.5 text-xs font-semibold` mais a classe de cor, contendo
`<Icon className="size-3.5 shrink-0" />` e o rótulo.

Mapa exato, conforme a tabela de Color da UI-SPEC (ícones de `lucide-react`):
- `a_vencer` → ícone `Clock`, rótulo `A vencer`, classe `text-muted-foreground` (neutro: ainda não é
  problema)
- `vencida` → ícone `AlertCircle`, rótulo `Vencida`, classe `text-status-critical`
- `paga` → ícone `CheckCircle2`, rótulo `Paga`, classe `text-status-good`
- `parcial` → ícone `CircleDollarSign`, rótulo `Parcial`, classe `text-status-warning`
- `conciliada` → ícone `Lock`, rótulo `Conciliada`, classe `text-muted-foreground`

Os três últimos não são alcançáveis com os dados desta fase (não existe baixa nem conciliação até as
Phases 6/7) e isso é deliberado: o componente nasce completo para que aquelas fases não precisem
reabri-lo. Peso de ênfase é 600 em todo componente novo desta fase — o peso 500 usado pelo irmão
mais antigo não é herdado. `gap-1.5` é permitido: é uma das duas exceções de espaçamento herdadas
declaradas na UI-SPEC, e é o que mantém o alinhamento com o badge de contrato ao lado.

**2. `web/src/components/financeiro/parcelas-table.tsx`** — duas mudanças:
- Troque o texto simples de situação pelo `<ParcelaSituacaoBadge situacao={linha.situacao} />`.
- Substitua o estado vazio único por dois, distinguidos por uma prop nova
  `vazio: "sem-contrato-ativo" | "sem-parcela-no-periodo"`. Quando `linhas` estiver vazia e não
  houver erro, renderize, no mesmo estilo de parágrafo do estado vazio de `ContractsTable`:
  - `sem-contrato-ativo` → `Nenhum contrato ativo no momento. Marque um contrato como ativo no board para ele começar a gerar parcelas automaticamente.`
  - `sem-parcela-no-periodo` → `Nenhuma parcela para este período. Contratos ativos sem parcela aqui já passaram do período contratado.`
  Copie os dois textos exatamente como estão no Copywriting Contract, acentuação inclusive.
- A célula de endereço continua quebrando linha naturalmente: não adicione `truncate`, `line-clamp`
  nem largura fixa (a UI Consideration `long-text` segue o precedente de `ContractsTable`).
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
- `cd web && npm run lint` e `cd web && npm run build` saem com código 0
- `test -f web/src/components/financeiro/parcela-situacao-badge.tsx`
- `grep -cE '"a_vencer"|"vencida"|"paga"|"parcial"|"conciliada"|a_vencer:|vencida:|paga:|parcial:|conciliada:' web/src/components/financeiro/parcela-situacao-badge.tsx` devolve 5 ou mais (os 5 estados mapeados)
- `grep -cE 'Clock|AlertCircle|CheckCircle2|CircleDollarSign|Lock' web/src/components/financeiro/parcela-situacao-badge.tsx` devolve 5 ou mais (os 5 ícones)
- `grep -cE 'text-status-critical|text-status-good|text-status-warning' web/src/components/financeiro/parcela-situacao-badge.tsx` devolve 3 ou mais
- `grep -vE '^\s*(//|\*|/\*)' web/src/components/financeiro/parcela-situacao-badge.tsx | grep -c 'font-medium'` devolve 0
- `grep -c 'Nenhum contrato ativo no momento. Marque um contrato como ativo no board para ele começar a gerar parcelas automaticamente.' web/src/components/financeiro/parcelas-table.tsx` devolve 1
- `grep -c 'Nenhuma parcela para este período. Contratos ativos sem parcela aqui já passaram do período contratado.' web/src/components/financeiro/parcelas-table.tsx` devolve 1
- `grep -c 'ParcelaSituacaoBadge' web/src/components/financeiro/parcelas-table.tsx` devolve 2 ou mais (import + uso)
- `grep -oE '\b(py|px|p|m|mt|mb|ml|mr|gap)-0\.5\b|\bp-1\.5\b' web/src/components/financeiro/parcela-situacao-badge.tsx web/src/components/financeiro/parcelas-table.tsx | wc -l` devolve 0
- `grep -vE '^\s*(//|\*|/\*)' web/src/components/financeiro/parcela-situacao-badge.tsx | grep -cE 'bg-primary|text-primary'` devolve 0 (situação é sinal de status, nunca a cor de destaque da marca)
  </acceptance_criteria>

  <done>
O badge existe com os 5 estados, cada um com ícone e rótulo em português e token de status; a tabela
usa o badge e distingue os dois textos de estado vazio exigidos pelo contrato de design; lint e build
passam.
  </done>
</task>

<task type="auto">
  <name>Task 2: Seletor Mês atual / Próximo mês e a view cliente</name>

  <files>
web/src/components/financeiro/mes-switcher.tsx
web/src/components/financeiro/financeiro-view.tsx
web/src/app/(app)/financeiro/page.tsx
  </files>

  <read_first>
- `web/src/components/reports/reports-view.tsx` — o `FilterChip` cuja linguagem visual o seletor segue (pílula `rounded-full`, `px-3 py-1`, `aria-pressed`) e o padrão de componente cliente que guarda o estado da tela
- `web/src/app/(app)/financeiro/page.tsx` — o arquivo a ser alterado (criado no plano 05-01)
- `web/src/components/financeiro/parcelas-table.tsx` — as props que a tabela agora expõe
- `web/src/lib/kanban/parcelas.ts` — `competenciasAlvo()`, `montarLinhas()` e o tipo `LinhaParcela`
- `web/src/app/(app)/page.tsx` — a caixa `Nenhum board encontrado.` a reaproveitar
- `.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-UI-SPEC.md` — Component Inventory (linha `mes-switcher.tsx`), Color (por que o segmento ativo NÃO usa a cor de destaque) e Visual Hierarchy
  </read_first>

  <action>
**1. `web/src/components/financeiro/mes-switcher.tsx`** (novo). Componente cliente `MesSwitcher`,
seleção única. Props: `value: "atual" | "proximo"` e `onChange: (value: "atual" | "proximo") => void`.

Marcação: um `<div role="group" aria-label="Competência">` com classes
`inline-flex items-center gap-1 rounded-full bg-muted p-1`, contendo dois `<button type="button">`
com `aria-pressed` refletindo a seleção. Rótulos exatos: `Mês atual` e `Próximo mês`.
Classes de cada botão: `rounded-full px-3 py-1 text-xs font-semibold transition-colors`; ativo
recebe `bg-card text-foreground shadow-sm`, inativo recebe
`text-muted-foreground hover:text-foreground`.

O segmento ativo é uma superfície elevada, não uma superfície colorida. Isso é deliberado: o
contrato de design reserva a cor de destaque da marca ao link ativo da navegação e à pilha
ativo/inativo do card, e um terceiro destaque na mesma tela competiria com eles. Não use
`bg-primary`, `text-primary` nem `border-primary` neste componente.

**2. `web/src/components/financeiro/financeiro-view.tsx`** (novo). Componente cliente
`FinanceiroView`, no molde de `ReportsView`: guarda a seleção com `React.useState` e nada mais.
Props: `linhasAtual: LinhaParcela[]`, `linhasProximo: LinhaParcela[]`, `temContratoAtivo: boolean`,
`erro?: boolean`. Renderiza, dentro de um wrapper `flex flex-col gap-4`:
- o `<MesSwitcher>` no topo;
- o `<ParcelasTable>` abaixo, recebendo as linhas do mês selecionado, o título
  `Parcelas — mês atual` ou `Parcelas — próximo mês` conforme a seleção, o flag `erro`, e
  `vazio={temContratoAtivo ? "sem-parcela-no-periodo" : "sem-contrato-ativo"}`.

Um único componente de lista alimentado por dois conjuntos de linhas — as duas visões são filtro de
competência sobre o mesmo dado, não dois modelos diferentes (D-12). Trocar de segmento não navega,
não recarrega e não vai ao banco: as duas competências já vieram montadas do servidor.

**3. `web/src/app/(app)/financeiro/page.tsx`** — troque a renderização direta da tabela por
`<FinanceiroView ...>`:
- Separe as parcelas lidas nas duas competências devolvidas por `competenciasAlvo(hojeISO)` e passe
  cada conjunto por `montarLinhas(..., hojeISO)`.
- Calcule `temContratoAtivo` com uma consulta enxuta:
  `.from("cards").select("id", { count: "exact", head: true }).eq("ativo", true)` — usar o `count`
  em vez de trazer as linhas mantém a página leve, e é o que distingue os dois estados vazios.
  Se essa contagem falhar, trate como `true` (mostrar o texto de período é menos enganoso do que
  dizer "nenhum contrato ativo" por causa de uma falha de rede).
- Mantenha o `<h1>` `Financeiro` e o subtítulo `Parcelas do mês atual e do próximo mês de cada
  contrato ativo.` na própria página, e mantenha o caminho de `Nenhum board encontrado.` intocado.
- Não mova a geração: `garantirParcelas` continua rodando no servidor antes da leitura, como no
  plano 05-01.
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
- `cd web && npm run lint` e `cd web && npm run build` saem com código 0
- `test -f web/src/components/financeiro/mes-switcher.tsx` e `test -f web/src/components/financeiro/financeiro-view.tsx`
- `head -1 web/src/components/financeiro/mes-switcher.tsx` e `head -1 web/src/components/financeiro/financeiro-view.tsx` contêm `"use client"`
- `grep -c 'Mês atual' web/src/components/financeiro/mes-switcher.tsx` devolve 1 e `grep -c 'Próximo mês' web/src/components/financeiro/mes-switcher.tsx` devolve 1
- `grep -c 'bg-muted' web/src/components/financeiro/mes-switcher.tsx` devolve 1 ou mais e `grep -c 'bg-card' web/src/components/financeiro/mes-switcher.tsx` devolve 1 ou mais
- `grep -c 'aria-pressed' web/src/components/financeiro/mes-switcher.tsx` devolve 1 ou mais
- `grep -vE '^\s*(//|\*|/\*)' web/src/components/financeiro/mes-switcher.tsx | grep -cE 'bg-primary|text-primary|border-primary'` devolve 0
- `grep -vE '^\s*(//|\*|/\*)' web/src/components/financeiro/mes-switcher.tsx web/src/components/financeiro/financeiro-view.tsx | grep -c 'font-medium'` devolve 0
- `grep -oE '\b(py|px|p|m|mt|mb|ml|mr|gap)-0\.5\b|\bp-1\.5\b' web/src/components/financeiro/mes-switcher.tsx web/src/components/financeiro/financeiro-view.tsx | wc -l` devolve 0
- `grep -c 'Parcelas — mês atual' web/src/components/financeiro/financeiro-view.tsx` devolve 1 e `grep -c 'Parcelas — próximo mês' web/src/components/financeiro/financeiro-view.tsx` devolve 1
- `grep -c 'FinanceiroView' "web/src/app/(app)/financeiro/page.tsx"` devolve 2 ou mais (import + uso)
- `grep -c 'Nenhum board encontrado.' "web/src/app/(app)/financeiro/page.tsx"` devolve 1
- `grep -c 'garantirParcelas' "web/src/app/(app)/financeiro/page.tsx"` devolve 2 ou mais (import + chamada — a geração continua no servidor)
- A saída do build não mostra a rota `/financeiro` como estática (ela lê cookies, então continua dinâmica)
  </acceptance_criteria>

  <done>
A aba tem o seletor `Mês atual` / `Próximo mês` funcionando como seleção única, alimentado por dois
conjuntos de linhas já montados no servidor; a página passa a renderizar `FinanceiroView`; lint e
build passam.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Conferir as duas visões e os estados de tela no navegador</name>

  <files>(nenhum arquivo é alterado — verificação visual e de comportamento)</files>

  <read_first>
- `.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-UI-SPEC.md` — as seções Visual Hierarchy, Color e Copywriting Contract são o gabarito desta conferência
- `.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-01-SUMMARY.md` — os números de parcelas por competência anotados na verificação da geração
  </read_first>

  <what-built>
O seletor `Mês atual` / `Próximo mês` como segmented control de seleção única, o badge de situação
com ícone e cor de status nos 5 estados, e os três textos de estado vazio distintos.
  </what-built>

  <how-to-verify>
Abra `/financeiro` em produção, logado com um e-mail da allowlist.

1. **Duas visões (FINUI-02).** O seletor aparece acima da tabela, com `Mês atual` selecionado.
   Clique em `Próximo mês`: a lista troca, o título da tabela vira `Parcelas — próximo mês`, a URL
   **não** muda e a página **não** recarrega (sem piscar o esqueleto de carregamento). Clique de volta
   em `Mês atual`: volta ao conjunto anterior. Só um dos dois fica selecionado por vez.
2. **Os dois conjuntos são de fato diferentes.** Compare a coluna Vencimento nas duas visões: as
   datas do próximo mês devem estar todas um mês à frente das do mês atual, e a contagem de linhas
   deve bater com os números por competência anotados no 05-01-SUMMARY.
3. **Situação (FINUI-03).** Cada linha traz ícone + palavra, nunca só cor. Uma parcela com vencimento
   já passado mostra `Vencida` com ícone de alerta em vermelho; uma com vencimento à frente mostra
   `A vencer` em cinza neutro. Na visão de próximo mês, praticamente tudo deve estar `A vencer`.
4. **Hierarquia visual.** O painel branco da tabela é a superfície elevada dominante; o seletor é o
   único controle em forma de pílula acima dela; o segmento ativo é uma superfície clara com sombra,
   **não** verde. O único verde da tela deve ser o link `Financeiro` aceso na barra lateral escura.
5. **Estado vazio de período.** Se alguma das duas visões estiver vazia, o texto mostrado deve ser
   `Nenhuma parcela para este período. Contratos ativos sem parcela aqui já passaram do período
   contratado.` (e não o texto de "nenhum contrato ativo", que só aparece quando não existe nenhum
   contrato marcado como ativo).
6. **Endereço longo.** Ache (ou observe) a linha com o endereço mais comprido: ela deve quebrar em
   mais de uma linha dentro da célula, sem reticências e sem empurrar a tabela para fora da tela.
7. **Rolagem e carregamento.** Com a lista cheia, role a página: a rolagem acontece na área de
   conteúdo, como no Board e em Relatórios. Ao clicar em `Financeiro` vindo de outra aba, aparece o
   esqueleto cinza de carregamento já existente — nenhuma tela de carregamento nova.
8. **Teclado.** Dê `Tab` até o seletor e alterne com `Enter`/`Espaço`: os dois botões são
   alcançáveis e o anel de foco aparece.
  </how-to-verify>

  <acceptance_criteria>
- Trocar de segmento troca as linhas e o título da tabela sem mudar a URL e sem recarregar a página
- Exatamente um dos dois segmentos fica selecionado por vez, e o ativo é uma superfície clara com sombra, não verde
- As datas de vencimento da visão `Próximo mês` estão um mês à frente das da visão `Mês atual`, e as contagens batem com os números anotados no 05-01-SUMMARY
- Toda linha mostra a situação como ícone + palavra em português; `Vencida` sai em vermelho de status e `A vencer` em cinza neutro
- O único elemento verde da tela é o link `Financeiro` ativo na barra lateral
- Quando uma visão está vazia, o texto exibido é o de período, não o de "nenhum contrato ativo"
- O endereço mais longo quebra linha dentro da célula, sem reticências e sem estourar a largura
- Os dois botões do seletor são alcançáveis por `Tab` e acionáveis por teclado, com anel de foco visível
  </acceptance_criteria>

  <resume-signal>Responda "aprovado" ou descreva o que divergiu, item por item</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Component → componente cliente | endereço, proprietário e valores saem do servidor e viram props serializadas enviadas ao navegador |
| dado de `cards` → JSX | texto digitado pelo usuário (endereço, proprietário) é renderizado numa tela nova |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Information Disclosure | props enviadas a `FinanceiroView` | low | accept | Só trafegam campos que o mesmo usuário já vê no Board e em Relatórios (endereço, proprietário, valor), e todos vieram de consultas já filtradas por RLS. Nenhum identificador de usuário, nenhum dado de outra pessoa. Nenhum campo novo é exposto por esta fase |
| T-05-07 | Tampering | renderização de texto vindo do banco em `parcelas-table.tsx` e `financeiro-view.tsx` | low | mitigate | Todo texto é renderizado como conteúdo JSX normal, com escape automático do React; asserção de fonte garante ausência de API de HTML cru nos componentes novos |
| T-05-08 | Information Disclosure | estado de erro da lista | medium | mitigate | O componente recebe apenas um booleano `erro` — não a mensagem do banco. O texto exibido é a constante do Copywriting Contract, então não existe caminho por onde uma mensagem do Postgres chegue à tela |
| T-05-SC | Tampering | npm/pip/cargo installs | low | accept | Nenhum pacote é instalado — `web/package.json` não é modificado. Os 5 ícones vêm de `lucide-react`, já instalado, e nenhum primitivo novo do shadcn é necessário (a UI-SPEC confirma na seção Registry Safety) |
</threat_model>

<verification>
1. `cd web && npm run lint && npm run build` saem com código 0
2. Os 4 componentes de `components/financeiro/` existem e nenhum usa o peso 500 nem valor de espaçamento fora da grade além dos dois herdados
3. O seletor não usa a cor de destaque da marca em nenhuma forma
4. Os três textos de estado vazio e os dois títulos de tabela batem literalmente com o Copywriting Contract
5. A conferência no navegador (Task 3) passa nos 8 itens
</verification>

<success_criteria>
- A aba apresenta as parcelas do mês atual e do próximo mês em visões separadas (FINUI-02)
- Cada parcela mostra situação, valor devido e valor pago, com a situação em ícone + palavra nos 5 estados (FINUI-03)
- Os estados vazios, o estado de erro e o comportamento de overflow seguem o contrato de design aprovado
</success_criteria>

<artifacts_this_phase_produces>
Criados neste plano:

**Componentes**
- `ParcelaSituacaoBadge` — `web/src/components/financeiro/parcela-situacao-badge.tsx`, prop `{ situacao: Situacao }`
- `MesSwitcher` — `web/src/components/financeiro/mes-switcher.tsx`, props `{ value: "atual" | "proximo"; onChange: (value: "atual" | "proximo") => void }`, componente cliente
- `FinanceiroView` — `web/src/components/financeiro/financeiro-view.tsx`, props `{ linhasAtual: LinhaParcela[]; linhasProximo: LinhaParcela[]; temContratoAtivo: boolean; erro?: boolean }`, componente cliente

**Alterações em símbolos existentes**
- `ParcelasTable` ganha a prop `vazio: "sem-contrato-ativo" | "sem-parcela-no-periodo"` e passa a renderizar `ParcelaSituacaoBadge`
- `FinanceiroPage` passa a renderizar `FinanceiroView` e a calcular `temContratoAtivo`
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-aba-financeiro-com-parcelas-autom-ticas/05-02-SUMMARY.md` when done
</output>
