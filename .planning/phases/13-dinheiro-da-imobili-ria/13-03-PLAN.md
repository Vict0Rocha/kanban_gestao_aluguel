---
phase: 13-dinheiro-da-imobili-ria
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - supabase/migrations/20260824000000_dinheiro_imobiliaria.sql
  - docs/data-model.md
autonomous: false
requirements: [IMOB-01, IMOB-02, IMOB-03, IMOB-04]

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A migração `20260824000000_dinheiro_imobiliaria` está aplicada no banco de produção — via SQL Editor (o Supabase CLI não está instalado neste projeto, confirmado desde a Fase 4), com o caminho registrado no SUMMARY"
    - "Depois da aplicação, `cards_total`, `parcelas_total`, `lancamentos_total` e `updated_at_max` continuam idênticos aos anotados no ensaio, e `count(*) where percentual_administracao <> 10 or percentual_comissao_primeiro_aluguel <> 50` devolve 0 — nada foi apagado, alterado ou pré-editado pela migração"
    - "`pg_constraint` lista as 8 CHECK constraints novas com os nomes exatos declarados no plano 13-01; `pg_policies` lista as duas policies novas, ambas com `is_team_member` em `qual` e `with_check`"
    - "O backstop de exclusão ampliado foi provado vivo em produção, dentro de `begin;...rollback;`: bloqueia por evento de taxa, bloqueia por evento de caução, continua bloqueando por lançamento em `parcela_lancamentos`, libera um card sem nenhum dos três"
    - "`pg_policies` para `public.cards`, `public.parcelas` e `public.parcela_lancamentos` continua listando exatamente as policies de sempre — nenhuma alterada"
    - "`docs/data-model.md` documenta as duas colunas novas de `cards`, as tabelas `taxas_imobiliaria`/`caucao_eventos` (diagrama incluso) e o porquê de cada decisão não-óbvia: a fronteira estrutural de D-04 (por que taxa nunca soma no status da parcela), o append-only de caução (D-06), e a ampliação do backstop de exclusão"
    - "O Board, o Financeiro e os Relatórios carregam normalmente depois da aplicação, com os mesmos contratos de antes — as adições são invisíveis para o código que ainda não as conhece"
  artifacts:
    - docs/data-model.md
  key_links:
    - "migração aplicada -> `salvarPercentuaisAction`/`registrarPagamentoAction`/`registrarEventoCaucaoAction`/`buscarReconciliacaoAction` (planos 13-04 a 13-07), que só podem existir depois deste plano"
    - "backstop ampliado -> `cardTemLancamento` (plano 13-04), que passa a checar as três tabelas para o pré-voo do diálogo de exclusão continuar coerente com o que o banco de fato recusa"
  prohibitions:
    - "MUST NOT aplicar esta migração em produção sem um `checkpoint:decision` explícito respondido pelo usuário — mesma régua de toda migração anterior deste projeto (Fase 4, 6.1, 6.2), reafirmada aqui porque esta é uma mudança nova de produção, não um trade-off já confirmado em fase anterior"
---

<objective>
Aplicar a migração `20260824000000_dinheiro_imobiliaria.sql` no banco de produção e provar, contra o
schema já gravado, que os dois percentuais, as duas tabelas novas e o backstop ampliado nasceram
corretos.

Este é o único plano da fase que altera o banco real. Aplicar em produção é **porta de mão única**:
desfazer exige outra migração contra dados de produção, e o app roda com ~46 imóveis reais. Não existe
staging. Por isso a aplicação está atrás de um `checkpoint:decision` explícito — mesma régua já usada
em toda migração anterior deste projeto (Fase 4, 6.1, 6.2). **O Supabase CLI não está instalado
(confirmado desde a Fase 4)** — a aplicação é via SQL Editor, colando a DDL completa, não `supabase db
push`.

Purpose: IMOB-01 a IMOB-04 (percentuais, taxa da imobiliária, caução) só existem de verdade depois de
o schema estar no banco onde os dados moram — nenhum código de aplicação (planos 13-04 a 13-07) tem
onde escrever antes disso.
Output: schema do dinheiro da imobiliária vivo em produção, verificação pós-aplicação registrada,
`docs/data-model.md` atualizado.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `13-01-PLAN.md`. Depois
deste plano, `public.cards.percentual_administracao`, `public.cards.percentual_comissao_primeiro_aluguel`,
`public.taxas_imobiliaria`, `public.caucao_eventos` e o `public.impedir_exclusao_de_card_com_lancamento()`
ampliado passam a existir de fato no banco.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-CONTEXT.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-02-SUMMARY.md
@supabase/verificacao_dinheiro_imobiliaria.sql
@supabase/migrations/20260824000000_dinheiro_imobiliaria.sql
@docs/data-model.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Autorizar a aplicação da migração do dinheiro da imobiliária no banco de produção</name>
  <read_first>
    - `supabase/verificacao_dinheiro_imobiliaria.sql` — o bloco `RESULTADO DO ENSAIO` gravado no plano
      13-02
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-02-SUMMARY.md` — o que o ensaio observou
  </read_first>
  <decision>
    Aplicar `20260824000000_dinheiro_imobiliaria.sql` agora contra o banco de produção, via SQL Editor
    (colando a DDL completa — o Supabase CLI não está instalado neste projeto).
  </decision>
  <context>
    O banco tem ~46 imóveis reais, com parcelas e lançamentos reais, e não existe ambiente de staging
    nem backup automático (plano gratuito do Supabase). A migração é estritamente aditiva — duas
    colunas com default, duas tabelas novas, e a ampliação (não substituição) de uma função de trigger
    já existente; nenhuma FK muda, nenhuma coluna some, nenhuma policy de tabela existente é tocada.

    **O que muda de comportamento no minuto seguinte à aplicação, mesmo sem nenhum código novo:** o
    backstop de exclusão passa a também recusar a exclusão de um card com qualquer linha em
    `taxas_imobiliaria`/`caucao_eventos` — mas como nenhuma linha existe ainda nessas tabelas (nenhum
    código de aplicação as escreve até os planos 13-04/13-06), essa ampliação não muda nenhum
    comportamento observável até lá. O risco real desta aplicação é estritamente o de qualquer DDL
    nova: um erro na migração afetando as ~46 linhas de `cards`.

    O que **não** é reversível: depois da aplicação, desfazer significa rodar um `drop` manual —
    válido só enquanto nenhum contrato real tiver percentual editado, taxa registrada ou caução
    recebida (ou seja, até os planos 13-04/13-05/13-06 chegarem). O ensaio do plano 13-02 já rodou
    essa mesma DDL contra o schema real dentro de uma transação desfeita no fim, e observou: aplicação
    limpa nas duas passagens, contagens inalteradas, dez recusas de validação, RLS negando/liberando
    corretamente, e os quatro lados do backstop ampliado corretos.
  </context>
  <options>
    <option id="aplicar-agora">
      <name>Aplicar agora</name>
      <pros>Desbloqueia os planos 13-04 a 13-07 imediatamente; o ensaio já cobriu tudo que dá para
      cobrir sem gravar; a ampliação do backstop não muda nenhum comportamento até haver dado real nas
      tabelas novas</pros>
      <cons>É porta de mão única sobre dado real</cons>
    </option>
    <option id="conferir-backup-antes">
      <name>Conferir o backup antes de aplicar</name>
      <pros>Abre uma saída independente de um `drop` manual — mesmo procedimento já usado nas fases
      anteriores (export CSV manual de `cards`, `parcelas`, `parcela_lancamentos` pelo Table Editor, ou
      conferir a última cópia automática do plano do Supabase)</pros>
      <cons>Atrasa a fase pelo tempo da conferência</cons>
    </option>
    <option id="adiar">
      <name>Adiar</name>
      <pros>Nada muda em produção hoje</pros>
      <cons>Os planos 13-04 a 13-07 ficam bloqueados — nenhum deles pode ler ou escrever em coluna/
      tabela que não existe</cons>
    </option>
  </options>
  <acceptance_criteria>
    - O usuário escolheu explicitamente uma das três opções
    - Se `conferir-backup-antes`, o backup foi anotado (quais tabelas, qual data) antes de seguir para
      a Task 2
    - Se `adiar`, as Tasks 2 e 3 não são executadas e o plano encerra com o motivo registrado
  </acceptance_criteria>
  <reversibility rating="one-way">A task seguinte grava DDL em produção. Desfazer exige um `drop`
  manual sobre o banco real, válido só enquanto nenhum dado novo (percentual editado, taxa registrada,
  caução recebida) tiver sido gravado nas adições desta migração.</reversibility>
  <resume-signal>Responda: `aplicar-agora`, `conferir-backup-antes` (com a data e as tabelas
  exportadas) ou `adiar` (com o motivo).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a migração em produção via SQL Editor e documentar em `docs/data-model.md`</name>
  <precondition>A Task 1 foi respondida com `aplicar-agora` ou `conferir-backup-antes`.</precondition>
  <files>supabase/migrations/20260824000000_dinheiro_imobiliaria.sql, docs/data-model.md</files>
  <read_first>
    - `supabase/migrations/20260824000000_dinheiro_imobiliaria.sql` — o arquivo a aplicar; conferir que
      é exatamente o estado aprovado no ensaio do plano 13-02
    - `.planning/phases/04-funda-o-financeira/04-03-SUMMARY.md` e
      `.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-03-SUMMARY.md` — como o push foi feito de
      verdade nas fases anteriores (CLI não instalado; DDL colada no SQL Editor de produção,
      confirmando "Success. No rows returned")
    - `docs/data-model.md` — o diagrama `mermaid`, a lista de entidades e a seção "Decisões de
      design", estilo "decisão + porquê" a seguir
  </read_first>
  <action>
Primeiro, checar se o Supabase CLI está instalado e linkado (`supabase migration list`, se o binário
existir no PATH). Dado o histórico deste projeto (CLI ausente desde a Fase 4, confirmado de novo em
cada migração seguinte), o caminho esperado é: colar a DDL completa de
`supabase/migrations/20260824000000_dinheiro_imobiliaria.sql` no SQL Editor de produção e confirmar
"Success. No rows returned" — sem o wrapper `begin`/`rollback` do ensaio, porque aqui a intenção é
gravar. Registrar no SUMMARY qual caminho foi de fato usado (CLI ou SQL Editor), mesmo que seja o
esperado.

Se a aplicação falhar, ler o erro, corrigir a causa no arquivo de migração, atualizar
`supabase/verificacao_dinheiro_imobiliaria.sql` para continuar batendo, voltar ao ensaio do plano
13-02 e nunca tentar aplicar de novo sem reensaiar.

Depois da aplicação bem-sucedida, atualizar `docs/data-model.md`:
- No diagrama de entidades (bloco `mermaid`), acrescentar `numeric percentual_administracao` e
  `numeric percentual_comissao_primeiro_aluguel` ao bloco `CARDS`, e dois blocos novos `TAXAS_IMOBILIARIA`
  e `CAUCAO_EVENTOS` com todas as colunas, mais as relações `CARDS ||--o{ TAXAS_IMOBILIARIA : recebe`,
  `PARCELAS ||--o{ TAXAS_IMOBILIARIA : gera`, `CARDS ||--o{ CAUCAO_EVENTOS : registra`.
- Na lista de entidades, acrescentar dois itens novos no mesmo estilo dos já existentes: `taxas_imobiliaria`
  ("o livro-razão paralelo da taxa que a própria imobiliária recebe — administração ou comissão do
  primeiro aluguel, sempre ligado a uma parcela e a um contrato, gerado automaticamente no momento da
  baixa mas com valor livre; ver
  [fronteira estrutural entre taxa e livro-razão do aluguel](#decisões-de-design)") e `caucao_eventos`
  ("o ciclo completo de caução — recebido, devolvido, usado — como histórico append-only ligado ao
  contrato, sem coluna de saldo gravada; ver [caução é histórico, não coluna](#decisões-de-design)").
  Estender a descrição de `cards` mencionando os dois percentuais novos, com os defaults 10%/50%.
- Acrescentar itens em "Decisões de design", no mesmo estilo "decisão + porquê" já usado ali:
  1. **A fronteira estrutural entre `taxas_imobiliaria` e o livro-razão do aluguel (D-04, Phase 13)** —
     `taxas_imobiliaria` é uma tabela paralela a `parcela_lancamentos`, nunca uma extensão dela.
     Nenhuma FK, trigger ou view liga as duas; `somarLancamentos`/`statusDeParcela`
     (`web/src/lib/kanban/parcelas.ts`) nunca leem `taxas_imobiliaria`, e nenhuma escrita ali aciona
     `recalcularEGravarStatus`. Explicar por quê: a taxa que a imobiliária recebe (administração,
     comissão do primeiro aluguel) é dinheiro que sai do bolso do proprietário para a imobiliária,
     depois que o inquilino já pagou — misturar isso no cálculo de `valorDevido`/`valorPago`/`status`
     da parcela faria o Financeiro/Relatórios/Relatório Financeiro dedicado (que mostram o valor bruto
     do aluguel) mudarem de comportamento sem o usuário ter pedido, violando a promessa explícita da
     fase de ser "estritamente aditiva — nenhuma tela existente muda".
  2. **Caução é histórico, não coluna (D-06, Phase 13)** — `caucao_eventos` não tem coluna de saldo;
     o saldo de um contrato é sempre a soma dos eventos (`recebido − devolvido − usado`), mesmo
     princípio já usado para `valorDevido`/`valorPago` de uma parcela. Cada evento é uma linha nova,
     nunca um UPDATE — não existe (e não deve existir) forma de editar um evento já gravado.
  3. **O backstop de exclusão ampliado (Phase 13, sobre a decisão original da Phase 6.2)** —
     `impedir_exclusao_de_card_com_lancamento()` ganhou dois `exists` adicionais, um por tabela nova,
     na mesma função (`create or replace`, não um segundo trigger). Explicar por que
     `taxas_imobiliaria`/`caucao_eventos` não caíam de graça sob o predicado antigo — são tabelas
     novas, não um `tipo` novo dentro de `parcela_lancamentos` (ao contrário de `destrava` e
     conciliação, que "entraram de graça" reusando a mesma tabela) — e por que isso importa: sem a
     ampliação, excluir um contrato que já recebeu uma caução real apagaria esse registro em cascata
     sem trava nenhuma.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && grep -q "taxas_imobiliaria" docs/data-model.md && grep -q "caucao_eventos" docs/data-model.md && grep -q "percentual_administracao" docs/data-model.md && grep -qi "fronteira estrutural" docs/data-model.md && grep -qi "append-only" docs/data-model.md && grep -qi "backstop" docs/data-model.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - A aplicação (CLI ou SQL Editor) terminou sem erro; se terminou com erro, o arquivo foi corrigido
      e o ensaio do plano 13-02 foi refeito antes de nova tentativa
    - O caminho usado (CLI ou SQL Editor) está registrado no SUMMARY
    - `docs/data-model.md` cita `taxas_imobiliaria`/`caucao_eventos` no diagrama `mermaid` e na lista
      de entidades, e os dois percentuais na descrição de `cards`
    - `docs/data-model.md` explica a fronteira estrutural de D-04, o append-only de caução (D-06), e a
      ampliação do backstop de exclusão
    - Nenhum arquivo fora de `supabase/migrations/20260824000000_dinheiro_imobiliaria.sql` e
      `docs/data-model.md` foi modificado por esta task
  </acceptance_criteria>
  <reversibility rating="one-way">Grava DDL no banco de produção. Desfazer exige um `drop` manual,
  válido só enquanto nenhum dado real tiver sido gravado nas adições.</reversibility>
  <done>A migração está aplicada em produção e `docs/data-model.md` documenta as duas colunas, as duas
  tabelas e as três decisões estruturais desta fase.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar em produção que tudo nasceu correto e que nada quebrou</name>
  <precondition>A Task 2 aplicou a migração com sucesso.</precondition>
  <read_first>
    - `supabase/verificacao_dinheiro_imobiliaria.sql` — Parte B, BLOCOs 4 a 7
    - O `RESULTADO DO ENSAIO` registrado no plano 13-02, para comparar os números
  </read_first>
  <what-built>
    `public.cards.percentual_administracao`/`percentual_comissao_primeiro_aluguel`, as tabelas
    `public.taxas_imobiliaria` e `public.caucao_eventos` (constraints, índices, RLS via
    `is_team_member()`), e a ampliação do backstop de exclusão, todos aplicados no banco de produção.
  </what-built>
  <how-to-verify>
    **No SQL Editor de produção, rodando a Parte B do runbook:**

    1. **BLOCO 4** — as duas colunas e as duas tabelas existem com tipo/nulabilidade corretos;
       `cards_total`/`updated_at_max` idênticos aos anotados no ensaio; zero cards com percentual
       diferente do default 10/50.
    2. **BLOCO 5** — `pg_constraint` lista as 8 CHECK constraints novas com os nomes exatos;
       `pg_policies` lista as duas policies novas com `is_team_member` em `qual` e `with_check`;
       `pg_get_functiondef` da função ampliada cita as três tabelas.
    3. **BLOCO 6** (transação revertida) — os quatro lados do backstop contra o banco já migrado:
       bloqueia por taxa, bloqueia por caução, continua bloqueando por pagamento, libera card sem
       nenhum dos três.
    4. **BLOCO 7** — `pg_policies` de `cards`/`parcelas`/`parcela_lancamentos` continua exatamente
       como antes.

    **No navegador, contra produção:**

    5. **Board** carrega com os mesmos contratos de antes, arrastar entre colunas continua
       funcionando, o modal de edição de card salva sem erro (a coluna nova é invisível para o código
       atual).
    6. **Financeiro** carrega, "vencendo hoje" continua sendo o padrão, os filtros continuam
       combinando, e Pagamento/Ajustar/Histórico continuam abrindo e gravando (regressão das Phases
       6/6.1/7/11/12).
    7. **Relatórios/Relatório Financeiro dedicado** carregam com os mesmos números de antes.
    8. **Excluir um card que TEM lançamento** (em `parcela_lancamentos`, o caso já provado na Phase
       6.2) — a exclusão deve continuar falhando, com a mesma mensagem de sempre.
  </how-to-verify>
  <acceptance_criteria>
    - BLOCO 4: colunas/tabelas com tipo/nulabilidade corretos, quatro números idênticos ao ensaio,
      zero percentual divergente do default
    - BLOCO 5: 8 CHECK constraints, 2 policies, função ampliada citando as três tabelas
    - BLOCO 6: os quatro lados do backstop observados
    - BLOCO 7: policies de `cards`/`parcelas`/`parcela_lancamentos` inalteradas
    - Board, Financeiro, Relatórios e Relatório Financeiro dedicado carregam sem erro, com os mesmos
      dados
    - Passo 8: a exclusão do card com lançamento em `parcela_lancamentos` continuou falhando
    - O resultado é reportado com os números observados, não com um "está tudo certo"
  </acceptance_criteria>
  <resume-signal>Responda com o resultado de cada um dos oito passos, ou com o que travou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → banco de produção | DDL definitiva sobre ~46 contratos reais; sem staging, sem backup automático |
| navegador → PostgREST | RLS de `cards`/`parcelas`/`parcela_lancamentos` inalterado; as duas tabelas novas herdam `is_team_member()` de novo, sem predicado diferente |
| aplicação → trigger ampliado | A partir deste plano, excluir um card com evento de taxa/caução passa a ser recusado — mudança de comportamento antes de qualquer código de aplicação existir para gerar esses eventos |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11 | Tampering | aplicação da DDL sobre produção | high | mitigate | `checkpoint:decision` bloqueante antes da aplicação (Task 1), com o resultado do ensaio do 13-02 como base; opção de backup manual |
| T-13-12 | Elevation of Privilege | policy aplicada com predicado errado | critical | mitigate | BLOCO 5 lê `pg_policies` no banco já aplicado e exige `is_team_member` em `qual`/`with_check` das duas tabelas novas; qualquer outro predicado é motivo de parar |
| T-13-13 | Tampering | regra de banco ausente depois da aplicação | high | mitigate | BLOCO 5 conta as 8 CHECK constraints por nome; BLOCO 6 reexecuta os quatro lados do backstop contra o schema real |
| T-13-14 | Information Disclosure | dado financeiro novo legível por login fora da allowlist | critical | mitigate | BLOCO 6/7 repetem as provas de RLS já contra as tabelas aplicadas |
| T-13-15 | Denial of Service | regressão silenciosa nas telas já existentes | medium | mitigate | Passos 5 a 8 do checkpoint cobrem Board, Financeiro, Relatórios/Relatório Financeiro dedicado e a exclusão travada, à mão, contra produção |
| T-13-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala pacote da aplicação. O único binário externo é o SQL Editor do Supabase, já em uso no projeto |
</threat_model>

<verification>
1. Parte B do runbook rodada contra o banco migrado: colunas, tabelas, constraints, policies, os
   quatro lados do backstop
2. Comparação numérica com o baseline do ensaio (`cards_total`, `parcelas_total`, `lancamentos_total`,
   `updated_at_max`)
3. `grep` sobre `docs/data-model.md` confirmando as duas tabelas, os dois percentuais e as três
   decisões estruturais
4. Teste manual no navegador cobrindo Board, Financeiro, Relatórios/Relatório Financeiro dedicado e a
   exclusão travada
</verification>

<success_criteria>
- `public.cards.percentual_administracao`/`percentual_comissao_primeiro_aluguel`,
  `public.taxas_imobiliaria`, `public.caucao_eventos` e o backstop ampliado existem em produção
- Zero contratos com percentual divergente do default, `updated_at_max` intocado
- O backstop bloqueia por taxa, por caução e por pagamento, e libera quando nenhum dos três existe
- `pg_policies` das três tabelas já existentes está inalterada
- `docs/data-model.md` documenta as duas colunas, as duas tabelas e as três decisões estruturais
- Board, Financeiro, Relatórios e Relatório Financeiro dedicado seguem funcionando como antes
</success_criteria>

<output>
Create `.planning/phases/13-dinheiro-da-imobili-ria/13-03-SUMMARY.md` when done
</output>
