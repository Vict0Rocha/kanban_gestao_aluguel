---
phase: 13-dinheiro-da-imobili-ria
plan: 05
type: execute
wave: 5
depends_on: ["13-04"]
files_modified:
  - web/src/lib/kanban/actions.ts
  - web/src/lib/kanban/queries.ts
  - web/src/components/financeiro/configurar-percentuais-dialog.tsx
  - web/src/app/(app)/financeiro/configuracao/page.tsx
  - web/src/components/financeiro/configuracao-financeira-view.tsx
  - web/src/components/financeiro/filtro-parcelas.tsx
autonomous: false
requirements: [IMOB-01]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe uma tela própria '/financeiro/configuracao', alcançada por um botão 'Configuração financeira' na aba Financeiro (mesma linha do botão 'Filtrar'), separada do modal de edição do card (D-02, IMOB-01)"
    - "A tela lista TODOS os contratos — ativos, inativos e arquivados — ordenados por `numero` crescente; percentuais e caução são configuração de contrato, não dado escopado por tempo como as parcelas (divergência deliberada da regra de visibilidade da Phase 6.2, UI-SPEC §1)"
    - "Cada linha mostra o percentual de administração e o de comissão do primeiro aluguel do contrato, com um botão 'Editar percentuais' que abre um diálogo com os dois campos pré-preenchidos com os valores atuais (defaults 10%/50%)"
    - "Salvar um percentual fora da faixa 0–100 é recusado com mensagem em português — a validação do formulário espelha a CHECK constraint do banco (mesma disciplina de FINSEG-02 herdada)"
    - "Editar os percentuais de um contrato não gera nem altera nenhuma taxa já registrada — a mudança vale só para pagamentos futuros (mesmo princípio de D-05: sem retroativo)"
    - "UI Consideration `empty` (tabela): 'Nenhum contrato cadastrado ainda.' quando não há nenhum card"
    - "UI Consideration `loading` (tabela + diálogo): a página não tem esqueleto de carregamento (Server Component, mesmo padrão de Financeiro/Relatórios); o diálogo mostra 'Salvando...' com o botão desabilitado durante o submit"
    - "UI Consideration `populated` (tabela): o conjunto completo de colunas ID/Imóvel/Proprietário/Administração/Comissão 1º aluguel/Ações renderiza sem paginação, mesmo padrão de `ParcelasTable`"
    - "UI Consideration `partial` (tabela): nenhuma linha parcial é possível — `endereco`/`proprietario` são `NOT NULL` e os percentuais sempre têm valor (default 10/50)"
    - "UI Consideration `zero-one-many` (tabela): coberto pelo condicional de estado vazio (0) mais a renderização normal de linhas (1..N) — nenhum texto de contagem é mostrado nesta tela"
    - "UI Consideration `error` (tabela, backstop): a falha ao carregar os contratos reusa o mesmo padrão `erro` boolean já estabelecido por `ParcelasTable`/`FinanceiroView` (`financeiro/page.tsx`'s try/catch → `erro: true`) — 'Não foi possível carregar os dados agora. Tente novamente.'"
    - "UI Consideration `overflow` (tabela, backstop): sem paginação nem virtualização, mesmo precedente de `ParcelasTable`/`RelatorioFinanceiroLista` na escala atual (~46 contratos)"
    - "UI Consideration `long-text` (colunas Imóvel/Proprietário, backstop): sem truncamento — mesma convenção de `ParcelasTable`, que não usa `truncate` nessas colunas"
  artifacts:
    - "web/src/app/(app)/financeiro/configuracao/page.tsx"
    - "web/src/components/financeiro/configuracao-financeira-view.tsx"
    - "web/src/components/financeiro/configurar-percentuais-dialog.tsx"
    - "web/src/lib/kanban/actions.ts — `salvarPercentuaisAction`"
  key_links:
    - "botão 'Configuração financeira' em `/financeiro` -> rota `/financeiro/configuracao` -> `ConfiguracaoFinanceiraView` -> `ConfigurarPercentuaisDialog` -> `salvarPercentuaisAction` -> `cards.percentual_administracao`/`percentual_comissao_primeiro_aluguel`"
    - "`salvarPercentuaisAction` -> `registrarPagamentoAction` (plano 13-04), que lê os mesmos dois percentuais para sugerir a taxa no próximo pagamento — não há caminho de leitura duplicado, os dois consomem `cards.percentual_*` diretamente"
---

<objective>
Entregar a tela de Configuração financeira: uma rota própria que lista todos os contratos com seus
dois percentuais (administração, comissão do primeiro aluguel) e permite editá-los por contrato,
separada do modal de edição do card (D-02).

Purpose: sem esta tela, os percentuais ficam presos nos defaults 10%/50% para sempre — o usuário
explicou que "a maioria dos casos é 10%... mas existe exceções onde alguns clientes cobramos menos ou
mais". A tela do plano 13-04 (registrar pagamento) já lê `cards.percentual_*`; este plano é o único
jeito de o usuário mudar esses valores.
Output: rota `/financeiro/configuracao` funcionando contra os ~46 contratos reais, com o botão de
entrada em `/financeiro`. A coluna/ação de Caução (13-CONTEXT.md, UI-SPEC §1/§3) entra no plano 13-06,
que estende a mesma tabela — esta task não a inclui.
</objective>

## Artifacts this phase produces

Este plano não repete a lista completa de artefatos da fase — ver `## Artifacts this phase produces`
em `13-01-PLAN.md`. Os símbolos que este plano especificamente cria: `salvarPercentuaisAction`
(`actions.ts`), `salvarPercentuais` (wrapper em `queries.ts`), `web/src/components/financeiro/configurar-percentuais-dialog.tsx`,
`web/src/app/(app)/financeiro/configuracao/page.tsx`,
`web/src/components/financeiro/configuracao-financeira-view.tsx` (tipo `ContratoConfig`, sem a
coluna/botão de Caução ainda — ver plano 13-06), e o botão de entrada em `filtro-parcelas.tsx`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-CONTEXT.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-PATTERNS.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-UI-SPEC.md
@web/src/app/(app)/relatorios/financeiro/page.tsx
@web/src/components/financeiro/ajustar-parcela-dialog.tsx
@web/src/components/financeiro/parcelas-table.tsx
@web/src/components/financeiro/filtro-parcelas.tsx
@web/src/lib/kanban/actions.ts
</context>

<planner_assumptions>
- **A-01 — a tabela desta tela usa um tipo bespoke `ContratoConfig`, não o tipo `Card` geral.** Mesmo
  padrão já usado por `ContratoFiltro` (`financeiro-view.tsx`) e `CardVisibilidade`
  (`visibilidade.ts`): declarar exatamente os campos que a query precisa, nunca o tipo `Card`
  completo. `web/src/lib/kanban/types.ts` não é tocado por este plano.
- **A-02 — a coluna "Caução" e o botão "Caução" da UI-SPEC §1 entram no plano 13-06, não aqui.** A
  tabela desta task tem as colunas ID/Imóvel/Proprietário/Administração/Comissão 1º aluguel/Ações
  (percentuais) — cinco das seis colunas da UI-SPEC final. O plano 13-06 estende
  `configuracao-financeira-view.tsx` para acrescentar a sexta coluna e o segundo botão de ação, porque
  a tabela precisa saber o saldo de caução de cada contrato (`caucao_eventos`, ainda inexistente do
  lado da aplicação até aquele plano). Entregar as duas metades no mesmo plano ultrapassaria o limite
  de arquivos por task; a divisão segue a mesma lógica de "cada plano entrega uma fatia que funciona
  sozinha" já usada em toda a Phase 6.2 (a tabela de `ParcelasTable` nasceu sem badge no plano 05-01 e
  ganhou o badge no 05-02).
</planner_assumptions>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `salvarPercentuaisAction` + `ConfigurarPercentuaisDialog`</name>

  <files>
web/src/lib/kanban/actions.ts
web/src/lib/kanban/queries.ts
web/src/components/financeiro/configurar-percentuais-dialog.tsx
  </files>

  <read_first>
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-UI-SPEC.md` — §1 (`ConfigurarPercentuaisDialog`),
      Copywriting Contract (título, labels, placeholders, texto de ajuda, erros)
    - `web/src/lib/kanban/actions.ts` — `createColumnAction` (linhas 196-221, a forma geral
      validar→escrever→`erroDoBanco` a seguir), `booleano`/`textoObrigatorio` (os validadores
      inline a imitar)
    - `web/src/components/financeiro/ajustar-parcela-dialog.tsx` — arquivo inteiro, o molde exato de
      "dois campos + texto de ajuda + uma linha de erro + `DialogFooter` com um botão" que
      `ConfigurarPercentuaisDialog` replica
    - `web/src/components/financeiro/registrar-pagamento-dialog.tsx` — o padrão `wasOpen` de
      resincronização (linhas 71-80), a reusar aqui
  </read_first>

  <action>
**1. `web/src/lib/kanban/actions.ts`** — novo validador `percentualValido(valor: unknown, campo:
string): string | null`: recusa não-número/não-finito, `< 0` ou `> 100`, com a mensagem `"${campo}
inválido — informe um percentual entre 0 e 100."`. Nova Server Action `salvarPercentuaisAction`:

```
export async function salvarPercentuaisAction(
  cardId: string,
  percentualAdministracao: number,
  percentualComissaoPrimeiroAluguel: number
): Promise<ActionResult> {
  const sessao = await requireUser()
  if (!sessao) return { ok: false, error: NAO_AUTENTICADO }

  const invalido =
    id(cardId, "Imóvel") ??
    percentualValido(percentualAdministracao, "Percentual de administração") ??
    percentualValido(percentualComissaoPrimeiroAluguel, "Percentual de comissão do primeiro aluguel")
  if (invalido) return { ok: false, error: invalido }

  const { data, error } = await sessao.supabase
    .from("cards")
    .update({
      percentual_administracao: percentualAdministracao,
      percentual_comissao_primeiro_aluguel: percentualComissaoPrimeiroAluguel,
    })
    .eq("id", cardId)
    .select("id")

  if (error) return { ok: false, error: erroDoBanco(error.code, "salvar os percentuais") }
  if (!data || data.length === 0) return { ok: false, error: semLinhas("salvar os percentuais") }
  return { ok: true, data: undefined }
}
```

Posicionar a nova função numa seção nova `// Configuração financeira (Phase 13)`, depois da seção de
Parcelas e antes de Relatório financeiro. Comentar que esta action não toca `taxas_imobiliaria` nem
qualquer taxa já registrada — editar o percentual do contrato vale só para pagamentos futuros (mesmo
princípio de D-05, sem retroativo), o mesmo raciocínio já usado em `vencimentoDaCompetencia` para "uma
mudança de regra de geração nunca reescreve o que já foi gerado".

**2. `web/src/lib/kanban/queries.ts`** — `salvarPercentuais(cardId, percentualAdministracao,
percentualComissaoPrimeiroAluguel)`, mesmo `unwrap(...)` de todo o resto do arquivo.

**3. `web/src/components/financeiro/configurar-percentuais-dialog.tsx`** (novo) — molde idêntico a
`ajustar-parcela-dialog.tsx`: `Dialog` + `DialogContent className="sm:max-w-md"`, `DialogHeader` com
título **"Editar percentuais — {endereco}"**, dois campos `flex flex-col gap-1.5` (`Label` + `Input
inputMode="decimal"`) — "Percentual de administração (%)" (placeholder "10") e "Percentual de comissão
do primeiro aluguel (%)" (placeholder "50") — pré-preenchidos com os valores atuais do contrato
recebidos por prop. Parágrafo de ajuda entre os campos e o rodapé, `text-sm text-muted-foreground`:
"No primeiro mês do contrato, a comissão substitui a administração — os dois percentuais não somam."
Uma linha de erro (`text-sm text-destructive`). `DialogFooter` com um botão "Salvar percentuais"/
"Salvando..." (`disabled={saving}`).

Props: `cardId: string`, `endereco: string`, `percentualAdministracao: number`,
`percentualComissaoPrimeiroAluguel: number`, `open: boolean`, `onOpenChange: (open: boolean) => void`.
Estado local dos dois campos como string (mesmo padrão de valor monetário: `useState(() =>
String(percentualAdministracao))`), resincronizado no `wasOpen` a cada reabertura. Validação
client-side: `Number(campo.replace(",", "."))` finito e entre 0 e 100 para os dois campos — se
qualquer um falhar, `setError("Informe um percentual válido para administração e comissão.")`.
`handleSubmit` chama `salvarPercentuais(cardId, parsedAdministracao, parsedComissao)`, em caso de erro
mostra `error.message` (fallback "Não foi possível salvar os percentuais. Tente novamente."), em caso
de sucesso `onOpenChange(false)` + `router.refresh()`.
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && npm run lint`/`npm run build` saem com código 0
    - `grep -c 'export async function salvarPercentuaisAction' web/src/lib/kanban/actions.ts` devolve 1
    - `grep -c 'percentual_administracao: percentualAdministracao' web/src/lib/kanban/actions.ts`
      devolve 1, e a mesma função não referencia `taxas_imobiliaria` nem `parcela_lancamentos`
      (`awk '/export async function salvarPercentuaisAction/,/^}/' web/src/lib/kanban/actions.ts |
      grep -cE 'taxas_imobiliaria|parcela_lancamentos'` devolve 0)
    - `test -f web/src/components/financeiro/configurar-percentuais-dialog.tsx` existe
    - `grep -c 'Salvar percentuais' web/src/components/financeiro/configurar-percentuais-dialog.tsx`
      devolve 1 ou mais
    - `grep -c 'No primeiro mês do contrato, a comissão substitui a administração'
      web/src/components/financeiro/configurar-percentuais-dialog.tsx` devolve 1
    - `grep -c 'wasOpen' web/src/components/financeiro/configurar-percentuais-dialog.tsx` devolve 1 ou
      mais
  </acceptance_criteria>

  <reversibility rating="reversible">Editar um percentual é uma atualização de configuração, não um
  lançamento — sobrescrever de novo desfaz, sem confirmação nem checkpoint necessário (UI-SPEC:
  "editing percentuais overwrites a configuration value, reversible by editing again").</reversibility>

  <done>`salvarPercentuaisAction` valida e grava os dois percentuais sem tocar `taxas_imobiliaria`;
  `ConfigurarPercentuaisDialog` existe com o molde de `AjustarParcelaDialog` e o texto exato do
  Copywriting Contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rota `/financeiro/configuracao`, tabela de contratos e botão de entrada</name>

  <files>
web/src/app/(app)/financeiro/configuracao/page.tsx
web/src/components/financeiro/configuracao-financeira-view.tsx
web/src/components/financeiro/filtro-parcelas.tsx
  </files>

  <read_first>
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-UI-SPEC.md` — §1 completo (Layout & Component
      Contract), Copywriting Contract (título/subtítulo da página, botão de entrada, colunas)
    - `web/src/app/(app)/relatorios/financeiro/page.tsx` — arquivo inteiro (43 linhas), o molde exato
      de página nova com back-link + h1 + subtítulo a copiar quase verbatim
    - `web/src/components/financeiro/parcelas-table.tsx` — o shell `rounded-2xl border border-border
      bg-card p-5` e o padrão `erro`/`vazio` (linhas 166-194) — o gap explícito que a UI-SPEC pede para
      esta tela (`p-6`, não `p-5` — ver UI-SPEC Spacing Scale)
    - `web/src/components/financeiro/filtro-parcelas.tsx` — arquivo inteiro, a linha `flex items-center
      justify-between gap-3` (linha 81) onde o botão de entrada é inserido, ao lado do
      `CollapsibleTrigger` de "Filtrar" (mesmo padrão de `filtro-relatorio-financeiro.tsx` linhas 80-88,
      que já insere um botão de entrada análogo para RELDED-01)
    - `web/src/components/financeiro/id-pill.tsx` — `IdPill`, variante `default`
  </read_first>

  <action>
**1. `web/src/app/(app)/financeiro/configuracao/page.tsx`** (novo) — Server Component, molde de
`relatorios/financeiro/page.tsx`: `createClient()`, consulta direta (não uma Server Action — mesmo
padrão de `financeiro/page.tsx` para a leitura inicial). Buscar **todos** os contratos, sem filtro de
visibilidade nem de `arquivado_em` (A-02/UI-SPEC §1 — "esta tabela mostra TODOS os contratos, ativo,
inativo e arquivado"):
```
const { data, error } = await supabase
  .from("cards")
  .select("id, numero, endereco, proprietario, percentual_administracao, percentual_comissao_primeiro_aluguel")
  .order("numero", { ascending: true })
```
Dentro de um `try/catch` (mesmo padrão de `financeiro/page.tsx`): em erro, `console.error("financeiro/configuracao",
erroCapturado)` e `erro = true`; nunca interpolar o erro do Supabase no JSX. Renderizar: back-link
`ChevronLeft` + "Financeiro" (para `/financeiro`, mesmo padrão de `relatorios/financeiro/page.tsx`
linhas 13-19); `h1` "Configuração financeira" (`font-heading text-2xl font-semibold text-foreground`);
subtítulo "Percentual de administração e de comissão do primeiro aluguel, por contrato. Defaults: 10%
de administração, 50% de comissão do primeiro aluguel." Passar `linhas={data ?? []}` e `erro` para
`<ConfiguracaoFinanceiraView>`.

**2. `web/src/components/financeiro/configuracao-financeira-view.tsx`** (novo, client component) —
props `{ linhas: ContratoConfig[]; erro?: boolean }`, onde `ContratoConfig = { id: string; numero:
number; endereco: string; proprietario: string; percentualAdministracao: number;
percentualComissaoPrimeiroAluguel: number }` (A-01 — declarado neste arquivo, não em `types.ts`; note
que a query de `page.tsx` usa `percentual_administracao`/`percentual_comissao_primeiro_aluguel`
snake_case — mapear para os campos camelCase de `ContratoConfig` na própria `page.tsx` antes de
passar, mesmo padrão de tradução snake_case→camelCase que `montarLinhas` já faz para `LinhaParcela`).

Wrapper `rounded-2xl border border-border bg-card p-6` (UI-SPEC: `p-6`, não o `p-5` de
`ParcelasTable` — a tabela é uma superfície de página inteira, mesmo raciocínio de
`RelatorioFinanceiroLista`). Dentro:
- `erro` verdadeiro → `<p className="text-sm text-muted-foreground">Não foi possível carregar os
  dados agora. Tente novamente.</p>`
- `linhas.length === 0` → `<p className="text-sm text-muted-foreground">Nenhum contrato cadastrado
  ainda.</p>`
- senão → `Table`/`TableHeader`/`TableBody` com as colunas, nesta ordem: ID (`<IdPill numero={...}
  />`, variante default), Imóvel (`endereco`, `text-sm font-semibold text-foreground`, sem
  truncamento), Proprietário (`proprietario`, `text-sm text-muted-foreground`, sem truncamento),
  Administração (`{percentualAdministracao}%`, `tabular-nums`, alinhado à direita), Comissão 1º
  aluguel (`{percentualComissaoPrimeiroAluguel}%`, `tabular-nums`, alinhado à direita), Ações (botão
  "Editar percentuais", `variant="ghost" size="sm"`, ícone `Percent` de `lucide-react`, que abre
  `ConfigurarPercentuaisDialog` para aquela linha — estado local `contratoEditando: string | null`
  guardando o `id` da linha, mesmo padrão de `dialogoAberto` em `AcoesCell`).

A coluna "Caução" e o segundo botão de ação **não** existem nesta task — o plano 13-06 os acrescenta.

**3. `web/src/components/financeiro/filtro-parcelas.tsx`** — dentro da `div className="flex
items-center justify-between gap-3"` (linha 81), antes do bloco `<div className="flex items-center
gap-3">` que já contém o contador de resultados e o `CollapsibleTrigger`, inserir um `Button
variant="outline" size="sm"` com `render={<Link href="/financeiro/configuracao" />}`, ícone
`SlidersHorizontal` de `lucide-react`, texto "Configuração financeira" — mesmo padrão exato de
`filtro-relatorio-financeiro.tsx` linhas 80-88 (botão de entrada não-destacado, ao lado do trigger de
filtro, nunca dentro de `AppShell`'s `NAV_ITEMS`). Importar `Link` de `next/link` (ainda não importado
neste arquivo).
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && npm run lint`/`npm run build` saem com código 0 e a saída do build lista a rota
      `/financeiro/configuracao`
    - `test -f "web/src/app/(app)/financeiro/configuracao/page.tsx"` existe
    - `test -f web/src/components/financeiro/configuracao-financeira-view.tsx` existe
    - `grep -c 'Nenhum contrato cadastrado ainda.'
      web/src/components/financeiro/configuracao-financeira-view.tsx` devolve 1
    - `grep -c 'Não foi possível carregar os dados agora. Tente novamente.'
      web/src/components/financeiro/configuracao-financeira-view.tsx` devolve 1
    - `grep -c 'rounded-2xl border border-border bg-card p-6'
      web/src/components/financeiro/configuracao-financeira-view.tsx` devolve 1 (não `p-5`)
    - `grep -c '"Caução"' web/src/components/financeiro/configuracao-financeira-view.tsx` devolve 0 —
      a coluna de caução ainda não existe nesta task
    - `grep -c 'order.*numero.*ascending: true\|order("numero"' "web/src/app/(app)/financeiro/configuracao/page.tsx"`
      devolve 1 ou mais
    - `grep -c '"/financeiro/configuracao"' web/src/components/financeiro/filtro-parcelas.tsx` devolve 1
    - `grep -c 'SlidersHorizontal' web/src/components/financeiro/filtro-parcelas.tsx` devolve 1 ou mais
  </acceptance_criteria>

  <reversibility rating="reversible">Nova rota e componentes de leitura/edição de configuração — sem
  efeito em dado financeiro já gravado.</reversibility>

  <done>`/financeiro/configuracao` lista todos os contratos com os dois percentuais, permite editá-los,
  e o botão de entrada aparece em `/financeiro` na mesma linha do filtro.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar em produção que a Configuração financeira lista e edita percentuais corretamente</name>

  <files>(nenhum arquivo é alterado — verificação operacional)</files>

  <read_first>
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-CONTEXT.md` — D-01, D-02
  </read_first>

  <what-built>
A rota `/financeiro/configuracao`, alcançada por um botão em `/financeiro`, listando todos os ~46
contratos (inclusive inativos/arquivados) com seus dois percentuais e um diálogo para editá-los.
  </what-built>

  <how-to-verify>
    **Passo 1 — abrir a tela.** Em `/financeiro`, clique em "Configuração financeira". Confirme que a
    tabela lista contratos que **não** aparecem no Financeiro por estarem inativos ou arquivados (se
    houver algum) — essa é a divergência deliberada da regra de visibilidade da Phase 6.2.

    **Passo 2 — editar um percentual.** Clique em "Editar percentuais" de um contrato de teste, mude
    os dois valores (ex.: 12% administração, 60% comissão), salve. Confirme que a linha atualiza sem
    reload manual e mostra os novos valores.

    **Passo 3 — validação.** Tente salvar um percentual negativo ou acima de 100 — a ação deve ser
    recusada com mensagem em português, sem gravar nada.

    **Passo 4 — confirmar no banco.**
    ```sql
    select percentual_administracao, percentual_comissao_primeiro_aluguel
    from public.cards where id = '<id do contrato editado>';
    ```
    Os valores devem bater com os que a tela mostrou depois do Passo 2.

    **Passo 5 — regressão.** Abra `/financeiro`, registre um pagamento de teste no contrato editado e
    confirme que a sugestão de taxa agora usa o novo percentual (12%/60% do exemplo), não mais o
    default 10%/50%.
  </how-to-verify>

  <acceptance_criteria>
    - A tabela lista contratos inativos/arquivados, se houver algum
    - Editar os dois percentuais funciona e reflete na tela sem reload manual
    - Um percentual inválido (negativo ou > 100) é recusado com mensagem clara
    - A consulta SQL confirma os valores gravados batendo com a tela
    - O próximo diálogo de "Registrar pagamento" naquele contrato sugere a taxa com o percentual novo
    - O operador registrou os valores usados no teste no SUMMARY
  </acceptance_criteria>

  <resume-signal>Responda "aprovado" com os valores testados, ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `salvarPercentuaisAction` | escreve `cards.percentual_administracao`/`percentual_comissao_primeiro_aluguel`; RLS de `cards` (já existente, `is_team_member()`) continua sendo a rede de proteção |
| `/financeiro/configuracao` → todos os contratos | esta é a única tela do projeto que lê `cards` sem filtro de `arquivado_em`/`ativo` (divergência deliberada, UI-SPEC §1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-21 | Tampering | percentual gravado fora de 0–100 | high | mitigate | Validação client-side (`ConfigurarPercentuaisDialog`) e server-side (`percentualValido`) espelham a CHECK constraint do banco (plano 13-01); banco é a autoridade final |
| T-13-22 | Elevation of Privilege | escrita em `cards.percentual_*` sem sessão do usuário | high | mitigate | `salvarPercentuaisAction` usa `sessao.supabase` (sessão via `requireUser()`), nunca `service_role` |
| T-13-23 | Information Disclosure | erro do Postgres vazando ao salvar percentuais | medium | mitigate | `erroDoBanco(error.code, "salvar os percentuais")`, mesmo padrão de sanitização do resto do arquivo |
| T-13-24 | Tampering | editar percentual reescrevendo taxa já gravada | medium | mitigate | `salvarPercentuaisAction` só faz `update` em `cards` — nunca toca `taxas_imobiliaria`; verificado por asserção de fonte no `<verify>` |
| T-13-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala pacote — `SlidersHorizontal`/`Percent` já existem em `lucide-react`, já instalado |
</threat_model>

<verification>
1. `cd web && npm run lint && npm run build` saem com código 0 e listam a rota nova
2. Asserções de fonte confirmando que `salvarPercentuaisAction` nunca toca `taxas_imobiliaria`
3. A tabela renderiza os estados vazio/erro/populado conforme o Copywriting Contract
4. O botão de entrada aparece em `/financeiro`, mesma linha do filtro
</verification>

<success_criteria>
- Existe uma tela própria de Configuração financeira, separada do modal de edição do card (IMOB-01,
  D-02)
- Os dois percentuais são editáveis por contrato, com defaults 10%/50%
- Editar o percentual nunca reescreve uma taxa já registrada
- A tabela mostra todos os contratos, inclusive inativos e arquivados
</success_criteria>

<output>
Create `.planning/phases/13-dinheiro-da-imobili-ria/13-05-SUMMARY.md` when done
</output>
