---
phase: 13-dinheiro-da-imobili-ria
plan: 06
type: execute
wave: 6
depends_on: ["13-05"]
files_modified:
  - web/src/lib/kanban/actions.ts
  - web/src/lib/kanban/queries.ts
  - web/src/lib/kanban/taxas.ts
  - web/src/components/financeiro/caucao-evento-label.tsx
  - web/src/components/financeiro/registrar-evento-caucao-dialog.tsx
  - web/src/components/financeiro/caucao-historico-sheet.tsx
  - web/src/components/financeiro/configuracao-financeira-view.tsx
autonomous: false
requirements: [IMOB-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Existe uma ação 'Caução' por contrato, alcançada de dentro da tela Configuração financeira (não no card do Board, não na linha do Financeiro — decisão de layout do UI-SPEC), que abre um histórico lateral do ciclo completo: recebido, devolvido, usado (D-06, IMOB-04)"
    - "Cada evento de caução é um registro novo (INSERT), nunca uma edição de um evento anterior — mesmo espírito append-only do resto do sistema"
    - "O rodapé do histórico mostra 0, 1 ou 2 botões conforme o saldo calculado (recebido − devolvido − usado): saldo 0 (nunca recebida ou totalmente encerrada) → só 'Registrar caução recebida'; saldo > 0 → 'Devolver caução' + 'Registrar uso'"
    - "O campo Valor do diálogo de evento vem vazio para recebimento e pré-preenchido com o saldo total para devolução/uso — mas totalmente editável, cobrindo devolução/uso parcial"
    - "Nenhum evento de caução participa de `somarLancamentos`/`statusDeParcela` nem de `taxas_imobiliaria` — é uma terceira tabela estruturalmente separada, ligada só a `card_id` (D-04 estendido a caução)"
    - "O backstop de exclusão do card reflete a existência de eventos de caução — `cardTemLancamento` já ampliada no plano 13-04 continua coerente, agora com dado real gravável"
    - "UI Consideration `empty` (Sheet): 'Nenhum evento de caução registrado ainda.'"
    - "UI Consideration `error` (Sheet + diálogo): 'Não foi possível registrar {tipo}. Tente novamente.' no diálogo; o Sheet em si não tem busca própria (lê dos mesmos dados já carregados pela página), então não tem estado de erro independente"
    - "UI Consideration `populated` (Sheet): lista de eventos em ordem cronológica ASCENDENTE (mais antigo primeiro) mais o rodapé de ações condicional — divergência deliberada da ordem descendente do relatório de reconciliação (plano 13-07), porque este Sheet é uma trilha de auditoria por contrato lida do início ao fim, não um worklist por período"
    - "UI Consideration `overflow` (Sheet, backstop): sem paginação, mesmo padrão `overflow-y-auto` de `ParcelaHistoricoSheet`"
    - "UI Consideration `zero-one-many` (rodapé): a máquina de estados de 0/1/2 botões é o próprio `zero-one-many` resolvido — não existe estado 'many' aqui, só 0/1/2"
    - "UI Consideration `long-text` (campo Observação, backstop): `Textarea` de comprimento livre, mesma convenção de todo campo `observacao`/`motivo` já existente no projeto"
  artifacts:
    - "web/src/components/financeiro/caucao-historico-sheet.tsx"
    - "web/src/components/financeiro/registrar-evento-caucao-dialog.tsx"
    - "web/src/components/financeiro/caucao-evento-label.tsx"
    - "web/src/lib/kanban/actions.ts — `registrarEventoCaucaoAction`"
    - "web/src/lib/kanban/taxas.ts — `saldoCaucao`, `statusCaucao`, tipos de caução"
  key_links:
    - "botão 'Caução' na linha de `configuracao-financeira-view.tsx` -> `CaucaoHistoricoSheet` -> `RegistrarEventoCaucaoDialog` -> `registrarEventoCaucaoAction` -> `public.caucao_eventos`"
    - "`saldoCaucao`/`statusCaucao` (taxas.ts) -> a coluna 'Caução' da tabela de Configuração financeira E o rodapé de 0/1/2 botões do Sheet — as duas superfícies usam exatamente a mesma função, nunca duas leituras divergentes do mesmo saldo"
  prohibitions:
    - "MUST NOT dar a `RegistrarEventoCaucaoDialog`/`registrarEventoCaucaoAction` nenhum caminho de UPDATE sobre uma linha de `caucao_eventos` já gravada — cada recebido/devolvido/usado é sempre um INSERT novo, mesmo quando o valor é uma correção do anterior (D-06)"
---

<objective>
Entregar o ciclo completo de caução: uma ação por contrato (recebido → devolvido/usado) reachável de
dentro da Configuração financeira, com histórico lateral append-only e rodapé de ações que muda
conforme o saldo.

Purpose: IMOB-04 pede acompanhamento do ciclo completo da caução, "mesmo espírito de histórico
append-only do resto do sistema" (D-06). Sem este plano, o dinheiro de caução que a imobiliária
recebe/devolve/usa não tem nenhum rastro no sistema.
Output: `configuracao-financeira-view.tsx` (do plano 13-05) ganha a coluna/botão de Caução; o Sheet e
o diálogo tipo-aware ficam prontos para o plano 13-07 (relatório de reconciliação) consumir.
</objective>

## Artifacts this phase produces

Este plano não repete a lista completa de artefatos da fase — ver `## Artifacts this phase produces`
em `13-01-PLAN.md`. Os símbolos que este plano especificamente cria/altera:
`web/src/components/financeiro/caucao-evento-label.tsx` (novo — `CAUCAO_TIPO`, `CaucaoEventoLabel`),
`web/src/components/financeiro/registrar-evento-caucao-dialog.tsx` (novo),
`web/src/components/financeiro/caucao-historico-sheet.tsx` (novo), `registrarEventoCaucaoAction`
(`actions.ts`), `registrarEventoCaucao` (wrapper em `queries.ts`), `saldoCaucao`/`statusCaucao`/
`TipoCaucao`/`CaucaoEventoResumo`/`CaucaoEventoDetalhado` (`taxas.ts`, estendido), a coluna/botão de
Caução em `configuracao-financeira-view.tsx` (do plano 13-05), e a segunda consulta de
`caucao_eventos` em `financeiro/configuracao/page.tsx`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-CONTEXT.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-PATTERNS.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-UI-SPEC.md
@web/src/components/financeiro/parcela-historico-sheet.tsx
@web/src/components/financeiro/cancelar-lancamento-dialog.tsx
@web/src/components/financeiro/lancamento-tipo-label.tsx
@web/src/components/financeiro/registrar-pagamento-dialog.tsx
@web/src/lib/kanban/actions.ts
</context>

<planner_assumptions>
- **A-01 — o Sheet lê os eventos que a própria página de Configuração financeira já buscou, não uma
  Server Action própria.** Mesmo padrão de `ParcelaHistoricoSheet`, que recebe `lancamentos` como
  prop (não busca sozinho). `configuracao-financeira-view.tsx`/`page.tsx` (plano 13-05) precisam
  buscar `caucao_eventos` de TODOS os contratos de uma vez (mesma query, `.in("card_id", cardIds)`) e
  agrupar por `card_id` em memória — mesmo padrão já usado para `primeiraCompetenciaPorCard` no plano
  13-04. Isso evita uma consulta por linha clicada e mantém o Sheet sem `useEffect`/fetch próprio.
- **A-02 — sem validação de saldo negativo no servidor.** 13-CONTEXT.md não pede uma trava contra
  devolver/usar mais do que o saldo disponível, e o UI-SPEC explicitamente permite valor livre no
  campo (pré-preenchido com o saldo, mas editável). Inventar essa trava seria escopo não pedido —
  registrado aqui para não ser "descoberto" como esquecimento depois.
- **A-03 — `statusCaucao` decide "devolvida" vs. "usada" pelo tipo do ÚLTIMO evento (por `criado_em`)
  quando o saldo chega a zero ou menos**, não por uma regra mais elaborada de "qual tipo trouxe o
  saldo a zero pela última vez" em cenários de eventos intercalados. UI-SPEC não detalha esse caso —
  a leitura mais simples e defensável do texto ("saldo voltou a 0 via devolução"/"via uso") é o tipo
  do evento mais recente, e é isso que este plano implementa.
</planner_assumptions>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Pure calc de caução + `registrarEventoCaucaoAction` + label + diálogo tipo-aware</name>

  <files>
web/src/lib/kanban/taxas.ts
web/src/lib/kanban/actions.ts
web/src/lib/kanban/queries.ts
web/src/components/financeiro/caucao-evento-label.tsx
web/src/components/financeiro/registrar-evento-caucao-dialog.tsx
  </files>

  <read_first>
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-UI-SPEC.md` — §3 completo, Color § Status tones,
      Copywriting Contract (todas as linhas de Caução)
    - `web/src/lib/kanban/lancamento-tipo-label.tsx` (leia via `web/src/components/financeiro/lancamento-tipo-label.tsx`)
      — o molde exato de `TIPO`/`LancamentoTipoLabel` (dual export: mapa de cópia + componente) que
      `CAUCAO_TIPO`/`CaucaoEventoLabel` replica
    - `web/src/components/financeiro/cancelar-lancamento-dialog.tsx` — o padrão tipo-aware via mapa
      (`TIPO[tipo].label`, linhas 27/50/85-87/105) que `RegistrarEventoCaucaoDialog` replica para
      copy, **não** para a estrutura `AlertDialog` (caução é aditiva, nunca destrutiva)
    - `web/src/components/financeiro/registrar-pagamento-dialog.tsx` — arquivo inteiro, o molde de
      `Dialog`+três campos+erro+`DialogFooter` que `RegistrarEventoCaucaoDialog` replica na estrutura
    - `web/src/lib/kanban/actions.ts` — `destravarParcelaAction` (linhas 1200-1275, o molde mais
      próximo de "validar → ler estado atual → inserir evento" sem tocar `parcela_lancamentos`)
  </read_first>

  <action>
**1. `web/src/lib/kanban/taxas.ts`** (estender, arquivo já existe do plano 13-04). Acrescentar:
- `export type TipoCaucao = "recebido" | "devolvido" | "usado"`.
- `export type CaucaoEventoResumo = { tipo: TipoCaucao; valor: number }` — o subconjunto mínimo para
  cálculo de saldo, mesmo espírito de `LancamentoResumo` em `parcelas.ts`.
- `saldoCaucao(eventos: CaucaoEventoResumo[] | null | undefined): number` — `recebido` soma, `devolvido`
  e `usado` subtraem. Array vazio/nulo devolve 0. Comentário citando D-06: "espelha
  `somarLancamentos` (parcelas.ts) na forma, nunca na função — este saldo nunca participa do cálculo
  de status de nenhuma parcela."
- `export type StatusCaucao = "nao-recebida" | "recebida" | "devolvida" | "usada"`.
- `statusCaucao(eventos: (CaucaoEventoResumo & { criado_em: string })[] | null | undefined):
  StatusCaucao` — implementa A-03: se `!eventos?.length`, `"nao-recebida"`; senão calcula
  `saldoCaucao(eventos)`; se `saldo > 0`, `"recebida"`; senão (saldo <= 0), ordena por `criado_em`
  descendente e devolve `"devolvida"` se o evento mais recente for `tipo === "devolvido"`, senão
  `"usada"`.

**2. `web/src/lib/kanban/actions.ts`** — novo validador `tipoCaucaoValido(valor: unknown): string |
null` (recusa fora de `["recebido","devolvido","usado"]`, mensagem "Tipo de evento de caução
inválido."). Nova Server Action, na mesma seção `// Configuração financeira (Phase 13)` criada no
plano 13-05:

```
export async function registrarEventoCaucaoAction(
  cardId: string,
  tipo: "recebido" | "devolvido" | "usado",
  valor: number,
  data: string,
  observacao: string | null
): Promise<ActionResult> {
  const sessao = await requireUser()
  if (!sessao) return { ok: false, error: NAO_AUTENTICADO }

  const invalido =
    id(cardId, "Imóvel") ??
    tipoCaucaoValido(tipo) ??
    valorLancamento(valor, "Informe um valor válido.") ??
    dataObrigatoria(data) ??
    textoOpcional(observacao, "Observação", 2000)
  if (invalido) return { ok: false, error: invalido }

  const { data: inserido, error } = await sessao.supabase
    .from("caucao_eventos")
    .insert({
      card_id: cardId,
      tipo,
      valor,
      data,
      observacao: observacao?.trim() || null,
      criado_por: sessao.user.id,
    })
    .select("id")

  if (error) return { ok: false, error: erroDoBanco(error.code, `registrar ${tipo === "recebido" ? "o recebimento" : tipo === "devolvido" ? "a devolução" : "o uso"} da caução`) }
  if (!inserido || inserido.length === 0) return { ok: false, error: semLinhas("registrar o evento de caução") }
  return { ok: true, data: undefined }
}
```

Comentar, verbatim: "Nenhuma trava de saldo — devolver/usar mais do que o `saldoCaucao` atual não é
recusado aqui (A-02, 13-05-PLAN.md); o campo já vem pré-preenchido com o saldo no diálogo, mas
permanece editável para cobrir devolução/uso parcial ou correção de um valor recebido incorretamente
como um NOVO evento, nunca uma edição do antigo (D-06)." `valorLancamento` (já existente) é reusado
sem alteração — caução exige `valor > 0`, mesma régua de pagamento/acréscimo/desconto.

**3. `web/src/lib/kanban/queries.ts`** — `registrarEventoCaucao(cardId, tipo, valor, data,
observacao)`, mesmo `unwrap(...)`.

**4. `web/src/components/financeiro/caucao-evento-label.tsx`** (novo) — dual export, molde idêntico a
`lancamento-tipo-label.tsx`: `export const CAUCAO_TIPO = { recebido: { icon: PiggyBank, label:
"Recebida", className: "text-status-good" }, devolvido: { icon: Undo2, label: "Devolvida", className:
"text-muted-foreground" }, usado: { icon: ShieldCheck, label: "Usada", className:
"text-status-warning" } } as const` e `export function CaucaoEventoLabel({ tipo }: { tipo: TipoCaucao
}) { ... }` (mesmo JSX de `LancamentoTipoLabel`: `<span className="inline-flex items-center gap-1.5
text-xs font-semibold" ...><Icon className="size-3.5 shrink-0" />{label}</span>`). Ícones de
`lucide-react`: `PiggyBank`, `Undo2`, `ShieldCheck` (UI-SPEC Design System, já listados como
necessários nesta fase).

**5. `web/src/components/financeiro/registrar-evento-caucao-dialog.tsx`** (novo). Mapa local
`TIPO_CAUCAO_DIALOGO` (título/label de submit/loading, distinto do mapa de ícones em
`caucao-evento-label.tsx` — este é só copy de diálogo): `recebido: { title: "Registrar caução
recebida", submitLabel: "Registrar recebimento", submitLoadingLabel: "Registrando..." }`, `devolvido:
{ title: "Registrar devolução da caução", submitLabel: "Registrar devolução", submitLoadingLabel:
"Registrando..." }`, `usado: { title: "Registrar uso da caução", submitLabel: "Registrar uso",
submitLoadingLabel: "Registrando..." }`.

Estrutura: `Dialog` + `DialogContent className="sm:max-w-md"` (nunca `AlertDialog` — caução é
aditiva). `DialogHeader` com `DialogTitle` = `TIPO_CAUCAO_DIALOGO[tipo].title`. Três campos `flex
flex-col gap-1.5`: "Valor (R$)" (`inputMode="decimal"`, vazio para `tipo === "recebido"`,
pré-preenchido com `saldoAtual.toFixed(2).replace(".", ",")` para `devolvido`/`usado` — prop
`saldoAtual: number` recebida do chamador), "Data" (`type="date"`, default hoje — mesma prop
`todayISO` de `RegistrarPagamentoDialog`), "Observação (opcional)" (`Textarea`). Uma linha de erro.
`DialogFooter` com um botão usando `TIPO_CAUCAO_DIALOGO[tipo].submitLabel`/`submitLoadingLabel`.

Props: `cardId: string`, `tipo: TipoCaucao`, `saldoAtual: number`, `todayISO: string`, `open: boolean`,
`onOpenChange: (open: boolean) => void`. Padrão `wasOpen` de resincronização (copiar de
`registrar-pagamento-dialog.tsx` linhas 71-80): ao reabrir, reinicializar `valor` conforme a regra
acima (vazio ou saldo), `data` para `todayISO`, `observacao` para `""`, `error` para `null`.
`handleSubmit`: `Number(valor.replace(",", "."))` finito e `> 0` (senão "Informe um valor válido.");
chama `registrarEventoCaucao(cardId, tipo, parsedValor, data, observacao.trim() || null)`; erro
fallback `` `Não foi possível registrar ${tipo === "recebido" ? "o recebimento" : tipo === "devolvido"
? "a devolução" : "o uso"}. Tente novamente.` ``.
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && npm run lint`/`npm run build` saem com código 0
    - `grep -cE 'export function (saldoCaucao|statusCaucao)' web/src/lib/kanban/taxas.ts` devolve 2
    - `grep -c 'export async function registrarEventoCaucaoAction' web/src/lib/kanban/actions.ts`
      devolve 1
    - Dentro de `registrarEventoCaucaoAction`, nenhuma referência a `parcela_lancamentos` nem
      `taxas_imobiliaria` (`awk '/export async function registrarEventoCaucaoAction/,/^}/'
      web/src/lib/kanban/actions.ts | grep -cE 'parcela_lancamentos|taxas_imobiliaria'` devolve 0) e
      nenhuma chamada a `recalcularEGravarStatus` (mesmo `awk` filtrado por `grep -c
      'recalcularEGravarStatus'` devolve 0)
    - `test -f web/src/components/financeiro/caucao-evento-label.tsx` existe, exporta `CAUCAO_TIPO` e
      `CaucaoEventoLabel` (`grep -cE 'export (const CAUCAO_TIPO|function CaucaoEventoLabel)'
      web/src/components/financeiro/caucao-evento-label.tsx` devolve 2)
    - `test -f web/src/components/financeiro/registrar-evento-caucao-dialog.tsx` existe
    - `grep -c 'AlertDialog' web/src/components/financeiro/registrar-evento-caucao-dialog.tsx` devolve
      0 — nunca usa a estrutura destrutiva
    - `grep -cE 'Registrar caução recebida|Registrar devolução da caução|Registrar uso da caução'
      web/src/components/financeiro/registrar-evento-caucao-dialog.tsx` devolve 3
  </acceptance_criteria>

  <reversibility rating="reversible">Escreve arquivos de código e uma Server Action nova; nenhum evento
  de caução real é gravado só por esta task — a UI que chama a action nasce na Task 2.</reversibility>

  <done>`taxas.ts` calcula saldo/status de caução; `registrarEventoCaucaoAction` grava um evento novo
  sem tocar as outras duas tabelas financeiras; o diálogo tipo-aware existe com a copy exata do
  Copywriting Contract, sem estrutura destrutiva.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: `CaucaoHistoricoSheet` + coluna/botão de Caução em Configuração financeira</name>

  <files>
web/src/components/financeiro/caucao-historico-sheet.tsx
web/src/components/financeiro/configuracao-financeira-view.tsx
  </files>

  <read_first>
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-UI-SPEC.md` — §1 (linha "Caução" da tabela) e §3
      completo (Sheet + `SheetFooter`), Color § Status tones
    - `web/src/components/financeiro/parcela-historico-sheet.tsx` — arquivo inteiro, o shell
      `Sheet`/`SheetContent`/`SheetHeader` (linhas 57-66), o corpo de lista (linhas 68-124),
      `prefixoValor` (linhas 26-30) — o molde de sinal `+`/`−`
    - `web/src/components/ui/sheet.tsx` — `SheetFooter` (linhas 93-101), já existe no projeto mas
      nenhum call site o usa ainda — este é o primeiro
    - `web/src/components/financeiro/configuracao-financeira-view.tsx` (escrito no plano 13-05) — a
      tabela e o estado `contratoEditando` a estender com o segundo diálogo/Sheet
  </read_first>

  <action>
**1. `web/src/components/financeiro/caucao-historico-sheet.tsx`** (novo). `Sheet open/onOpenChange` →
`SheetContent side="right" className="w-full p-0 sm:max-w-md"` → `SheetHeader className="border-b
border-border"` com `SheetTitle` **"Caução — {endereco}"** e subtítulo `<p className="text-sm
text-muted-foreground">Contrato #{numero} · {proprietario} · Saldo atual:
{formatCurrency(saldo)}</p>`.

Corpo: `<div className="flex-1 overflow-y-auto p-4">`. Se `eventos.length === 0`: `<p
className="text-sm text-muted-foreground">Nenhum evento de caução registrado ainda.</p>`. Senão:
`<ul className="flex flex-col gap-3">` de `<li className="flex flex-col gap-2 rounded-xl
border border-border bg-card p-3">` — cada `li` mostra `<CaucaoEventoLabel tipo={evento.tipo} />` +
valor com sinal (`prefixoValorCaucao`: `+` para `recebido`, `−` para `devolvido`/`usado` — nova
função local, mesmo molde de `prefixoValor` em `parcela-historico-sheet.tsx` mas sem o caso
`destrava`, que não existe aqui), data (`formatDate`), quem (`profiles?.full_name ?? profiles?.email
?? "—"`), observação se presente. **Sem** botão de cancelar por evento (diferente de
`ParcelaHistoricoSheet` linhas 107-118 — caução não tem mecanismo de cancelamento nesta fase). Ordem:
`eventos` já vem ordenado ascendente por `criado_em` de quem monta a prop (mesma ordem que
`LancamentoDetalhado[]` já usa) — não reordenar dentro do Sheet.

`SheetFooter` (novo neste codebase, usar o componente já existente em `sheet.tsx` sem modificá-lo):
calcular `saldo = saldoCaucao(eventos)` (de `taxas.ts`). Se `eventos.length === 0 || saldo <= 0`:
um botão "Registrar caução recebida" (`Button variant="default"`, abre
`RegistrarEventoCaucaoDialog` com `tipo="recebido"`). Se `saldo > 0`: dois botões lado a lado —
"Devolver caução" (`variant="outline"`, `tipo="devolvido"`) + "Registrar uso" (`variant="default"`,
`tipo="usado"`) — nenhum com estilo destrutivo.

Props: `cardId: string`, `numero: number`, `endereco: string`, `proprietario: string`, `eventos:
CaucaoEventoDetalhado[]`, `todayISO: string`, `open: boolean`, `onOpenChange: (open: boolean) =>
void`, onde `CaucaoEventoDetalhado` é um tipo novo em `taxas.ts` (acrescentar nesta task): `{ id:
string; tipo: TipoCaucao; valor: number; data: string; observacao: string | null; criado_em: string;
profiles: { full_name: string | null; email: string | null } | null }` — mesmo molde de
`LancamentoDetalhado` (`parcelas.ts`).

Estado local `dialogoAberto: TipoCaucao | null` controla qual `RegistrarEventoCaucaoDialog` está
aberto (mesmo padrão de `dialogoAberto` em `AcoesCell`). Renderizar o `RegistrarEventoCaucaoDialog`
uma vez, com `tipo={dialogoAberto ?? "recebido"}` e `open={dialogoAberto !== null}` (mesmo truque de
fallback que `ParcelaHistoricoSheet` já usa para `CancelarLancamentoDialog`, linhas 126-136).

**2. `web/src/components/financeiro/configuracao-financeira-view.tsx`** (estender, do plano 13-05).
`ContratoConfig` ganha `caucaoEventos: CaucaoEventoDetalhado[]` (a página, `page.tsx`, precisa buscar
e agrupar por `card_id` — A-01; esta task assume que a prop já chega pronta, a busca em si é ajuste
mínimo em `page.tsx` descrito abaixo). Nova coluna "Caução" na tabela, entre "Comissão 1º aluguel" e
"Ações": ícone+label conforme `statusCaucao(linha.caucaoEventos)` — `nao-recebida`: `CircleDashed`/
`text-muted-foreground`/"Não recebida"; `recebida`: `PiggyBank`/`text-status-good`/"Recebida";
`devolvida`: `Undo2`/`text-muted-foreground`/"Devolvida"; `usada`: `ShieldCheck`/
`text-status-warning`/"Usada" (mapa local `STATUS_CAUCAO_LABEL`, distinto de `CAUCAO_TIPO` — este é
o status agregado do contrato, aquele é o tipo do evento individual). Botão "Caução" na coluna Ações
(`Button variant="outline" size="sm"`, ícone `PiggyBank`), ao lado de "Editar percentuais", abrindo
`CaucaoHistoricoSheet` para aquela linha — novo estado `caucaoAberta: string | null` (id do contrato).

**3. `web/src/app/(app)/financeiro/configuracao/page.tsx`** (ajuste mínimo, mesmo arquivo do plano
13-05) — depois de buscar `cards`, uma segunda consulta: `.from("caucao_eventos").select("id, card_id,
tipo, valor, data, observacao, criado_em, profiles(full_name, email)").in("card_id", cardIds)` (todos
os `card_id` da página, mesma técnica de agrupamento em memória do plano 13-04 —
`primeiraCompetenciaPorCard`). Agrupar por `card_id` num `Record<string, CaucaoEventoDetalhado[]>`,
ordenando cada grupo ascendente por `criado_em` antes de anexar a cada `ContratoConfig`. Em erro desta
segunda consulta, capturar e usar `[]` para todos (não derruba a página — mesmo padrão de fallback já
usado no plano 13-04 para `primeiraCompetenciaPorCard`).
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && npm run lint`/`npm run build` saem com código 0
    - `test -f web/src/components/financeiro/caucao-historico-sheet.tsx` existe
    - `grep -c 'SheetFooter' web/src/components/financeiro/caucao-historico-sheet.tsx` devolve 2 ou
      mais (import + uso)
    - `grep -c 'Nenhum evento de caução registrado ainda.'
      web/src/components/financeiro/caucao-historico-sheet.tsx` devolve 1
    - `grep -c 'Trash2\|Cancelar' web/src/components/financeiro/caucao-historico-sheet.tsx` devolve 0
      — sem botão de cancelar por evento
    - `grep -c 'saldoCaucao' web/src/components/financeiro/caucao-historico-sheet.tsx` devolve 1 ou mais
    - `grep -c '"Caução"' web/src/components/financeiro/configuracao-financeira-view.tsx` devolve 1 ou
      mais (agora sim a coluna/botão existem, ao contrário do plano 13-05)
    - `grep -c 'statusCaucao' web/src/components/financeiro/configuracao-financeira-view.tsx` devolve 1
      ou mais
    - `grep -c 'from("caucao_eventos")' "web/src/app/(app)/financeiro/configuracao/page.tsx"` devolve 1
  </acceptance_criteria>

  <reversibility rating="reversible">Estende componentes de leitura/apresentação e uma consulta a mais
  na página já existente; a única gravação (Server Action da Task 1) só acontece se o operador clicar
  em um dos botões do rodapé.</reversibility>

  <done>`CaucaoHistoricoSheet` mostra o histórico ordenado, o rodapé de 0/1/2 botões conforme o saldo;
  a tabela de Configuração financeira ganha a coluna/botão de Caução, completando as seis colunas da
  UI-SPEC §1.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar em produção o ciclo completo de caução</name>

  <files>(nenhum arquivo é alterado — verificação operacional)</files>

  <read_first>
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-CONTEXT.md` — D-06
  </read_first>

  <what-built>
    O botão "Caução" em `/financeiro/configuracao` abre um histórico lateral por contrato, com um
    rodapé de ações que muda conforme o saldo (recebido − devolvido − usado), gravando cada evento
    como uma linha nova em `public.caucao_eventos`.
  </what-built>

  <how-to-verify>
    **Passo 0 — pré-voo.** No SQL Editor: `select count(*) as eventos_antes from
    public.caucao_eventos;` — deve ser 0. Escolha um contrato de teste sem caução ainda.

    **Passo 1 — ciclo completo.** Abra "Caução" no contrato de teste — deve mostrar "Nenhum evento de
    caução registrado ainda." e só o botão "Registrar caução recebida". Registre um recebimento (ex.:
    R$ 1.000,00). O Sheet deve atualizar mostrando o evento e trocar o rodapé para dois botões
    ("Devolver caução" + "Registrar uso"). Registre um uso parcial (ex.: R$ 300,00 — confirme que o
    campo Valor veio pré-preenchido com R$ 1.000,00 mas você conseguiu editar para 300). O saldo
    exibido no subtítulo deve cair para R$ 700,00 e o rodapé continuar com dois botões. Devolva o
    restante (R$ 700,00 — confirme que o campo veio pré-preenchido com 700). O rodapé deve voltar a
    mostrar só "Registrar caução recebida", e o status na tabela deve mostrar "Devolvida" (o último
    evento foi uma devolução).

    **Passo 2 — conferir no banco.**
    ```sql
    select tipo, valor, data from public.caucao_eventos
    where card_id = '<id do contrato de teste>' order by criado_em asc;
    ```
    Esperado: três linhas (recebido 1000, usado 300, devolvido 700), nenhuma editada — três INSERTs
    distintos, não um UPDATE.

    **Passo 3 — regressão.** Confirme que `taxas_imobiliaria`, `parcela_lancamentos`,
    `valorDevido`/`valorPago` de qualquer parcela do contrato de teste continuam sem nenhuma linha ou
    valor relacionado aos eventos de caução — nenhuma tabela financeira além de `caucao_eventos`
    recebeu escrita.

    **Passo 4 — backstop de exclusão.** Tente excluir o contrato de teste (sem confirmar) — o diálogo
    deve indicar movimentação financeira e oferecer arquivar em vez de excluir.
  </how-to-verify>

  <acceptance_criteria>
    - `eventos_antes` era 0
    - O rodapé mudou corretamente nos três estados: 0 eventos (1 botão), saldo > 0 (2 botões), saldo
      de volta a 0 via devolução (1 botão)
    - Os campos de valor vieram pré-preenchidos com o saldo para uso/devolução e editáveis
    - A consulta SQL mostra três linhas distintas (INSERT), nenhuma edição
    - O status "Devolvida" apareceu corretamente na tabela depois do ciclo completo
    - Nenhuma tabela financeira além de `caucao_eventos` foi afetada
    - O diálogo de exclusão do contrato de teste reflete a movimentação de caução
    - O operador registrou os valores testados no SUMMARY
  </acceptance_criteria>

  <resume-signal>Responda "aprovado" com os valores testados, ou descreva o que falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `registrarEventoCaucaoAction` | o único ponto de escrita de `caucao_eventos`; RLS (`is_team_member()`, plano 13-01) é a rede de proteção final |
| `caucao_eventos` → cálculo de status da parcela/taxa | terceira tabela estruturalmente isolada — nenhuma leitura dela participa de `somarLancamentos`/`statusDeParcela` nem de `taxas_imobiliaria` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-25 | Tampering | `caucao_eventos` acoplada a `parcela_lancamentos`/`taxas_imobiliaria` | critical | mitigate | Asserção de fonte no `<verify>` da Task 1: `registrarEventoCaucaoAction` não referencia nenhuma das duas outras tabelas nem `recalcularEGravarStatus`; verificado em produção na Task 3 (Passo 3) |
| T-13-26 | Tampering | edição in-place de um evento de caução já gravado | high | mitigate | `registrarEventoCaucaoAction` só implementa `.insert()`, nunca `.update()`; nenhum componente novo tem caminho de edição — o "editar" que o diálogo faz é sempre um novo evento (correção via evento novo, não sobrescrita) |
| T-13-27 | Elevation of Privilege | escrita em `caucao_eventos` sem sessão do usuário | high | mitigate | `registrarEventoCaucaoAction` usa `sessao.supabase`, nunca `service_role` |
| T-13-28 | Information Disclosure | erro do Postgres vazando ao registrar evento | medium | mitigate | `erroDoBanco(error.code, ...)`, mesmo padrão de sanitização do resto do arquivo |
| T-13-29 | Repudiation | evento de caução sem rastro de quem registrou | medium | mitigate | `criado_por` vem exclusivamente da sessão do servidor (`sessao.user.id`), nunca do que o cliente mandou — mesmo padrão de `criado_por` em todo o resto do arquivo |
| T-13-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | `PiggyBank`/`Undo2`/`ShieldCheck`/`CircleDashed` já existem em `lucide-react`, já instalado — nenhum pacote novo |
</threat_model>

<verification>
1. `cd web && npm run lint && npm run build` saem com código 0
2. Asserções de fonte confirmando que `registrarEventoCaucaoAction` só grava `caucao_eventos`, sem
   tocar `parcela_lancamentos`/`taxas_imobiliaria`/`recalcularEGravarStatus`
3. Produção: o ciclo completo (recebido → uso parcial → devolução do restante) grava três linhas
   distintas, nenhuma edição, e o status/saldo derivados batem em cada etapa
4. Nenhuma outra tabela financeira é afetada pelo ciclo de caução
</verification>

<success_criteria>
- Existe uma ação de caução por contrato com ciclo completo (recebido, devolvido, usado), append-only
  (IMOB-04)
- O rodapé do histórico reflete corretamente o saldo em cada um dos três estados
- Nenhum evento de caução afeta `parcelas`/`parcela_lancamentos`/`taxas_imobiliaria`
- O backstop de exclusão (plano 13-01) reflete a movimentação de caução real
</success_criteria>

<output>
Create `.planning/phases/13-dinheiro-da-imobili-ria/13-06-SUMMARY.md` when done
</output>
