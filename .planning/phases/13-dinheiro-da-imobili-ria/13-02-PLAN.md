---
phase: 13-dinheiro-da-imobili-ria
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - supabase/verificacao_dinheiro_imobiliaria.sql
autonomous: false
requirements: [IMOB-01, IMOB-02, IMOB-03, IMOB-04]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Parte A do runbook foi executada contra o banco de produção real de um jeito que garante continuidade de sessão — Parte A inteira num único clique de Run, ou psql/Supabase CLI (D-19). Rodar bloco a bloco no SQL Editor do Studio não conta como ensaio"
    - "O BLOCO 3 pós-`rollback` confirma que `percentual_administracao`, `percentual_comissao_primeiro_aluguel`, `taxas_imobiliaria` e `caucao_eventos` **não** existem mais — se algum sobreviveu, o ensaio virou push e o plano para para reportar, mesmo acidente já visto na Fase 6.1"
    - "`cards_total`, `parcelas_total`, `lancamentos_total` e `updated_at_max` depois do `rollback` são idênticos aos do BLOCO 1"
    - "As nove recusas de validação (CHECK/FK) foram observadas de verdade contra o schema real, uma por uma, com o texto `OK recusado:` de cada uma anotado"
    - "RLS foi observado negando leitura/escrita nas duas tabelas novas para um e-mail fora da allowlist, e liberando para um e-mail da allowlist"
    - "O backstop de exclusão ampliado foi observado nos quatro lados: bloqueia por evento de taxa, bloqueia por evento de caução, continua bloqueando por lançamento em `parcela_lancamentos` (regressão da Phase 6.2), libera um card sem nenhum dos três"
    - "A segunda passagem da mesma DDL dentro da transação não errou e não alterou nada (reexecutabilidade provada, não presumida)"
    - "O bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_dinheiro_imobiliaria.sql` está preenchido com os números reais observados, os ids usados e o caminho de execução usado (único Run vs. psql/CLI)"
  artifacts:
    - supabase/verificacao_dinheiro_imobiliaria.sql
  key_links:
    - "ensaio -> `checkpoint:decision` do plano 13-03: sem o resultado registrado aqui, aquele portão não tem base para decidir"
---

<objective>
Ensaiar a migração `20260824000000_dinheiro_imobiliaria.sql` contra o banco de produção dentro de uma
transação revertida, e registrar o resultado real — números, ids usados e caminho de execução — no
próprio runbook.

Este plano **não** aplica nada. Segue a mesma disciplina que a Fase 6.1 estabeleceu depois de um
acidente real: em 2026-08-18 a instrução "rode um bloco de cada vez, na mesma aba" transformou um
ensaio em push real de produção, e ninguém percebeu até comparar `updated_at_max` antes e depois
(D-19). A tarefa aqui é tão sobre *como* rodar quanto sobre *o que* observar — e esta migração tem
mais superfície a provar que qualquer uma anterior deste projeto (duas colunas, duas tabelas, nove
regras de validação, RLS em duas tabelas novas e um trigger ampliado).

Purpose: o `checkpoint:decision` do plano 13-03 só tem base se este ensaio existir e estiver
registrado. Um trigger de exclusão ampliado é o tipo de objeto que, se estiver errado, ou trava algo
legítimo ou deixa passar exclusão de dado financeiro real — precisa ser visto funcionando nos quatro
lados antes de virar permanente.
Output: `RESULTADO DO ENSAIO` preenchido em `supabase/verificacao_dinheiro_imobiliaria.sql`.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `13-01-PLAN.md`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-CONTEXT.md
@supabase/verificacao_dinheiro_imobiliaria.sql
@supabase/migrations/20260824000000_dinheiro_imobiliaria.sql
@supabase/verificacao_cards_arquivado_em.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rodar a Parte A contra produção e anotar o que aconteceu</name>
  <precondition>`supabase/migrations/20260824000000_dinheiro_imobiliaria.sql` e `supabase/verificacao_dinheiro_imobiliaria.sql` existem no repositório (plano 13-01), e nenhuma das quatro adições (`percentual_administracao`, `percentual_comissao_primeiro_aluguel`, `taxas_imobiliaria`, `caucao_eventos`) existe ainda no banco de produção.</precondition>
  <read_first>
    - `supabase/verificacao_dinheiro_imobiliaria.sql` — Parte A inteira, e principalmente o aviso de
      pooling no cabeçalho
    - `supabase/verificacao_cards_arquivado_em.sql` — a seção `RESULTADO DO ENSAIO`, para ver o formato
      do registro que a Task 2 vai reproduzir
  </read_first>
  <what-built>
    O runbook de ensaio (`supabase/verificacao_dinheiro_imobiliaria.sql`, Parte A) e a migração que ele
    ensaia (`20260824000000_dinheiro_imobiliaria.sql`): os dois percentuais em `cards`, as tabelas
    `taxas_imobiliaria` e `caucao_eventos`, e o backstop de exclusão ampliado.
  </what-built>
  <how-to-verify>
    **Antes de qualquer coisa — escolha o caminho de execução (D-19). Só existem dois aceitos:**

    - **(a)** Supabase Studio → SQL Editor, colando a **Parte A INTEIRA** (BLOCO 2 do `begin;` até o
      `rollback;`) num **único** clique de "Run". Nunca bloco a bloco.
    - **(b)** `psql` ou Supabase CLI apontando para o banco de produção.

    **Passo 1 — baseline (BLOCO 1, fora de transação).** Anotar `cards_total`, `updated_at_max`,
    `parcelas_total`, `lancamentos_total`.

    **Passo 2 — escolher os ids que as provas vão usar** (consultas prontas no runbook): um
    `parcelas.id` real (para a Prova 2.5 de FK), um `cards.id` real qualquer (para a Prova 2.2). A
    Prova 2.7 cria seus próprios cards temporários dentro da transação — não precisa de id real.

    **Passo 3 — rodar o BLOCO 2 inteiro de uma vez.**

    **Passo 4 — ler cada prova e anotar o resultado real:**
    - 2.1/2.3/2.4: colunas e tabelas nascem com o tipo/nulabilidade/default certos
    - 2.2: zero cards com percentual diferente do default logo após o `alter table`
    - 2.5: as nove mensagens `OK recusado:` apareceram (origem inválida, valor negativo/estourado/zero
      em cada tabela conforme a regra de cada uma, observação longa demais, percentual fora de
      0–100) mais a recusa de FK por `parcela_id` inexistente — **dez recusas no total**. Se alguma
      passou em vez de ser recusada, **pare aqui e reporte**
    - 2.6: RLS negou tudo para o e-mail fora da allowlist e liberou tudo para o e-mail da allowlist
    - 2.7: os quatro lados do backstop — bloqueou por taxa, bloqueou por caução, continuou bloqueando
      por pagamento, liberou o card sem nenhum dos três. Se qualquer um saiu diferente do esperado,
      **pare aqui e reporte** — um backstop errado é o item de maior severidade desta fase
    - 2.8: `max(updated_at)` igual ao do Passo 1

    **Passo 5 — rodar o BLOCO 3 (pós-`rollback`) e conferir a coisa mais importante do ensaio
    inteiro:** nenhuma das quatro adições pode existir mais, e os quatro números do Passo 1 têm de
    estar idênticos. Se qualquer uma sobreviveu, o `rollback` não pegou — o ensaio virou push. Nesse
    caso **não** siga para a Task 2 como se tudo tivesse dado certo: registre o acidente com
    honestidade e reporte antes de continuar.
  </how-to-verify>
  <acceptance_criteria>
    - O caminho de execução usado está declarado: "(a) único Run" ou "(b) psql/CLI"
    - Os quatro números do baseline e os ids escolhidos estão anotados
    - As dez recusas de validação (nove CHECK + uma FK) têm resultado observado registrado, não
      presumido
    - Os quatro lados da Prova 2.7 (backstop ampliado) foram todos observados e nenhum saiu diferente
      do esperado — se saiu, o plano parou e reportou
    - Depois do `rollback`, nenhuma das quatro adições existe e os quatro números batem com o baseline
  </acceptance_criteria>
  <reversibility rating="reversible">O ensaio inteiro vive dentro de `begin; ... rollback;` e não deixa
  rastro no banco — desde que o caminho de execução do D-19 seja respeitado. É exatamente essa
  condição que o Passo 5 verifica.</reversibility>
  <resume-signal>Responda com os resultados dos Passos 1 a 5, ou com o que travou.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar o resultado do ensaio dentro do próprio runbook</name>
  <precondition>A Task 1 foi executada e os resultados de todas as provas estão disponíveis na conversa.</precondition>
  <files>supabase/verificacao_dinheiro_imobiliaria.sql</files>
  <read_first>
    - `supabase/verificacao_cards_arquivado_em.sql` — a seção `RESULTADO DO ENSAIO`: contexto primeiro,
      depois os números, depois as lições. É esse o formato a repetir
    - `supabase/verificacao_dinheiro_imobiliaria.sql` — o cabeçalho `RESULTADO DO ENSAIO` vazio deixado
      pela Task 2 do plano 13-01
  </read_first>
  <action>
Preencher o bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_dinheiro_imobiliaria.sql` com o que a
Task 1 realmente observou. Escrever em comentário SQL, em português, no mesmo tom dos registros
anteriores deste projeto: contexto primeiro, números depois, e o que ficou aprendido no fim.

Conteúdo obrigatório:
- a data do ensaio e qual dos dois caminhos de D-19 foi usado — não deixar isso implícito
- os quatro números do baseline e os quatro depois do `rollback`, lado a lado
- os ids usados nas provas, para que a Parte B (pós-push, plano 13-03) possa repetir contra alvos
  equivalentes
- o resultado literal de cada uma das dez recusas de validação e dos quatro lados do backstop
  ampliado
- a confirmação explícita de que as quatro adições deixaram de existir depois do `rollback` — ou, se
  não deixaram, o registro honesto do que aconteceu e o que foi feito a respeito
- qualquer divergência entre o que o runbook previa e o que o banco respondeu, inclusive correção
  feita na hora; se o arquivo de migração ou o runbook precisou ser corrigido, dizer o quê e por quê

Se alguma correção foi feita em `20260824000000_dinheiro_imobiliaria.sql` durante o ensaio, o ensaio
precisa ser refeito por inteiro com o arquivo corrigido antes de este bloco ser dado como fechado —
registrar quantas rodadas foram necessárias.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f=supabase/verificacao_dinheiro_imobiliaria.sql && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "cards_total" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qiE "psql|CLI|único Run|unico Run" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | wc -l | awk '$1 > 15 {print "ENSAIO_REGISTRADO"; exit} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - A seção `RESULTADO DO ENSAIO` tem mais de 15 linhas e cita `cards_total`
    - O caminho de execução (único Run ou psql/CLI) está nomeado por escrito
    - Os ids usados nas provas estão registrados
    - As dez recusas de validação e os quatro lados do backstop têm resultado escrito
    - Está escrito, em uma frase direta, se as quatro adições sobreviveram ou não ao `rollback`
    - Nenhum arquivo além de `supabase/verificacao_dinheiro_imobiliaria.sql` foi modificado por esta
      task
  </acceptance_criteria>
  <done>O runbook carrega o registro do ensaio real, com números, ids, caminho de execução e o
  veredito sobre o `rollback`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → banco de produção | Este plano executa SQL contra o banco real. A única coisa que separa "ensaio" de "push" é a continuidade da sessão que envolve o `begin;`/`rollback;` |
| runbook → operador | O texto do runbook é a única instrução que o operador segue; uma instrução errada aqui já causou perda de controle uma vez (Fase 6.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-07 | Tampering | ensaio que commita DDL em produção (pool de conexões do Studio) | high | mitigate | D-19 aplicado literalmente: só dois caminhos aceitos; o Passo 5 checa `information_schema` depois do `rollback` e manda parar se qualquer adição sobreviveu |
| T-13-08 | Tampering | cards/eventos de teste da Prova 2.7 apagados fora da transação | medium | mitigate | Toda gravação de teste vive dentro de `begin;...rollback;`; o Passo 5 confirma `cards_total` idêntico ao baseline |
| T-13-09 | Denial of Service | backstop ampliado largo demais em produção | high | mitigate | O ensaio exige observar o quarto lado da Prova 2.7 (card sem nenhum dos três continua excluível) antes de qualquer autorização de push; falha ali interrompe o plano |
| T-13-10 | Repudiation | ensaio não registrado, decisão do 13-03 sem base | medium | mitigate | Task 2 grava números, ids e caminho de execução dentro do próprio runbook, versionado no git |
| T-13-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala nenhum pacote. Se o operador optar por instalar o Supabase CLI para o caminho (b), é ferramenta oficial do fornecedor já usada no projeto |
</threat_model>

<verification>
- Observação direta do resultado de cada prova contra o banco de produção, dentro de transação
  revertida
- Comparação numérica antes/depois do `rollback` (`cards_total`, `parcelas_total`,
  `lancamentos_total`, `updated_at_max`)
- Checagem de `information_schema` depois do `rollback` — a prova de que o ensaio foi ensaio
- `grep` sobre o bloco `RESULTADO DO ENSAIO` confirmando que o registro existe e nomeia o caminho de
  execução
</verification>

<success_criteria>
- O ensaio rodou por um caminho que garante continuidade de sessão, e isso está escrito
- As dez recusas de validação e os quatro lados do backstop ampliado foram observados
- Nada persistiu no banco: as quatro adições ausentes e quatro números idênticos depois do `rollback`
- `supabase/verificacao_dinheiro_imobiliaria.sql` carrega o registro completo, commitado no git
</success_criteria>

<output>
Create `.planning/phases/13-dinheiro-da-imobili-ria/13-02-SUMMARY.md` when done
</output>
