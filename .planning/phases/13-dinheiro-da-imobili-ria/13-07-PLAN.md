---
phase: 13-dinheiro-da-imobili-ria
plan: 07
type: execute
wave: 7
depends_on: ["13-06"]
files_modified:
  - web/src/lib/kanban/actions.ts
  - web/src/lib/kanban/reconciliacao.ts
  - web/src/app/(app)/relatorios/imobiliaria/page.tsx
  - web/src/components/reports/dinheiro-imobiliaria-view.tsx
  - web/src/components/reports/filtro-relatorio-financeiro.tsx
autonomous: false
requirements: [IMOB-05]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Existe uma rota nova `/relatorios/imobiliaria`, alcançada por um botão 'Dinheiro da imobiliária' em `/relatorios`, ao lado do botão 'Relatório financeiro' já existente (RELDED-01), mesmo peso visual — dois pontos de entrada, nenhum mais destacado que o outro (IMOB-05)"
    - "Um filtro de Período (mês) ao vivo — sem botão 'Gerar' — recalcula os seis tiles e a lista imediatamente a cada mudança, mesmo padrão D-01 já usado pelo Relatório Financeiro dedicado (Phase 10)"
    - "Os seis tiles mostram, nesta ordem: Administração, Comissão 1º aluguel, Total recebido no período, Caução recebida, Caução devolvida, Caução usada — onde 'Total recebido no período' soma só as três categorias de entrada de caixa real (administração + comissão + caução recebida), excluindo devolução e uso deliberadamente"
    - "A lista abaixo dos tiles mostra uma linha por evento (taxa ou caução) do período filtrado, ordenada DESCENDENTE por data (mais recente primeiro) — divergência deliberada da ordem ascendente do Relatório Financeiro dedicado, porque este relatório é lido 'acabei de receber o extrato, confiro as entradas mais recentes primeiro'"
    - "Não existe exportação em PDF nesta fase — IMOB-05 pede 'um relatório/área', não um documento exportável, e 13-CONTEXT.md já avisa que o usuário vai pedir ajustes depois de ver o relatório em uso"
    - "UI Consideration `empty` (tiles+lista): zero eventos no período ainda renderiza os seis tiles em R$ 0,00 (legítimo, não escondido) mais 'Nenhuma taxa ou movimento de caução no período selecionado.' para a lista"
    - "UI Consideration `loading` (tiles+lista): Server Component sem esqueleto de cliente; mudar o período nunca dispara um novo round-trip ao servidor nem um flash de carregamento — filtragem em memória no cliente, mesmo padrão D-01"
    - "UI Consideration `error` (carga inicial): 'Não foi possível carregar os dados agora. Tente novamente.'"
    - "UI Consideration `populated` (tiles+lista): seis tiles em `grid gap-4 sm:grid-cols-2 lg:grid-cols-3` mais a lista, sem paginação"
    - "UI Consideration `overflow` (lista): sem paginação inventada — a tabela rola, o cabeçalho não repete (superfície de tela, não impressão)"
    - "UI Consideration `zero-one-many` (tiles+lista): nenhuma linha de contagem 'N de M' é mostrada — os seis tiles já comunicam volume pelos próprios números, e a contagem de linhas da lista é visualmente autoevidente"
    - "UI Consideration `long-text` (coluna Contrato, backstop): sem truncamento, mesma convenção da coluna Imóvel de Configuração financeira (E1)"
  artifacts:
    - "web/src/lib/kanban/reconciliacao.ts"
    - "web/src/app/(app)/relatorios/imobiliaria/page.tsx"
    - "web/src/components/reports/dinheiro-imobiliaria-view.tsx"
    - "web/src/lib/kanban/actions.ts — `buscarReconciliacaoAction`"
  key_links:
    - "`taxas_imobiliaria`/`caucao_eventos` (planos 13-01/13-04/13-06) -> `buscarReconciliacaoAction` -> `reconciliacao.ts` (agregação pura) -> `DinheiroImobiliariaView` (seis tiles + lista)"
    - "botão 'Dinheiro da imobiliária' em `/relatorios` -> rota `/relatorios/imobiliaria`, mesmo padrão de entrada não-destacada já usado por RELDED-01 (Phase 10)"
---

<objective>
Entregar o relatório de reconciliação: uma rota própria que soma taxas de administração, comissão do
primeiro aluguel e movimento de caução por período, com filtro ao vivo e lista detalhada — o
entregável central desta fase, para o usuário conferir contra o extrato bancário.

Purpose: sem este relatório, o dinheiro que a imobiliária recebe fica espalhado em duas tabelas sem
nenhuma visão consolidada — exatamente a lacuna que motivou a fase inteira ("o mais importante é
controlar o que estamos recebendo... vou prestar contas, conferir se bate com o saldo/extrato do
nosso banco").
Output: rota `/relatorios/imobiliaria` funcionando contra os dados reais gravados pelos planos 13-04 e
13-06, com o botão de entrada em `/relatorios`. É a última peça da fase — depois deste plano, IMOB-01
a IMOB-05 estão todos entregues.
</objective>

## Artifacts this phase produces

Este plano não repete a lista completa de artefatos da fase — ver `## Artifacts this phase produces`
em `13-01-PLAN.md`. Os símbolos que este plano especificamente cria: `web/src/lib/kanban/reconciliacao.ts`
(novo — `TaxaImobiliariaRelatorio`, `CaucaoEventoRelatorio`, `passaFiltroPeriodoReconciliacao`,
`calcularReconciliacao`), `buscarReconciliacaoAction` (`actions.ts`),
`web/src/app/(app)/relatorios/imobiliaria/page.tsx`,
`web/src/components/reports/dinheiro-imobiliaria-view.tsx`, e o segundo botão de entrada em
`filtro-relatorio-financeiro.tsx`. Este é o último plano da fase — depois dele, todos os símbolos
listados em `13-01-PLAN.md` existem.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-CONTEXT.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-PATTERNS.md
@.planning/phases/13-dinheiro-da-imobili-ria/13-UI-SPEC.md
@web/src/app/(app)/relatorios/financeiro/page.tsx
@web/src/components/reports/relatorio-financeiro-dedicado.tsx
@web/src/components/reports/filtro-relatorio-financeiro-live.tsx
@web/src/lib/kanban/relatorio-financeiro.ts
@web/src/components/reports/stat-tile.tsx
@web/src/lib/kanban/actions.ts
</context>

<planner_assumptions>
- **A-01 — `reconciliacao.ts` é um arquivo novo, não uma extensão de `taxas.ts`.** Mesmo raciocínio
  de `relatorio-financeiro.ts` existir separado de `parcelas.ts`: um arquivo por relatório, contendo
  só os tipos/filtros/agregação daquele relatório. `taxas.ts` (13-04/13-06) guarda o cálculo de
  origem/percentual/saldo reusado por MÚLTIPLAS superfícies (diálogo de pagamento, tabela de
  Configuração financeira, Sheet de caução); `reconciliacao.ts` guarda só o que este relatório
  precisa (tipos de linha, filtro por período, os seis totais).
- **A-02 — a "linha unificada" da lista (taxa + caução no mesmo array) é montada no componente de
  cliente, não na Server Action.** `buscarReconciliacaoAction` devolve `taxas` e `caucaoEventos`
  como dois arrays separados (espelhando as duas tabelas reais) — mesclar em uma única lista
  ordenável por data é responsabilidade de apresentação, calculada com `React.useMemo` dentro de
  `DinheiroImobiliariaView`, mesmo padrão de `linhasFiltradas` em `relatorio-financeiro-dedicado.tsx`.
- **A-03 — "Tipo" da lista usa três rótulos de taxa (Administração/Comissão 1º aluguel, ícones
  `Percent`/`Sparkles`) mais os três de `CaucaoEventoLabel` já existente (13-06), num mapa local só
  desta view — não um componente novo compartilhado, porque nenhuma outra tela precisa rotular taxa
  por origem.**
</planner_assumptions>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: `reconciliacao.ts` + `buscarReconciliacaoAction` + rota + view + botão de entrada</name>

  <files>
web/src/lib/kanban/reconciliacao.ts
web/src/lib/kanban/actions.ts
web/src/app/(app)/relatorios/imobiliaria/page.tsx
web/src/components/reports/dinheiro-imobiliaria-view.tsx
web/src/components/reports/filtro-relatorio-financeiro.tsx
  </files>

  <read_first>
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-UI-SPEC.md` — §4 completo, Copywriting Contract
      (todas as linhas de Reconciliação), Color § Accent reserved for (os dois botões de entrada não
      são accent)
    - `web/src/lib/kanban/relatorio-financeiro.ts` — arquivo inteiro, o molde de tipos +
      `passaFiltroPeriodo` + agregação pura que `reconciliacao.ts` replica
    - `web/src/app/(app)/relatorios/financeiro/page.tsx` — arquivo inteiro (43 linhas), copiado quase
      verbatim para `relatorios/imobiliaria/page.tsx` (back-link, h1, subtítulo, ternário `resultado.ok
      ? ... : <p>...</p>`)
    - `web/src/components/reports/relatorio-financeiro-dedicado.tsx` + `filtro-relatorio-financeiro-live.tsx`
      — arquivos inteiros, o padrão de filtro ao vivo sem `Collapsible`/submit (linhas 52-67 e 20-45) e
      o grid de tiles (linhas 174-192)
    - `web/src/components/reports/stat-tile.tsx` — props exatas `{ label, value, hint?, icon, tone? }`
    - `web/src/components/reports/filtro-relatorio-financeiro.tsx` — linhas 80-88, o botão de entrada
      de RELDED-01 a replicar para o segundo botão desta task
    - `web/src/components/financeiro/caucao-evento-label.tsx` (do plano 13-06) — `CAUCAO_TIPO`/
      `CaucaoEventoLabel`, reusado na lista desta task
    - `web/src/components/financeiro/id-pill.tsx` — `IdPill`, variante default
  </read_first>

  <action>
**1. `web/src/lib/kanban/reconciliacao.ts`** (novo, espelha `relatorio-financeiro.ts`). Mesma
restrição de cabeçalho: sem `@/lib/supabase/server`, sem `next/headers` — consumido por
`dinheiro-imobiliaria-view.tsx` ("use client").

Exportar:
- `export type TaxaImobiliariaRelatorio = { id: string; data: string; valor: number; origem:
  OrigemTaxa; observacao: string | null; cards: { endereco: string; proprietario: string; numero:
  number } | null }` (importar `OrigemTaxa` de `./taxas`).
- `export type CaucaoEventoRelatorio = { id: string; data: string; valor: number; tipo: TipoCaucao;
  observacao: string | null; cards: { endereco: string; proprietario: string; numero: number } | null
  }` (importar `TipoCaucao` de `./taxas`).
- `passaFiltroPeriodoReconciliacao(data: string, periodo: string): boolean` — mesmo corpo de
  `passaFiltroPeriodo` (`relatorio-financeiro.ts`), mas comparando `data` (não `competencia`) contra
  `"YYYY-MM"`; período fora do formato é ignorado silenciosamente (`return true`).
- `type ReconciliacaoTotais = { administracao: number; comissao: number; caucaoRecebida: number;
  caucaoDevolvida: number; caucaoUsada: number; totalRecebido: number }`.
- `calcularReconciliacao(taxas: TaxaImobiliariaRelatorio[], caucaoEventos: CaucaoEventoRelatorio[],
  periodo: string): ReconciliacaoTotais` — soma `valor` de `taxas` filtradas por período e `origem ===
  "administracao"` → `administracao`; `origem === "comissao_primeiro_aluguel"` → `comissao`; soma de
  `caucaoEventos` filtrados por período e `tipo === "recebido"/"devolvido"/"usado"` →
  `caucaoRecebida`/`caucaoDevolvida`/`caucaoUsada`. `totalRecebido = administracao + comissao +
  caucaoRecebida` — **deliberadamente sem** `caucaoDevolvida`/`caucaoUsada` (UI-SPEC §4: devolvida é
  saída de caixa, usada não é movimento bancário nenhum). Comentar isso explicitamente no código, não
  só na UI.

**2. `web/src/lib/kanban/actions.ts`** — nova Server Action, seção `// Relatório de reconciliação
(Phase 13)`:
```
export async function buscarReconciliacaoAction(): Promise<
  ActionResult<{
    taxas: TaxaImobiliariaRelatorio[]
    caucaoEventos: CaucaoEventoRelatorio[]
    hojeISO: string
  }>
> {
  const sessao = await requireUser()
  if (!sessao) return { ok: false, error: NAO_AUTENTICADO }

  const { data: taxas, error: erroTaxas } = await sessao.supabase
    .from("taxas_imobiliaria")
    .select("id, data, valor, origem, observacao, cards(endereco, proprietario, numero)")

  if (erroTaxas) return { ok: false, error: erroDoBanco(erroTaxas.code, "carregar o relatório") }

  const { data: caucaoEventos, error: erroCaucao } = await sessao.supabase
    .from("caucao_eventos")
    .select("id, data, valor, tipo, observacao, cards(endereco, proprietario, numero)")

  if (erroCaucao) return { ok: false, error: erroDoBanco(erroCaucao.code, "carregar o relatório") }

  return {
    ok: true,
    data: {
      taxas: (taxas ?? []) as unknown as TaxaImobiliariaRelatorio[],
      caucaoEventos: (caucaoEventos ?? []) as unknown as CaucaoEventoRelatorio[],
      hojeISO: hojeEmCuiaba(),
    },
  }
}
```
Sem filtro de período no servidor (mesmo padrão de `buscarParcelasRelatorioAction` — o filtro roda em
memória no cliente, D-01). Sem filtro de `arquivado_em`/`ativo` — o relatório de reconciliação, como
o Relatório Financeiro (D-05, Phase 8), inclui contrato arquivado/inativo: dinheiro que a imobiliária
já recebeu não deixa de ter sido recebido só porque o contrato mudou de estado depois.

**3. `web/src/app/(app)/relatorios/imobiliaria/page.tsx`** (novo) — cópia quase verbatim de
`relatorios/financeiro/page.tsx`: `buscarReconciliacaoAction()` no lugar de
`buscarParcelasRelatorioAction()`; back-link "Relatórios" (mesmo destino `/relatorios`); `h1`
"Dinheiro da imobiliária"; subtítulo "Taxas de administração, comissão do primeiro aluguel e
movimento de caução — confira contra o extrato do banco."; `resultado.ok ? <DinheiroImobiliariaView
taxas={...} caucaoEventos={...} hojeISO={...} /> : <p ...>Não foi possível carregar os dados agora.
Tente novamente.</p>`.

**4. `web/src/components/reports/dinheiro-imobiliaria-view.tsx`** (novo, client component). Estado
`periodo: string`, default = mês de `hojeEmCuiaba()` (`hojeISO.slice(0, 7)`). Sem `Collapsible`, sem
"Gerar" — o `Input type="month"` muda `periodo` direto no `onChange` (A-02, mesmo padrão de D-01).

Header: `<div className="flex flex-wrap items-center justify-between gap-4">` — nada à esquerda (os
seis tiles já resumem), à direita o `Input` de Período com `Label` "Período", sempre visível, sem
toggle de filtro (UI-SPEC §4: "um único campo não justifica o padrão Filtrar/Fechar filtros").

Grid de tiles: `grid gap-4 sm:grid-cols-2 lg:grid-cols-3`, `totais = React.useMemo(() =>
calcularReconciliacao(taxas, caucaoEventos, periodo), [taxas, caucaoEventos, periodo])`. Seis
`StatTile`, nesta ordem: "Administração" (ícone `Percent`, `value={formatCurrency(totais.administracao)}`),
"Comissão 1º aluguel" (ícone `Sparkles`, `value={formatCurrency(totais.comissao)}`), "Total recebido
no período" (ícone `Wallet`, `value={formatCurrency(totais.totalRecebido)}`), "Caução recebida" (ícone
`ArrowDownToLine`, `value={formatCurrency(totais.caucaoRecebida)}`), "Caução devolvida" (ícone
`ArrowUpFromLine`, `value={formatCurrency(totais.caucaoDevolvida)}`), "Caução usada" (ícone
`ShieldCheck`, `value={formatCurrency(totais.caucaoUsada)}`, `hint="Não é uma nova entrada no banco —
caução já recebida antes, reclassificada."`). Nenhum `tone="alert"` — este relatório não tem estado
de urgência.

Lista: `React.useMemo` mesclando `taxas`/`caucaoEventos` filtrados por período (A-02) num array
unificado `{ id, data, valor, tipoLabel: ReactNode, cards }`, ordenado DESCENDENTE por `data` (inverter
o comparador de `linhasFiltradas` em `relatorio-financeiro-dedicado.tsx`, que é ascendente). Mapa
local de rótulo por origem de taxa: `{ administracao: { icon: Percent, label: "Administração" },
comissao_primeiro_aluguel: { icon: Sparkles, label: "Comissão 1º aluguel" } }` (A-03); para eventos de
caução, reusar `CaucaoEventoLabel`/`CAUCAO_TIPO` de `@/components/financeiro/caucao-evento-label`.
Tabela `rounded-2xl border border-border bg-card p-6`, colunas Data (`formatDate`) / Contrato
(`<IdPill numero={...} /> + endereco`, sem truncamento) / Tipo (badge conforme o mapa acima) / Valor
(`formatCurrency`, `tabular-nums`) / Observação. Estado vazio: "Nenhuma taxa ou movimento de caução no
período selecionado."

Sem exportação em PDF, sem `Collapsible`, sem `exportando`/`erroExportacao` — este relatório é mais
simples que `RelatorioFinanceiroDedicado` de propósito (UI-SPEC §4, ponto 5).

**5. `web/src/components/reports/filtro-relatorio-financeiro.tsx`** — ao lado do botão "Relatório
financeiro" já existente (linhas 81-88), na mesma `div className="flex items-center gap-2"`, inserir
um segundo `Button variant="outline" size="sm"` com `render={<Link href="/relatorios/imobiliaria" />}`,
ícone `Landmark` de `lucide-react`, texto "Dinheiro da imobiliária" — mesmo peso visual do primeiro
botão, nenhum dos dois mais destacado.
  </action>

  <verify>
    <automated>cd web && npm run lint && npm run build</automated>
  </verify>

  <acceptance_criteria>
    - `cd web && npm run lint`/`npm run build` saem com código 0 e a saída do build lista a rota
      `/relatorios/imobiliaria`
    - `test -f web/src/lib/kanban/reconciliacao.ts` existe; `grep -c 'export function
      calcularReconciliacao' web/src/lib/kanban/reconciliacao.ts` devolve 1
    - `grep -c 'caucaoDevolvida\|caucaoUsada' web/src/lib/kanban/reconciliacao.ts` mostra as duas
      variáveis existindo, mas dentro da linha que monta `totalRecebido` elas NÃO aparecem
      (`grep -A2 'totalRecebido:' web/src/lib/kanban/reconciliacao.ts | grep -c
      'caucaoDevolvida\|caucaoUsada'` devolve 0 — confirma a exclusão deliberada)
    - `grep -c 'export async function buscarReconciliacaoAction' web/src/lib/kanban/actions.ts`
      devolve 1
    - `test -f "web/src/app/(app)/relatorios/imobiliaria/page.tsx"` existe
    - `test -f web/src/components/reports/dinheiro-imobiliaria-view.tsx` existe
    - `grep -c 'exportarRelatorioFinanceiroPDF\|FileDown\|Collapsible'
      web/src/components/reports/dinheiro-imobiliaria-view.tsx` devolve 0 — sem exportação nem painel
      colapsável
    - `grep -c 'sort' web/src/components/reports/dinheiro-imobiliaria-view.tsx` devolve 1 ou mais, e o
      comparador usado ordena DESCENDENTE (`grep -A1 '\.sort(' web/src/components/reports/dinheiro-imobiliaria-view.tsx`
      mostra `b.data` antes de `a.data`, ou `>`/`<` invertidos em relação a
      `relatorio-financeiro-dedicado.tsx`)
    - `grep -c 'Nenhuma taxa ou movimento de caução no período selecionado.'
      web/src/components/reports/dinheiro-imobiliaria-view.tsx` devolve 1
    - `grep -c '"/relatorios/imobiliaria"' web/src/components/reports/filtro-relatorio-financeiro.tsx`
      devolve 1
    - `grep -c 'Landmark' web/src/components/reports/filtro-relatorio-financeiro.tsx` devolve 1 ou mais
  </acceptance_criteria>

  <reversibility rating="reversible">Rota e componentes de leitura pura — nenhuma escrita nova, só
  agregação de dado já gravado pelos planos 13-04/13-06.</reversibility>

  <done>`/relatorios/imobiliaria` mostra os seis tiles e a lista descendente, filtrando ao vivo por
  período, com o botão de entrada em `/relatorios` ao lado do já existente.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirmar em produção que o relatório de reconciliação bate com o banco</name>

  <files>(nenhum arquivo é alterado — verificação operacional)</files>

  <read_first>
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-CONTEXT.md` — a frase-guia sobre prestação de
      contas
  </read_first>

  <what-built>
    A rota `/relatorios/imobiliaria`, alcançada por um botão em `/relatorios`, com seis tiles e uma
    lista de eventos filtrados ao vivo por mês, somando `taxas_imobiliaria` e `caucao_eventos`.
  </what-built>

  <how-to-verify>
    **Passo 1 — navegar até a tela.** Em `/relatorios`, confirme os dois botões lado a lado
    ("Relatório financeiro" e "Dinheiro da imobiliária"), mesmo peso visual. Clique no segundo.

    **Passo 2 — período com dado real.** Selecione o mês em que você registrou os eventos de teste dos
    planos 13-04/13-06 (pagamento com taxa, ciclo de caução). Confirme que os tiles refletem esses
    valores.

    **Passo 3 — conferir contra o banco.**
    ```sql
    select origem, sum(valor) from public.taxas_imobiliaria
    where data >= '<primeiro dia do mês>' and data < '<primeiro dia do mês seguinte>'
    group by origem;

    select tipo, sum(valor) from public.caucao_eventos
    where data >= '<primeiro dia do mês>' and data < '<primeiro dia do mês seguinte>'
    group by tipo;
    ```
    Os totais devem bater exatamente com os tiles "Administração"/"Comissão 1º aluguel"/"Caução
    recebida"/"Caução devolvida"/"Caução usada". Confirme manualmente que "Total recebido no período"
    = administração + comissão + caução recebida (sem somar devolvida/usada).

    **Passo 4 — filtro ao vivo.** Mude o período para um mês sem nenhum evento — os seis tiles devem
    zerar (R$ 0,00, não sumir) e a lista mostrar "Nenhuma taxa ou movimento de caução no período
    selecionado.", sem nenhum flash de carregamento nem chamada de rede visível (Network tab).

    **Passo 5 — lista.** Confirme que a lista mostra a taxa e os eventos de caução do período testado,
    ordenados do mais recente para o mais antigo, com o rótulo/ícone correto para cada tipo.
  </how-to-verify>

  <acceptance_criteria>
    - Os dois botões de entrada em `/relatorios` têm o mesmo peso visual
    - Os cinco totais individuais batem exatamente com as somas SQL do período testado
    - "Total recebido no período" exclui devolvida/usada, confirmado pela conta manual
    - Trocar o período não gera chamada de rede nem flash de carregamento
    - Um período sem dado mostra os seis tiles em R$ 0,00 e a mensagem de lista vazia
    - A lista está em ordem descendente por data, com o tipo certo em cada linha
    - O operador registrou os totais observados no SUMMARY
  </acceptance_criteria>

  <resume-signal>Responda "aprovado" com os totais observados, ou descreva o que não bateu.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador → `buscarReconciliacaoAction` | leitura agregada de `taxas_imobiliaria`/`caucao_eventos`; RLS (`is_team_member()`, plano 13-01) continua filtrando as linhas — um usuário fora da allowlist recebe relatório vazio, não dado de terceiro |
| filtro de período → dados já carregados | o filtro roda inteiramente em memória no cliente; nenhum dado extra é buscado ao trocar o período (D-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-30 | Information Disclosure | `buscarReconciliacaoAction` legível fora da allowlist | high | mitigate | Mesma sessão via `requireUser()`, nunca `service_role`; RLS das duas tabelas já provado no plano 13-01/13-03 |
| T-13-31 | Tampering | "Total recebido no período" somando devolução/uso por engano | medium | mitigate | Asserção de fonte no `<verify>`: a linha que monta `totalRecebido` não referencia `caucaoDevolvida`/`caucaoUsada`; comentário explícito no código; prova manual em produção na Task 2 (Passo 3) |
| T-13-32 | Information Disclosure | erro do Postgres vazando ao carregar o relatório | medium | mitigate | `erroDoBanco(erroTaxas.code/erroCaucao.code, "carregar o relatório")`, mesmo padrão do resto do arquivo; a página renderiza uma constante em caso de falha |
| T-13-33 | Denial of Service | recálculo do relatório a cada troca de período | low | accept | Filtro em memória sobre um volume baixo (taxas/caução por natureza são eventos pouco frequentes, muito menor que o volume de parcelas) — nenhuma consulta nova é disparada |
| T-13-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | `Percent`/`Sparkles`/`ArrowDownToLine`/`ArrowUpFromLine`/`Landmark`/`Wallet` já existem em `lucide-react`, já instalado — nenhum pacote novo |
</threat_model>

<verification>
1. `cd web && npm run lint && npm run build` saem com código 0 e listam a rota nova
2. Asserção de fonte confirmando que `totalRecebido` exclui `caucaoDevolvida`/`caucaoUsada`
3. Produção: os seis tiles e a lista batem com as somas SQL do período testado
4. Filtro de período roda ao vivo, sem chamada de rede nova
</verification>

<success_criteria>
- Existe um relatório de reconciliação mostrando o total recebido pela imobiliária num período, para
  bater com o extrato bancário (IMOB-05)
- Os totais batem exatamente com os dados reais gravados por `taxas_imobiliaria`/`caucao_eventos`
- "Total recebido no período" soma só as três categorias de entrada de caixa real
- O filtro é ao vivo, sem exportação em PDF nesta fase
</success_criteria>

<output>
Create `.planning/phases/13-dinheiro-da-imobili-ria/13-07-SUMMARY.md` when done
</output>
