---
phase: 20-filtro-por-tipo-de-movimento-no-relat-rio-da-imobili-ria
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/kanban/reconciliacao.ts
  - web/src/components/reports/filtro-reconciliacao.tsx
  - web/src/components/reports/dinheiro-imobiliaria-view.tsx
autonomous: true
requirements: [TIPOIMOB-01, TIPOIMOB-02, TIPOIMOB-03, COLIMOB-01, COLIMOB-02, COLIMOB-03]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Abrir o painel 'Filtrar' em /relatorios/imobiliaria mostra, depois dos 5 campos já existentes, uma linha de chips (Todos/Administração/Comissão 1º aluguel/Caução); clicar num chip filtra a lista e os 6 StatTile juntos, sem nenhum chip marcado por padrão (D-01, TIPOIMOB-01)"
    - "'Caução' é um único chip cobrindo os três subtipos já existentes (recebida/devolvida/usada) — nunca um chip por subtipo (D-02, TIPOIMOB-02)"
    - "Desmarcar um tipo zera o StatTile correspondente (nunca esconde o tile do grid) e para de somá-lo em 'Total recebido' — calcularReconciliacao passa a exigir o filtro de tipo como argumento obrigatório, sem valor padrão (D-03/D-04, TIPOIMOB-03)"
    - "Ambas as cadeias de filtro do useMemo de linhas (taxa e caução) recebem a nova cláusula de tipo; a cadeia de caução usa a constante 'caucao', nunca evento.tipo (D-02, Pitfall 3 de 20-RESEARCH.md)"
    - "reconciliacao-pdf.ts permanece byte a byte inalterado — o PDF exportado passa a receber linhas já filtradas pelo tipo automaticamente, sem nenhuma mudança de layout (D-08/D-09)"
    - "A célula 'Contrato' mantém o formato IdPill+texto — só troca o texto de endereço para proprietário; uma nova coluna 'Inquilino' aparece logo depois; endereço não aparece em nenhum lugar da linha renderizada (D-05/D-06/D-07, COLIMOB-01/02/03)"
    - "Nenhum campo de endereço é removido de nenhum tipo (LinhaLista, TaxaImobiliariaRelatorio.cards, CaucaoEventoRelatorio.cards) — só o JSX desta tabela deixa de lê-lo; zero migração de banco"
  artifacts:
    - web/src/lib/kanban/reconciliacao.ts
    - web/src/components/reports/filtro-reconciliacao.tsx
    - web/src/components/reports/dinheiro-imobiliaria-view.tsx
  key_links:
    - "FilterChip/toggle (reports-view.tsx) -> chip row (filtro-reconciliacao.tsx) -> filtro.tipos (Set<TipoMovimentoReconciliacao>, estado de dinheiro-imobiliaria-view.tsx) -> passaFiltroTipoReconciliacao -> linhas useMemo (2 cadeias de .filter()) + calcularReconciliacao (4º parâmetro) -> lista renderizada + 6 StatTile + exportarReconciliacaoPDF(linhas, totais, ...)"
    - "resetKey pipe-joined + [...filtro.tipos].sort().join(',') -> usePagination -> itensDaPagina"
    - "linha.cards.proprietario/inquilino (já buscados pela Server Action desde a Phase 19, sem mudança nesta fase) -> célula Contrato/Inquilino"
  prohibitions:
    - "MUST NOT modificar web/src/components/reports/reconciliacao-pdf.ts — layout do PDF fora de escopo (D-08/D-09); o arquivo já se beneficia automaticamente de linhas pré-filtradas, sem nenhuma mudança de código"
    - "MUST NOT remover o campo do endereço de LinhaLista, TaxaImobiliariaRelatorio.cards ou CaucaoEventoRelatorio.cards — reconciliacao-pdf.ts ainda depende dele para a coluna Contrato do PDF"
    - "MUST NOT dar valor padrão ao quarto parâmetro novo de calcularReconciliacao — o tripwire de compilação (o call site desatualizado falhando) é intencional"
    - "MUST NOT filtrar a cadeia de caução por evento.tipo — usar sempre a constante 'caucao' (D-02)"
    - "MUST NOT juntar [...filtro.tipos] sem .sort() no resetKey — ordem de clique não pode mudar a chave de reset de paginação"
    - "MUST NOT criar nenhum arquivo em supabase/migrations/ — fase zero-schema"
---

<objective>
Fecha TIPOIMOB-01..03 e COLIMOB-01..03 (20-CONTEXT.md D-01..D-09): o painel suspenso já shipado na Phase 19
em `/relatorios/imobiliaria` ganha um sexto controle — uma linha de chips multi-select de tipo de movimento
(Todos/Administração/Comissão 1º aluguel/Caução), mesmo componente `FilterChip`/`toggle` já usado pela
"Situação" no Relatório Financeiro dedicado — e a lista abaixo troca a coluna de endereço por proprietário na
célula "Contrato" (mesmo formato visual) e ganha uma coluna "Inquilino" separada.

O filtro de tipo afeta tudo ao mesmo tempo: a lista, os 6 `StatTile` (zerando, nunca escondendo, o tile do
tipo desmarcado) e o PDF exportado (que passa a receber `linhas` já filtradas, sem nenhuma mudança de código
em `reconciliacao-pdf.ts` — D-08/D-09, redesenho do PDF explicitamente fora de escopo e adiado para uma fase
futura). "Caução" é um único chip cobrindo os três subtipos já existentes (recebida/devolvida/usada) — nunca
um chip por subtipo (D-02). O novo tipo `TipoMovimentoReconciliacao` reusa `OrigemTaxa` como superset
(`OrigemTaxa | "caucao"`), sem nenhuma função de mapeamento (Pattern 2, 20-RESEARCH.md).

Purpose: o usuário pediu o seletor de tipo imediatamente depois de confirmar a Phase 19 em produção, e pediu
a troca de colunas durante a mesma discussão.
Output: `reconciliacao.ts` ganha `TipoMovimentoReconciliacao`/`passaFiltroTipoReconciliacao`/`tipos` (em
`FiltroReconciliacaoValores`)/um 4º parâmetro obrigatório em `calcularReconciliacao`; `filtro-reconciliacao.tsx`
ganha a linha de chips; `dinheiro-imobiliaria-view.tsx` ganha as duas cadeias de filtro de tipo, o `resetKey`
composto, e a troca/adição de colunas na tabela. `reconciliacao-pdf.ts` não é tocado.
</objective>

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-filtro-por-tipo-de-movimento-no-relat-rio-da-imobili-ria/20-CONTEXT.md
@.planning/phases/20-filtro-por-tipo-de-movimento-no-relat-rio-da-imobili-ria/20-RESEARCH.md
@.planning/phases/20-filtro-por-tipo-de-movimento-no-relat-rio-da-imobili-ria/20-PATTERNS.md
@web/src/lib/kanban/reconciliacao.ts
@web/src/lib/kanban/taxas.ts
@web/src/components/reports/filtro-reconciliacao.tsx
@web/src/components/reports/dinheiro-imobiliaria-view.tsx
@web/src/components/reports/reports-view.tsx
@web/src/components/reports/filtro-relatorio-financeiro-live.tsx
@web/src/components/reports/relatorio-financeiro-dedicado.tsx
</context>

## Artifacts this phase produces

- **`TipoMovimentoReconciliacao`**, **`passaFiltroTipoReconciliacao`**, `tipos` field on
  `FiltroReconciliacaoValores`/`filtroReconciliacaoVazio`, and a required 4th parameter on
  `calcularReconciliacao` (all new/widened, `web/src/lib/kanban/reconciliacao.ts`).
- **Chip row** (`Todos`/`Administração`/`Comissão 1º aluguel`/`Caução`) inside `FiltroReconciliacao`
  (`web/src/components/reports/filtro-reconciliacao.tsx`), mirroring the "Situação" row already shipped in
  `filtro-relatorio-financeiro-live.tsx`.
- **`DinheiroImobiliariaView`** (modified) — `linhas` useMemo gains a type-filter clause on both chains,
  `calcularReconciliacao` call site gains the 4th argument, `resetKey` gains a sorted-Set segment, the
  "Contrato" cell swaps endereço→proprietário, a new "Inquilino" column is added.
- **`reconciliacao-pdf.ts`** — untouched, verified by this plan's `<verify>`.

<planner_assumptions>
- **A-01 — prefixos de requisito `TIPOIMOB`/`COLIMOB`**: esta fase tem duas capacidades distintas (filtro de
  tipo + troca de colunas), mesmo formato de `FILTIMOB`+`PDFIMOB` na Phase 19 e `CANDEST`+`PAGIN` na Phase 15
  — dois prefixos curtos ligados a cada capacidade.
- **A-02 — um único task, `type="tracer"`, cobre a fase inteira** (tracer-first, permitido explicitamente para
  este escopo pelo orquestrador — 3 arquivos já existentes, um único caminho vertical: chip clicado → filtro →
  lista+totais+PDF, sem corte natural de expansão horizontal separado do próprio slice, mesmo formato de
  18-01-PLAN.md).
- **A-03 — nenhuma atualização em `docs/data-model.md`**: esta fase não introduz nenhuma decisão de schema
  nem de arquitetura nova — é extensão client-side de um filtro já documentado como padrão desde a Phase 10,
  sobre um tipo (`OrigemTaxa`) já existente e documentado.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Filtro por tipo de movimento ponta a ponta — chip row, filtro de tipo (lista+totais) e troca de colunas</name>

  <files>web/src/lib/kanban/reconciliacao.ts, web/src/components/reports/filtro-reconciliacao.tsx, web/src/components/reports/dinheiro-imobiliaria-view.tsx</files>

  <read_first>
    - `web/src/lib/kanban/reconciliacao.ts` (arquivo inteiro, 180 linhas) — import de `OrigemTaxa`/`TipoCaucao`
      já existente no topo (linha 1), `FiltroReconciliacaoValores`/`filtroReconciliacaoVazio` (linhas 58-69) a
      ampliar, `calcularReconciliacao` (linhas 141-180) — assinatura (141-145) e os dois laços de acumulação
      (taxas ~152-156, caucaoEventos ~158-163) a instrumentar
    - `web/src/lib/kanban/taxas.ts` linha 13 (`OrigemTaxa = "administracao" | "comissao_primeiro_aluguel"`) —
      confirma que nenhuma função de mapeamento é necessária do lado da taxa
    - `web/src/components/reports/filtro-reconciliacao.tsx` (arquivo inteiro, 114 linhas) — `temFiltroPreenchido`
      (linhas 35-41) a ampliar, grid de 5 campos (linhas 44-100) — a nova linha de chips entra logo depois do
      `</div>` de fechamento (linha 100) e antes do bloco `{temFiltroPreenchido && (...)}` (linha 102)
    - `web/src/components/reports/reports-view.tsx` linhas 31-64 — `toggle<T>` e `FilterChip`, componente e
      função a importar sem alteração
    - `web/src/components/reports/filtro-relatorio-financeiro-live.tsx` linhas 83-109 — a linha de chips
      "Situação" já em produção (chip "Todas" + `.map()` de opções), precedente 1:1 a espelhar para "Tipo"
    - `web/src/components/reports/relatorio-financeiro-dedicado.tsx` linhas 69-80 — precedente de `resetKey`
      compondo um `Set` (`[...set].sort().join(",")`), a espelhar no passo 4
    - `web/src/components/reports/dinheiro-imobiliaria-view.tsx` (arquivo inteiro, 308 linhas) — import de
      `@/lib/kanban/reconciliacao` (linhas 18-26) a ampliar, memo de `totais` (linhas 93-96), memo de `linhas`
      (linhas 102-137, as duas cadeias `taxaLinhas`/`caucaoLinhas`), `resetKey` (linha 143),
      `<TableHeader>`/`<TableRow>` (linhas 264-271) e a célula "Contrato" (linhas 279-286) — todos a modificar
      neste passo
    - `.planning/phases/20-filtro-por-tipo-de-movimento-no-relat-rio-da-imobili-ria/20-RESEARCH.md` seção
      "Code Examples" e "Common Pitfalls" (Pitfall 1-4) — sketches prontos e as quatro armadilhas específicas
      desta fase
  </read_first>

  <action>
1. Em `web/src/lib/kanban/reconciliacao.ts`: logo antes de `FiltroReconciliacaoValores` (linha 58), declarar
`export type TipoMovimentoReconciliacao = OrigemTaxa | "caucao"` — reusa `OrigemTaxa` (já importado no topo do
arquivo) como superset dos dois valores de taxa, sem nenhuma função de mapeamento (Pattern 2, RESEARCH.md;
D-02 do lado da caução: os três subtipos de `TipoCaucao` nunca aparecem aqui, colapsam para a constante
`"caucao"`). Ampliar `FiltroReconciliacaoValores` com um sexto campo, `tipos: Set<TipoMovimentoReconciliacao>`,
e `filtroReconciliacaoVazio` com `tipos: new Set()`. Atualizar o comentário de linha 58 (hoje cita só
FILTIMOB-01..03/D-03 da Phase 19) para citar também D-01 desta fase. Logo depois de `filtroReconciliacaoVazio`,
acrescentar `export function passaFiltroTipoReconciliacao(tipo: TipoMovimentoReconciliacao, tipos:
Set<TipoMovimentoReconciliacao>): boolean` — `tipos.size === 0` sempre retorna `true` (D-01, mesma semântica
de Set-vazio-é-"sem filtro" já usada por `situacoes`), senão `tipos.has(tipo)`.

2. Ainda em `reconciliacao.ts`, ampliar `calcularReconciliacao` (linhas 141-145) com um quarto parâmetro
**obrigatório**, `tipos: Set<TipoMovimentoReconciliacao>` — sem valor padrão algum, de propósito: é o
tripwire de compilação do Pitfall 2 de RESEARCH.md — o único call site hoje, com 3 argumentos, em
`dinheiro-imobiliaria-view.tsx`, precisa deixar de compilar até ser atualizado no passo 4. Dentro do laço de
`taxas`, logo depois do guard de período já existente, acrescentar `if (!passaFiltroTipoReconciliacao(taxa.origem,
tipos)) continue`. Dentro do laço de `caucaoEventos`, no mesmo lugar, acrescentar `if
(!passaFiltroTipoReconciliacao("caucao", tipos)) continue` — a constante `"caucao"`, nunca o campo de tipo do
próprio evento (D-02: os três subtipos de caução colapsam para uma única categoria; ler o campo do evento aqui
reintroduziria filtragem por subtipo, um bug silencioso de comportamento que o compilador não pega — Pitfall
3, RESEARCH.md). O resto de cada laço (a ramificação existente por tipo de taxa/evento para decidir qual
acumulador somar) continua exatamente como está — é uma decisão diferente (qual total incrementar), não o
filtro de tipo que decide se a linha entra ou não.

3. Em `web/src/components/reports/filtro-reconciliacao.tsx`: importar `FilterChip, toggle` de
`@/components/reports/reports-view` e ampliar o import já existente de `@/lib/kanban/reconciliacao` com `type
TipoMovimentoReconciliacao`. Declarar `const TIPO_MOVIMENTO_OPTIONS: { value: TipoMovimentoReconciliacao;
label: string }[]` com as três entradas `{ value: "administracao", label: "Administração" }`, `{ value:
"comissao_primeiro_aluguel", label: "Comissão 1º aluguel" }`, `{ value: "caucao", label: "Caução" }`. Logo
depois do `</div>` que fecha o grid de 5 campos e antes do bloco `{temFiltroPreenchido && (...)}`, inserir uma
nova `<div className="mt-3 flex flex-wrap items-center gap-2">` com um rótulo "Tipo" (mesmo estilo
`text-xs font-semibold text-muted-foreground uppercase` já usado nos outros rótulos deste arquivo), um
`FilterChip` "Todos" (`active={campos.tipos.size === 0}`, `onClick` chamando `onChange((atual) => ({ ...atual,
tipos: new Set() }))` — nunca inserindo um literal `"todos"` no Set, D-01), e um `.map()` de
`TIPO_MOVIMENTO_OPTIONS` renderizando um `FilterChip` por opção (`active={campos.tipos.has(option.value)}`,
`onClick` chamando `onChange((atual) => ({ ...atual, tipos: toggle(atual.tipos, option.value) }))`). Ampliar
`temFiltroPreenchido` (linhas 35-41) com `|| campos.tipos.size > 0` — sem essa linha, "Limpar filtros" nunca
apareceria com só um chip de tipo marcado.

4. Em `web/src/components/reports/dinheiro-imobiliaria-view.tsx`: ampliar o import já existente de
`@/lib/kanban/reconciliacao` com `passaFiltroTipoReconciliacao`. No memo de `totais` (linhas 93-96),
acrescentar `filtro.tipos` como quarto argumento de `calcularReconciliacao` e ao array de dependências. No
memo de `linhas` (linhas 102-137), encadear `.filter((taxa) => passaFiltroTipoReconciliacao(taxa.origem,
filtro.tipos))` na cadeia de `taxaLinhas` (logo depois do filtro de cards já existente) e `.filter(() =>
passaFiltroTipoReconciliacao("caucao", filtro.tipos))` na cadeia de `caucaoLinhas` — a mesma constante
`"caucao"`, nunca o campo de tipo do próprio evento, pelo mesmo motivo do passo 2. No `resetKey` (linha 143),
acrescentar um sexto segmento pipe-joined: `${[...filtro.tipos].sort().join(",")}` — sempre ordenado antes do
`join`, nunca a ordem de iteração crua do `Set` (Pitfall 4, RESEARCH.md: ordem de clique diferente produziria
uma chave diferente para a mesma seleção final, resetando a paginação sem o filtro ter mudado de verdade). No
`<TableHeader>` (linhas 264-271), inserir `<TableHead>Inquilino</TableHead>` logo depois de
`<TableHead>Contrato</TableHead>`. Na célula "Contrato" (linhas 279-286), trocar qual campo de `cards` o
`<span>` ao lado do `IdPill` lê — hoje mostra o endereço do imóvel, passa a mostrar o proprietário do
contrato — mantendo exatamente o mesmo formato visual (D-05), e logo em seguida acrescentar uma nova
`<TableCell className="text-sm text-muted-foreground">` mostrando o inquilino do contrato (D-06). Não remover
nenhum campo de nenhum tipo (`LinhaLista`, `TaxaImobiliariaRelatorio.cards`, `CaucaoEventoRelatorio.cards`
continuam com o campo do endereço intocado, D-07) — só a linha de JSX desta tabela que hoje lê esse campo deixa
de existir; o mesmo campo continua alimentando `reconciliacao-pdf.ts` sem nenhuma mudança nesse arquivo
(D-08/D-09 — não abrir nem editar `reconciliacao-pdf.ts` nesta fase).
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && r=src/lib/kanban/reconciliacao.ts && f=src/components/reports/filtro-reconciliacao.tsx && v=src/components/reports/dinheiro-imobiliaria-view.tsx && p=src/components/reports/reconciliacao-pdf.ts && grep -q 'export type TipoMovimentoReconciliacao = OrigemTaxa | "caucao"' "$r" && grep -q "tipos: Set<TipoMovimentoReconciliacao>" "$r" && grep -q "export function passaFiltroTipoReconciliacao" "$r" && grep -q "tipos: new Set()" "$r" && ! grep -q "tipos: Set<TipoMovimentoReconciliacao> = " "$r" && grep -q "passaFiltroTipoReconciliacao(taxa.origem, tipos)" "$r" && grep -q 'passaFiltroTipoReconciliacao("caucao", tipos)' "$r" && grep -q 'import { FilterChip, toggle } from "@/components/reports/reports-view"' "$f" && grep -q "TIPO_MOVIMENTO_OPTIONS" "$f" && grep -q "campos.tipos.size === 0" "$f" && grep -q "campos.tipos.has(option.value)" "$f" && grep -q "campos.tipos.size > 0" "$f" && grep -q "Todos" "$f" && grep -q "Administração" "$f" && grep -q "Comissão 1º aluguel" "$f" && grep -q "Caução" "$f" && grep -q "passaFiltroTipoReconciliacao" "$v" && grep -q "calcularReconciliacao(taxas, caucaoEventos, filtro.periodo, filtro.tipos)" "$v" && grep -q "passaFiltroTipoReconciliacao(taxa.origem, filtro.tipos)" "$v" && grep -q 'passaFiltroTipoReconciliacao("caucao", filtro.tipos)' "$v" && grep -q '\[...filtro.tipos\].sort().join(",")' "$v" && grep -q "linha.cards?.proprietario" "$v" && grep -q "linha.cards?.inquilino" "$v" && grep -q "TableHead>Inquilino<" "$v" && ! grep -qF "cards?.endereco" "$v" && git diff --quiet -- "$p" && test -z "$(git status --porcelain -- ../supabase/migrations 2>/dev/null)" && npx tsc --noEmit && npm run lint && npm run build && echo TASK1_TRACER_OK</automated>
    <human-check>
      <test>Em `/relatorios/imobiliaria` (local ou produção), abrir o painel "Filtrar" e clicar em cada chip
      de tipo (Administração, Comissão 1º aluguel, Caução) isoladamente e em combinações: confirmar que a
      lista mostra só as linhas do(s) tipo(s) marcado(s), que os `StatTile` dos tipos desmarcados mostram
      R$ 0,00 (sem sumir do grid) e que "Total recebido" reflete só os tipos ainda marcados. Confirmar que o
      chip "Caução" traz recebido/devolvido/usado juntos (nunca separado por subtipo). Clicar "Todos" e
      confirmar que tudo volta a aparecer. Conferir a tabela: coluna "Contrato" mostra ID+proprietário (não
      mais endereço), nova coluna "Inquilino" logo depois, nenhum endereço em lugar nenhum da linha. Por fim,
      com um filtro de tipo ativo, clicar "Exportar PDF" e abrir o arquivo: confirmar que a lista do PDF só
      traz os tipos filtrados, com o mesmo layout de sempre (colunas Data/Contrato/Tipo/Valor/Observação,
      Contrato ainda mostrando "#numero endereco" — o PDF não muda de layout nesta fase, D-08/D-09).</test>
      <expected>Os chips filtram lista+StatTiles+PDF juntos; tipo desmarcado zera o StatTile em vez de
      escondê-lo; "Caução" é um chip único cobrindo os três subtipos; colunas Contrato/Inquilino trocadas
      corretamente; PDF exportado respeita o filtro de tipo sem mudar de layout.</expected>
      <why_human>O comportamento renderizado do zeramento de StatTile, a combinação de múltiplos chips, e a
      estrutura real do PDF exportado com filtro de tipo ativo só são observáveis interagindo com a tela e o
      arquivo reais — grep/tsc confirmam a forma do código (as duas cadeias de filtro, o 4º parâmetro
      obrigatório), não o resultado visual depois de uma interação real do usuário.</why_human>
    </human-check>
  </verify>

  <acceptance_criteria>
    - `TipoMovimentoReconciliacao`/`passaFiltroTipoReconciliacao`/`tipos` (em `FiltroReconciliacaoValores`)
      existem em `reconciliacao.ts`; `calcularReconciliacao` tem 4 parâmetros, o 4º sem valor padrão
    - As duas cadeias de `linhas` (taxa e caução) e a chamada de `calcularReconciliacao` usam
      `filtro.tipos`/`passaFiltroTipoReconciliacao`; a cadeia de caução usa a constante `"caucao"`, nunca o
      campo de tipo do evento
    - `filtro-reconciliacao.tsx` tem a linha de chips (Todos/Administração/Comissão 1º aluguel/Caução) e
      `temFiltroPreenchido` inclui `campos.tipos.size > 0`
    - `resetKey` inclui `[...filtro.tipos].sort().join(",")`
    - A célula "Contrato" lê proprietário (não mais endereço); existe uma nova coluna/célula "Inquilino"
    - `reconciliacao-pdf.ts` não tem nenhuma mudança (`git diff --quiet`); zero arquivo novo em
      `supabase/migrations/`
    - `npx tsc --noEmit`, `npm run lint` e `npm run build` (de dentro de `web/`) não reportam erro novo
  </acceptance_criteria>

  <reversibility rating="reversible">Extensão client-side aditiva sobre três arquivos já em produção — um
  novo tipo/função pura, um 4º parâmetro obrigatório, uma linha de chips, duas cláusulas de `.filter()` e uma
  troca de coluna JSX. Nenhuma migração, nenhum dado gravado, nenhuma Server Action nova. Reverter é restaurar
  os três arquivos tocados.</reversibility>

  <done>Chips de tipo (Todos/Administração/Comissão 1º aluguel/Caução) filtram a lista, os 6 StatTile (zerando
  o tipo desmarcado) e o PDF exportado juntos; "Caução" cobre os três subtipos num único chip; célula
  "Contrato" mostra proprietário, nova coluna "Inquilino" ao lado; `reconciliacao-pdf.ts` permanece
  inalterado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `filtro.tipos` (chips clicados pelo usuário) → matchers em memória (`passaFiltroTipoReconciliacao`, `reconciliacao.ts`) | nenhum valor sai do navegador — sempre um dos 3 literais do union `TipoMovimentoReconciliacao`, nunca texto livre, nunca vira parte de uma query SQL |
| `linhas`/`totais` já filtrados por tipo (client-side) → `reconciliacao-pdf.ts` (jsPDF, client-side, não tocado nesta fase) | o PDF herda o filtro de tipo automaticamente sem nenhuma mudança de código — mesma fronteira já descrita em T-19-* (19-01-PLAN.md) |
| `linha.cards?.proprietario`/`inquilino` (já buscados desde a Phase 19) → colunas Contrato/Inquilino renderizadas | troca de coluna puramente JSX — nenhum campo novo cruza do servidor para o cliente nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-20-01 | Information Disclosure | troca de coluna Contrato/Inquilino (JSX) | low | accept | `proprietario`/`inquilino` já eram buscados e legíveis pela sessão desde a Phase 19 (mesma linha que `endereco`/`numero` já mostravam) — só a apresentação muda; RLS row-level (`is_team_member()`) continua sendo o boundary real, inalterado |
| T-20-02 | Tampering | `tipos: Set<TipoMovimentoReconciliacao>` — chip clicado nunca é texto livre | low | mitigate | union TypeScript de 3 literais fixos (`OrigemTaxa \| "caucao"`) — o compilador recusa qualquer valor fora desses três; nenhum `<Input>` de texto alimenta este filtro |
| T-20-03 | Tampering | cadeia de filtro de caução lendo o campo de tipo do próprio evento em vez da constante `"caucao"` (Pitfall 3, RESEARCH.md) — regressão silenciosa de comportamento, não pega pelo compilador | low | mitigate | `<verify>` desta task tem uma asserção `grep` explícita exigindo `passaFiltroTipoReconciliacao("caucao", ...)` nas duas cadeias (`reconciliacao.ts` e `dinheiro-imobiliaria-view.tsx`) |
| T-20-04 | Denial of Service (client-side) | `calcularReconciliacao` ganha um 4º parâmetro obrigatório sem valor padrão | low | mitigate | intencional (tripwire de compilação) — `npx tsc --noEmit` falha até o único call site ser atualizado; não chega a produção sem o argumento correto |
| T-20-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | zero pacote novo — fase reusa só código já em produção (`FilterChip`/`toggle`, `OrigemTaxa`); Package Legitimacy Audit não aplicável (confirmado em 20-RESEARCH.md) |
</threat_model>

<verification>
1. `grep`/`npx tsc --noEmit`/`npm run lint`/`npm run build` confirmam a forma do código: o novo tipo/função em
   `reconciliacao.ts`, o 4º parâmetro obrigatório de `calcularReconciliacao` (sem default), a linha de chips
   em `filtro-reconciliacao.tsx`, as duas cadeias de filtro de `linhas` usando a constante `"caucao"` do lado
   da caução, o `resetKey` ordenado, a troca de colunas — além de confirmar que `reconciliacao-pdf.ts`
   permanece byte a byte inalterado e zero arquivo novo em `supabase/migrations/`
2. `<human-check>` cobre o que grep/tsc não provam sozinhos: o zeramento visual dos `StatTile` desmarcados, a
   combinação de múltiplos chips, e a estrutura real do PDF exportado com um filtro de tipo ativo
</verification>

<success_criteria>
- Chips de tipo (Todos/Administração/Comissão 1º aluguel/Caução) no painel suspenso filtram a lista e os 6
  StatTile juntos, sem nenhum chip marcado por padrão (D-01, TIPOIMOB-01)
- "Caução" é um único chip cobrindo os três subtipos já existentes — nunca um chip por subtipo (D-02,
  TIPOIMOB-02)
- Desmarcar um tipo zera o StatTile correspondente (nunca esconde o tile) e o PDF exportado passa a receber
  linhas já filtradas, sem nenhuma mudança de layout em `reconciliacao-pdf.ts` (D-03/D-04/D-08/D-09,
  TIPOIMOB-03)
- Célula "Contrato" mostra proprietário (mesmo formato IdPill+texto); nova coluna "Inquilino" ao lado;
  endereço sai completamente da tela (D-05/D-06/D-07, COLIMOB-01/02/03)
- Zero migração de banco, zero Server Action nova, `reconciliacao-pdf.ts` intocado
</success_criteria>

<output>
Create `.planning/phases/20-filtro-por-tipo-de-movimento-no-relat-rio-da-imobili-ria/20-01-SUMMARY.md` when done
</output>
