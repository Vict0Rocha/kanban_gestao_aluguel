---
phase: 07-concilia-o-e-destrava-rastreada
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - web/src/lib/kanban/actions.ts
  - web/src/lib/kanban/queries.ts
  - web/src/components/financeiro/parcelas-table.tsx
  - web/src/components/financeiro/destravar-parcela-dialog.tsx
  - web/src/components/financeiro/parcela-historico-sheet.tsx
autonomous: true
requirements: [CONCIL-03, CONCIL-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Destravar substitui Pagamento/Ajustar/Conciliar na linha de uma parcela conciliada — nunca aparece ao lado deles (D-08 da 07-CONTEXT.md)"
    - "Destravar exige `motivo` não vazio: o cliente bloqueia o envio com \"Informe o motivo da destrava.\" antes de qualquer round-trip, e o servidor recusa de novo via `textoObrigatorio` mesmo que o cliente seja contornado (CONCIL-03, D-04)"
    - "`destravarParcelaAction` só aceita destravar uma parcela cujo `status` atual é `\"conciliada\"` — chamada direto numa parcela paga/aberta/parcial é recusada antes de qualquer INSERT ou UPDATE"
    - "Destravar grava um lançamento `tipo='destrava'`, `valor=0`, `motivo` e `criado_por` da sessão, e devolve `parcelas.status` para `'paga'` (D-05) — dois passos, ambos decididos e executados no servidor"
    - "O histórico de destravas (quem, quando, motivo) fica visível em `ParcelaHistoricoSheet`: o bloco de `motivo` é acrescentado ao lado do bloco de `observacao` já existente, sem substituí-lo (CONCIL-04)"
    - "A trava de exclusão de contrato (`cardTemLancamento`) continua cobrindo lançamentos `tipo='destrava'` automaticamente, sem nenhuma mudança de código nesta fase — `cardTemLancamento` conta qualquer linha de `parcela_lancamentos`, D-06 desta fase e D-17 de `06.2-CONTEXT.md`. Confirmado por leitura, não reimplementado"
    - "loading: o botão Destravar da linha só abre o diálogo, sem chamada assíncrona no clique — o estado de carregamento pertence ao submit do diálogo, não ao gatilho (UI Consideration, dismissed)"
    - "loading: o submit de `DestravarParcelaDialog` troca o rótulo para \"Destravando...\" e desabilita via `disabled={saving}`, idêntico a `RegistrarPagamentoDialog`/`AjustarParcelaDialog`"
    - "empty: motivo vazio bloqueia o envio no cliente com \"Informe o motivo da destrava.\" antes de qualquer round-trip ao servidor"
    - "error: uma recusa do servidor em `DestravarParcelaDialog` (CHECK constraint, rede) aparece no slot inline já existente `text-sm text-destructive`, via `erroDoBanco()`"
    - "long-text: o campo de motivo usa `Textarea` padrão do shadcn (quebra de linha automática); o teto real do servidor é **500 caracteres** via a CHECK `parcela_lancamentos_motivo_tamanho` — a UI-SPEC citou 2000 por engano (achou que `motivo` compartilha o teto de `observacao`; o schema tem duas CHECKs distintas, 500 para `motivo` e 2000 para `observacao`). Esta correção está aplicada na Task 1, banco é a autoridade final"
    - "long-text: a linha de motivo em `ParcelaHistoricoSheet` reaproveita o mesmo bloco `text-sm text-muted-foreground` já provado para `observacao`, e quebra naturalmente dentro do container `overflow-y-auto` do sheet"
    - "zero-one-many: `ParcelasTable` já renderiza situações heterogêneas por linha desde a Phase 5/6; esta fase só acrescenta variantes de ação por linha, sem mudar como a tabela lida com 0/1/muitas linhas (dismissed)"
    - statement: "`ParcelaHistoricoSheet` nunca deveria mostrar o estado vazio para uma parcela conciliada, porque conciliar exige `status='paga'`, que por sua vez exige pelo menos um lançamento `tipo='pagamento'` — logicamente sólido a partir de `avaliarVisibilidadeParcela`/`statusDeParcela`, mas nunca exercitado contra uma linha conciliada real em produção antes desta fase"
      verification: backstop
  artifacts:
    - web/src/components/financeiro/destravar-parcela-dialog.tsx
  key_links:
    - "Botão Destravar (linha conciliada) -> DestravarParcelaDialog -> destravarParcela (queries.ts) -> destravarParcelaAction (actions.ts) -> INSERT tipo='destrava' + UPDATE status='paga'"
    - "destravarParcelaAction -> CHECK parcela_lancamentos_destrava_exige_motivo (já viva desde a Phase 4) como backstop atrás de textoObrigatorio"
    - "ParcelaHistoricoSheet -> lancamento.motivo -> o histórico visível de CONCIL-04"
---

<objective>
Fechar o par de ações da fase: destravar uma parcela conciliada com motivo obrigatório, rastreado, e tornar esse histórico visível na própria parcela. É a última peça do modelo de livro-razão desenhado desde a Phase 4 — depois deste plano os quatro `tipo` de `parcela_lancamentos` (`pagamento`, `acrescimo`, `desconto`, `destrava`) estão todos alcançáveis pela interface.

Purpose: CONCIL-03 (destravar exige motivo, sempre) e CONCIL-04 (o histórico de destravas fica visível). Expande o slice provado no plano 07-01 (Server Action -> bridge -> UI) para o caminho que tem diálogo e histórico, em vez do clique único de Conciliar.
Output: `destravarParcelaAction`/`destravarParcela`, `DestravarParcelaDialog`, o botão Destravar substituindo Pagamento/Ajustar/Conciliar numa linha conciliada, e o `motivo` visível em `ParcelaHistoricoSheet`.
</objective>

Este plano não repete a lista de artefatos da fase — ver `## Artifacts this phase produces` em `07-01-PLAN.md`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-concilia-o-e-destrava-rastreada/07-CONTEXT.md
@.planning/phases/07-concilia-o-e-destrava-rastreada/07-UI-SPEC.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/07-concilia-o-e-destrava-rastreada/07-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Destravar — Server Action e bridge</name>
  <precondition>Plano 07-01 aplicado (conciliarParcelaAction/conciliarParcela existem, o projeto compila).</precondition>
  <files>web/src/lib/kanban/actions.ts, web/src/lib/kanban/queries.ts</files>
  <read_first>
    - `web/src/lib/kanban/actions.ts` — o estado pós-07-01: `exigirParcelaNaoConciliada`, `conciliarParcelaAction`, `textoObrigatorio`, `registrarPagamentoAction` (o formato de inserir um lançamento com `criado_por: sessao.user.id`)
    - `.planning/phases/07-concilia-o-e-destrava-rastreada/07-CONTEXT.md` — D-04, D-05, D-09
    - `.planning/phases/07-concilia-o-e-destrava-rastreada/07-UI-SPEC.md` — Copywriting Contract, linhas "Error state — Destravar, empty motivo" e "Error state — Destravar, server rejection", e a nota "Max length" em "DestravarParcelaDialog — field-level copy" (**a corrigir**, ver abaixo)
    - `supabase/migrations/20260816000000_financeiro_schema.sql` linhas 177-213 — as CHECKs `parcela_lancamentos_tipo_valido`, `parcela_lancamentos_valor_exigido` (permite `valor=0` só para `destrava`), `parcela_lancamentos_destrava_exige_motivo`, e **`parcela_lancamentos_motivo_tamanho` (`length(motivo) <= 500`)** — este é o teto real, distinto da CHECK de `observacao` (2000). A UI-SPEC (linha 163) cita 2000 para `motivo` por engano; use 500 nesta task, porque o banco é a autoridade final (mesmo princípio já documentado no cabeçalho de `actions.ts`)
  </read_first>
  <action>
Em `actions.ts`, seção `// --- Parcelas ---`, depois de `conciliarParcelaAction`: nova função exportada `destravarParcelaAction(parcelaId: string, motivo: string): Promise<ActionResult>`.

Corpo, nesta ordem:
1. `requireUser()`.
2. Validação: `id(parcelaId, "Parcela") ?? textoObrigatorio(motivo, "Motivo", 500)` — **500, não 2000** (ver a correção no `read_first`; documente em comentário que o teto reflete a CHECK `parcela_lancamentos_motivo_tamanho`, não o teto de `observacao`).
3. Leitura do estado atual: `SELECT status FROM parcelas WHERE id = parcelaId` via `.maybeSingle()`. Erro ou linha ausente → `erroDoBanco(erro?.code, "destravar a parcela")`. Se `status !== "conciliada"` → devolva `{ ok: false, error: "Esta parcela não está conciliada — não há o que destravar." }` **antes de qualquer INSERT/UPDATE** — chamar `destravarParcelaAction` numa parcela paga/aberta/parcial não pode gravar um lançamento `destrava` fantasma.
4. **Não chame `exigirParcelaVisivel`** — mesma razão de D-09 documentada em `conciliarParcelaAction`: uma parcela conciliada sempre tem lançamento, então já é visível por outro caminho. Comente isso no ponto certo, como no plano 07-01.
5. `INSERT` em `parcela_lancamentos`: `parcela_id: parcelaId`, `tipo: "destrava"`, `valor: 0`, `motivo: motivo.trim()`, `criado_por: sessao.user.id` — sem `observacao` (este lançamento usa `motivo`, não `observacao`). Erro → `erroDoBanco(error.code, "destravar a parcela")`; zero linhas → `semLinhas("destravar a parcela")`.
6. `UPDATE` em `parcelas` setando `status: "paga"` `.eq("id", parcelaId)` (sem condição adicional de status aqui — o passo 3 já confirmou `conciliada` poucos milissegundos antes). Erro → `erroDoBanco`; zero linhas → `semLinhas("destravar a parcela")`.
7. Retorna `{ ok: true, data: undefined }`.

Em `queries.ts`: bridge nova `export async function destravarParcela(parcelaId: string, motivo: string) { return unwrap(destravarParcelaAction(parcelaId, motivo)) }`, e acrescente `destravarParcelaAction` ao import de `"./actions"`.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && f1=web/src/lib/kanban/actions.ts && f2=web/src/lib/kanban/queries.ts && grep -q "export async function destravarParcelaAction" "$f1" && grep -q 'textoObrigatorio(motivo, "Motivo", 500)' "$f1" && grep -q 'tipo: "destrava"' "$f1" && grep -q "valor: 0" "$f1" && grep -q 'status !== "conciliada"' "$f1" && grep -q 'status: "paga"' "$f1" && grep -q "criado_por: sessao.user.id" "$f1" && grep -q "export async function destravarParcela" "$f2" && grep -q "destravarParcelaAction" "$f2" && echo DESTRAVAR_ACTION_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` e `npm run build` terminam com código 0
    - `destravarParcelaAction` recusa antes de qualquer gravação quando `status !== "conciliada"`, valida `motivo` com teto 500 (não 2000), grava `tipo='destrava'`/`valor=0`/`criado_por` da sessão, e devolve `status` a `"paga"`
    - `destravarParcela` existe em `queries.ts` e delega via `unwrap`
    - Nenhum arquivo além de `actions.ts` e `queries.ts` foi modificado por esta task
  </acceptance_criteria>
  <reversibility rating="reversible">Destravar é, por definição, o próprio mecanismo de correção da fase — não há "desfazer" a construir, ele É o desfazer de Conciliar.</reversibility>
  <done>destravarParcelaAction existe, só aceita parcela conciliada, grava o lançamento com motivo obrigatório e devolve o status a paga; a bridge está pronta para o diálogo da Task 2.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DestravarParcelaDialog e a troca de botões na linha conciliada</name>
  <precondition>Task 1 aplicada e o projeto compila.</precondition>
  <files>web/src/components/financeiro/destravar-parcela-dialog.tsx, web/src/components/financeiro/parcelas-table.tsx</files>
  <read_first>
    - `web/src/components/financeiro/ajustar-parcela-dialog.tsx` — o molde estrutural exato a copiar: `wasOpen` resync, estados `error`/`saving`, `descricaoCompetencia` local (cada diálogo tem sua própria cópia, não compartilhada), `DialogHeader`/`DialogFooter`, botão de submit sem `variant` explícito (herda o acento)
    - `web/src/components/financeiro/parcelas-table.tsx` — estado pós-07-01: `AcoesCell` com o botão Conciliar condicionado a `situacao === "paga"`, o `dialogoAberto` union, os quatro botões e os três diálogos renderizados incondicionalmente no fim de `AcoesCell`
    - `.planning/phases/07-concilia-o-e-destrava-rastreada/07-UI-SPEC.md` — seção "Component Notes" (bloco `situacao === "conciliada"`) e "DestravarParcelaDialog — field-level copy" (todas as frases verbatim, exceto o teto de caracteres corrigido na Task 1)
    - `web/src/lib/kanban/format.ts` — `formatCurrency`
  </read_first>
  <action>
**1. `destravar-parcela-dialog.tsx` — `DestravarParcelaDialog` (novo arquivo).**
Estrutura idêntica a `AjustarParcelaDialog`: `"use client"`, props `{ parcelaId: string, endereco: string, competencia: string, valorPago: number, open: boolean, onOpenChange: (open: boolean) => void }`. Copie a função local `descricaoCompetencia` verbatim (mesmo helper, arquivo próprio — não importe de outro lugar, mesmo padrão dos dois diálogos irmãos). Estado: `motivo` (string, inicial `""`), `error` (string|null), `saving` (boolean), e o mesmo truque `wasOpen` para resincronizar ao reabrir (reset `motivo`/`error`).

`handleSubmit`: se `!motivo.trim()`, `setError("Informe o motivo da destrava.")` e retorna sem chamar o servidor. Senão `setSaving(true)`, `setError(null)`, `try { await destravarParcela(parcelaId, motivo.trim()); onOpenChange(false); router.refresh() } catch (error) { setError(error instanceof Error ? error.message : "Não foi possível destravar a parcela. Tente novamente.") } finally { setSaving(false) }`.

JSX: `DialogTitle` = "Destravar parcela". Duas linhas de descrição: `descricaoCompetencia(endereco, competencia)`, depois `` `Valor pago: ${formatCurrency(valorPago)}` ``. Campo único: `Label` "Motivo da destrava" (**sem** sufixo "(opcional)" — é o único campo obrigatório do módulo Financeiro), `Textarea` `id="motivo-destrava"`, `placeholder="Ex.: valor lançado errado, corrigir e destravar para nova baixa"`, `value={motivo}`, `onChange`. Sem atributo `maxLength` no `Textarea` — os diálogos irmãos também não capam no cliente, a validação de tamanho é 100% server-side (Task 1). Erro em `text-sm text-destructive`. `DialogFooter` com um botão de submit sem `variant` explícito (herda o acento, igual aos irmãos): rótulo "Destravar"/"Destravando..." conforme `saving`, `disabled={saving}`.

**2. `parcelas-table.tsx` — a troca de botões.**
Em `AcoesCell`: acrescente `"destravar"` ao union type de `dialogoAberto`. Acrescente `Unlock` ao import de `"lucide-react"` (junto a `Banknote`, `History`, `Lock`).

Reestruture o bloco de botões (hoje: Pagamento, Ajustar, Conciliar condicional, Histórico, todos sempre renderizados juntos) para um `if`/`else` sobre `linha.situacao === "conciliada"`:
- **Ramo conciliada:** só dois botões — `Button` `variant="outline"`, ícone `Unlock`, `aria-label={`Destravar parcela — ${linha.endereco}`}`, `onClick={() => setDialogoAberto("destravar")}`, texto "Destravar"; depois o botão Histórico existente, sem alteração (`variant="ghost" size="icon"`, ícone `History`).
- **Ramo padrão (else):** exatamente a sequência que a Task 1 do plano 07-01 deixou — Pagamento (outline), Ajustar (ghost), Conciliar condicionado a `situacao === "paga"` (ghost), Histórico (ghost icon) — sem nenhuma mudança de conteúdo, só reindentado dentro do `else`.

Os quatro diálogos (`RegistrarPagamentoDialog`, `AjustarParcelaDialog`, `ParcelaHistoricoSheet`, e o novo `DestravarParcelaDialog`) continuam **todos** renderizados incondicionalmente no fim de `AcoesCell`, fora do `if`/`else` de botões — mesmo padrão já usado para os três existentes, controlados só por `dialogoAberto`. Passe a `DestravarParcelaDialog`: `parcelaId={linha.id}`, `endereco={linha.endereco}`, `competencia={linha.competencia}`, `valorPago={linha.valorPago}`, `open={dialogoAberto === "destravar"}`, `onOpenChange={(open) => setDialogoAberto(open ? "destravar" : null)}`.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && fd=web/src/components/financeiro/destravar-parcela-dialog.tsx && ft=web/src/components/financeiro/parcelas-table.tsx && test -f "$fd" && grep -q "Motivo da destrava" "$fd" && grep -q "Informe o motivo da destrava." "$fd" && grep -q "Ex.: valor lançado errado, corrigir e destravar para nova baixa" "$fd" && grep -vq "maxLength" "$fd" && grep -vE '^\s*(//|\*|/\*)' "$fd" | grep -cE 'bg-\[|text-\[|p-\[|gap-\[' | grep -qx 0 && grep -q "DestravarParcelaDialog" "$ft" && grep -q '"destravar"' "$ft" && grep -q 'situacao === "conciliada"' "$ft" && grep -q "Unlock" "$ft" && echo DIALOG_OK</automated>
    <human-check>
      <test>Destravar uma parcela conciliada: clicar em "Destravar" na linha, tentar submeter com o campo de motivo vazio, depois preencher e submeter.</test>
      <expected>Vazio: o diálogo mostra "Informe o motivo da destrava." sem sair da tela (sem round-trip). Preenchido: o botão troca para "Destravando...", o diálogo fecha, a linha volta a mostrar Pagamento/Ajustar/Conciliar (não mais Destravar) e a Situação volta a "Paga". Numa parcela não-conciliada, os botões Pagamento/Ajustar/Conciliar continuam aparecendo normalmente — nenhuma regressão nas outras linhas.</expected>
      <why_human>Fluxo de formulário e a troca visual de botões por linha — grep confirma a existência do código, não o comportamento no navegador.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` e `npm run build` terminam com código 0
    - `destravar-parcela-dialog.tsx` existe, exporta `DestravarParcelaDialog`, e todas as frases visíveis batem palavra por palavra com a UI-SPEC (exceto o teto de caracteres, corrigido na Task 1)
    - O `Textarea` de motivo não declara `maxLength`
    - Ignorando linhas de comentário, `destravar-parcela-dialog.tsx` não contém valor arbitrário entre colchetes para cor, tamanho ou espaçamento
    - `parcelas-table.tsx`: uma linha `conciliada` renderiza exatamente Destravar + Histórico; qualquer outra situação renderiza a sequência inalterada de Pagamento/Ajustar/Conciliar-condicional/Histórico
    - Os quatro diálogos/sheets continuam renderizados incondicionalmente, controlados só por `dialogoAberto`
  </acceptance_criteria>
  <reversibility rating="reversible">Mudança de UI sobre uma Server Action já travada no servidor (Task 1) — nenhum caminho de escrita nova é aberto por este diálogo que o servidor não recusaria por conta própria.</reversibility>
  <done>Uma linha conciliada mostra só Destravar e Histórico; o diálogo exige motivo antes do round-trip; destravar com sucesso devolve a linha ao conjunto normal de ações.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Histórico de destravas em ParcelaHistoricoSheet</name>
  <precondition>Task 1 aplicada (LancamentoDetalhado.motivo já existe no tipo desde a Phase 6; nenhuma mudança de tipo é necessária).</precondition>
  <files>web/src/components/financeiro/parcela-historico-sheet.tsx</files>
  <read_first>
    - `web/src/components/financeiro/parcela-historico-sheet.tsx` — inteiro, em especial o bloco `{lancamento.observacao && (...)}` dentro do `<li>` de cada lançamento, e `prefixoValor` (já trata `destrava` retornando "—", sem mudança necessária)
    - `.planning/phases/07-concilia-o-e-destrava-rastreada/07-UI-SPEC.md` — seção "ParcelaHistoricoSheet — CONCIL-04 gap closed here", com o bloco JSX exato a acrescentar
  </read_first>
  <action>
Acrescente um bloco condicional novo, **ao lado** (não em substituição) do bloco `{lancamento.observacao && (...)}` já existente, dentro do mesmo `<li>`: `{lancamento.motivo && (<p className="text-sm text-muted-foreground"><span className="font-medium text-foreground">Motivo: </span>{lancamento.motivo}</p>)}`. Mesma classe base do parágrafo de `observacao`, com o prefixo `<span className="font-medium text-foreground">Motivo: </span>` para diferenciar visualmente o motivo de uma destrava de uma observação livre. Os dois blocos são mutuamente exclusivos na prática (um lançamento `destrava` só grava `motivo`, os outros três tipos só gravam `observacao`), então nenhuma lógica condicional compartilhada é necessária — apenas os dois `{condição && (...)}` lado a lado.

Nenhuma outra linha do arquivo muda: `prefixoValor`, `LancamentoTipoLabel`, o estado vazio, e a estrutura do `<Sheet>` ficam exatamente como estão.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && f=web/src/components/financeiro/parcela-historico-sheet.tsx && grep -q "lancamento.motivo" "$f" && grep -q "Motivo: " "$f" && grep -q "lancamento.observacao" "$f" && echo HISTORICO_OK</automated>
    <human-check>
      <test>Abrir o Histórico (ícone) de uma parcela que já foi conciliada e destravada pelo menos uma vez (Tasks 1/2 deste plano).</test>
      <expected>A entrada `destrava` na lista mostra o rótulo "Destrava" (ícone Unlock), o valor "—" (não uma moeda), a data e quem fez, e uma linha "Motivo: " seguida do texto digitado no diálogo — ao lado de, não no lugar de, qualquer observação de outros lançamentos da mesma parcela.</expected>
      <why_human>Confere a renderização real dentro do Sheet, incluindo o texto longo quebrando dentro do container com scroll — grep só confirma que o JSX existe no arquivo.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `npm run lint` e `npm run build` terminam com código 0
    - `parcela-historico-sheet.tsx` renderiza `lancamento.motivo` num bloco `<p className="text-sm text-muted-foreground">` com o prefixo `<span className="font-medium text-foreground">Motivo: </span>`, ao lado do bloco de `observacao` já existente (nenhum dos dois foi removido)
    - `prefixoValor`, `LancamentoTipoLabel` e a estrutura do `<Sheet>` permanecem inalterados
    - Nenhum arquivo além de `parcela-historico-sheet.tsx` foi modificado por esta task
  </acceptance_criteria>
  <reversibility rating="reversible">Adição de leitura pura — renderiza um campo que já existe no tipo e já chega do banco desde a Phase 6; nenhuma escrita nova.</reversibility>
  <done>Uma destrava mostra seu motivo no histórico da parcela, junto com quem e quando, fechando CONCIL-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| navegador -> Server Action | `DestravarParcelaDialog` é conveniência; a trava real (motivo obrigatório, só parcela conciliada) vive em `destravarParcelaAction`, reconsultando o banco |
| Server Action -> banco | `destravarParcelaAction` sempre relê `status` antes de gravar, e nunca aceita `criado_por` do cliente |
| leitura do histórico | `ParcelaHistoricoSheet` só renderiza campos que já chegam da query server-side filtrada por RLS — nenhuma consulta nova é aberta por este plano |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-06 | Tampering | `destravarParcelaAction` chamada direto numa parcela não-conciliada | high | mitigate | SELECT de `status` antes de qualquer INSERT/UPDATE; `status !== "conciliada"` recusa imediatamente, sem gravar um lançamento `destrava` fantasma |
| T-07-07 | Repudiation | lançamento `destrava` sem motivo | high | mitigate | `textoObrigatorio(motivo, "Motivo", 500)` no servidor (independente da validação do cliente) **mais** a CHECK `parcela_lancamentos_destrava_exige_motivo` (viva desde a Phase 4) como backstop de banco — dupla camada, cliente é só conveniência |
| T-07-08 | Spoofing | `criado_por` forjado pelo cliente no lançamento de destrava | high | mitigate | `criado_por` sempre atribuído de `sessao.user.id` no servidor, mesmo padrão de todo outro INSERT em `parcela_lancamentos` em `actions.ts` |
| T-07-09 | Elevation of Privilege | qualquer membro da allowlist pode destravar, não só quem conciliou | low | accept | Decisão de produto explícita (D-04: "qualquer membro destrava, mas fica registrado"; `REQUIREMENTS.md` § Out of Scope confirma "papéis/permissões diferenciadas para destravar" fora do escopo) — rastreabilidade por `criado_por`/`motivo` substitui restrição por papel |
| T-07-SC | Tampering | instalação de pacote npm/pip/cargo | n/a | accept | Zero pacotes novos — `Unlock`, `Textarea`, `Dialog` já instalados desde a Phase 5/6; nenhum `npx shadcn add` é executado nesta fase |

</threat_model>

<verification>
- `npm run lint` e `npm run build` em `web/`
- Asserção de fonte: `destravarParcelaAction` recusa antes de gravar quando `status !== "conciliada"`, valida motivo com teto 500, e grava `criado_por` da sessão
- Asserção de fonte: `DestravarParcelaDialog` bloqueia motivo vazio no cliente antes do round-trip
- Asserção de fonte: `parcelas-table.tsx` mostra Destravar+Histórico só em linha conciliada, e a sequência padrão nas demais
- Asserção de fonte: `ParcelaHistoricoSheet` renderiza `motivo` ao lado de `observacao`, sem remover nenhum dos dois
- Teste manual: destravar com motivo vazio (bloqueado no cliente), destravar com motivo preenchido (linha volta a paga, botões voltam ao normal), e ver o motivo no histórico
</verification>

<success_criteria>
- Destravar exige motivo em todo o caminho — cliente e servidor, com CHECK de banco como backstop
- Uma linha conciliada mostra só Destravar/Histórico; destravar com sucesso devolve os botões normais e o status a "paga"
- O histórico de destravas (quem, quando, motivo) é visível na própria parcela, sem superfície nova
- Os quatro `tipo` de `parcela_lancamentos` estão todos alcançáveis pela interface, fechando o modelo de livro-razão desde a Phase 4
</success_criteria>

<output>
Create `.planning/phases/07-concilia-o-e-destrava-rastreada/07-02-SUMMARY.md` when done
</output>