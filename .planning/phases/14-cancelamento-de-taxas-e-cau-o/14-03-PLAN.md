---
phase: 14-cancelamento-de-taxas-e-cau-o
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - supabase/migrations/20260826000000_taxas_imobiliaria_lancamento_id.sql
  - docs/data-model.md
autonomous: false
requirements: [CANIMOB-03]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A migração `20260826000000_taxas_imobiliaria_lancamento_id` está aplicada no banco de produção — via SQL Editor (o Supabase CLI não está instalado neste projeto), com o caminho registrado no SUMMARY"
    - "Depois da aplicação, `taxas_total`, `lancamentos_total`, `cards_total` e `cards_updated_at_max` continuam idênticos aos anotados no ensaio, e `count(*) where lancamento_id is not null` devolve 0 — nada foi apagado, alterado ou pré-preenchido pela migração"
    - "`information_schema.columns` confirma `lancamento_id` como `uuid`/nullable; `pg_indexes` confirma `taxas_imobiliaria_lancamento_id_idx`; `pg_policies` de `taxas_imobiliaria` continua com exatamente 1 linha (nenhuma policy nova)"
    - "`docs/data-model.md` documenta a coluna nova no diagrama `mermaid` (TAXAS_IMOBILIARIA + relação com PARCELA_LANCAMENTOS) e explica, no mesmo estilo 'decisão + porquê' já usado, que esta FK reabre D-04 (13-CONTEXT.md) só para cascata, nunca para cálculo"
    - "O Board, o Financeiro e os Relatórios carregam normalmente depois da aplicação, com os mesmos contratos de antes — a coluna nova é invisível para o código que ainda não a usa (planos 14-04/14-05)"
  artifacts:
    - docs/data-model.md
  key_links:
    - "migração aplicada -> `registrarPagamentoAction`/`cancelarTaxaImobiliariaAction`/`cancelarEventoCaucaoAction` (planos 14-04/14-05), que só podem existir depois deste plano"
  prohibitions:
    - "MUST NOT aplicar esta migração em produção sem um `checkpoint:decision` explícito respondido pelo usuário — mesma régua de toda migração anterior deste projeto, reafirmada aqui porque D-03 (14-CONTEXT.md) está explicitamente marcado `one-way`"
---

<objective>
Aplicar a migração `20260826000000_taxas_imobiliaria_lancamento_id.sql` no banco de produção e provar,
contra o schema já gravado, que a coluna, o índice e a RLS nasceram corretos e que nada mais mudou.

Este é o único plano da fase que altera o banco real. Aplicar em produção é **porta de mão única**
(D-03, 14-CONTEXT.md — `rating="one-way"`): desfazer exige outra migração contra dados de produção. A
migração em si é de baixo risco (uma coluna nullable + um índice, sem CHECK, sem policy nova), mas a
disciplina deste projeto trata toda mudança de schema em produção com o mesmo `checkpoint:decision`,
independente do tamanho — mesma régua de toda migração anterior (Fase 4, 6.1, 6.2, 13). **O Supabase CLI
não está instalado** — a aplicação é via SQL Editor, colando a DDL completa, não `supabase db push`.

Purpose: CANIMOB-03 (cancelar um pagamento cancela automaticamente a taxa vinculada) só existe de verdade
depois de a coluna estar no banco onde o dado mora — os planos 14-04/14-05 não têm onde escrever/o que
disparar antes disso.
Output: `taxas_imobiliaria.lancamento_id` vivo em produção, verificação pós-aplicação registrada,
`docs/data-model.md` atualizado.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `14-01-PLAN.md`. Depois
deste plano, `public.taxas_imobiliaria.lancamento_id` e `taxas_imobiliaria_lancamento_id_idx` passam a
existir de fato no banco.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-CONTEXT.md
@.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-02-SUMMARY.md
@supabase/verificacao_taxas_imobiliaria_lancamento_id.sql
@supabase/migrations/20260826000000_taxas_imobiliaria_lancamento_id.sql
@docs/data-model.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Autorizar a aplicação da migração `taxas_imobiliaria.lancamento_id` no banco de produção</name>
  <read_first>
    - `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` — o bloco `RESULTADO DO ENSAIO` gravado
      no plano 14-02
    - `.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-02-SUMMARY.md` — o que o ensaio observou,
      incluindo o resultado da Prova 2.3 (cascata)
  </read_first>
  <decision>
    Aplicar `20260826000000_taxas_imobiliaria_lancamento_id.sql` agora contra o banco de produção, via
    SQL Editor (colando a DDL completa — o Supabase CLI não está instalado neste projeto).
  </decision>
  <context>
    O banco tem dado financeiro real (parcelas, lançamentos e taxas geradas nos testes da Phase 13), sem
    staging nem backup automático (plano gratuito do Supabase). A migração é estritamente aditiva: uma
    coluna nullable sem default + um índice, sem CHECK novo, sem policy nova. O ensaio do plano 14-02 já
    rodou essa mesma DDL contra o schema real dentro de uma transação desfeita no fim, e observou:
    aplicação limpa nas duas passagens, contagens inalteradas, a cascata de exclusão funcionando de
    verdade (card+parcela+lançamento+taxa de teste — apagar o lançamento removeu a taxa), `lancamento_id`
    nulo continuando aceito, e zero mudança na contagem de policies.

    **O que muda de comportamento no minuto seguinte à aplicação, mesmo sem nenhum código novo:** nada
    observável — nenhum código de aplicação grava `lancamento_id` ainda (isso só começa no plano 14-04),
    então toda taxa continua sendo inserida exatamente como hoje, só que agora com uma coluna extra
    sempre nula. O risco real desta aplicação é estritamente o de qualquer DDL nova: um erro afetando as
    linhas já existentes de `taxas_imobiliaria`.

    O que **não** é reversível: depois da aplicação, desfazer significa rodar um `alter table ... drop
    column` manual — sempre possível aqui porque a coluna nunca é usada para nada além da cascata (D-04
    continua intocado), mas ainda assim uma mudança de schema em produção, não uma correção de código.
  </context>
  <options>
    <option id="aplicar-agora">
      <name>Aplicar agora</name>
      <pros>Desbloqueia os planos 14-04/14-05 imediatamente; o ensaio já cobriu a cascata de verdade,
      não só a forma da coluna; a mudança não altera nenhum comportamento observável até o plano 14-04
      chegar</pros>
      <cons>É porta de mão única sobre dado real, mesmo sendo uma mudança pequena</cons>
    </option>
    <option id="conferir-backup-antes">
      <name>Conferir o backup antes de aplicar</name>
      <pros>Abre uma saída independente de um `drop` manual — mesmo procedimento já usado nas fases
      anteriores (export CSV manual pelo Table Editor, ou conferir a última cópia automática do plano do
      Supabase)</pros>
      <cons>Atrasa a fase pelo tempo da conferência</cons>
    </option>
    <option id="adiar">
      <name>Adiar</name>
      <pros>Nada muda em produção hoje</pros>
      <cons>Os planos 14-04/14-05 ficam bloqueados — CANIMOB-03 (cascata) não pode existir sem esta
      coluna</cons>
    </option>
  </options>
  <acceptance_criteria>
    - O usuário escolheu explicitamente uma das três opções
    - Se `conferir-backup-antes`, o backup foi anotado (quais tabelas, qual data) antes de seguir para a
      Task 2
    - Se `adiar`, as Tasks 2 e 3 não são executadas e o plano encerra com o motivo registrado
  </acceptance_criteria>
  <reversibility rating="one-way">A task seguinte grava DDL em produção. Desfazer exige um `alter table
  ... drop column` manual sobre o banco real.</reversibility>
  <resume-signal>Responda: `aplicar-agora`, `conferir-backup-antes` (com a data e as tabelas exportadas)
  ou `adiar` (com o motivo).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Aplicar a migração em produção via SQL Editor e documentar em `docs/data-model.md`</name>
  <precondition>A Task 1 foi respondida com `aplicar-agora` ou `conferir-backup-antes`.</precondition>
  <files>supabase/migrations/20260826000000_taxas_imobiliaria_lancamento_id.sql, docs/data-model.md</files>
  <read_first>
    - `supabase/migrations/20260826000000_taxas_imobiliaria_lancamento_id.sql` — o arquivo a aplicar;
      conferir que é exatamente o estado aprovado no ensaio do plano 14-02
    - `.planning/phases/13-dinheiro-da-imobili-ria/13-03-SUMMARY.md` — como o push foi feito de verdade
      na fase mais recente (CLI não instalado; DDL colada no SQL Editor de produção, confirmando
      "Success. No rows returned")
    - `docs/data-model.md` linhas 1-19 (bloco `mermaid`, relações e `TAXAS_IMOBILIARIA`), linha 123
      (bullet da entidade `taxas_imobiliaria`), linha 153 (bullet "A fronteira estrutural entre
      `taxas_imobiliaria` e o livro-razão do aluguel (D-04, Phase 13)")
  </read_first>
  <action>
Primeiro, checar se o Supabase CLI está instalado e linkado (`supabase migration list`, se o binário
existir no PATH). Dado o histórico deste projeto (CLI ausente desde a Fase 4, confirmado de novo em
cada migração seguinte, inclusive a mais recente da Phase 13), o caminho esperado é: colar a DDL
completa de `supabase/migrations/20260826000000_taxas_imobiliaria_lancamento_id.sql` no SQL Editor de
produção e confirmar "Success. No rows returned". Registrar no SUMMARY qual caminho foi de fato usado
(CLI ou SQL Editor), mesmo que seja o esperado.

Se a aplicação falhar, ler o erro, corrigir a causa no arquivo de migração, atualizar
`supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` para continuar batendo, voltar ao ensaio do
plano 14-02 e nunca tentar aplicar de novo sem reensaiar.

Depois da aplicação bem-sucedida, atualizar `docs/data-model.md`:
- No diagrama de entidades (bloco `mermaid`), adicionar `uuid lancamento_id FK` ao bloco
  `TAXAS_IMOBILIARIA` (depois de `card_id FK`, antes de `origem`) e uma relação nova:
  `PARCELA_LANCAMENTOS |o--o{ TAXAS_IMOBILIARIA : "cascade (Fase 14)"` (cardinalidade "zero ou um" do
  lado de `PARCELA_LANCAMENTOS`, já que `lancamento_id` é nullable), logo abaixo das relações já
  existentes de `TAXAS_IMOBILIARIA`.
- Na descrição da entidade `taxas_imobiliaria` (linha 123), acrescentar ao final: uma frase citando que
  `lancamento_id` (Phase 14, nullable, `on delete cascade`) liga a taxa ao lançamento de pagamento
  específico que a gerou — só para limpeza automática quando esse pagamento é cancelado, nunca para
  cálculo; linhas de taxa geradas antes da Phase 14 ficam com `lancamento_id` nulo para sempre (sem
  backfill).
- Em "Decisões de design", logo depois da bullet "A fronteira estrutural entre `taxas_imobiliaria` e o
  livro-razão do aluguel (D-04, Phase 13)" (linha 153), acrescentar uma bullet nova: "**`taxas_imobiliaria.lancamento_id`
  reabre D-04 só para cascata (D-03, Phase 14)**" — explicando que a fronteira estrutural acima continua
  valendo por inteiro para cálculo de valor/status; a coluna nova existe com um propósito único e
  estreito: quando um pagamento (`parcela_lancamentos`) é cancelado (Phase 11/12), o `on delete cascade`
  desta FK remove automaticamente a taxa que aquele pagamento específico gerou, sem nenhum código de
  aplicação novo em `cancelarLancamentoAction` — a cascata é 100% do banco. Nullable e sem backfill:
  taxas geradas antes da Phase 14 (inclusive as de teste da Phase 13) ficam com `lancamento_id` nulo
  para sempre, porque não há como inferir retroativamente qual pagamento gerou qual taxa quando uma
  parcela paga em partes acumulou mais de uma. Fechar citando que o mecanismo de cancelamento
  independente da taxa (sem cascata) e o cancelamento sequencial de caução chegam no plano 14-05 — ver
  "Cancelamento de taxa e de caução" (Phase 14) em "Decisões de design".
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && grep -q "lancamento_id FK" docs/data-model.md && grep -q "cascade (Fase 14)" docs/data-model.md && grep -qi "reabre D-04 só para cascata" docs/data-model.md && grep -c "on delete cascade" docs/data-model.md | grep -qE '^[1-9]' && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - A aplicação (CLI ou SQL Editor) terminou sem erro; se terminou com erro, o arquivo foi corrigido e
      o ensaio do plano 14-02 foi refeito antes de nova tentativa
    - O caminho usado (CLI ou SQL Editor) está registrado no SUMMARY
    - `docs/data-model.md` cita `lancamento_id FK` no diagrama `mermaid`, a relação
      `PARCELA_LANCAMENTOS ||--o{ TAXAS_IMOBILIARIA`/`|o--o{` de cascata, e a bullet nova explicando a
      reabertura pontual de D-04
    - Nenhum arquivo fora de `supabase/migrations/20260826000000_taxas_imobiliaria_lancamento_id.sql` e
      `docs/data-model.md` foi modificado por esta task
  </acceptance_criteria>
  <reversibility rating="one-way">Grava DDL no banco de produção. Desfazer exige um `alter table ...
  drop column` manual.</reversibility>
  <done>A migração está aplicada em produção e `docs/data-model.md` documenta a coluna, o índice e a
  reabertura pontual de D-04 para cascata.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar em produção que a coluna nasceu correta e que nada quebrou</name>
  <precondition>A Task 2 aplicou a migração com sucesso.</precondition>
  <read_first>
    - `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` — Parte B, BLOCOs 4 a 6
    - O `RESULTADO DO ENSAIO` registrado no plano 14-02, para comparar os números
  </read_first>
  <what-built>
    `public.taxas_imobiliaria.lancamento_id` (FK para `parcela_lancamentos.id`, `on delete cascade`) e
    `taxas_imobiliaria_lancamento_id_idx`, aplicados no banco de produção.
  </what-built>
  <how-to-verify>
    **No SQL Editor de produção, rodando a Parte B do runbook:**

    1. **BLOCO 4** — a coluna e o índice existem com tipo/nulabilidade corretos.
    2. **BLOCO 5** — `select count(*) from public.taxas_imobiliaria where lancamento_id is not null`
       devolve **0** — nenhuma linha existente ganhou valor por engano.
    3. **BLOCO 6** — `pg_policies` de `taxas_imobiliaria` continua com exatamente 1 linha.

    **No navegador, contra produção:**

    4. **Board** carrega com os mesmos contratos de antes, arrastar entre colunas continua funcionando.
    5. **Financeiro** carrega, "vencendo hoje" continua sendo o padrão, Pagamento/Ajustar/Histórico
       continuam abrindo e gravando (regressão das Phases 6/6.1/7/11/12/13).
    6. **Relatórios/Relatório de imobiliária** (`/relatorios/imobiliaria`) carrega com os mesmos números
       de antes — a coluna nova é invisível para o código atual, que ainda não a lê.
  </how-to-verify>
  <acceptance_criteria>
    - BLOCO 4: coluna/índice com tipo/nulabilidade corretos
    - BLOCO 5: zero linhas com `lancamento_id` preenchido
    - BLOCO 6: `pg_policies` de `taxas_imobiliaria` inalterada (1 linha)
    - Board, Financeiro e os Relatórios (incluindo `/relatorios/imobiliaria`) carregam sem erro, com os
      mesmos dados
    - O resultado é reportado com os números observados, não com um "está tudo certo"
  </acceptance_criteria>
  <resume-signal>Responda com o resultado de cada um dos seis passos, ou com o que travou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| operador → banco de produção | DDL definitiva sobre dado financeiro real; sem staging, sem backup automático |
| navegador → PostgREST | RLS de `taxas_imobiliaria` inalterada — a coluna nova herda a mesma policy `for all` já existente, sem predicado diferente |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08 | Tampering | aplicação da DDL sobre produção | medium | mitigate | `checkpoint:decision` bloqueante antes da aplicação (Task 1), com o resultado do ensaio do 14-02 (incluindo a cascata observada) como base |
| T-14-09 | Elevation of Privilege | policy aplicada com predicado diferente por engano | critical | mitigate | BLOCO 6 lê `pg_policies` no banco já aplicado e exige exatamente 1 linha inalterada; qualquer mudança é motivo de parar |
| T-14-10 | Information Disclosure | dado financeiro novo legível por login fora da allowlist | critical | mitigate | Nenhuma policy nova é criada — a coluna herda a RLS já provada nas fases anteriores |
| T-14-11 | Denial of Service | regressão silenciosa nas telas já existentes | medium | mitigate | Passos 4 a 6 do checkpoint cobrem Board, Financeiro e Relatórios (incluindo `/relatorios/imobiliaria`) à mão, contra produção |
| T-14-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala pacote da aplicação. O único binário externo é o SQL Editor do Supabase, já em uso no projeto |
</threat_model>

<verification>
1. Parte B do runbook rodada contra o banco migrado: coluna, índice, policies
2. Comparação numérica com o baseline do ensaio
3. `grep` sobre `docs/data-model.md` confirmando a coluna, a relação e a bullet de decisão
4. Teste manual no navegador cobrindo Board, Financeiro e Relatórios
</verification>

<success_criteria>
- `public.taxas_imobiliaria.lancamento_id` e `taxas_imobiliaria_lancamento_id_idx` existem em produção
- Zero linhas com `lancamento_id` preenchido, `pg_policies` inalterada
- `docs/data-model.md` documenta a coluna e a reabertura pontual de D-04
- Board, Financeiro e Relatórios seguem funcionando como antes
</success_criteria>

<output>
Create `.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-03-SUMMARY.md` when done
</output>
