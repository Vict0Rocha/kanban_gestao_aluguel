---
phase: 14-cancelamento-de-taxas-e-cau-o
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - supabase/verificacao_taxas_imobiliaria_lancamento_id.sql
autonomous: false
requirements: [CANIMOB-03]

estimate:
  tokens: 18000
  raw_tokens: 18000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Parte A do runbook foi executada contra o banco de produção real de um jeito que garante continuidade de sessão — Parte A inteira num único clique de Run, ou psql/Supabase CLI (D-19). Rodar bloco a bloco no SQL Editor do Studio não conta como ensaio"
    - "O BLOCO 3 pós-`rollback` confirma que `lancamento_id` e `taxas_imobiliaria_lancamento_id_idx` **não** existem mais — se algum sobreviveu, o ensaio virou push e o plano para para reportar"
    - "`taxas_total`, `lancamentos_total`, `cards_total` e `cards_updated_at_max` depois do `rollback` são idênticos aos do BLOCO 1"
    - "A cascata de exclusão (Prova 2.3) foi observada de verdade contra o schema real: criar card+parcela+lançamento+taxa de teste, apagar o lançamento, e confirmar que a linha de taxa some junto — não presumida, observada"
    - "`lancamento_id` nulo continua aceito (Prova 2.4), e a contagem de policies de `taxas_imobiliaria` continua 1 (Prova 2.5)"
    - "A segunda passagem da mesma DDL dentro da transação não errou e não alterou nada (reexecutabilidade provada, não presumida)"
    - "O bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` está preenchido com os números reais observados, os ids usados e o caminho de execução usado (único Run vs. psql/CLI)"
  artifacts:
    - supabase/verificacao_taxas_imobiliaria_lancamento_id.sql
  key_links:
    - "ensaio -> `checkpoint:decision` do plano 14-03: sem o resultado registrado aqui, aquele portão não tem base para decidir"
---

<objective>
Ensaiar a migração `20260826000000_taxas_imobiliaria_lancamento_id.sql` contra o banco de produção
dentro de uma transação revertida, e registrar o resultado real — números, ids usados, e a observação
direta da cascata de exclusão funcionando — no próprio runbook.

Este plano **não** aplica nada. Segue a mesma disciplina que a Fase 6.1 estabeleceu depois de um
acidente real (D-19): rodar um ensaio bloco a bloco, na mesma aba do SQL Editor, já transformou um
ensaio em push real de produção numa fase anterior deste projeto, só percebido comparando `updated_at_max`
antes e depois.

Purpose: o `checkpoint:decision` do plano 14-03 só tem base se este ensaio existir e estiver registrado.
Uma FK com `on delete cascade` é o tipo de objeto que, se estiver errada, ou nunca dispara (deixando
taxas órfãs) ou dispara largo demais — precisa ser vista funcionando de verdade antes de virar
permanente.
Output: `RESULTADO DO ENSAIO` preenchido em `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql`.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `14-01-PLAN.md`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-CONTEXT.md
@supabase/verificacao_taxas_imobiliaria_lancamento_id.sql
@supabase/migrations/20260826000000_taxas_imobiliaria_lancamento_id.sql
@supabase/verificacao_dinheiro_imobiliaria.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rodar a Parte A contra produção e observar a cascata funcionando</name>
  <precondition>`supabase/migrations/20260826000000_taxas_imobiliaria_lancamento_id.sql` e
  `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` existem no repositório (plano 14-01), e
  `taxas_imobiliaria.lancamento_id` ainda não existe no banco de produção.</precondition>
  <read_first>
    - `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` — Parte A inteira, e principalmente o
      aviso de pooling no cabeçalho
    - `supabase/verificacao_dinheiro_imobiliaria.sql` — a seção `RESULTADO DO ENSAIO`, para ver o
      formato do registro que a Task 2 vai reproduzir
  </read_first>
  <what-built>
    O runbook de ensaio (`supabase/verificacao_taxas_imobiliaria_lancamento_id.sql`, Parte A) e a
    migração que ele ensaia (`20260826000000_taxas_imobiliaria_lancamento_id.sql`): a coluna
    `taxas_imobiliaria.lancamento_id` (FK cascade) e o índice correspondente.
  </what-built>
  <how-to-verify>
    **Antes de qualquer coisa — escolha o caminho de execução (D-19). Só existem dois aceitos:**

    - **(a)** Supabase Studio → SQL Editor, colando a **Parte A INTEIRA** (BLOCO 2 do `begin;` até o
      `rollback;`) num **único** clique de "Run". Nunca bloco a bloco.
    - **(b)** `psql` ou Supabase CLI apontando para o banco de produção.

    **Passo 1 — baseline (BLOCO 1, fora de transação).** Anotar `taxas_total`, `lancamentos_total`,
    `cards_total`, `cards_updated_at_max`.

    **Passo 2 — rodar o BLOCO 2 inteiro de uma vez.**

    **Passo 3 — ler cada prova e anotar o resultado real:**
    - 2.1/2.2: a coluna nasce `uuid`/nullable/sem default, e o índice existe
    - 2.3 — **a prova mais importante deste ensaio:** o bloco `do $$` criou card+parcela+lançamento+taxa
      de teste, apagou o lançamento, e a mensagem `OK cascata: ...` apareceu (não a exceção de falha).
      Se a exceção de falha apareceu em vez disso, **pare aqui e reporte** — uma cascata que não
      funciona significa que CANIMOB-03 nunca vai disparar em produção
    - 2.4: o segundo INSERT (sem `lancamento_id`) foi aceito normalmente
    - 2.5: a contagem de policies de `taxas_imobiliaria` continua 1
    - 2.6: `max(updated_at)` de `cards` igual ao do Passo 1 (ignorando o card de teste, que desaparece
      no `rollback`)

    **Passo 4 — rodar o BLOCO 3 (pós-`rollback`) e conferir a coisa mais importante do ensaio inteiro:**
    a coluna e o índice não podem existir mais, e os quatro números do Passo 1 têm de estar idênticos.
    Se qualquer um sobreviveu, o `rollback` não pegou — o ensaio virou push. Nesse caso **não** siga para
    a Task 2 como se tudo tivesse dado certo: registre o acidente com honestidade e reporte antes de
    continuar.
  </how-to-verify>
  <acceptance_criteria>
    - O caminho de execução usado está declarado: "(a) único Run" ou "(b) psql/CLI"
    - Os quatro números do baseline estão anotados
    - A Prova 2.3 (cascata) foi observada com sucesso — mensagem `OK cascata`, não a exceção de falha
    - As Provas 2.1/2.2/2.4/2.5/2.6 têm resultado observado registrado, não presumido
    - Depois do `rollback`, a coluna e o índice não existem e os quatro números batem com o baseline
  </acceptance_criteria>
  <reversibility rating="reversible">O ensaio inteiro vive dentro de `begin; ... rollback;` e não deixa
  rastro no banco — desde que o caminho de execução do D-19 seja respeitado. É exatamente essa condição
  que o Passo 4 verifica.</reversibility>
  <resume-signal>Responda com os resultados dos Passos 1 a 4, ou com o que travou.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar o resultado do ensaio dentro do próprio runbook</name>
  <precondition>A Task 1 foi executada e os resultados de todas as provas estão disponíveis na
  conversa.</precondition>
  <files>supabase/verificacao_taxas_imobiliaria_lancamento_id.sql</files>
  <read_first>
    - `supabase/verificacao_dinheiro_imobiliaria.sql` — a seção `RESULTADO DO ENSAIO`: contexto
      primeiro, depois os números, depois as lições. É esse o formato a repetir
    - `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` — o cabeçalho `RESULTADO DO ENSAIO`
      vazio deixado pela Task 2 do plano 14-01
  </read_first>
  <action>
Preencher o bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql`
com o que a Task 1 realmente observou. Escrever em comentário SQL, em português, no mesmo tom dos
registros anteriores deste projeto: contexto primeiro, números depois, e o que ficou aprendido no fim.

Conteúdo obrigatório:
- a data do ensaio e qual dos dois caminhos de D-19 foi usado — não deixar isso implícito
- os quatro números do baseline e os quatro depois do `rollback`, lado a lado
- o resultado literal da Prova 2.3 (cascata) — a mensagem exata observada, não um resumo genérico
  "funcionou"
- o resultado das demais provas (2.1, 2.2, 2.4, 2.5, 2.6)
- a confirmação explícita de que a coluna e o índice deixaram de existir depois do `rollback` — ou, se
  não deixaram, o registro honesto do que aconteceu e o que foi feito a respeito
- qualquer divergência entre o que o runbook previa e o que o banco respondeu, inclusive correção feita
  na hora; se o arquivo de migração ou o runbook precisou ser corrigido, dizer o quê e por quê

Se alguma correção foi feita em `20260826000000_taxas_imobiliaria_lancamento_id.sql` durante o ensaio, o
ensaio precisa ser refeito por inteiro com o arquivo corrigido antes de este bloco ser dado como fechado
— registrar quantas rodadas foram necessárias.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f=supabase/verificacao_taxas_imobiliaria_lancamento_id.sql && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "taxas_total" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qiE "psql|CLI|único Run|unico Run" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "cascata" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | wc -l | awk '$1 > 12 {print "ENSAIO_REGISTRADO"; exit} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - A seção `RESULTADO DO ENSAIO` tem mais de 12 linhas, cita `taxas_total` e cita explicitamente o
      resultado da cascata
    - O caminho de execução (único Run ou psql/CLI) está nomeado por escrito
    - Está escrito, em uma frase direta, se a coluna/índice sobreviveram ou não ao `rollback`
    - Nenhum arquivo além de `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` foi modificado
      por esta task
  </acceptance_criteria>
  <done>O runbook carrega o registro do ensaio real, com números, o resultado observado da cascata,
  caminho de execução e o veredito sobre o `rollback`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| operador → banco de produção | Este plano executa SQL contra o banco real. A única coisa que separa "ensaio" de "push" é a continuidade da sessão que envolve o `begin;`/`rollback;` |
| runbook → operador | O texto do runbook é a única instrução que o operador segue; uma instrução errada aqui já causou perda de controle uma vez (Fase 6.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05 | Tampering | ensaio que commita DDL em produção (pool de conexões do Studio) | high | mitigate | D-19 aplicado literalmente: só dois caminhos aceitos; o Passo 4 checa `information_schema`/`pg_indexes` depois do `rollback` e manda parar se algo sobreviveu |
| T-14-06 | Tampering | card/parcela/lançamento/taxa de teste da Prova 2.3 sobrevivendo fora da transação | medium | mitigate | Toda gravação de teste vive dentro de `begin;...rollback;`; o Passo 4 confirma `cards_total` idêntico ao baseline |
| T-14-07 | Repudiation | ensaio não registrado, decisão do 14-03 sem base | medium | mitigate | Task 2 grava números, ids e caminho de execução dentro do próprio runbook, versionado no git |
| T-14-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala nenhum pacote. Se o operador optar por instalar o Supabase CLI para o caminho (b), é ferramenta oficial do fornecedor já usada no projeto |
</threat_model>

<verification>
- Observação direta do resultado de cada prova contra o banco de produção, dentro de transação
  revertida
- Comparação numérica antes/depois do `rollback`
- Checagem de `information_schema`/`pg_indexes` depois do `rollback` — a prova de que o ensaio foi
  ensaio
- `grep` sobre o bloco `RESULTADO DO ENSAIO` confirmando que o registro existe e nomeia o caminho de
  execução e o resultado da cascata
</verification>

<success_criteria>
- O ensaio rodou por um caminho que garante continuidade de sessão, e isso está escrito
- A cascata de exclusão (Prova 2.3) foi observada funcionando de verdade
- Nada persistiu no banco: coluna/índice ausentes e quatro números idênticos depois do `rollback`
- `supabase/verificacao_taxas_imobiliaria_lancamento_id.sql` carrega o registro completo, commitado no
  git
</success_criteria>

<output>
Create `.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-02-SUMMARY.md` when done
</output>
