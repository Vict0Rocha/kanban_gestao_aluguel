---
phase: 14-cancelamento-de-taxas-e-cau-o
plan: 05
type: execute
wave: 5
depends_on: ["14-04"]
files_modified:
  - web/src/lib/kanban/actions.ts
  - web/src/lib/kanban/queries.ts
  - web/src/components/financeiro/cancelar-lancamento-dialog.tsx
  - web/src/components/financeiro/caucao-historico-sheet.tsx
  - docs/data-model.md
autonomous: false
requirements: [CANIMOB-04, CANIMOB-05]

estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No histórico de caução (`CaucaoHistoricoSheet`, ordem ascendente), o botão 'Cancelar' aparece SÓ no último item do array `eventos` (`index === eventos.length - 1`) — nunca no evento mais antigo nem em todos ao mesmo tempo (D-05, Pitfall 3 de 14-RESEARCH.md)"
    - "`cancelarEventoCaucaoAction` reconfirma no SERVIDOR, a cada chamada, que o `eventoId` recebido é de fato o mais recente (`order by criado_em desc limit 1`) antes de apagar — nunca confia que o botão só apareceu no evento certo na tela (mesmo princípio de toda trava financeira do arquivo)"
    - "Cancelar o evento mais recente de caução libera o que ficou no novo topo para ser cancelado em seguida — o ciclo inteiro pode ser desfeito, evento por evento, sempre a partir do topo, nunca pulando para o meio (D-05, CANIMOB-04)"
    - "`cancelarEventoCaucaoAction` nunca toca `parcela_lancamentos` nem `taxas_imobiliaria` — `caucao_eventos` continua uma terceira tabela estruturalmente isolada (D-06, 13-CONTEXT.md)"
    - "O diálogo de confirmação de caução reusa `CancelarLancamentoDialog` (`acao=\"caucao\"`) — mesmo padrão sem motivo obrigatório, DELETE real, já usado para lançamento e taxa (D-06, CANIMOB-05)"
    - "`docs/data-model.md` documenta o mecanismo de cancelamento de taxa e de caução desta fase — sem contradizer a bullet existente de caução append-only (Phase 13): cada evento continua nascendo como linha nova, nunca editado; o que muda é a possibilidade de apagar o topo atual, sequencialmente"
  artifacts:
    - "web/src/lib/kanban/actions.ts — `cancelarEventoCaucaoAction`"
    - "web/src/lib/kanban/queries.ts — `cancelarEventoCaucao`"
    - docs/data-model.md
  key_links:
    - "`CaucaoHistoricoSheet` (botão 'Cancelar' só no último `evento`) -> `CancelarLancamentoDialog` (`acao=\"caucao\"`, `parentId=cardId`, `itemId=evento.id`) -> `cancelarEventoCaucao` (queries.ts) -> `cancelarEventoCaucaoAction` (actions.ts) -> SELECT mais recente + DELETE condicionado -> `router.refresh()`"
  prohibitions:
    - "MUST NOT permitir que `cancelarEventoCaucaoAction` apague um evento sem antes reconfirmar, no servidor, que ele é o mais recente por `criado_em` — a checagem client-side (índice do array) é só UX, nunca a trava real (D-05)"
    - "MUST NOT criar uma função RPC/SQL nova para a checagem atômica — mesmo padrão de duas etapas (SELECT + DELETE condicionado) que `destravarParcelaAction` já usa e já documenta a janela de corrida tolerada"
---

<objective>
Fechar a fase: o histórico de caução ganha cancelamento, mas só do evento mais recente por vez —
cancelar o topo libera o que sobrou no novo topo, permitindo desfazer o ciclo inteiro (recebido → uso
parcial → devolução, por exemplo) sequencialmente, sempre a partir do topo, nunca de um evento do meio.

Este plano reusa integralmente o diálogo de confirmação já generalizado no plano 14-04
(`CancelarLancamentoDialog`, `acao="lancamento" | "taxa"`), ampliando o union para `"caucao"` — nenhum
diálogo novo é criado (D-06, CANIMOB-05). A trava "sou eu o mais recente" é reconfirmada no servidor a
cada chamada, no mesmo padrão de duas etapas (SELECT + DELETE condicionado) que `destravarParcelaAction`
já usa e já documenta a janela de corrida tolerada.

Purpose: CANIMOB-04 (cancelamento sequencial de caução, sempre a partir do topo) e CANIMOB-05 (mesmo
diálogo, agora cobrindo os três domínios: lançamento, taxa e caução). Com isso encerram as cinco fases
de requisitos desta fase (CANIMOB-01 a CANIMOB-05).
Output: `cancelarEventoCaucaoAction` nova, `CaucaoHistoricoSheet` com botão "Cancelar" só no topo,
`CancelarLancamentoDialog` ampliado para `acao="caucao"`, `docs/data-model.md` documentando o mecanismo,
e a verificação final de toda a fase em produção.
</objective>

Este plano não repete a lista completa de artefatos da fase — ver `## Artifacts this phase produces` em
`14-01-PLAN.md`. Os símbolos que este plano especificamente cria/altera: `cancelarEventoCaucaoAction`/
`cancelarEventoCaucao` (novas); `CancelarLancamentoDialog` (`acao` ampliado para incluir `"caucao"`);
`CaucaoHistoricoSheet` (botão "Cancelar" só no último evento); `docs/data-model.md` (bullet nova
"Cancelamento de taxa e de caução").

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-CONTEXT.md
@.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-RESEARCH.md
@.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-PATTERNS.md
@web/src/lib/kanban/actions.ts
@web/src/components/financeiro/caucao-historico-sheet.tsx
@web/src/components/financeiro/cancelar-lancamento-dialog.tsx
</context>

<planner_assumptions>
- **A-01 — "sou eu o mais recente" é sempre `eventos[eventos.length - 1]` no cliente** (ordem
  ascendente já usada por `CaucaoHistoricoSheet`), **nunca `eventos[0]`** — a inversão desses dois
  índices é exatamente o Pitfall 3 de 14-RESEARCH.md. No servidor, a mesma pergunta é respondida do
  zero, por `order by criado_em desc limit 1`, sem depender do array que o cliente mandou.
- **A-02 — o rótulo de caução no diálogo usa o mesmo separador " · " de taxa** (`Caução · Recebida`),
  por consistência visual entre os dois rótulos "compostos" que o diálogo generalizado agora recebe —
  discrição do pattern-mapper/planner, deixada em aberto por D-06 (14-CONTEXT.md).
- **A-03 — a posição do wrapper `cancelarEventoCaucao` em `queries.ts`** fica ao lado de
  `registrarEventoCaucao` (agrupamento por funcionalidade, não ordem alfabética — mesmo padrão já
  observado no restante do arquivo, ex.: `conciliarParcela`/`destravarParcela`/`cancelarLancamento`
  agrupados por fluxo, não por ordem alfabética). O *import* de `./actions`, esse sim, segue ordem
  alfabética estrita (convenção já estabelecida): `cancelarEventoCaucaoAction` entra entre
  `arquivarCardAction` e `cancelarLancamentoAction` ("E" antes de "L").
</planner_assumptions>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Cancelar evento de caução mais recente — Server Action + diálogo ampliado + wiring (CANIMOB-04, CANIMOB-05)</name>

  <files>
web/src/lib/kanban/actions.ts
web/src/lib/kanban/queries.ts
web/src/components/financeiro/cancelar-lancamento-dialog.tsx
web/src/components/financeiro/caucao-historico-sheet.tsx
  </files>

  <reversibility rating="one-way">O DELETE que `cancelarEventoCaucaoAction` executa é real e
  irreversível, mesmo padrão já confirmado pelo usuário em Phase 11/12 e repetido no plano 14-04 para
  taxa. D-06 (14-CONTEXT.md) pede explicitamente "mesmo diálogo... já usado" — nenhum `checkpoint:decision`
  novo é inserido, o trade-off já foi atravessado com consentimento registrado antes.</reversibility>

  <read_first>
    - `.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-RESEARCH.md` — "Pattern 3: Cancelar evento de
      caução mais recente" (código de exemplo completo, já resolvido) e "Pitfall 3" (o risco de inverter
      `eventos[0]`/`eventos[length-1]`)
    - `web/src/lib/kanban/actions.ts` linhas 1321-1396 (`destravarParcelaAction` — o molde de checagem em
      duas etapas: SELECT decide, DELETE/UPDATE confia na leitura recente por uma janela pequena) e as
      linhas do plano 14-04 (`cancelarTaxaImobiliariaAction` — a forma do DELETE condicionado com dois
      `.eq()`)
    - `web/src/lib/kanban/queries.ts` — bloco de import de `./actions` (ordem alfabética) e os wrappers
      `registrarEventoCaucao`/`cancelarTaxaImobiliaria` (os moldes a copiar)
    - `web/src/components/financeiro/cancelar-lancamento-dialog.tsx` — versão já generalizada pelo plano
      14-04 (`parentId`/`itemId`/`rotulo`/`acao: "lancamento" | "taxa"`) — este plano só AMPLIA o union,
      não recria o componente
    - `web/src/components/financeiro/caucao-historico-sheet.tsx` — arquivo inteiro (135 linhas): o
      comentário de cabeçalho (linhas 26-33, "Sem botão de cancelar... caução é append-only sem
      mecanismo de cancelamento nesta fase" — comentário que esta fase invalida), o `.map` (linhas
      77-100), o rodapé com `RegistrarEventoCaucaoDialog` (linhas 123-132)
    - `web/src/components/financeiro/caucao-evento-label.tsx` — `CAUCAO_TIPO` (linhas 14-30, o mapa de
      rótulo a reusar para o texto do diálogo, `CAUCAO_TIPO[tipo].label`)
    - Esta seção `<planner_assumptions>` (A-01 a A-03)
  </read_first>

  <action>
**1. `web/src/lib/kanban/actions.ts`** — nova Server Action `cancelarEventoCaucaoAction`, logo depois de
`registrarEventoCaucaoAction` (antes do comentário "Relatório financeiro"):
```typescript
/**
 * CANIMOB-04/D-05 (14-CONTEXT.md): só o evento mais recente de caução pode
 * ser cancelado por vez — cancelar o mais recente libera o que sobrou no
 * novo topo, permitindo desfazer o ciclo inteiro sequencialmente, nunca um
 * evento do meio. A checagem "sou eu o mais recente" é reconfirmada aqui,
 * no servidor, a cada chamada — nunca confia que o botão só apareceu no
 * evento certo na tela (mesmo princípio de toda trava financeira deste
 * arquivo).
 *
 * Duas etapas (SELECT decide, DELETE confia na leitura recente por uma
 * janela pequena) — mesmo padrão de `destravarParcelaAction` acima, que já
 * documenta essa janela de corrida tolerada. Nunca toca
 * `parcela_lancamentos`/`taxas_imobiliaria` — `caucao_eventos` continua uma
 * terceira tabela isolada (D-06, 13-CONTEXT.md).
 */
export async function cancelarEventoCaucaoAction(
  cardId: string,
  eventoId: string
): Promise<ActionResult> {
  const sessao = await requireUser()
  if (!sessao) return { ok: false, error: NAO_AUTENTICADO }

  const invalido = id(cardId, "Imóvel") ?? id(eventoId, "Evento de caução")
  if (invalido) return { ok: false, error: invalido }

  const { data: maisRecente, error: erroLeitura } = await sessao.supabase
    .from("caucao_eventos")
    .select("id")
    .eq("card_id", cardId)
    .order("criado_em", { ascending: false })
    .limit(1)
    .maybeSingle()

  if (erroLeitura || !maisRecente) {
    console.error("cancelarEventoCaucao (leitura)", erroLeitura)
    return { ok: false, error: erroDoBanco(erroLeitura?.code, "cancelar o evento de caução") }
  }
  if (maisRecente.id !== eventoId) {
    return {
      ok: false,
      error: "Este não é mais o evento mais recente de caução — atualize a página.",
    }
  }

  const { data, error } = await sessao.supabase
    .from("caucao_eventos")
    .delete()
    .eq("id", eventoId)
    .eq("card_id", cardId)
    .select("id")

  if (error) {
    console.error("cancelarEventoCaucao (delete)", error)
    return { ok: false, error: erroDoBanco(error.code, "cancelar o evento de caução") }
  }
  if (!data || data.length === 0) {
    return { ok: false, error: semLinhas("cancelar o evento de caução") }
  }

  return { ok: true, data: undefined }
}
```

**2. `web/src/lib/kanban/queries.ts`** — importar `cancelarEventoCaucaoAction` de `./actions` (posição
alfabética: entre `arquivarCardAction` e `cancelarLancamentoAction`, já que "E" vem antes de "L").
Acrescentar o wrapper logo depois de `registrarEventoCaucao` (A-03):
```typescript
export async function cancelarEventoCaucao(cardId: string, eventoId: string) {
  return unwrap(cancelarEventoCaucaoAction(cardId, eventoId))
}
```

**3. `web/src/components/financeiro/cancelar-lancamento-dialog.tsx`** — ampliar (não recriar):
- Trocar `acao: "lancamento" | "taxa"` por `acao: "lancamento" | "taxa" | "caucao"`.
- Importar `cancelarEventoCaucao` ao lado de `cancelarLancamento`/`cancelarTaxaImobiliaria`
  (`@/lib/kanban/queries`).
- `handleConfirm`: acrescentar o terceiro ramo — `else if (acao === "caucao") await
  cancelarEventoCaucao(parentId, itemId)` — antes do `else` genérico (`cancelarLancamento`), que passa a
  cobrir só `acao === "lancamento"`.
- `descricaoEfeito`: acrescentar o terceiro caso — `acao === "caucao" ? "O evento de caução é apagado.
  Cancelar este evento libera o cancelamento do que ficou mais recente." : ...` (mantendo os dois casos
  já existentes de `lancamento`/`taxa` do plano 14-04 sem mudança).

**4. `web/src/components/financeiro/caucao-historico-sheet.tsx`** — estender:
- Importar `Trash2` de `lucide-react`, `CancelarLancamentoDialog` de
  `@/components/financeiro/cancelar-lancamento-dialog`, `CAUCAO_TIPO` de
  `@/components/financeiro/caucao-evento-label` (já importa `CaucaoEventoLabel` de lá — acrescentar
  `CAUCAO_TIPO` ao mesmo import), e `Button` (já importado).
- Novo estado: `const [cancelando, setCancelando] = React.useState<CaucaoEventoDetalhado | null>(null)`.
- No `.map`, trocar `eventos.map((evento) => {` por `eventos.map((evento, index) => {` e calcular `const
  ultimo = index === eventos.length - 1` (A-01: array em ordem ASCENDENTE, o mais recente é sempre o
  ÚLTIMO índice, nunca o primeiro).
- Dentro do `<li>`, depois do bloco de `observacao` (linhas 95-97 do arquivo atual), acrescentar o botão
  condicional ao `ultimo`:
  ```tsx
  {ultimo && (
    <div className="flex justify-end">
      <Button variant="ghost" size="xs" onClick={() => setCancelando(evento)}>
        <Trash2 className="size-3" />
        Cancelar
      </Button>
    </div>
  )}
  ```
- Depois de `<RegistrarEventoCaucaoDialog>` (linhas 123-132), acrescentar a instância do diálogo
  generalizado:
  ```tsx
  <CancelarLancamentoDialog
    parentId={cardId}
    itemId={cancelando?.id ?? ""}
    rotulo={cancelando ? `Caução · ${CAUCAO_TIPO[cancelando.tipo].label}` : ""}
    acao="caucao"
    valor={cancelando?.valor ?? 0}
    data={cancelando?.data ?? ""}
    open={cancelando !== null}
    onOpenChange={(open) => { if (!open) setCancelando(null) }}
  />
  ```
- Atualizar o comentário de cabeçalho do componente (linhas 26-33) que hoje diz "Sem botão de cancelar
  por evento — caução é append-only sem mecanismo de cancelamento nesta fase." — trocar por uma frase
  citando que a Phase 14 (D-05, CANIMOB-04) acrescentou cancelamento sequencial a partir do evento mais
  recente, sem contradizer o restante do parágrafo (a leitura direto da prop `eventos`, sem
  `useEffect`/fetch próprio, e a ordem ascendente continuam exatamente como estão).
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && f1="web/src/lib/kanban/actions.ts" && f2="web/src/lib/kanban/queries.ts" && f3="web/src/components/financeiro/cancelar-lancamento-dialog.tsx" && f4="web/src/components/financeiro/caucao-historico-sheet.tsx" && grep -q "export async function cancelarEventoCaucaoAction" "$f1" && body=$(awk '/export async function cancelarEventoCaucaoAction/,/^}/' "$f1") && echo "$body" | grep -q 'order("criado_em", { ascending: false })' && echo "$body" | grep -q "maisRecente.id !== eventoId" && echo "$body" | grep -q '\.eq("id", eventoId)' && echo "$body" | grep -q '\.eq("card_id", cardId)' && ! echo "$body" | grep -qE "parcela_lancamentos|taxas_imobiliaria" && grep -q "export async function cancelarEventoCaucao(" "$f2" && grep -q 'acao: "lancamento" | "taxa" | "caucao"' "$f3" && grep -q 'acao === "caucao"' "$f3" && grep -q "cancelarEventoCaucao(parentId, itemId)" "$f3" && grep -q "eventos.map((evento, index)" "$f4" && grep -q "index === eventos.length - 1" "$f4" && grep -q "CancelarLancamentoDialog" "$f4" && grep -q 'acao="caucao"' "$f4" && echo CANCELAR_CAUCAO_OK</automated>
  </verify>

  <acceptance_criteria>
    - `npm run lint` e `npm run build` (em `web/`) terminam com código 0
    - `cancelarEventoCaucaoAction` reconfirma "mais recente" via `order by criado_em desc limit 1`,
      recusa se `eventoId` não bater, condiciona o DELETE a dois `.eq()`, e nunca referencia
      `parcela_lancamentos`/`taxas_imobiliaria` no corpo
    - `queries.ts` exporta `cancelarEventoCaucao`
    - `cancelar-lancamento-dialog.tsx` aceita `acao="caucao"` e despacha para `cancelarEventoCaucao`
    - `caucao-historico-sheet.tsx` calcula `ultimo` por índice (`index === eventos.length - 1`, nunca
      `index === 0`) e só mostra "Cancelar" nesse item
  </acceptance_criteria>

  <done>O evento de caução mais recente pode ser cancelado (mesmo diálogo, sem motivo), com a checagem
  "sou eu o mais recente" reconfirmada no servidor a cada chamada — nunca um evento do meio.</done>
</task>

<task type="auto">
  <name>Task 2: Documentar o mecanismo de cancelamento de taxa e de caução em `docs/data-model.md`</name>

  <files>docs/data-model.md</files>

  <read_first>
    - `docs/data-model.md` linha 153 (bullet "A fronteira estrutural entre `taxas_imobiliaria`..." —
      ampliada pelo plano 14-03) e linha 154 (bullet "Caução é histórico, não coluna (D-06, Phase 13)" —
      a claim "não existe forma de editar" continua literalmente verdadeira, cancelamento é DELETE do
      topo, não edição; ainda assim vale um ponteiro para a nova capacidade)
    - `.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-CONTEXT.md` — D-02, D-04, D-05
  </read_first>

  <action>
Em `docs/data-model.md`, na bullet "Caução é histórico, não coluna (D-06, Phase 13)" (linha ~154),
acrescentar ao final uma frase curta: "(cancelamento sequencial a partir do evento mais recente foi
acrescentado na Phase 14 — ver [Cancelamento de taxa e de caução](#decisões-de-design) — sem contradizer
o parágrafo acima: cada evento continua nascendo como linha nova, nunca editado; o que muda é a
possibilidade de apagar o topo atual.)" — sem reescrever nem remover nenhuma outra parte da bullet.

Acrescentar uma bullet NOVA em "Decisões de design", logo depois da bullet
"`taxas_imobiliaria.lancamento_id` reabre D-04 só para cascata (D-03, Phase 14)" (adicionada pelo plano
14-03): "**Cancelamento de taxa e de caução (D-02/D-04/D-05, Phase 14)**" — cobrindo, num único
parágrafo: (1) uma taxa da imobiliária ganha seu próprio botão "Cancelar" no histórico da parcela
(`cancelarTaxaImobiliariaAction`), com a mesma trava de parcela conciliada e sem nunca chamar
`recalcularEGravarStatus` — cancelar uma taxa nunca mexe no valor devido/pago/status da parcela; (2)
cancelar o pagamento que gerou aquela taxa (Phase 11/12) agora também remove a taxa automaticamente, via
`on delete cascade` na FK `lancamento_id` (ver bullet acima) — nenhum código novo em
`cancelarLancamentoAction`; (3) o histórico de caução (`caucao_eventos`, append-only desde a Phase 13,
D-06) ganha `cancelarEventoCaucaoAction`, mas só sobre o evento mais recente por vez — o servidor relê
`order by criado_em desc limit 1` e recusa se o `id` recebido não bater com o mais recente, mesmo padrão
de checagem em duas etapas que `destravarParcelaAction` já usa. Cancelar o mais recente libera o que
ficou no novo topo, permitindo desfazer o ciclo inteiro, evento por evento, sempre a partir do topo —
nunca um evento do meio. Fechar citando que as três operações usam o mesmo diálogo de confirmação
simples (`CancelarLancamentoDialog`, generalizado por uma prop `rotulo: string` e `acao: "lancamento" |
"taxa" | "caucao"` em vez de um union de tipo específico), sem motivo obrigatório, DELETE real, mesmo
padrão já estabelecido em Phase 11/12.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f="docs/data-model.md" && grep -q "Cancelamento de taxa e de caução" "$f" && grep -qi "cancelarEventoCaucaoAction" "$f" && grep -qi "cancelarTaxaImobiliariaAction" "$f" && grep -qi "sempre a partir do topo" "$f" && grep -q "cancelamento sequencial a partir do evento mais recente" "$f" && grep -c "não existe (e não deve existir) forma de editar" "$f" | grep -qE '^[1-9]' && echo DOCS_CANIMOB_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/data-model.md` tem uma bullet nova "Cancelamento de taxa e de caução (D-02/D-04/D-05, Phase
      14)" citando as três operações (taxa isolada, cascata, caução sequencial) e o diálogo generalizado
    - A bullet de caução (Phase 13) continua afirmando que nenhum evento é editado, com um ponteiro
      curto para a nova capacidade de cancelamento sequencial — sem contradição
    - Nenhuma bullet ou seção pré-existente do arquivo é removida ou reescrita além do texto ampliado
      descrito acima
  </acceptance_criteria>
  <reversibility rating="reversible">Mudança só em documentação — reverter o commit não afeta nenhum
  dado nem comportamento do sistema.</reversibility>
  <done>`docs/data-model.md` documenta o mecanismo de cancelamento de taxa e de caução desta fase, sem
  contradizer as decisões já registradas na Phase 13.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verificação final da fase — cancelamento sequencial de caução e CANIMOB-01..05 em produção</name>

  <files>(nenhum arquivo é alterado — verificação operacional)</files>

  <read_first>
    - `.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-CONTEXT.md` — D-05 (a regra de sequência,
      completa) e as frases-guia do usuário na seção `<specifics>`
    - `.planning/ROADMAP.md` — Phase 14, os 5 critérios de sucesso
  </read_first>

  <what-built>
Botão "Cancelar" no histórico de caução, mas só no evento mais recente por vez — cancelar libera o que
sobrou no novo topo, permitindo desfazer o ciclo inteiro (recebido → uso → devolução, por exemplo)
sequencialmente. Com os planos 14-01 a 14-04, esta task também fecha a verificação de toda a Phase 14:
taxa visível e cancelável no histórico da parcela, cascata pagamento→taxa, e agora caução sequencial —
os cinco critérios de sucesso do ROADMAP.
  </what-built>

  <how-to-verify>
**Passo 1 — três (ou mais) eventos de caução, um contrato de teste.** Em `/financeiro/configuracao`,
abra "Caução" de um contrato de teste. Registre: recebido R$1.000, uso parcial R$300, devolução dos
R$700 restantes — três eventos distintos. Confirme que **só o último** (a devolução) mostra o botão
"Cancelar" — nem o recebido nem o uso mostram, mesmo estando mais acima na lista visualmente (ordem
ascendente: recebido no topo da tela, devolução embaixo).

**Passo 2 — cancelar sequencialmente, a partir do topo.** Cancele a devolução (o único com botão).
Confirme: o diálogo mostra "Cancelar caução · devolvida?" (ou o rótulo certo), sem campo de motivo. Após
confirmar, a devolução some da lista e agora o **uso parcial** (novo último item) passa a mostrar
"Cancelar". Cancele o uso — agora o **recebido** (novo e único item restante) mostra "Cancelar". Cancele
o recebido — a lista fica vazia e o rodapé volta a mostrar "Registrar caução recebida".

**Passo 3 — nunca um evento do meio.** Repita o Passo 1 (recebido + uso + devolução). Tente, via
DevTools/requisição direta ou simplesmente observando a tela, confirmar que NENHUM botão "Cancelar"
aparece no evento de uso (o do meio) enquanto a devolução (topo) ainda existir.

**Passo 4 — confirmação final de CANIMOB-01..05 (fecha a fase).**
- CANIMOB-01: taxa aparece na lista cronológica da parcela, com rótulo (reconfirme rapidamente numa
  parcela qualquer)
- CANIMOB-02: taxa tem botão "Cancelar" próprio, bloqueado em parcela conciliada
- CANIMOB-03: cancelar um pagamento remove a taxa vinculada (já confirmado no plano 14-04 — reconfirme
  que continua valendo)
- CANIMOB-04: caução só cancela do topo, sequencialmente (Passos 1-3 acima)
- CANIMOB-05: os três diálogos (lançamento/taxa/caução) usam a mesma estrutura visual, sem motivo
  obrigatório

**Passo 5 — o relatório de reconciliação reflete tudo automaticamente.** Em `/relatorios/imobiliaria`,
confirme que os eventos de caução cancelados no Passo 2 não aparecem mais na lista nem nos totais — sem
nenhum código novo ali (13-CONTEXT.md/14-CONTEXT.md já previam isso).
  </how-to-verify>

  <acceptance_criteria>
    - Passo 1: só o último evento (ordem ascendente) mostra "Cancelar"
    - Passo 2: cancelar o topo libera o novo topo, sequencialmente, até a lista esvaziar
    - Passo 3: nenhum evento do meio mostra "Cancelar" enquanto o topo existir
    - Passo 4: os cinco critérios de sucesso do ROADMAP (Phase 14) confirmados
    - Passo 5: `/relatorios/imobiliaria` reflete os cancelamentos sem código novo
    - O operador registrou os resultados observados no SUMMARY
  </acceptance_criteria>

  <resume-signal>Responda "aprovado" com os resultados observados de cada passo, ou descreva qual passo
  falhou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| navegador → `cancelarEventoCaucaoAction` | o único ponto de DELETE de `caucao_eventos`; a checagem "sou eu o mais recente" é reconfirmada no servidor, nunca confiada ao índice do array no cliente |
| leitura (SELECT mais recente) → escrita (DELETE) | janela de corrida pequena, mesmo trade-off já aceito por `destravarParcelaAction` — não eliminada, mitigada por reconfirmar a cada chamada |
| `caucao_eventos` → `parcela_lancamentos`/`taxas_imobiliaria` | a fronteira de D-06 (13-CONTEXT.md): `cancelarEventoCaucaoAction` nunca toca as outras duas tabelas |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-17 | Tampering (TOCTOU) | corrida entre o SELECT "mais recente" e o DELETE de `cancelarEventoCaucaoAction` | medium | mitigate | Janela pequena, mesma ordem de grandeza já tolerada por `destravarParcelaAction` (equipe pequena, baixa concorrência real); mitigada por reconfirmar no servidor a cada chamada, nunca confiar no cliente |
| T-14-18 | Tampering | cliente tenta cancelar um evento do meio passando o `eventoId` errado diretamente à Server Action, fora da UI | high | mitigate | `cancelarEventoCaucaoAction` recusa com `error` explícito se `eventoId` não bater com o `id` do SELECT "mais recente" — a trava é sempre no servidor, nunca só a ausência do botão na tela |
| T-14-19 | Tampering | inversão de índice no cliente (`eventos[0]` em vez de `eventos[eventos.length-1]`) faz o botão aparecer no evento errado | medium | mitigate | Pitfall 3 (14-RESEARCH.md) documentado explicitamente; asserção de fonte no `<verify>` confirma `index === eventos.length - 1`, nunca `index === 0` |
| T-14-20 | Elevation of Privilege | escrita em `caucao_eventos` sem sessão do usuário | high | mitigate | `cancelarEventoCaucaoAction` usa `sessao.supabase` (via `requireUser()`), nunca `service_role`; RLS de `caucao_eventos` (Phase 13) é a rede de proteção final |
| T-14-21 | Repudiation | cancelamento de evento de caução não deixa rastro de quem cancelou, quando ou por quê | medium | accept | Mesmo trade-off já confirmado pelo usuário em Phase 11/12/14-04, repetido aqui por pedido explícito (D-06, 14-CONTEXT.md) — DELETE real, sem estorno |
| T-14-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala nenhum pacote — `web/package.json` não é modificado |
</threat_model>

<verification>
1. `cd web && npm run lint && npm run build` saem com código 0
2. Asserções de fonte confirmando: `cancelarEventoCaucaoAction` reconfirma "mais recente" antes do
   DELETE, condiciona a dois `.eq()`, nunca toca `parcela_lancamentos`/`taxas_imobiliaria`; o cliente usa
   `eventos.length - 1`, nunca índice 0
3. Produção: cancelamento sequencial de caução observado ponta a ponta (recebido→uso→devolução, três
   cancelamentos em sequência, sempre a partir do topo); os cinco critérios de sucesso do ROADMAP da
   Phase 14 confirmados
</verification>

<success_criteria>
- No histórico de caução, existe um botão "Cancelar" apenas no evento mais recente; cancelar libera o
  cancelamento do que ficou no topo, sequencialmente (CANIMOB-04)
- O diálogo de confirmação de taxa/caução segue o mesmo padrão já existente — sem motivo obrigatório
  (CANIMOB-05)
- `docs/data-model.md` documenta o mecanismo completo sem contradizer a Phase 13
- CANIMOB-01 a CANIMOB-05 confirmados em produção, fechando a Phase 14
</success_criteria>

<output>
Create `.planning/phases/14-cancelamento-de-taxas-e-cau-o/14-05-SUMMARY.md` when done
</output>
