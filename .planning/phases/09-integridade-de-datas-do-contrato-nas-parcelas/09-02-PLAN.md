---
phase: 09-integridade-de-datas-do-contrato-nas-parcelas
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/limpeza_parcelas_orfas.sql
  - docs/data-model.md
autonomous: false
requirements: [INTEG-05]

estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "As parcelas órfãs já confirmadas em produção antes desta fase são removidas por um script SQL revisável (não uma migração) que mostra a lista exata (BLOCO 1, só leitura) antes de qualquer DELETE (BLOCO 2) (D-08)"
    - "O predicado do DELETE do script bate exatamente com o critério de D-02 (status='aberta' E zero parcela_lancamentos E fora do período atual do card), escrito diretamente em SQL — não uma aproximação mais frouxa"
    - "Depois do BLOCO 2 rodar, as órfãs identificadas no BLOCO 1 não existem mais em `parcelas`, e a contagem de parcelas de contratos não relacionados não muda"
    - "`docs/data-model.md` documenta que D-03 ('nada é apagado quando uma parcela deixa de aparecer') foi deliberadamente revertida por D-01 na Phase 9, com o novo critério e o porquê"
  artifacts:
    - supabase/limpeza_parcelas_orfas.sql
    - docs/data-model.md
  key_links:
    - "supabase/limpeza_parcelas_orfas.sql BLOCO 2 -> RLS 'team full access parcelas' (DELETE já coberto, nenhuma policy nova) -- roda na sessão do próprio operador no SQL Editor, mesmo acesso de produção que todo outro runbook deste projeto"
    - "supabase/limpeza_parcelas_orfas.sql -> mesmo critério de parcelaOrfaApagavel (plano 09-01), reescrito em SQL puro"
---

<objective>
Remover, de uma vez, as parcelas órfãs que já existiam em produção antes desta fase (2 contratos de teste, "A" e "outro", confirmadas por SQL numa sessão anterior) via um script revisável no SQL Editor — nunca uma migração automática — e registrar em `docs/data-model.md` que D-03 (Phase 6.2, "nada é apagado") foi deliberadamente revertida por D-01 nesta fase.

Este plano é independente do plano 09-01: não depende do código de `updateCardAction` existir, porque o script SQL reimplementa o mesmo critério de D-02 diretamente em SQL, contra o estado atual do banco. Pode rodar antes, depois ou em paralelo ao 09-01.

Purpose: INTEG-05 — fechar o rastro concreto que motivou a fase (as 27 linhas encontradas na verificação da Phase 8), sem deixar "informação solta" no banco (razão dada pelo próprio usuário em `09-CONTEXT.md`).
Output: `supabase/limpeza_parcelas_orfas.sql` (BLOCO 1 leitura + BLOCO 2 exclusão) e `docs/data-model.md` atualizado.
</objective>

Este plano não cria símbolo de código novo — ver `## Artifacts this phase produces` em `09-01-PLAN.md` para a lista canônica de toda a Phase 9. Depois deste plano, `supabase/limpeza_parcelas_orfas.sql` passa a existir como runbook, e `docs/data-model.md` passa a documentar a reversão de D-03.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-integridade-de-datas-do-contrato-nas-parcelas/09-CONTEXT.md
@.planning/phases/09-integridade-de-datas-do-contrato-nas-parcelas/09-RESEARCH.md
@docs/data-model.md
@supabase/verificacao_financeiro.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever o runbook de limpeza (BLOCO 1 leitura + BLOCO 2 exclusão) e documentar a reversão de D-03</name>
  <files>supabase/limpeza_parcelas_orfas.sql, docs/data-model.md</files>
  <read_first>
    - `supabase/verificacao_financeiro.sql` — o tom e a estrutura BLOCO-numerada a seguir (cabeçalho explicando o runbook, comentário de aviso antes de qualquer bloco sensível)
    - `docs/data-model.md` — a seção "Por que nada é apagado quando uma parcela deixa de aparecer (D-03)" (dentro de "Decisões de design") e o estilo "decisão + porquê" do restante da lista
    - `web/src/lib/kanban/parcelas.ts` — `competenciaNoPeriodo` (linha 156) — o critério a reescrever em SQL puro
    - `.planning/phases/09-integridade-de-datas-do-contrato-nas-parcelas/09-CONTEXT.md` — D-01, D-02, D-08
  </read_first>
  <action>
Crie `supabase/limpeza_parcelas_orfas.sql`, fora de `supabase/migrations/` (D-08 é explícito: nunca uma migração — o operador precisa ver a lista exata antes de qualquer exclusão, e este script comita o DELETE de verdade depois da revisão, ao contrário de todo outro runbook deste projeto que termina em `rollback`). Cabeçalho no mesmo tom de `verificacao_financeiro.sql`, explicando que este é um script de limpeza pontual (D-08), não repetível como parte normal do fluxo — depois de rodado uma vez sobre as órfãs que já existiam, a poda síncrona do plano 09-01 impede acúmulo novo.

**BLOCO 1 — só leitura.** Um `select` sobre `public.parcelas p join public.cards c on c.id = p.card_id`, trazendo `p.id, c.numero, c.endereco, p.competencia, p.status, p.vencimento, p.valor_original`, com `where p.status = 'aberta' and not exists (select 1 from public.parcela_lancamentos pl where pl.parcela_id = p.id) and ((c.periodo_inicio is not null and p.competencia < date_trunc('month', c.periodo_inicio)::date) or (c.periodo_fim is not null and p.competencia > date_trunc('month', c.periodo_fim)::date))` — a tradução direta, em SQL, do mesmo critério de `parcelaOrfaApagavel`/D-02 (status aberta E zero lançamento E fora do período atual do card, nas duas direções de D-03). Ordene por `c.numero, p.competencia` para facilitar a conferência visual. Comente acima do bloco que o operador deve rodar isto sozinho primeiro, conferir a lista contra o que já se sabe (2 contratos de teste, "A" e "outro", ~27 linhas de uma sessão anterior) e só then decidir se segue para o BLOCO 2.

**BLOCO 2 — exclusão real, separado por um comentário de aviso bem visível** (algo como `-- ATENÇÃO: BLOCO DESTRUTIVO. Rode só depois de revisar o BLOCO 1 e ter autorização explícita.`). Um `delete from public.parcelas p using public.cards c where c.id = p.card_id and p.status = 'aberta' and not exists (select 1 from public.parcela_lancamentos pl where pl.parcela_id = p.id) and ((c.periodo_inicio is not null and p.competencia < date_trunc('month', c.periodo_inicio)::date) or (c.periodo_fim is not null and p.competencia > date_trunc('month', c.periodo_fim)::date)) returning p.id, c.numero, p.competencia` — MESMO predicado do BLOCO 1, palavra por palavra (nenhum drift entre o que foi mostrado e o que é apagado). Não envolva este bloco em `begin`/`rollback` — ao contrário de todo outro script deste projeto, a intenção aqui é gravar de verdade depois da revisão humana (D-08).

Depois do BLOCO 2, acrescente um BLOCO 3 — só leitura — de conferência pós-exclusão: contagem total de `parcelas` antes/depois (para o operador colar o número do BLOCO 1 e comparar), e uma repetição do próprio `select` do BLOCO 1 (deve devolver zero linhas se a exclusão funcionou).

Em `docs/data-model.md`, na lista "Decisões de design", logo depois do item existente "Por que nada é apagado quando uma parcela deixa de aparecer (D-03)": acrescente um novo item "**A poda ativa de parcelas órfãs reverte D-03 (D-01, Phase 9)**" explicando que, ao contrário do que o item anterior diz, uma parcela `aberta` sem nenhum `parcela_lancamentos` e fora do período atual do card passa a ser apagada de verdade (não só escondida) quando o período do contrato é editado — critério exato: `status='aberta'` E zero lançamento E fora do novo período, nas duas direções. Explique o porquê (a razão dada pelo usuário: evitar informação solta e desnecessária no banco, depois de 27 parcelas órfãs vazarem nos relatórios da Phase 8) e a consequência aceita (reabrir o período depois gera uma parcela nova, com `valor_original` fotografando o valor atual do card, que pode já divergir do original — mesma consequência que o item de D-03 original já previa para o caso de regenerar). Deixe o item de D-03 original no lugar (não apague), só acrescente o novo item logo depois, deixando claro no texto que ele estreita o que o item anterior descreve.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f=supabase/limpeza_parcelas_orfas.sql && test -f "$f" && grep -q "BLOCO 1" "$f" && grep -q "BLOCO 2" "$f" && grep -q "BLOCO 3" "$f" && grep -qi "not exists" "$f" && grep -q "status = 'aberta'" "$f" && ! grep -qi "rollback;" "$f" && ! grep -qi "^begin;" "$f" && grep -q "date_trunc('month', c.periodo_inicio)" "$f" && grep -q "date_trunc('month', c.periodo_fim)" "$f" && grep -q "D-01" docs/data-model.md && grep -q "reverte D-03" docs/data-model.md && echo LIMPEZA_OK</automated>
  </verify>
  <acceptance_criteria>
    - `supabase/limpeza_parcelas_orfas.sql` existe fora de `supabase/migrations/`
    - O predicado do BLOCO 1 e do BLOCO 2 são idênticos, palavra por palavra
    - O BLOCO 2 não está dentro de `begin;`/`rollback;` — comita de verdade
    - Existe um BLOCO 3 de conferência pós-exclusão, incluindo repetir o `select` do BLOCO 1 (deve devolver zero linhas)
    - `docs/data-model.md` acrescenta o novo item sobre a reversão de D-03, sem apagar o item original
  </acceptance_criteria>
  <reversibility rating="reversible">Esta task só escreve o script e a documentação — nenhum DELETE roda aqui. A Task 3 é quem executa o BLOCO 2 de verdade, atrás do checkpoint da Task 2.</reversibility>
  <done>`supabase/limpeza_parcelas_orfas.sql` existe, pronto para o operador rodar BLOCO 1 e depois BLOCO 2 no SQL Editor; `docs/data-model.md` documenta a reversão de D-03.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Autorizar a exclusão real das parcelas órfãs já existentes em produção</name>
  <precondition>A Task 1 criou `supabase/limpeza_parcelas_orfas.sql`.</precondition>
  <read_first>
    - `supabase/limpeza_parcelas_orfas.sql` — BLOCO 1, o predicado exato
    - `.planning/phases/09-integridade-de-datas-do-contrato-nas-parcelas/09-CONTEXT.md` — D-08
  </read_first>
  <decision>
    Rodar o BLOCO 2 (exclusão real, sem `rollback`) de `supabase/limpeza_parcelas_orfas.sql` contra o banco de produção.
  </decision>
  <context>
    Antes de decidir, rode o BLOCO 1 (só leitura) no SQL Editor de produção e confira a lista contra o que já se sabia de uma sessão anterior: ~27 parcelas órfãs, concentradas em 2 contratos de teste ("A" e "outro"). Se a lista bater (mesma ordem de grandeza, mesmos contratos), o BLOCO 2 apaga exatamente essas linhas. Se a lista vier muito diferente (outros contratos aparecendo, número muito maior), pare e investigue antes de autorizar — pode ser sinal de um contrato real (não de teste) com período editado manualmente no banco, ou de um predicado com bug.

    Esta é porta de mão única: uma vez rodado, o BLOCO 2 não tem `rollback` — comita de verdade, sem staging, sem backup automático (plano gratuito do Supabase). O critério é o mesmo que a poda automática do plano 09-01 vai usar dali pra frente, então rodar isto agora não deixa nenhuma órfã nova reaparecer sem passar pela poda.
  </context>
  <options>
    <option id="aplicar-agora">
      <name>Aplicar agora</name>
      <pros>Fecha de vez o rastro concreto que motivou a fase; a lista do BLOCO 1 já foi conferida contra o que se sabia</pros>
      <cons>Porta de mão única sobre dado real, sem backup automático</cons>
    </option>
    <option id="investigar-divergencia">
      <name>A lista do BLOCO 1 veio diferente do esperado — investigar antes</name>
      <pros>Evita apagar algo que não deveria (ex.: um contrato real com edição legítima recente)</pros>
      <cons>Atrasa o fechamento de INTEG-05</cons>
    </option>
    <option id="adiar">
      <name>Adiar</name>
      <pros>Nada muda hoje</pros>
      <cons>As órfãs continuam no banco, e continuam sendo um risco de vazar em relatórios futuros até alguém rodar o script manualmente</cons>
    </option>
  </options>
  <acceptance_criteria>
    - O usuário rodou o BLOCO 1 e reportou o resultado (contagem e contratos) antes de escolher uma opção
    - Se `aplicar-agora`, a Task 3 roda o BLOCO 2
    - Se `investigar-divergencia`, a Task 3 não roda até a divergência ser explicada e uma nova decisão ser tomada
    - Se `adiar`, a Task 3 não roda e o plano encerra com o motivo registrado
  </acceptance_criteria>
  <reversibility rating="one-way">O BLOCO 2 não tem `rollback` — uma vez rodado, as linhas apagadas não voltam (D-01/D-08).</reversibility>
  <resume-signal>Responda com o resultado do BLOCO 1 (contagem e contratos) e a opção escolhida: `aplicar-agora`, `investigar-divergencia` ou `adiar`.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Rodar o BLOCO 2 e confirmar o resultado</name>
  <precondition>A Task 2 foi respondida com `aplicar-agora`.</precondition>
  <files>supabase/limpeza_parcelas_orfas.sql (executado, não modificado)</files>
  <read_first>
    - `supabase/limpeza_parcelas_orfas.sql` — BLOCO 2 e BLOCO 3
  </read_first>
  <action>
Nenhum arquivo é alterado por esta task. A ação é operacional: instrua/acompanhe o operador rodando o BLOCO 2 de `supabase/limpeza_parcelas_orfas.sql` no SQL Editor de produção (mesma aba/sessão do BLOCO 1, para não depender de estado entre sessões diferentes do pool de conexão — mesmo cuidado documentado em `supabase/verificacao_cards_arquivado_em.sql`), na sequência rodando o BLOCO 3 (conferência pós-exclusão). Registre no SUMMARY o `returning` do BLOCO 2 (quantas linhas, quais `numero`/`competencia`) e o resultado do BLOCO 3 (contagem total antes/depois, e que o `select` repetido do BLOCO 1 devolveu zero linhas).
  </action>
  <verify>
    <human-check>
      <test>Rodar o BLOCO 2 de `supabase/limpeza_parcelas_orfas.sql` no SQL Editor de produção, na mesma aba usada para o BLOCO 1, e em seguida o BLOCO 3.</test>
      <expected>O `returning` do BLOCO 2 lista exatamente as linhas que o BLOCO 1 (Task 2) já tinha mostrado — mesma contagem, mesmos contratos. O BLOCO 3 confirma: a contagem total de `parcelas` caiu pelo mesmo número de linhas apagadas, e o `select` do BLOCO 1 repetido devolve zero linhas. Nenhuma outra tabela (`cards`, `parcela_lancamentos`) muda de contagem.</expected>
      <why_human>DELETE real e definitivo em produção — só o operador tem acesso ao SQL Editor; a confirmação precisa vir do resultado real da query, não de suposição.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - O `returning` do BLOCO 2 bate, linha por linha, com o que o BLOCO 1 já tinha mostrado na Task 2
    - O `select` do BLOCO 1 repetido no BLOCO 3 devolve zero linhas
    - `parcela_lancamentos` e `cards` não mudam de contagem
  </acceptance_criteria>
  <reversibility rating="one-way">Execução real do DELETE autorizado na Task 2. Sem `rollback` — as linhas apagadas não voltam.</reversibility>
  <done>As parcelas órfãs identificadas no BLOCO 1 não existem mais em produção, confirmado pelo BLOCO 3, e o resultado está registrado no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → SQL Editor de produção | Exclusão manual e definitiva sobre dado financeiro real, sem staging, sem backup automático |
| BLOCO 1 (leitura) → BLOCO 2 (exclusão) | O predicado precisa ser idêntico nos dois blocos — qualquer drift entre eles apaga algo que o operador nunca viu na revisão |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-06 | Tampering | o predicado do BLOCO 2 diverge do BLOCO 1 (ou do critério de D-02), apagando mais do que o revisado | critical | mitigate | BLOCO 1 e BLOCO 2 usam exatamente o mesmo `where`, palavra por palavra (acceptance_criteria da Task 1); `checkpoint:decision` (Task 2) exige que o operador já tenha rodado e conferido o BLOCO 1 antes de autorizar o BLOCO 2 |
| T-09-07 | Tampering | o script vira uma migração auto-aplicada em vez de um runbook revisado | high | mitigate | Arquivo vive em `supabase/`, fora de `supabase/migrations/` (Pitfall 4 do RESEARCH.md); BLOCO 2 nunca é envolvido em `begin`/`rollback` — a intenção declarada é gravar depois da revisão humana, não ensaiar |
| T-09-08 | Information Disclosure | o BLOCO 1 expõe linhas de `parcelas` para alguém fora da allowlist | low | accept | Acesso ao SQL Editor do Supabase já é restrito ao mesmo operador que tem acesso a todo outro runbook deste projeto — nenhuma superfície nova |
| T-09-SC | Tampering | instalação de pacote npm/pip/cargo | n/a | accept | Nenhum pacote — SQL puro e documentação |

</threat_model>

<verification>
- Grep confirma BLOCO 1/2/3 presentes, predicado idêntico entre BLOCO 1 e BLOCO 2, ausência de `begin;`/`rollback` no BLOCO 2
- `docs/data-model.md` cita D-01 e a reversão de D-03, sem apagar o item original
- Confirmação humana, por SQL, de que as órfãs identificadas no BLOCO 1 saíram de produção e nada mais mudou
</verification>

<success_criteria>
- As parcelas órfãs já existentes antes desta fase não existem mais em produção
- O script usado para isso vive fora de `supabase/migrations/`, revisável, com leitura antes de exclusão
- `docs/data-model.md` registra a reversão de D-03 com o novo critério
</success_criteria>

<output>
Create `.planning/phases/09-integridade-de-datas-do-contrato-nas-parcelas/09-02-SUMMARY.md` when done
</output>
