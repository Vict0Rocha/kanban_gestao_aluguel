---
phase: 06.2-ciclo-de-vida-do-contrato
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260819000000_cards_arquivado_em.sql
  - supabase/verificacao_cards_arquivado_em.sql
autonomous: true
requirements: [VIDA-05, VIDA-06]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "O arquivo de migração declara `public.cards.arquivado_em timestamptz` **nulável, sem default e sem backfill** — nulo = não arquivado (D-07) — e nenhuma instrução destrutiva (sem `drop table`, `drop column`, `alter column ... type`, `rename`, sem troca de `on delete` de FK existente)"
    - "A migração declara o backstop de banco contra o cascade: a função `public.impedir_exclusao_de_card_com_lancamento()` e o trigger `cards_impede_exclusao_com_lancamento` (`before delete on public.cards for each row`), que recusa a exclusão quando existe qualquer `parcela_lancamentos` ligado a qualquer `parcelas` do card — exatamente o predicado de D-14, nem mais largo nem mais estreito"
    - "A migração é reexecutável: `add column if not exists`, `create or replace function`, `drop trigger if exists` antes de `create trigger` — a segunda passagem não erra e não muda nada"
    - "A migração não cria, altera nem remove nenhuma policy de RLS; a coluna nova herda `team full access cards` (`is_team_member()`, de `20260811000000_security_hardening.sql`) e o trigger roda como `security invoker`, então o SELECT interno dele passa pelo mesmo RLS do usuário que está apagando"
    - "`supabase/verificacao_cards_arquivado_em.sql` existe, abre com o aviso de pooling do D-19 (colar a Parte A inteira num único Run, ou usar psql/CLI), e o BLOCO 1 de baseline **não referencia `arquivado_em`** — a coluna ainda não existe quando ele roda (o bug real do ensaio da Phase 6.1)"
    - "O runbook prova as duas metades do backstop: que ele **bloqueia** um card com lançamento, e que ele **não bloqueia** um card que só tem parcelas geradas sem nenhum lançamento (D-14 — nada de valor humano se perde ali)"
    - "O runbook prova também o segundo caminho de cascade descoberto nesta fase: excluir uma `columns` que contém um card com lançamento também é recusado, porque `columns → cards` é `on delete cascade` e o trigger dispara por linha cascateada"
  artifacts:
    - supabase/migrations/20260819000000_cards_arquivado_em.sql
    - supabase/verificacao_cards_arquivado_em.sql
  key_links:
    - "`public.cards.arquivado_em` -> policy existente `team full access cards`, não uma policy nova"
    - "trigger `cards_impede_exclusao_com_lancamento` -> `public.parcelas.card_id` -> `public.parcela_lancamentos.parcela_id`, a mesma cadeia de `on delete cascade` que hoje apaga histórico em silêncio"
    - "`raise exception` do trigger -> SQLSTATE `P0001` -> mapeamento em `deleteCardAction`/`deleteColumnAction` (plano 06.2-05)"
---

<objective>
Escrever a migração aditiva do ciclo de vida do contrato — `public.cards.arquivado_em timestamptz null` (D-07) mais o backstop de banco contra o cascade que hoje apaga histórico financeiro sem trava — junto com o runbook que prova essa mecânica antes de encostar em produção.

**Nada é aplicado em produção neste plano.** Só nascem dois arquivos `.sql`. O ensaio contra o banco real acontece no plano 06.2-02 e a aplicação no 06.2-03, cada um atrás do seu portão — mesma sequência de três planos da Phase 4 e da Phase 6.1 (D-18), escalada para o tamanho real desta mudança: uma coluna nulável e um trigger.

Purpose: VIDA-05 (arquivamento) não existe sem a coluna, e VIDA-06 (trava de exclusão) fica com uma única linha de defesa — a Server Action — se o banco não tiver backstop. O alvo é o mesmo banco de produção com ~48 imóveis reais, com parcelas e lançamentos reais, sem staging.
Output: `supabase/migrations/20260819000000_cards_arquivado_em.sql` e `supabase/verificacao_cards_arquivado_em.sql`.
</objective>

## Artifacts this phase produces — Artefatos que toda a Phase 6.2 produz

Lista canônica de todo símbolo criado ou alterado pela Phase 6.2 inteira. Os planos 06.2-02 a 06.2-07 se referem a esta lista em vez de repeti-la.

**Arquivos novos**
- `supabase/migrations/20260819000000_cards_arquivado_em.sql` (este plano)
- `supabase/verificacao_cards_arquivado_em.sql` (este plano)
- `web/src/lib/kanban/visibilidade.ts` (plano 06.2-04) — **a espinha da fase**
- `web/src/components/kanban/arquivar-contrato-dialog.tsx` (plano 06.2-06)
- `web/src/components/kanban/excluir-contrato-dialog.tsx` (plano 06.2-06)
- `web/src/app/(app)/arquivados/page.tsx` (plano 06.2-07)
- `web/src/components/arquivados/arquivados-view.tsx` (plano 06.2-07)

**Arquivos alterados**
- `docs/data-model.md` (planos 06.2-03 e 06.2-04)
- `web/src/lib/kanban/parcelas.ts` (plano 06.2-04 — tipo do embed `cards`, filtro `arquivado_em` em `garantirParcelas`)
- `web/src/lib/kanban/actions.ts` (planos 06.2-04 e 06.2-05)
- `web/src/lib/kanban/queries.ts` (plano 06.2-05)
- `web/src/lib/kanban/types.ts` (plano 06.2-05 — `Card.arquivado_em`)
- `web/src/app/(app)/financeiro/page.tsx` (planos 06.2-04, 06.2-05 e 06.2-07)
- `web/src/app/(app)/layout.tsx` (plano 06.2-05 — alertas)
- `web/src/app/(app)/page.tsx` (plano 06.2-05 — Board)
- `web/src/app/(app)/relatorios/page.tsx` (plano 06.2-05 — Relatórios)
- `web/src/components/kanban/card-item.tsx` (plano 06.2-06)
- `web/src/components/kanban/board.tsx` (plano 06.2-06)
- `web/src/components/app-shell.tsx` (plano 06.2-07 — nav)
- `web/src/components/financeiro/financeiro-view.tsx` (plano 06.2-07)
- `web/src/components/financeiro/parcelas-table.tsx` (plano 06.2-07)

**Coluna nova em tabela existente**
- `public.cards.arquivado_em` — `timestamptz null`, sem default, sem backfill (D-07)

**Função e trigger novos no banco**
- `public.impedir_exclusao_de_card_com_lancamento()` — `returns trigger`, `language plpgsql`, `set search_path = ''`
- `cards_impede_exclusao_com_lancamento` — `before delete on public.cards for each row`

**Funções puras novas em `web/src/lib/kanban/visibilidade.ts`** (plano 06.2-04)
- `avaliarVisibilidadeParcela` — **a única implementação da regra de D-01**, consumida pela leitura E pela escrita
- `parcelaVisivel` — invólucro booleano que delega a `avaliarVisibilidadeParcela`, não a reimplementa
- `filtrarParcelasVisiveis` — aplica `parcelaVisivel` sobre o resultado da query do Financeiro (D-06)
- Tipos `CardVisibilidade`, `MotivoOcultacao`, `ResultadoVisibilidade`
- Constantes `MENSAGEM_PARCELA_OCULTA`, `EXCLUSAO_BLOQUEADA_POR_LANCAMENTO`, `EXCLUSAO_COLUNA_BLOQUEADA_POR_LANCAMENTO`

**Server Actions novas em `actions.ts`** (planos 06.2-04/06.2-05)
- `arquivarCardAction(cardId)` / `desarquivarCardAction(cardId)`
- `contarParcelasEmAbertoAction(cardId)` — o aviso de pendência do D-10
- `cardTemLancamentoAction(cardId)` — o pré-voo do diálogo de exclusão
- `exigirParcelaVisivel(supabase, parcelaId)` — helper interno (não exportado), a trava de escrita do D-04

**Server Actions alteradas**
- `registrarPagamentoAction` / `ajustarParcelaAction` — ganham a trava de D-04
- `deleteCardAction` — ganha a trava de D-14/D-15 e o mapeamento de `P0001`
- `deleteColumnAction` — ganha o mapeamento de `P0001` (o cascade `columns → cards` alcança o mesmo histórico)

**Componentes novos** (planos 06.2-06/06.2-07)
- `ArquivarContratoDialog`, `ExcluirContratoDialog`, `ArquivadosView`

**Nada é removido do banco.** A migração deste plano é 100% aditiva — nenhuma FK muda de `on delete cascade` para outra coisa, nenhuma coluna some, nenhum tipo troca.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-CONTEXT.md
@supabase/migrations/20260818000000_cards_numero_sequencial.sql
@supabase/migrations/20260816000000_financeiro_schema.sql
@supabase/migrations/20260811010000_security_advisor_fixes.sql
@supabase/verificacao_cards_numero.sql
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Fatia vertical no banco — coluna `arquivado_em` e o backstop de exclusão, da DDL ao bloco que prova os dois lados</name>
  <files>supabase/migrations/20260819000000_cards_arquivado_em.sql</files>
  <read_first>
    - `supabase/migrations/20260818000000_cards_numero_sequencial.sql` — cabeçalho em português, seções numeradas, `if not exists`, o bloco final que declara explicitamente que nenhuma RLS é tocada
    - `supabase/migrations/20260816000000_financeiro_schema.sql` — linhas 47 e 162, as duas FKs `on delete cascade` que formam a cadeia `cards → parcelas → parcela_lancamentos`; e a Seção 1 (`cards.ativo`) como exemplo de coluna acrescentada a `cards`
    - `supabase/migrations/20260811010000_security_advisor_fixes.sql` — BLOCO 1: a convenção deste projeto para função de trigger (`language plpgsql`, `set search_path = ''`, nomes totalmente qualificados)
    - `.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-CONTEXT.md` — D-07, D-14, D-15, D-18
  </read_first>
  <action>
Criar o arquivo `supabase/migrations/20260819000000_cards_arquivado_em.sql` (nome exato). Esta é a fatia vertical: coluna → função de guarda → trigger → declaração de RLS, num único arquivo reexecutável.

**Cabeçalho**, em português, no estilo do cabeçalho de `20260818000000_cards_numero_sequencial.sql`. Deve dizer: que esta migração implementa D-07 (arquivamento rastreável) e o backstop de D-14/D-15; que é estritamente aditiva — cria uma coluna nulável, uma função e um trigger, e **não** altera nenhuma FK existente, não apaga nada, não renomeia nada, não troca tipo; que é reexecutável; e apontar para `supabase/verificacao_cards_arquivado_em.sql` como o runbook que prova tudo isso. Mencionar o alvo real: produção, ~48 imóveis com parcelas e lançamentos reais, sem staging.

**Seção 1 — a coluna.** `alter table public.cards add column if not exists arquivado_em timestamptz;` — nulável por nascimento, **sem** `default`, **sem** `not null`, **sem** UPDATE de backfill. Comentar o porquê: nulo já significa "não arquivado" (D-07), então não existe estado a preencher; e como não há UPDATE, o trigger `cards_set_updated_at` não dispara e o `updated_at` dos ~48 cards existentes fica intacto — é isso que o runbook compara antes/depois. Comentar também a escolha de `timestamptz` em vez de `boolean` (D-07): guarda **quando**, e rastreabilidade é barata agora e cara de acrescentar depois.

**Seção 1b — por que NÃO existe índice aqui.** Comentário explícito, não uma omissão: `public.cards` tem ~48 linhas; o planner do Postgres vai varrer a tabela inteira de qualquer jeito, e um índice em `arquivado_em` só acrescentaria custo em todo insert/update sem devolver nada. Registrar o gatilho para revisitar: se `cards` passar da ordem de milhares de linhas, o índice parcial sobre `arquivado_em is not null` é o primeiro passo. Esse mesmo raciocínio vai para `docs/data-model.md` no plano 06.2-03.

**Seção 2 — o backstop de exclusão (D-14/D-15).** Criar `public.impedir_exclusao_de_card_com_lancamento()`:
- `returns trigger`, `language plpgsql`, `set search_path = ''` (mesma blindagem de `public.set_updated_at()` na migração `20260811010000`), nomes de tabela sempre totalmente qualificados por causa do search_path vazio
- **`security invoker` — o default, declarado por omissão e explicado em comentário.** `security definer` seria errado aqui: faria o SELECT interno ignorar o RLS, e o comportamento correto é o oposto — quem não é `is_team_member()` nem consegue enxergar a linha de `cards` para apagar, então o trigger nem chega a rodar. Não há brecha a fechar com privilégio extra.
- corpo: se existir qualquer linha de `public.parcela_lancamentos` cujo `parcela_id` aponte para alguma `public.parcelas` com `card_id = old.id`, disparar `raise exception` com mensagem em português; senão `return old`
- o predicado é **exatamente** o de D-14: qualquer lançamento de qualquer tipo (pagamento, acréscimo, desconto, destrava, e no futuro conciliação). Card com parcelas geradas e **zero** lançamentos passa e é apagado normalmente — comentar isso, porque é a metade da regra que um leitor futuro tende a "consertar" para mais restritiva. **D-17:** como a Phase 7 (conciliação) também vai gravar um `parcela_lancamentos`, ela entra automaticamente sob este mesmo predicado — nenhum código novo será necessário na trava de exclusão quando a Phase 7 chegar, só confirmar
- o `raise exception` fica com o SQLSTATE default `P0001` (`raise_exception`), sem código customizado. Comentar que hoje este é o **único** `raise exception` do schema, por isso `P0001` é um sinal não ambíguo para o mapeamento em `deleteCardAction`/`deleteColumnAction` (plano 06.2-05) — e que acrescentar outro `raise` ao schema no futuro obriga a revisitar esse mapeamento
- mensagem da exceção, em português, sabendo que ela normalmente **não** chega à tela (a Server Action mapeia `P0001` para o próprio texto): algo como "Contrato com lançamento financeiro registrado não pode ser excluído. Arquive o contrato em vez de excluir."

Depois: `drop trigger if exists cards_impede_exclusao_com_lancamento on public.cards;` seguido de `create trigger cards_impede_exclusao_com_lancamento before delete on public.cards for each row execute function public.impedir_exclusao_de_card_com_lancamento();`

**Seção 2b — o segundo caminho de cascade, comentado no próprio arquivo.** `public.columns → public.cards` também é `on delete cascade` (`20260728000000_init_schema.sql`), e o diálogo de excluir coluna já avisa que os imóveis da coluna serão excluídos junto. Como o trigger é `for each row`, ele dispara também para cada card cascateado — ou seja, apagar uma coluna que contém um contrato com lançamento passa a ser recusado, atomicamente, junto com a transação inteira. Comentar que isso é intencional e é a razão principal de existir um backstop no banco: a Server Action de exclusão de card nunca cobriu esse caminho.

**Seção 3 — declaração de RLS.** Bloco de comentário, sem DDL, no mesmo formato da seção final de `20260818000000_cards_numero_sequencial.sql`: nenhuma policy é criada, alterada ou removida; `public.cards` já está coberta por `team full access cards` usando `public.is_team_member()` desde `20260811000000_security_hardening.sql`, e a coluna nova herda essa proteção sem predicado novo; o trigger roda com o papel de quem chama, então o SELECT dentro dele é filtrado pelo mesmo RLS.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f=supabase/migrations/20260819000000_cards_arquivado_em.sql && grep -q "add column if not exists arquivado_em timestamptz" "$f" && grep -q "impedir_exclusao_de_card_com_lancamento" "$f" && grep -q "before delete on public.cards" "$f" && grep -q "set search_path = ''" "$f" && grep -vE "^\s*--" "$f" | grep -cE "drop (table|column)|alter column .* type|rename to|on delete restrict" | grep -qx 0 && echo MIGRACAO_OK</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe com o nome exato `supabase/migrations/20260819000000_cards_arquivado_em.sql`
    - `grep "arquivado_em timestamptz"` bate e a mesma linha **não** contém `not null` nem `default`
    - `grep "create trigger cards_impede_exclusao_com_lancamento"` bate, precedido por um `drop trigger if exists` do mesmo nome
    - A função é declarada com `language plpgsql` e `set search_path = ''`, e **não** contém `security definer`
    - Ignorando as linhas de comentário (`grep -vE "^\s*--"`), o arquivo não contém `drop table`, `drop column`, `alter column ... type`, `rename to` nem `on delete restrict`
    - Nenhum `create policy`, `alter policy` ou `drop policy` aparece no arquivo
    - Existe comentário explicando por que não há índice, por que `timestamptz` e não `boolean`, e por que card sem lançamento continua excluível
  </acceptance_criteria>
  <reversibility rating="reversible">Este plano só escreve arquivos `.sql` no repositório. Nada toca o banco — a decisão irreversível está no plano 06.2-03, atrás de um `checkpoint:decision`.</reversibility>
  <done>A migração existe, é aditiva, reexecutável, declara a coluna e o trigger, e não contém nenhuma instrução destrutiva nem mudança de RLS.</done>
</task>

<task type="auto">
  <name>Task 2: Runbook de ensaio e verificação, com o aviso de pooling do D-19 no topo</name>
  <files>supabase/verificacao_cards_arquivado_em.sql</files>
  <read_first>
    - `supabase/verificacao_cards_numero.sql` — o estilo (Parte A ensaio / Parte B pós-push, blocos numerados), o aviso "LIÇÃO DO ENSAIO REAL DE 2026-08-18" no cabeçalho, e o bloco `do $$ ... $$` que prova uma constraint capturando a exceção
    - `supabase/migrations/20260819000000_cards_arquivado_em.sql` — a DDL que este runbook precisa ensaiar (escrita na Task 1)
    - `.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-CONTEXT.md` — D-19
  </read_first>
  <action>
Criar `supabase/verificacao_cards_arquivado_em.sql`, no mesmo formato de duas partes de `supabase/verificacao_cards_numero.sql`.

**Cabeçalho — o aviso de D-19 vem primeiro, antes de qualquer instrução.** Reproduzir, em português e com a mesma força, a lição já gravada no cabeçalho de `verificacao_cards_numero.sql`: o SQL Editor do Supabase Studio usa pool de conexões e **não** garante a mesma sessão de banco entre cliques separados de "Run", então um `begin;` colado num clique pode não amarrar os comandos do clique seguinte. Na Phase 6.1 isso transformou um ensaio em push real que só foi percebido comparando `updated_at_max` antes e depois. Instrução operacional obrigatória, com as duas saídas aceitas: **(a)** colar a Parte A INTEIRA num único clique de "Run", ou **(b)** rodar por `psql`/Supabase CLI, onde a continuidade de sessão é garantida por construção. Deixar explícito que rodar bloco a bloco no Studio **não** é uma terceira opção.

**PARTE A — ENSAIO** (roda ANTES do push; termina em `rollback;`).

- **BLOCO 1 — baseline, fora de transação.** Contagens e marcas d'água que serão comparadas depois do `rollback`: `count(*)` e `max(updated_at)` de `public.cards`; `count(*)` de `public.parcelas`; `count(*)` de `public.parcela_lancamentos`. **Este bloco não pode citar a coluna `arquivado_em`** — ela ainda não existe, e foi exatamente esse o bug encontrado no ensaio real da Phase 6.1 (`column "numero" does not exist`). Registrar o aviso em comentário para que ninguém "melhore" o bloco depois.
- **BLOCO 2 — a transação.** `begin;` → a DDL completa da migração (colada, não `\i`) → **segunda passagem da mesma DDL** para provar reexecutabilidade → os blocos de prova abaixo → `rollback;`. Tudo isso é o que precisa ir num único "Run".
  - **Prova 2.1 — a coluna nasceu certa.** Consulta a `information_schema.columns` filtrando `table_schema='public' and table_name='cards' and column_name='arquivado_em'`, conferindo `data_type = 'timestamp with time zone'`, `is_nullable = 'YES'` e `column_default is null`.
  - **Prova 2.2 — ninguém foi arquivado pela migração.** `select count(*) as arquivados from public.cards where arquivado_em is not null;` deve devolver 0.
  - **Prova 2.3 — o trigger existe e é BEFORE DELETE ROW.** Consulta a `pg_trigger` juntada com `pg_class`, filtrando `tgname = 'cards_impede_exclusao_com_lancamento'`, mostrando `tgtype` e `tgenabled`.
  - **Prova 2.4 — o backstop BLOQUEIA (o lado que importa).** Bloco `do $$ ... $$` no molde do teste de constraint única do runbook da Phase 6.1: escolher o `id` de um card que tenha pelo menos um lançamento (subselect via `parcelas`/`parcela_lancamentos`), tentar `delete from public.cards where id = ...`, capturar a exceção com `exception when raise_exception then raise notice '...'` e sinalizar sucesso. Se o `delete` passar, o bloco deve `raise exception` denunciando que o backstop falhou — o ensaio precisa gritar, não passar em silêncio.
  - **Prova 2.5 — o backstop NÃO bloqueia demais (D-14).** Escolher um card que tenha parcelas mas **zero** lançamentos, apagá-lo e conferir que a contagem de linhas afetadas é 1. Se nenhum card assim existir no banco no momento do ensaio, criar um card temporário dentro da própria transação e usar esse — anotar em comentário qual dos dois caminhos o operador seguiu.
  - **Prova 2.6 — o cascade de coluna também é recusado.** Escolher o `column_id` de um card que tenha lançamento, tentar `delete from public.columns where id = ...` e capturar a exceção do mesmo jeito da Prova 2.4. Esta prova é o motivo de o backstop existir no banco e não só na Server Action.
  - **Prova 2.7 — `updated_at` intocado.** Reconferir `max(updated_at)` de `public.cards` e comparar, à vista, com o valor do BLOCO 1.
  - `rollback;`
- **BLOCO 3 — confirmação pós-rollback, fora de transação.** Repetir as consultas do BLOCO 1 e mais uma checagem de que `arquivado_em` **não** existe em `information_schema.columns` (se existir depois do rollback, o ensaio virou push — exatamente o acidente do D-19, e o operador precisa parar e reportar). Deixar essa instrução escrita, não implícita.

**PARTE B — VERIFICAÇÃO PÓS-PUSH** (blocos independentes, rodados só depois do plano 06.2-03).

- **BLOCO 4** — a coluna existe com o tipo/nulabilidade corretos; `arquivado_em is not null` conta 0; `cards_total` e `updated_at_max` idênticos aos anotados na Parte A.
- **BLOCO 5** — o trigger existe e está habilitado (`tgenabled = 'O'`).
- **BLOCO 6** — repetir as Provas 2.4, 2.5 e 2.6 **dentro de `begin; ... rollback;`** (mesmo aviso de pooling se aplica: um único Run) contra o banco já migrado, para confirmar que o backstop está vivo em produção sem apagar nada de verdade.
- **BLOCO 7** — a lista de policies de `public.cards` (`pg_policies`) para confirmar que continua exatamente `team full access cards`, nada acrescentado nem removido.
- **BLOCO 8 — rollback de emergência**, marcado em comentário como válido **apenas enquanto nenhum contrato tiver sido arquivado**: `drop trigger if exists`, `drop function if exists`, `drop column if exists arquivado_em`. Depois que a coluna carregar dado real de arquivamento, este bloco deixa de ser uma saída e vira perda de informação — escrever isso no comentário, não deixar implícito.

Deixar preparado, no fim do arquivo, um cabeçalho vazio `-- RESULTADO DO ENSAIO — <data>` para o plano 06.2-02 preencher.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f=supabase/verificacao_cards_arquivado_em.sql && grep -qi "pool de conex" "$f" && grep -qi "um único" "$f" && grep -q "PARTE A" "$f" && grep -q "PARTE B" "$f" && grep -q "RESULTADO DO ENSAIO" "$f" && grep -q "rollback;" "$f" && sed -n '/BLOCO 1/,/BLOCO 2/p' "$f" | grep -c "arquivado_em" | grep -qx 0 && echo RUNBOOK_OK</automated>
  </verify>
  <acceptance_criteria>
    - O arquivo existe com o nome exato `supabase/verificacao_cards_arquivado_em.sql`
    - O aviso de pooling do D-19 está no cabeçalho, acima de qualquer instrução operacional, e nomeia as duas saídas aceitas (Parte A inteira num único Run, ou psql/CLI)
    - O trecho entre `BLOCO 1` e `BLOCO 2` não contém nenhuma ocorrência de `arquivado_em` (baseline pré-migração não pode citar coluna inexistente)
    - Existem blocos que provam separadamente: coluna criada corretamente, nenhum card arquivado, trigger presente, backstop bloqueia card com lançamento, backstop **não** bloqueia card sem lançamento, cascade de `columns` também recusado, `updated_at` inalterado
    - A Parte A termina em `rollback;` e o BLOCO 3 checa explicitamente que `arquivado_em` deixou de existir depois dele
    - O bloco de rollback de emergência está marcado como válido só enquanto nenhum contrato tiver sido arquivado
    - Existe um cabeçalho `RESULTADO DO ENSAIO` vazio no fim do arquivo
  </acceptance_criteria>
  <done>O runbook existe, carrega o aviso de D-19 no topo e prova os dois lados do backstop mais a integridade da coluna, tudo dentro de uma transação revertida.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → banco de produção | Um humano cola DDL contra o único banco que existe; ~48 contratos reais com parcelas e lançamentos, sem staging e sem backup automático (plano gratuito do Supabase) |
| navegador → PostgREST | O RLS de `public.cards` (`is_team_member()`) é a última linha; nada nesta migração pode afrouxá-lo |
| aplicação → cascade do schema | `cards → parcelas → parcela_lancamentos` e `columns → cards → ...`; hoje ambas as pontas apagam histórico financeiro sem trava |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-01 | Tampering | cascade `cards → parcelas → parcela_lancamentos` | critical | mitigate | Trigger `cards_impede_exclusao_com_lancamento` (`before delete ... for each row`) recusa a exclusão quando existe qualquer lançamento — backstop de banco, independente da Server Action do plano 06.2-05 |
| T-06.2-02 | Tampering | cascade `columns → cards → parcelas → parcela_lancamentos` | critical | mitigate | O mesmo trigger dispara por linha cascateada, então apagar coluna com contrato movimentado também é recusado, atomicamente. Provado na Prova 2.6 do runbook |
| T-06.2-03 | Elevation of Privilege | `impedir_exclusao_de_card_com_lancamento()` | high | mitigate | Função `security invoker` (não `definer`) com `set search_path = ''` e nomes totalmente qualificados — não concede privilégio nenhum e não é sequestrável por schema malicioso no path |
| T-06.2-04 | Information Disclosure | `public.cards.arquivado_em` | low | accept | Coluna nova numa tabela já coberta por `team full access cards` (`is_team_member()`); herda a policy sem predicado novo. Nenhuma policy é criada, alterada ou removida — verificado no BLOCO 7 do runbook |
| T-06.2-05 | Denial of Service | ensaio que vira push (D-19) | high | mitigate | Aviso de pooling no topo do runbook com as duas saídas aceitas; BLOCO 3 checa explicitamente que a coluna sumiu depois do `rollback` e manda o operador parar se não sumiu |
| T-06.2-06 | Tampering | backstop largo demais bloqueando exclusão legítima | medium | mitigate | Prova 2.5 do runbook exige demonstrar que card com parcelas e zero lançamentos continua excluível — D-14 ao pé da letra |
| T-06.2-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala nenhum pacote — só escreve dois arquivos `.sql`. Nenhuma auditoria de legitimidade é necessária |
</threat_model>

<verification>
- `grep` confirma a forma da coluna, do trigger e da função, e a ausência de instrução destrutiva ou de mudança de RLS no arquivo de migração
- `grep` confirma que o aviso de D-19 está no cabeçalho do runbook e que o BLOCO 1 não cita `arquivado_em`
- Nenhuma verificação contra o banco acontece neste plano — é o objeto do plano 06.2-02
</verification>

<success_criteria>
- `supabase/migrations/20260819000000_cards_arquivado_em.sql` e `supabase/verificacao_cards_arquivado_em.sql` existem e passam nas asserções de `grep` das duas tasks
- A migração é aditiva, reexecutável e não toca RLS nem FK existente
- O runbook prova os dois lados do backstop (bloqueia com lançamento, libera sem lançamento) e o caminho de cascade por coluna
- O banco de produção continua exatamente como estava — este plano não o toca
</success_criteria>

<output>
Create `.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-01-SUMMARY.md` when done
</output>
