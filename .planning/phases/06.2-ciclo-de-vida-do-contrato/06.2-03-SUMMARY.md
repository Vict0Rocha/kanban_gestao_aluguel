---
phase: 06.2-ciclo-de-vida-do-contrato
plan: 03
subsystem: database
tags: [postgresql, supabase, rls, trigger, push, producao, arquivamento]

requires: ["06.2-01", "06.2-02"]
provides:
  - "public.cards.arquivado_em (timestamptz, nulável) vivo em produção"
  - "trigger cards_impede_exclusao_com_lancamento (before delete on cards for each row) vivo em produção, bloqueando exclusão de card/coluna com lançamento financeiro"
affects: ["06.2-04", "06.2-05", "06.2-06", "06.2-07"]

actuals:
  tokens: 0
  tasks: 3
  commits: 1

tech-stack:
  added: []
  patterns:
    - "Aplicação via SQL Editor do Supabase em vez do CLI (`supabase db push`) — mesmo caminho de 04-03-SUMMARY.md, CLI ausente na máquina do operador"
    - "Verificação pós-push com múltiplas SELECTs coladas juntas devolve só o resultado da última no SQL Editor — cada BLOCO com mais de uma consulta precisou ser rodado em cliques separados (mesma limitação documentada em D-19)"

key-files:
  created: []
  modified:
    - supabase/verificacao_cards_arquivado_em.sql
    - docs/data-model.md

key-decisions:
  - "Checkpoint de decisão (Task 1) resolvido como `aplicar-agora`: o backstop contra o cascade — item de maior severidade da fase — passou a valer em produção antes de qualquer código novo, aceitando a janela de mensagem genérica de exclusão até o plano 06.2-05"
  - "BLOCO 6 (os três lados do backstop pós-push) rodado com uma adaptação ad-hoc da técnica da via (c) do ensaio 06.2-02 (bloco `do $$ ... $$;` único terminando em `raise exception`), em vez do `begin;...rollback;` literal do runbook — mesma garantia de conexão única do pool, sem depender de dois comandos separados"
  - "Executor original (worktree isolado) corretamente não tentou fabricar o push: sem Supabase CLI, psql ou credenciais no ambiente isolado, halted e reportou — mesmo padrão de 04-03. Task 2 e Task 3 conduzidas em chat entre o agente principal e o operador"

patterns-established: []

requirements-completed: [VIDA-05, VIDA-06]

coverage:
  - id: D1
    description: "arquivado_em e o trigger existem em produção, zero contratos arquivados, zero linhas alteradas, updated_at_max intocado"
    requirement: VIDA-05
    verification:
      - kind: other
        ref: "BLOCO 4: data_type=timestamp with time zone, is_nullable=YES, column_default=null, arquivados=0, cards_total=50, updated_at_max idêntico ao baseline do ensaio (2026-08-19 14:11:32.099956+00). BLOCO 5: trigger presente, tgenabled=O"
        status: pass
    human_judgment: true
    rationale: "Números conferidos pelo operador contra o baseline anotado no ensaio 06.2-02 — updated_at_max idêntico é a prova mais forte de que nenhuma linha existente foi tocada pelo push."
  - id: D2
    description: "Backstop bloqueia card com lançamento e coluna com card movimentado, libera card sem lançamento — contra o banco já migrado"
    requirement: VIDA-06
    verification:
      - kind: other
        ref: "BLOCO 6 (via adaptação da técnica (c)): 2.4 OK recusado (card 88706ed9-778f-4dde-8478-addaa13a2a81), 2.5 OK excluído (card 12a909c5-d135-4ee7-aa8f-69070c5cdaa5, real, não temporário), 2.6 OK recusado (column cab7bbfe-95bb-4af6-be6a-9e2b26947468)"
        status: pass
    human_judgment: true
    rationale: "Rodado pelo operador contra produção real (não mais ensaio), com rollback automático via exceção proposital desfazendo a exclusão de teste do card sem lançamento."
  - id: D3
    description: "pg_policies de public.cards inalterada"
    requirement: VIDA-06
    verification:
      - kind: other
        ref: "BLOCO 7: uma linha, team full access cards, cmd=ALL, qual=with_check=is_team_member()"
        status: pass
    human_judgment: true
    rationale: "Confirmado diretamente pelo operador contra pg_policies real."
  - id: D4
    description: "docs/data-model.md documenta arquivado_em, o trigger e as decisões de design (timestamptz vs boolean, backstop vs on delete restrict, ausência de índice)"
    verification:
      - kind: automated
        ref: "grep -q arquivado_em && grep -q cards_impede_exclusao_com_lancamento && grep -qi cascade docs/data-model.md -> DOCS_OK"
        status: pass
    human_judgment: false
    rationale: "Verificação automatizada do plano, rodada após as três edições no arquivo."
  - id: D5
    description: "Board, Financeiro e Relatórios carregam sem erro em produção, com os mesmos dados; exclusão com/sem lançamento comporta-se como esperado"
    requirement: VIDA-06
    verification:
      - kind: other
        ref: "Operador confirmou: Board ok (toggle Ativo/Inativo salvando), Financeiro ok (regressão D-20), Relatórios ok. Exclusão de card com lançamento falhou, card+lançamento confirmados por SQL (lancamentos_do_card=1). Exclusão de card sem lançamento funcionou"
        status: pass
    human_judgment: true
    rationale: "Passo 8 exigia prova por SQL, não pela tela — obtida explicitamente (select do card + count de lançamentos), não apenas aceita por afirmação."
---

# Phase 6.2 Plan 3: Push em produção — arquivado_em + backstop de exclusão Summary

**Migração `20260819000000_cards_arquivado_em.sql` aplicada em produção via SQL Editor do Supabase (CLI ausente). Verificação pós-push completa: coluna `arquivado_em` nasceu com tipo/nulabilidade corretos e zero contratos arquivados; trigger `cards_impede_exclusao_com_lancamento` confirmado vivo, bloqueando exclusão de card e de coluna com lançamento financeiro, liberando exclusão de card sem lançamento; policies de `cards` inalteradas; `docs/data-model.md` documentado. Board, Financeiro e Relatórios seguem funcionando normalmente em produção.**

## Performance

- **Started:** 2026-08-19
- **Completed:** 2026-08-19
- **Tasks:** 3 (checkpoint:decision → push + docs → checkpoint:human-verify)
- **Files modified:** 2 (`supabase/verificacao_cards_arquivado_em.sql`, `docs/data-model.md`)

## Accomplishments

- Checkpoint de decisão respondido: `aplicar-agora`
- Migração aplicada em produção via SQL Editor: `cards.arquivado_em`, `impedir_exclusao_de_card_com_lancamento()`, `cards_impede_exclusao_com_lancamento` — "Success. No rows returned"
- Integridade de dados confirmada: `cards_total=50`, `arquivados=0`, `updated_at_max` idêntico ao baseline do ensaio
- Coluna confirmada com tipo/nulabilidade corretos (`timestamp with time zone`, `YES`, sem default); trigger confirmado presente e habilitado (`tgenabled=O`)
- Backstop reprovado contra o schema real: card com lançamento recusado, card sem lançamento liberado, coluna em cascade recusada
- `pg_policies` de `cards` confirmada inalterada
- `docs/data-model.md` atualizado: diagrama mermaid, descrição da entidade `cards`, três novas entradas em "Decisões de design"
- Verificação manual em produção: Board, Financeiro (regressão D-20) e Relatórios carregando normalmente; exclusão de card com lançamento falhou (confirmado por SQL que card e lançamento sobrevivem); exclusão de card sem lançamento funcionou

## Task Commits

1. **Task 1 (checkpoint:decision):** resolvida em chat — `aplicar-agora`, sem commit próprio
2. **Task 2 (push + docs):** push sem commit de código (só grava no banco); `docs/data-model.md` editado nesta sessão
3. **Task 3 (checkpoint:human-verify):** verificação pós-push conduzida em chat, com pushback explícito em três respostas parciais do operador até reunir evidência completa dos nove passos

**Plan metadata:** este commit (docs: complete plan + registro no runbook)

## Files Created/Modified

- `supabase/verificacao_cards_arquivado_em.sql` - Seção "RESULTADO DO PUSH E DA VERIFICAÇÃO PÓS-PUSH — 2026-08-19" acrescentada
- `docs/data-model.md` - `arquivado_em` no diagrama mermaid e na descrição de `cards`; três novas entradas em "Decisões de design" (timestamptz vs boolean, trigger como backstop de banco + rejeição de `on delete restrict`, ausência deliberada de índice)

## Decisions Made

Ver `key-decisions` no frontmatter. A mais notável: o operador optou por `aplicar-agora` em vez de esperar um backup adicional, já que o ensaio do plano 06.2-02 (técnica (c), bloco único auto-revertido) já havia coberto tudo que dava para cobrir sem gravar de verdade.

## Deviations from Plan

### Deviations Requiring Follow-up

**1. [Ferramenta] `supabase db push` substituído por SQL Editor**
- **Encontrado em:** Task 2
- **Motivo:** Supabase CLI não instalado na máquina do operador — mesma situação de 04-03
- **Impacto:** Nenhum no resultado — mesma DDL, mesmo banco
- **Ação:** Nenhuma agora; mesmo padrão já registrado em 04-03-SUMMARY.md

**2. [Técnica de verificação] BLOCO 6 pós-push rodado via bloco `do $$` único, não via `begin;...rollback;` literal**
- **Encontrado em:** Task 3
- **Motivo:** Reutilizar a técnica da via (c), já provada no ensaio 06.2-02, elimina qualquer risco de connection-hopping do pool do SQL Editor entre `begin` e `rollback` — mais robusta que o literal do runbook
- **Impacto:** Nenhum no resultado — mesmas três provas, mesmo predicado testado, resultado equivalente
- **Ação:** Nenhuma agora. O script ad-hoc usado (`pos-push-06.2-bloco6.sql`) não foi versionado por ser específico deste checkpoint; o resultado está registrado no runbook

**3. [Escopo de verificação] BLOCO 4 rodado em consultas separadas, não coladas juntas**
- **Encontrado em:** Task 3
- **Motivo:** SQL Editor só devolve o resultado da última consulta quando várias são coladas num único Run — mesma limitação já documentada no runbook para outras partes deste ensaio
- **Impacto:** Nenhum — todas as três consultas do BLOCO 4 foram obtidas, só em cliques separados
- **Ação:** Nenhuma

---

**Total deviations:** 3, todas de processo/ferramenta ou de técnica de verificação — nenhuma alterou o resultado técnico (migração aplicada corretamente, backstop funcionando, dados intactos)

## Issues Encountered

Mesma situação de 04-03: o executor original (`gsd-executor` em worktree isolado) corretamente não tentou fabricar o push — sem Supabase CLI, `psql` ou credenciais no ambiente isolado, halted e reportou em detalhe. Task 1, Task 2 (a parte de push) e Task 3 foram conduzidas em chat entre o agente principal e o operador, já que dependem de decisão humana e de navegador/SQL Editor autenticado contra produção. A parte de documentação da Task 2 (`docs/data-model.md`) foi escrita pelo agente principal depois da confirmação do push.

## User Setup Required

None além do já feito.

## Next Phase Readiness

- Os sete critérios de aceite da Task 3 estão satisfeitos com evidência real contra produção, não só ensaiados.
- `public.cards.arquivado_em` e o trigger `cards_impede_exclusao_com_lancamento` estão vivos em produção — os planos 06.2-04 (spinha de visibilidade) e 06.2-05 (guarda de exclusão + arquivar/desarquivar) podem se apoiar neles sem reimplementar nada.
- Nenhum bloqueador para o plano 06.2-04.

## Self-Check

- `supabase/verificacao_cards_arquivado_em.sql` contém o registro do push e da verificação pós-push, com data 2026-08-19 — confirmado
- `cards_total`/`arquivados`/`updated_at_max` pós-push conferidos contra o baseline do ensaio (06.2-02) e idênticos onde esperado — confirmado
- Trigger e policy confirmados por consulta direta do operador contra `pg_trigger`/`pg_policies` reais — confirmado
- Backstop reprovado contra as tabelas reais (não mais ensaio): recusa/libera/recusa nos três lados — confirmado
- `docs/data-model.md` passa no grep automatizado do plano (`arquivado_em`, `cards_impede_exclusao_com_lancamento`, `cascade`) — confirmado
- Exclusão com lançamento: falhou, e card + lançamento confirmados por SQL (não pela tela) — confirmado

## Self-Check: PASSED (com 3 desvios documentados, nenhum bloqueante)

---
*Phase: 06.2-ciclo-de-vida-do-contrato*
*Completed: 2026-08-19*
