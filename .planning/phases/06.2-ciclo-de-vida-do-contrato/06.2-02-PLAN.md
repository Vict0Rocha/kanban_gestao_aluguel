---
phase: 06.2-ciclo-de-vida-do-contrato
plan: 02
type: execute
wave: 2
depends_on: ["06.2-01"]
files_modified:
  - supabase/verificacao_cards_arquivado_em.sql
autonomous: false
requirements: [VIDA-05, VIDA-06]

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A Parte A do runbook foi executada contra o banco de produção real de um jeito que garante continuidade de sessão — Parte A inteira num único clique de Run, ou psql/Supabase CLI (D-19). Rodar bloco a bloco no SQL Editor do Studio não conta como ensaio"
    - "O BLOCO 3 pós-`rollback` confirma que `arquivado_em` **não** existe mais em `information_schema.columns` — se existir, o ensaio virou push e o plano para para reportar, exatamente o acidente da Phase 6.1"
    - "`cards_total`, `parcelas_total`, `lancamentos_total` e `updated_at_max` depois do `rollback` são idênticos aos do BLOCO 1"
    - "O backstop foi observado BLOQUEANDO a exclusão de um card real com lançamento, e LIBERANDO a exclusão de um card sem lançamento nenhum — os dois lados, com o `id` real usado em cada caso anotado"
    - "A exclusão de uma `columns` que contém um card com lançamento foi observada sendo recusada — o segundo caminho de cascade"
    - "A segunda passagem da mesma DDL dentro da transação não errou e não alterou nada (reexecutabilidade provada, não presumida)"
    - "O bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_cards_arquivado_em.sql` está preenchido com os números reais observados e com o caminho de execução usado (único Run vs. psql/CLI)"
  artifacts:
    - supabase/verificacao_cards_arquivado_em.sql
  key_links:
    - "ensaio -> `checkpoint:decision` do plano 06.2-03: sem o resultado registrado aqui, aquele portão não tem base para decidir"
---

<objective>
Ensaiar a migração `20260819000000_cards_arquivado_em.sql` contra o banco de produção dentro de uma transação revertida, e registrar o resultado real — números, `id`s usados e caminho de execução — no próprio runbook.

Este plano **não** aplica nada. Ele existe porque a Phase 6.1 provou que um ensaio mal executado é pior que nenhum ensaio: em 2026-08-18 a instrução "rode um bloco de cada vez, na mesma aba" transformou um ensaio em push real de produção, e ninguém percebeu até comparar `updated_at_max` antes e depois (D-19). A tarefa aqui é tão sobre *como* rodar quanto sobre *o que* observar.

Purpose: o `checkpoint:decision` do plano 06.2-03 só tem base se este ensaio existir e estiver registrado. Um trigger que bloqueia exclusão é o tipo de objeto que, se estiver errado, quebra uma funcionalidade em produção sem aviso — precisa ser visto funcionando nos dois sentidos antes de virar permanente.
Output: `RESULTADO DO ENSAIO` preenchido em `supabase/verificacao_cards_arquivado_em.sql`.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `06.2-01-PLAN.md`.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-CONTEXT.md
@supabase/verificacao_cards_arquivado_em.sql
@supabase/migrations/20260819000000_cards_arquivado_em.sql
@supabase/verificacao_cards_numero.sql
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Rodar a Parte A contra produção e anotar o que aconteceu</name>
  <precondition>`supabase/migrations/20260819000000_cards_arquivado_em.sql` e `supabase/verificacao_cards_arquivado_em.sql` existem no repositório (plano 06.2-01) e a coluna `arquivado_em` ainda NÃO existe no banco de produção.</precondition>
  <read_first>
    - `supabase/verificacao_cards_arquivado_em.sql` — Parte A inteira, e principalmente o aviso de pooling no cabeçalho
    - `supabase/verificacao_cards_numero.sql` — a seção `RESULTADO DO ENSAIO — 2026-08-18`, para ver o formato do registro e o que deu errado da última vez
  </read_first>
  <what-built>
    O runbook de ensaio (`supabase/verificacao_cards_arquivado_em.sql`, Parte A) e a migração que ele ensaia (`20260819000000_cards_arquivado_em.sql`): a coluna `cards.arquivado_em`, a função `impedir_exclusao_de_card_com_lancamento()` e o trigger `cards_impede_exclusao_com_lancamento`.
  </what-built>
  <how-to-verify>
    **Antes de qualquer coisa — escolha o caminho de execução (D-19). Só existem dois aceitos:**

    - **(a)** Supabase Studio → SQL Editor, colando a **Parte A INTEIRA** (BLOCO 2 do `begin;` até o `rollback;`) num **único** clique de "Run". Nunca bloco a bloco.
    - **(b)** `psql` ou Supabase CLI apontando para o banco de produção, onde a sessão é garantida por construção.

    Rodar bloco a bloco no Studio **não é uma terceira opção**: o Studio usa pool de conexões, o `begin;` de um clique não amarra os comandos do clique seguinte, e foi assim que o ensaio da Phase 6.1 commitou DDL em produção sem ninguém notar.

    **Passo 1 — baseline (BLOCO 1, pode rodar sozinho, está fora de transação).**
    Anotar os quatro números: `cards_total`, `parcelas_total`, `lancamentos_total`, `updated_at_max`.

    **Passo 2 — escolher os três `id`s que as provas vão usar** (consultas prontas no runbook):
    - um `cards.id` que **tenha** pelo menos um lançamento (Prova 2.4)
    - um `cards.id` que tenha parcelas mas **nenhum** lançamento (Prova 2.5) — se não existir nenhum, usar o card temporário criado dentro da própria transação, como o runbook descreve
    - o `columns.id` da coluna onde mora o card do primeiro item (Prova 2.6)
    Anotar os três.

    **Passo 3 — rodar o BLOCO 2 inteiro de uma vez.** Ele contém `begin;`, a DDL, a segunda passagem da DDL, as Provas 2.1 a 2.7 e o `rollback;`.

    **Passo 4 — ler cada prova e anotar o resultado real:**
    - 2.1: `data_type` = `timestamp with time zone`, `is_nullable` = `YES`, `column_default` vazio
    - 2.2: `arquivados` = 0
    - 2.3: o trigger aparece, com `tgenabled`
    - 2.4: a exclusão do card COM lançamento foi **recusada** (o bloco `do $$` avisa que capturou a exceção). Se ela passar, o backstop está errado — **pare aqui e reporte**
    - 2.5: a exclusão do card SEM lançamento **funcionou** (1 linha). Se ela também for recusada, o predicado está largo demais e contraria D-14 — **pare aqui e reporte**
    - 2.6: a exclusão da coluna foi **recusada**
    - 2.7: `max(updated_at)` igual ao do Passo 1

    **Passo 5 — rodar o BLOCO 3 (pós-`rollback`) e conferir a coisa mais importante do ensaio inteiro:**
    `arquivado_em` **não pode** aparecer em `information_schema.columns`, e os quatro números do Passo 1 têm de estar idênticos. Se `arquivado_em` ainda existir, o `rollback` não pegou — o ensaio virou push. Nesse caso **não** siga para a Task 2 como se tudo tivesse dado certo: registre o acidente com honestidade, exatamente como a Phase 6.1 fez, e reporte antes de continuar.
  </how-to-verify>
  <acceptance_criteria>
    - O caminho de execução usado está declarado: "(a) único Run" ou "(b) psql/CLI"
    - Os quatro números do baseline e os três `id`s escolhidos estão anotados
    - As sete provas têm resultado observado registrado, não presumido
    - A Prova 2.4 recusou e a Prova 2.5 passou — se qualquer uma das duas saiu diferente, o plano parou e reportou em vez de seguir
    - Depois do `rollback`, `arquivado_em` não existe e os quatro números batem com o baseline
  </acceptance_criteria>
  <reversibility rating="reversible">O ensaio inteiro vive dentro de `begin; ... rollback;` e não deixa rastro no banco — desde que o caminho de execução do D-19 seja respeitado. É exatamente essa condição que o Passo 5 verifica.</reversibility>
  <resume-signal>Responda com os resultados dos Passos 1 a 5, ou com o que travou.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Registrar o resultado do ensaio dentro do próprio runbook</name>
  <precondition>A Task 1 foi executada e os resultados das sete provas estão disponíveis na conversa.</precondition>
  <files>supabase/verificacao_cards_arquivado_em.sql</files>
  <read_first>
    - `supabase/verificacao_cards_numero.sql` — a seção `RESULTADO DO ENSAIO — 2026-08-18`: contexto primeiro, depois os números, depois as lições. É esse o formato a repetir
    - `supabase/verificacao_cards_arquivado_em.sql` — o cabeçalho `RESULTADO DO ENSAIO` vazio deixado pela Task 2 do plano 06.2-01
  </read_first>
  <action>
Preencher o bloco `RESULTADO DO ENSAIO` de `supabase/verificacao_cards_arquivado_em.sql` com o que a Task 1 realmente observou. Escrever em comentário SQL, em português, no mesmo tom do registro da Phase 6.1: contexto primeiro, números depois, e o que ficou aprendido no fim.

Conteúdo obrigatório:
- a data do ensaio e qual dos dois caminhos de D-19 foi usado (único Run no Studio, ou psql/CLI) — não deixar isso implícito, é a informação que faltou na Phase 6.1
- os quatro números do baseline e os quatro depois do `rollback`, lado a lado
- os três `id`s usados nas provas, para que a verificação pós-push (Parte B) possa repetir contra os mesmos alvos
- o resultado literal de cada uma das sete provas
- a confirmação explícita de que `arquivado_em` deixou de existir depois do `rollback` — ou, se não deixou, o registro honesto de que a DDL foi commitada de verdade e o que foi feito a respeito
- qualquer divergência entre o que o runbook previa e o que o banco respondeu, inclusive erro de sintaxe corrigido na hora; se o arquivo de migração ou o runbook precisou ser corrigido, dizer o quê e por quê

Se alguma correção foi feita em `20260819000000_cards_arquivado_em.sql` durante o ensaio, o ensaio precisa ser refeito por inteiro com o arquivo corrigido antes de este bloco ser dado como fechado — registrar quantas rodadas foram necessárias.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && f=supabase/verificacao_cards_arquivado_em.sql && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qi "cards_total" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | grep -qiE "psql|CLI|único Run|unico Run" && sed -n '/RESULTADO DO ENSAIO/,$p' "$f" | wc -l | awk '$1 > 15 {print "ENSAIO_REGISTRADO"; exit} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - A seção `RESULTADO DO ENSAIO` tem mais de 15 linhas e cita `cards_total`
    - O caminho de execução (único Run ou psql/CLI) está nomeado por escrito
    - Os três `id`s usados nas provas estão registrados
    - As sete provas têm resultado escrito, incluindo o par bloqueia/não-bloqueia do backstop
    - Está escrito, em uma frase direta, se `arquivado_em` sobreviveu ou não ao `rollback`
    - Nenhum arquivo além de `supabase/verificacao_cards_arquivado_em.sql` foi modificado por esta task
  </acceptance_criteria>
  <done>O runbook carrega o registro do ensaio real, com números, `id`s, caminho de execução e o veredito sobre o `rollback`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → banco de produção | Este plano executa SQL contra o banco real. A única coisa que separa "ensaio" de "push" é a continuidade da sessão que envolve o `begin;`/`rollback;` |
| runbook → operador | O texto do runbook é a única instrução que o operador segue; uma instrução errada aqui já causou perda de controle uma vez (Phase 6.1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-07 | Tampering | ensaio que commita DDL em produção (pool de conexões do Studio) | high | mitigate | D-19 aplicado literalmente: só dois caminhos aceitos (Parte A inteira num único Run, ou psql/CLI); o Passo 5 checa `information_schema.columns` depois do `rollback` e manda parar se a coluna sobreviveu |
| T-06.2-08 | Tampering | Prova 2.5 apaga um card real dentro da transação | medium | mitigate | O `delete` só existe dentro do `begin;...rollback;` do BLOCO 2, e o Passo 5 confirma `cards_total` idêntico ao baseline. Se o operador escolheu o caminho (b), a transação é garantida por construção |
| T-06.2-09 | Denial of Service | backstop largo demais entra em produção | high | mitigate | O ensaio exige observar a Prova 2.5 passando (card sem lançamento continua excluível) antes de qualquer autorização de push; falha ali interrompe o plano |
| T-06.2-10 | Repudiation | ensaio não registrado, decisão do 06.2-03 sem base | medium | mitigate | Task 2 grava números, `id`s e caminho de execução dentro do próprio runbook, versionado no git |
| T-06.2-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala pacote nenhum. Se o operador optar por instalar o Supabase CLI para o caminho (b), isso é ferramenta oficial do fornecedor já usada no projeto, fora do fluxo de dependência da aplicação |
</threat_model>

<verification>
- Observação direta do resultado de cada prova contra o banco de produção, dentro de transação revertida
- Comparação numérica antes/depois do `rollback` (`cards_total`, `parcelas_total`, `lancamentos_total`, `updated_at_max`)
- Checagem de `information_schema.columns` depois do `rollback` — a prova de que o ensaio foi ensaio
- `grep` sobre o bloco `RESULTADO DO ENSAIO` confirmando que o registro existe e nomeia o caminho de execução
</verification>

<success_criteria>
- O ensaio rodou por um caminho que garante continuidade de sessão, e isso está escrito
- Os dois lados do backstop foram observados: bloqueia card com lançamento, libera card sem lançamento
- O cascade por `columns` foi observado sendo recusado
- Nada persistiu no banco: coluna ausente e quatro números idênticos depois do `rollback`
- `supabase/verificacao_cards_arquivado_em.sql` carrega o registro completo, commitado no git
</success_criteria>

<output>
Create `.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-02-SUMMARY.md` when done
</output>
