---
phase: 06.2-ciclo-de-vida-do-contrato
plan: 03
type: execute
wave: 3
depends_on: ["06.2-02"]
files_modified:
  - supabase/migrations/20260819000000_cards_arquivado_em.sql
  - supabase/verificacao_cards_arquivado_em.sql
  - docs/data-model.md
autonomous: false
requirements: [VIDA-05, VIDA-06]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A migração `20260819000000_cards_arquivado_em` está aplicada no banco de produção (via `supabase db push` ou, se o CLI não estiver disponível, colando a DDL no SQL Editor — mesmo fallback documentado em `04-03-SUMMARY.md`), e o caminho usado está registrado"
    - "Depois do push, `cards_total`, `parcelas_total`, `lancamentos_total` e `updated_at_max` continuam idênticos aos anotados no ensaio, e `select count(*) from public.cards where arquivado_em is not null` devolve 0 — nada foi apagado, alterado ou arquivado pela migração"
    - "`information_schema.columns` mostra `arquivado_em` como `timestamp with time zone`, `is_nullable = YES`, sem default; `pg_trigger` lista `cards_impede_exclusao_com_lancamento` habilitado"
    - "O backstop foi provado vivo em produção: dentro de `begin;...rollback;`, excluir um card com lançamento é recusado, excluir um card sem lançamento passa, e excluir a coluna que contém o card movimentado é recusado"
    - "`pg_policies` para `public.cards` continua listando exatamente `team full access cards` — nenhuma policy criada, alterada ou removida"
    - "`docs/data-model.md` documenta `cards.arquivado_em` (no diagrama e na lista de entidades), o trigger `cards_impede_exclusao_com_lancamento` com o porquê do backstop, e a decisão consciente de não criar índice a ~48 linhas"
    - "O Board, o Financeiro e os Relatórios carregam normalmente depois do push, com os mesmos contratos de antes — a coluna nova é invisível para o código que ainda não a conhece"
  artifacts:
    - docs/data-model.md
  key_links:
    - "migração aplicada -> `public.is_team_member()`, já existente, não redefinida aqui"
    - "`public.cards.arquivado_em` -> `arquivarCardAction`/`desarquivarCardAction` (plano 06.2-05), que só podem existir depois desta coluna"
    - "trigger `cards_impede_exclusao_com_lancamento` -> `deleteCardAction`/`deleteColumnAction` (plano 06.2-05), que passam a mapear `P0001`"
---

<objective>
Aplicar a migração `cards.arquivado_em` + backstop de exclusão no banco de produção, provar contra o schema já gravado que a coluna e o trigger nasceram corretos, e documentar a decisão em `docs/data-model.md`.

Este é o único plano da fase que altera o banco real. `supabase db push` (ou o equivalente via SQL Editor) é **porta de mão única** — mesma régua da Phase 4 e da Phase 6.1 (D-18). Por isso o push está atrás de um `checkpoint:decision` explícito.

Há uma diferença importante em relação às migrações anteriores: além de uma coluna, esta migração instala um **trigger que recusa exclusões**. Ele muda o comportamento de duas funcionalidades que hoje funcionam (excluir card e excluir coluna) antes mesmo de qualquer código novo ser escrito. É por isso que a Task 3 confere as duas, e não só a existência da coluna.

Purpose: nada do resto da Phase 6.2 pode ser construído sobre uma coluna que não existe — o plano 06.2-04 lê `arquivado_em` no embed do Financeiro, e o 06.2-05 escreve nela.
Output: `arquivado_em` e o trigger vivos em produção, verificação pós-push registrada, `docs/data-model.md` atualizado.
</objective>

Este plano não cria símbolo novo — ver `## Artifacts this phase produces` em `06.2-01-PLAN.md`. Depois deste plano, `public.cards.arquivado_em`, `public.impedir_exclusao_de_card_com_lancamento()` e o trigger `cards_impede_exclusao_com_lancamento` passam a existir de fato no banco.

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-CONTEXT.md
@.planning/phases/04-funda-o-financeira/04-03-SUMMARY.md
@supabase/verificacao_cards_arquivado_em.sql
@supabase/migrations/20260819000000_cards_arquivado_em.sql
@docs/data-model.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Autorizar a aplicação da migração `cards.arquivado_em` + backstop no banco de produção</name>
  <read_first>
    - `supabase/verificacao_cards_arquivado_em.sql` — o bloco `RESULTADO DO ENSAIO` gravado no plano 06.2-02
    - `.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-02-SUMMARY.md` — o que o ensaio observou
  </read_first>
  <decision>
    Aplicar `20260819000000_cards_arquivado_em.sql` agora contra o banco de produção.
  </decision>
  <context>
    O banco tem ~48 imóveis reais, com parcelas e lançamentos reais, e não existe ambiente de staging nem backup automático (plano gratuito do Supabase). A migração é estritamente aditiva — uma coluna nulável, uma função e um trigger; nenhuma FK muda, nenhuma coluna some, nenhuma policy é tocada.

    **O que muda de comportamento no minuto seguinte ao push, mesmo sem nenhum código novo:** o trigger passa a recusar duas ações que hoje funcionam — excluir um card que tenha lançamento, e excluir uma coluna que contenha um card com lançamento. Essa recusa é o objetivo (D-14/D-15, VIDA-06: o estado perigoso é o de hoje), mas até o plano 06.2-05 mapear o `P0001`, a mensagem que aparece na tela vai ser o texto genérico "Não foi possível excluir o imóvel. Tente novamente." — funcionalmente correto, mas pouco explicativo. Essa janela dura o tempo entre este plano e o 06.2-05.

    O que **não** é reversível: depois do push, desfazer significa rodar o BLOCO 8 do runbook (drop trigger + drop function + drop column), que só é saída legítima enquanto nenhum contrato tiver sido arquivado — o que ainda é verdade agora, já que nenhuma UI de arquivamento existe.
  </context>
  <options>
    <option id="aplicar-agora">
      <name>Aplicar agora</name>
      <pros>Desbloqueia os planos 06.2-04 em diante imediatamente; o backstop contra o cascade — o item de maior severidade da fase — passa a valer hoje, antes de qualquer código novo; o ensaio já cobriu tudo que dá para cobrir sem gravar</pros>
      <cons>É porta de mão única sobre dado real, e a janela de mensagem genérica na exclusão existe até o plano 06.2-05</cons>
    </option>
    <option id="conferir-backup-antes">
      <name>Conferir o backup antes de aplicar</name>
      <pros>Abre uma saída independente do BLOCO 8 — mesmo procedimento já usado em 04-03 (export CSV manual de `cards`, `parcelas` e `parcela_lancamentos` pelo Table Editor)</pros>
      <cons>Atrasa a fase pelo tempo da conferência</cons>
    </option>
    <option id="aplicar-so-a-coluna">
      <name>Aplicar só a coluna, adiar o trigger</name>
      <pros>Elimina a janela de mensagem genérica: nada muda de comportamento até o plano 06.2-05 escrever a trava na Server Action</pros>
      <cons>Deixa o risco crítico em aberto por mais tempo — hoje um clique errado no card OU na coluna apaga histórico financeiro sem trava nenhuma. E o caminho de exclusão de coluna nunca é coberto pela Server Action de card, só pelo trigger</cons>
    </option>
    <option id="adiar">
      <name>Adiar</name>
      <pros>Nada muda em produção hoje</pros>
      <cons>Os planos 06.2-04 a 06.2-07 ficam bloqueados — nenhum deles pode ler ou escrever uma coluna que não existe</cons>
    </option>
  </options>
  <acceptance_criteria>
    - O usuário escolheu explicitamente uma das quatro opções
    - Se `conferir-backup-antes`, o backup foi anotado (quais tabelas, qual data) antes de seguir para a Task 2
    - Se `aplicar-so-a-coluna`, a Task 2 aplica apenas a Seção 1 do arquivo e registra que a Seção 2 fica pendente, e o plano 06.2-05 herda a obrigação de reabrir este portão
    - Se `adiar`, as Tasks 2 e 3 não são executadas e o plano encerra com o motivo registrado
  </acceptance_criteria>
  <reversibility rating="one-way">A task seguinte grava DDL em produção. Desfazer exige o BLOCO 8 do runbook, que só é saída legítima enquanto nenhum contrato tiver sido arquivado.</reversibility>
  <resume-signal>Responda: `aplicar-agora`, `conferir-backup-antes` (com a data e as tabelas exportadas), `aplicar-so-a-coluna` ou `adiar` (com o motivo).</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Aplicar a migração em produção e documentar a decisão em `docs/data-model.md`</name>
  <precondition>A Task 1 foi respondida com `aplicar-agora`, `conferir-backup-antes` ou `aplicar-so-a-coluna`.</precondition>
  <files>supabase/migrations/20260819000000_cards_arquivado_em.sql, docs/data-model.md</files>
  <read_first>
    - `supabase/migrations/20260819000000_cards_arquivado_em.sql` — o arquivo a aplicar; conferir que é o estado aprovado no ensaio do plano 06.2-02
    - `.planning/phases/04-funda-o-financeira/04-03-SUMMARY.md` — como o push foi feito de verdade na Phase 4 (CLI não instalado na máquina do operador; aplicado via SQL Editor colando a DDL) — repetir o mesmo caminho se o CLI continuar ausente
    - `docs/data-model.md` — o diagrama `mermaid`, a lista de entidades e a seção "Decisões de design", estilo "decisão + porquê" a seguir
  </read_first>
  <action>
Primeiro, checar se o Supabase CLI está instalado e linkado (`supabase migration list`). Se funcionar, rodar `supabase db push` a partir da raiz do repositório e confirmar com `supabase migration list` que `20260819000000` passa a aparecer em Local **e** Remote. Se o CLI não estiver instalado ou pedir autenticação interativa sem TTY, seguir o caminho documentado em `04-03-SUMMARY.md`: colar a DDL completa do arquivo de migração no SQL Editor de produção e confirmar "Success. No rows returned" — sem o wrapper `begin`/`rollback` do ensaio, porque aqui a intenção é gravar. Registrar qual caminho foi usado.

Se a Task 1 foi respondida com `aplicar-so-a-coluna`, aplicar **apenas** a Seção 1 do arquivo (o `alter table ... add column if not exists arquivado_em timestamptz`) e registrar por escrito, no SUMMARY, que a Seção 2 (função + trigger) ficou pendente e que o portão precisa ser reaberto antes do plano 06.2-05.

Se o push falhar, ler o erro, corrigir a causa no arquivo de migração, atualizar `supabase/verificacao_cards_arquivado_em.sql` para continuar batendo, voltar ao ensaio do plano 06.2-02 e nunca tentar um segundo push sem reensaiar.

Depois do push bem-sucedido, atualizar `docs/data-model.md`:
- no diagrama de entidades (bloco `mermaid`), acrescentar `timestamptz arquivado_em` ao bloco `CARDS`
- na lista de entidades, estender a descrição de `cards` mencionando `arquivado_em` — nulo significa em operação; preenchido significa arquivado, e a data é a do arquivamento mais recente
- acrescentar itens em "Decisões de design", no mesmo estilo "decisão + porquê" já usado ali:
  1. **`timestamptz` em vez de `boolean`** (D-07): rastreabilidade — o "quando" é barato agora e caro de acrescentar depois; e nulo já é um estado válido, então não há backfill
  2. **O trigger `cards_impede_exclusao_com_lancamento` como backstop de banco**: a cadeia `cards → parcelas → parcela_lancamentos` é `on delete cascade` desde `20260816000000_financeiro_schema.sql`, e `columns → cards` é cascade desde o schema inicial — ou seja, existem **dois** caminhos que apagam histórico financeiro, e a Server Action de exclusão de card só cobre um. O trigger cobre os dois e sobrevive a um caminho de código futuro que esqueça a trava, que é exatamente como o buraco atual nasceu (`deleteCardAction` foi escrita antes de `parcelas` existir e nunca foi revisitada). Registrar também por que **não** se trocou a FK para `on delete restrict`: isso bloquearia também a exclusão de um contrato que só tem parcelas geradas automaticamente, sem nenhum lançamento — o que D-14 explicitamente quer permitir — e seria uma alteração de schema existente em vez de uma adição
  3. **Por que não há índice em `arquivado_em`**: ~48 linhas em `cards`; o planner varre a tabela de qualquer jeito e um índice só custaria em insert/update. Gatilho para revisitar: `cards` na ordem de milhares de linhas, aí o índice parcial sobre `arquivado_em is not null` é o primeiro passo
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel" && grep -q "arquivado_em" docs/data-model.md && grep -q "cards_impede_exclusao_com_lancamento" docs/data-model.md && grep -qi "cascade" docs/data-model.md && echo DOCS_OK</automated>
  </verify>
  <acceptance_criteria>
    - O comando de push (CLI ou SQL Editor) terminou sem erro; se terminou com erro, o arquivo foi corrigido e o ensaio do plano 06.2-02 foi refeito antes de nova tentativa
    - O caminho usado (CLI ou SQL Editor) está registrado no SUMMARY
    - `docs/data-model.md` cita `arquivado_em` no diagrama `mermaid` e na lista de entidades
    - `docs/data-model.md` cita `cards_impede_exclusao_com_lancamento` e explica os dois caminhos de cascade, além de dizer por que a FK **não** virou `on delete restrict`
    - `docs/data-model.md` registra a decisão de não criar índice e o gatilho para revisitá-la
    - Nenhum arquivo fora de `supabase/migrations/20260819000000_cards_arquivado_em.sql` e `docs/data-model.md` foi modificado por esta task
  </acceptance_criteria>
  <reversibility rating="one-way">Grava DDL no banco de produção. Desfazer exige o BLOCO 8 do runbook, válido só enquanto nenhum contrato tiver sido arquivado.</reversibility>
  <done>A migração está aplicada em produção e `docs/data-model.md` documenta a coluna, o trigger e as duas alternativas rejeitadas.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar em produção que a coluna e o backstop nasceram corretos e que nada quebrou</name>
  <precondition>A Task 2 aplicou a migração com sucesso.</precondition>
  <read_first>
    - `supabase/verificacao_cards_arquivado_em.sql` — Parte B, BLOCOs 4 a 7
    - O `RESULTADO DO ENSAIO` registrado no plano 06.2-02, para comparar os números
  </read_first>
  <what-built>
    A coluna `public.cards.arquivado_em` (`timestamptz`, nulável, sem default), a função `public.impedir_exclusao_de_card_com_lancamento()` e o trigger `cards_impede_exclusao_com_lancamento` (`before delete on public.cards for each row`), aplicados no banco de produção.
  </what-built>
  <how-to-verify>
    **Parte 1 — no SQL Editor (ou psql), rodando a Parte B do runbook.** O BLOCO 6 contém um `begin;...rollback;` e vale o mesmo aviso do D-19: cole-o inteiro num único Run, ou use psql.

    1. **BLOCO 4** — a coluna existe com `data_type = timestamp with time zone`, `is_nullable = YES`, sem default; `select count(*) from public.cards where arquivado_em is not null` devolve **0**; `cards_total`, `parcelas_total`, `lancamentos_total` e `updated_at_max` idênticos aos do ensaio. Se `updated_at_max` mudou, algo escreveu em `cards` — investigue antes de seguir.
    2. **BLOCO 5** — o trigger `cards_impede_exclusao_com_lancamento` aparece em `pg_trigger` com `tgenabled = 'O'`.
    3. **BLOCO 6** (transação revertida, um único Run) — os três lados do backstop contra o banco já migrado:
       - excluir um card **com** lançamento → recusado
       - excluir um card **sem** nenhum lançamento → passa (1 linha)
       - excluir a `columns` que contém o card movimentado → recusado
    4. **BLOCO 7** — `pg_policies` para `public.cards` continua listando exatamente `team full access cards`.

    **Parte 2 — no navegador, contra produção.** A coluna é invisível para o código atual, então nada deveria mudar. Conferir à mão:

    5. **Board** carrega com os mesmos contratos de antes, arrastar entre colunas continua funcionando, o toggle Ativo/Inativo no diálogo de detalhes continua salvando.
    6. **Financeiro** carrega, "Vencendo hoje" continua sendo o padrão, os quatro filtros continuam combinando em E lógico, e Pagamento/Ajustar/Histórico continuam abrindo e gravando (regressão da Phase 6/6.1 — D-20).
    7. **Relatórios** carrega com os mesmos números.
    8. **Excluir um card que TEM lançamento** — a exclusão deve falhar. Neste momento a mensagem ainda é o texto genérico ("Não foi possível excluir o imóvel. Tente novamente."), porque o mapeamento de `P0001` só chega no plano 06.2-05. Confirmar que **falhou** é o ponto; a mensagem feia é esperada e temporária. Conferir depois, por SQL, que o card continua lá e que os lançamentos dele continuam lá.
    9. **Excluir um card que NÃO tem nenhum lançamento** (crie um card de teste, deixe o Financeiro gerar parcela para ele, e não registre nada) — a exclusão deve **funcionar**. Se falhar, o backstop está largo demais e contraria D-14; reporte.
  </how-to-verify>
  <acceptance_criteria>
    - BLOCO 4: coluna com tipo/nulabilidade corretos, zero arquivados, quatro números idênticos ao ensaio
    - BLOCO 5: trigger presente e habilitado
    - BLOCO 6: os três resultados observados (recusa, passa, recusa)
    - BLOCO 7: policy de `cards` inalterada
    - Board, Financeiro e Relatórios carregam sem erro, com os mesmos dados
    - Passo 8: a exclusão do card com lançamento falhou, e o card e seus lançamentos continuam no banco (confirmado por SQL, não pela tela)
    - Passo 9: a exclusão do card sem lançamento funcionou
  </acceptance_criteria>
  <resume-signal>Responda com o resultado de cada um dos nove passos, ou com o que travou.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operador → banco de produção | DDL definitiva sobre ~48 contratos reais com histórico financeiro; sem staging, sem backup automático |
| navegador → PostgREST | RLS de `public.cards` (`is_team_member()`); a coluna nova precisa herdar isso sem policy nova |
| aplicação → trigger novo | A partir deste plano, exclusões que hoje passam passam a ser recusadas pelo banco — mudança de comportamento antes de qualquer código novo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06.2-11 | Tampering | `supabase db push` sobre produção | high | mitigate | `checkpoint:decision` bloqueante antes do push, com o resultado do ensaio do 06.2-02 como base; opção de backup manual das três tabelas; BLOCO 8 do runbook como saída enquanto nada estiver arquivado |
| T-06.2-12 | Information Disclosure | `public.cards.arquivado_em` exposta via PostgREST | low | accept | Coluna nova em tabela já coberta por `team full access cards`; nenhuma policy criada, alterada ou removida — verificado no BLOCO 7 |
| T-06.2-13 | Denial of Service | trigger recusando exclusão legítima | high | mitigate | BLOCO 6 e Passo 9 exigem observar um card sem lançamento sendo excluído com sucesso, contra o banco já migrado |
| T-06.2-14 | Denial of Service | janela de mensagem genérica entre este plano e o 06.2-05 | medium | accept | Reconhecida por escrito no `checkpoint:decision` e oferecida como opção `aplicar-so-a-coluna`; o comportamento (recusar) já é o correto, só o texto é pobre, e a janela é curta |
| T-06.2-15 | Tampering | regressão silenciosa nas Phases 6/6.1 | medium | mitigate | Passos 5 a 7 do checkpoint cobrem Board, Financeiro (padrão "Vencendo hoje", quatro filtros, diálogos da Phase 6) e Relatórios à mão (D-20) |
| T-06.2-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | Este plano não instala pacote da aplicação. Se o Supabase CLI for instalado para o push, é ferramenta oficial do fornecedor já usada no projeto, fora do grafo de dependências do app |
</threat_model>

<verification>
- Parte B do runbook rodada contra o banco migrado: coluna, trigger, os três lados do backstop e a lista de policies
- Comparação numérica com o baseline do ensaio (`cards_total`, `parcelas_total`, `lancamentos_total`, `updated_at_max`)
- `grep` sobre `docs/data-model.md` confirmando coluna, trigger e o raciocínio das duas alternativas rejeitadas
- Teste manual no navegador cobrindo Board, Financeiro (D-20) e Relatórios
- Prova por SQL — não pela tela — de que o card cuja exclusão falhou continua no banco com seus lançamentos
</verification>

<success_criteria>
- `public.cards.arquivado_em`, `public.impedir_exclusao_de_card_com_lancamento()` e `cards_impede_exclusao_com_lancamento` existem em produção
- Zero contratos arquivados, zero linhas alteradas, `updated_at_max` intocado
- O backstop bloqueia card com lançamento e coluna com card movimentado, e libera card sem lançamento
- `pg_policies` de `public.cards` inalterada
- `docs/data-model.md` documenta a coluna, o trigger, a rejeição do `on delete restrict` e a ausência deliberada de índice
- Board, Financeiro e Relatórios seguem funcionando como antes
</success_criteria>

<output>
Create `.planning/phases/06.2-ciclo-de-vida-do-contrato/06.2-03-SUMMARY.md` when done
</output>
