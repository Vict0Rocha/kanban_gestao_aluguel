---
phase: 21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/components/reports/relatorio-financeiro-pdf.ts
  - web/src/components/reports/reconciliacao-pdf.ts
  - .planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md
autonomous: true
requirements: [PDFMODELO-01, PDFMODELO-02, PDFMODELO-03, PDFMODELO-04, PDFMODELO-05]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Exportar o PDF de qualquer um dos dois relatórios baixa um A4 paisagem, com a paleta cinza (texto #262626, cabeçalho/Total #f2f2f2, bordas #d9d9d9, rótulos/rodapé #6b6b6b) — nenhuma cor viva em lugar nenhum do documento (D-01/D-03, PDFMODELO-01)"
    - "A lista principal de cada PDF mostra uma linha horizontal sutil entre as linhas (sem grade vertical) e zebra branco/cinza claro alternando (D-04, PDFMODELO-02)"
    - "A última linha da lista é 'Total', em negrito com o mesmo fundo cinza do cabeçalho, somando a coluna Valor de todas as linhas efetivamente listadas (já respeitando o filtro aplicado); nunca se repete em cada página de um PDF com múltiplas páginas (showFoot:'lastPage', D-06, PDFMODELO-03)"
    - "Os três blocos existentes em cada PDF (título+timestamp, tabela de filtros aplicados, bloco de totais/resumo) continuam presentes, com a mesma estrutura de antes — só recoloridos (D-07, PDFMODELO-04)"
    - "reconciliacao-pdf.ts ganha a coluna 'Inquilino' e a célula 'Contrato' passa a mostrar o proprietário (mesmo formato #numero+texto) em vez do endereço — fechando a mudança que a Phase 20 deixou pendente só no PDF (D-02 corrigido, PDFMODELO-05)"
    - "relatorio-financeiro-pdf.ts mantém suas colunas atuais inalteradas (Imóvel/Proprietário/Competência/Vencimento/Situação/Valor) — só paleta/orientação/estilo de lista/linha de Total mudam"
    - "10-UI-SPEC.md § 'PDF Export Layout Contract' documenta o novo contrato (paisagem/paleta cinza/lista sem grade+zebra/linha de Total), cobrindo os dois módulos, sem ficar desatualizado em relação ao código"
  artifacts:
    - web/src/components/reports/relatorio-financeiro-pdf.ts
    - web/src/components/reports/reconciliacao-pdf.ts
    - .planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md
  key_links:
    - "new jsPDF({ orientation: 'landscape', ... }) -> autoTable(theme:'plain', styles.lineWidth bottom-only, alternateRowStyles) -> foot/footStyles/showFoot:'lastPage' -> doc.save() — mesmo mecanismo nos dois módulos (RESEARCH.md Pattern 1/2, verificado contra jspdf-autotable@5.0.8 instalado)"
    - "bodyRows (valor calculado uma única vez em relatorio-financeiro-pdf.ts) -> célula renderizada da lista + soma do Total — nunca recalculado uma segunda vez (Don't Hand-Roll, RESEARCH.md)"
    - "reconciliacao-pdf.ts: l.cards?.proprietario / l.cards?.inquilino (já buscados desde a Phase 19) -> colunas Contrato/Inquilino renderizadas; Valor desloca do índice 3 para o 4, colSpan do Total de 3 para 4 (PATTERNS.md)"
  prohibitions:
    - "MUST NOT deixar showFoot no default 'everyPage' — sempre 'lastPage' explícito nos dois módulos (Pitfall 3, RESEARCH.md)"
    - "MUST NOT confiar em columnStyles para alinhar a célula de valor do Total — halign:'right' direto no CellDef do foot, columnStyles não alcança a seção foot (Pitfall 2, RESEARCH.md)"
    - "MUST NOT setar lineWidth top e bottom juntos no mesmo styles da lista — dobra a linha renderizada em cada fronteira de linha (Anti-Pattern, RESEARCH.md)"
    - "MUST NOT mudar as colunas de relatorio-financeiro-pdf.ts — só reconciliacao-pdf.ts ganha a mudança de colunas nesta fase (D-02 corrigido)"
    - "MUST NOT re-derivar o valor por linha uma segunda vez para somar o Total — reusar o mesmo valor já computado por linha, tanto em relatorio-financeiro-pdf.ts (situação-dependente) quanto em reconciliacao-pdf.ts (l.valor direto)"
    - "MUST NOT criar nenhum arquivo em supabase/migrations/ — fase zero-schema, puramente client-side"
    - "MUST NOT usar import estático de jspdf/jspdf-autotable no topo de nenhum dos dois módulos — só import() dinâmico dentro da função exportada (SSR safety, já documentado nos dois arquivos)"
---

<objective>
Fecha PDFMODELO-01..05 (21-CONTEXT.md D-01..D-07, corrigido; 21-RESEARCH.md; 21-PATTERNS.md): os dois PDFs
exportáveis já em produção — `relatorio-financeiro-pdf.ts` (Relatório Financeiro dedicado, Phase 10) e
`reconciliacao-pdf.ts` (Dinheiro da imobiliária, Phase 19) — trocam o contrato de layout verde/retrato/sem-total
por um novo contrato cinza/paisagem/lista-sem-grade-com-zebra/com-linha-de-Total, aplicado identicamente aos
dois. Os três blocos estruturais já existentes (título+timestamp, filtros aplicados, totais/resumo) continuam
com a mesma estrutura, só recoloridos (D-07). Adicionalmente, e só em `reconciliacao-pdf.ts`, a lista ganha uma
sexta coluna ("Inquilino") e a célula "Contrato" troca endereço por proprietário — fechando, nesta fase, a
mudança de colunas que a Phase 20 deixou deliberadamente pendente só no PDF (D-09 daquela fase, D-02 corrigido
desta).

Todo o mecanismo técnico (orientação paisagem, paleta cinza, `theme:"plain"` + linha horizontal sutil + zebra
na lista, e a nova linha de Total via `foot`/`footStyles`/`showFoot:"lastPage"`) foi verificado nesta pesquisa
contra o código-fonte real do `jspdf-autotable@5.0.8` efetivamente instalado — não contra memória/docs
genéricos — e não exige nenhuma biblioteca nova.

Purpose: o usuário pediu um visual mais profissional/corporativo para os dois PDFs (referência Sienge,
"adaptar, não copiar", D-08), com uma linha de total que hoje não existe em nenhum dos dois.
Output: `relatorio-financeiro-pdf.ts` e `reconciliacao-pdf.ts` redesenhados com o novo contrato de layout;
`10-UI-SPEC.md` § "PDF Export Layout Contract" atualizado in-place para documentar o contrato novo, cobrindo os
dois módulos.
</objective>

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros/21-CONTEXT.md
@.planning/phases/21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros/21-RESEARCH.md
@.planning/phases/21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros/21-PATTERNS.md
@web/src/components/reports/relatorio-financeiro-pdf.ts
@web/src/components/reports/reconciliacao-pdf.ts
@.planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md
</context>

## Artifacts this phase produces

- **`relatorio-financeiro-pdf.ts`** (redesenhado) — paisagem, paleta cinza, lista `theme:"plain"` com linha
  horizontal sutil + zebra, nova linha de Total via `foot`/`footStyles`/`showFoot:"lastPage"`. Colunas
  inalteradas.
- **`reconciliacao-pdf.ts`** (redesenhado) — mesmo tratamento visual do arquivo acima, mais a mudança de
  colunas corrigida (D-02): "Contrato" troca endereço por proprietário, nova coluna "Inquilino" inserida logo
  depois — 5 colunas viram 6.
- **`10-UI-SPEC.md` § "PDF Export Layout Contract"** (atualizado in-place) — documenta o contrato novo
  (paisagem/paleta cinza/lista sem grade+zebra/linha de Total), cobrindo explicitamente os dois módulos.

<planner_assumptions>
- **A-01 — prefixo único de requisito `PDFMODELO`**: ao contrário de Phase 19/20 (duas capacidades
  independentes discutidas separadamente, cada uma com seu próprio prefixo), esta fase é uma única capacidade
  — o redesenho visual — aplicada identicamente aos dois módulos; a mudança de colunas de `reconciliacao-pdf.ts`
  é um item dentro do mesmo redesenho (fecha D-09 da Phase 20), não uma segunda capacidade com discussão
  própria. Um único prefixo, 5 requisitos.
- **A-02 — 2 tasks (tracer + expansão), sem `checkpoint:decision`**: Task 1 prova o mecanismo visual novo
  inteiro (paisagem/paleta/lista/Total) de ponta a ponta em `relatorio-financeiro-pdf.ts`, com
  `tsc`/`lint`/`build` reais. Task 2 espelha o mesmo tratamento em `reconciliacao-pdf.ts`, soma a mudança de
  colunas corrigida (D-02), e atualiza `10-UI-SPEC.md` in-place (recomendação da própria 21-RESEARCH.md, Open
  Question 1) agora que a forma final dos dois arquivos é conhecida. `reversibility_gates: true`, mas toda
  decisão desta fase é de apresentação, client-side, plenamente reversível — nenhum `checkpoint:decision`
  inserido. `human_verify_mode: end-of-phase` (config.json) — um único `<human-check>` consolidado, no fim da
  Task 2, cobrindo os dois PDFs já prontos.
- **A-03 — nenhuma atualização em `docs/data-model.md`**: esta fase não introduz nenhuma decisão de schema nem
  de arquitetura nova — é um redesenho de renderização/estilo puramente client-side sobre módulos e entidades já
  documentados.
- **A-04 — REQUIREMENTS.md/ROADMAP.md atualizados pelo planner nesta mesma sessão**: por pedido explícito do
  orquestrador para esta fase, a seção `### PDFMODELO` (com a tabela de Coverage) e o preenchimento de
  Goal/Requirements/Plans do Phase 21 no ROADMAP.md são escritos diretamente nesta sessão de planejamento, não
  deixados para o fechamento de fase.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="false">
  <name>Task 1: Novo contrato visual ponta a ponta — relatorio-financeiro-pdf.ts (paisagem, paleta cinza, lista sem grade+zebra, linha de Total)</name>

  <files>web/src/components/reports/relatorio-financeiro-pdf.ts</files>

  <read_first>
    - `web/src/components/reports/relatorio-financeiro-pdf.ts` (arquivo inteiro, 251 linhas) — construtor do
      `jsPDF` (linha 72), constantes de cor (linhas 81-84), Blocos 1-2 (filtros/resumo, linhas 95-160,
      `theme:"grid"`, usam os identificadores `foreground`/`border`), a lista (linhas 164-221, especialmente o
      `body: linhas.map(...)` que já calcula `valor` situação-dependente dentro do próprio `.map`, linhas
      180-201), ramo de lista vazia (linhas 165-173), rodapé (linhas 223-246, usa `muted`)
    - `.planning/phases/21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros/21-PATTERNS.md` seção
      "`web/src/components/reports/relatorio-financeiro-pdf.ts`" (linhas 18-146) — excertos antes/depois exatos
      desta task: construtor (linha 44-50), cores (linha 52-67), Blocos 1-3 recolor-only (linha 69-91), lista
      redesenhada completa com `bodyRows`/`totalValor`/`foot` (linha 93-142)
    - `.planning/phases/21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros/21-RESEARCH.md` seção
      "Pattern 1" (linha sem grade vertical + zebra), "Pattern 2" (Total via `foot`, primeiro exemplo — Valor
      índice 5, `colSpan:5`), "Pitfall 2" (`columnStyles` não alcança `foot`), "Pitfall 3" (`showFoot` default
      `"everyPage"`), Anti-Pattern (nunca `top`+`bottom` juntos em `lineWidth`)
  </read_first>

  <action>
1. No construtor `new jsPDF({ orientation: "portrait", unit: "pt", format: "a4" })` (linha 72), trocar
`"portrait"` por `"landscape"` (D-01) — `unit`/`format` ficam como estão.

2. Trocar as quatro constantes de cor (linhas 81-84) pelas cinco novas, mantendo os mesmos nomes onde já
existem (`foreground`, `border`, `muted`, `rowShade`) e acrescentando `headerFill` (novo — antes o cabeçalho da
lista usava fundo branco fixo): `foreground = [38, 38, 38]` (#262626), `headerFill = [242, 242, 242]` (#f2f2f2,
cabeçalho da lista e linha de Total), `border = [217, 217, 217]` (#d9d9d9), `muted = [107, 107, 107]` (#6b6b6b),
`rowShade = [247, 247, 247]` (zebra, mais claro que `headerFill`, distinto dele). Como os Blocos 1-2 (linhas
95-160) e o rodapé (linhas 223-246) já leem esses identificadores por nome (`textColor: foreground`,
`lineColor: border`, `doc.setTextColor(...muted)`), nenhuma outra linha desses blocos precisa mudar — a troca
de valor nas cinco constantes já recolore título, tabela de filtros, tabela de resumo e rodapé inteiros (D-03,
D-07).

3. Redesenhar a `autoTable` da lista (linhas 175-220, dentro do `else` do `if (linhas.length === 0)`):
   a. Extrair o `.map` de `linhas` para uma constante `bodyRows` que retorna, por linha, tanto o `valor`
   calculado (a mesma expressão situação-dependente já existente — `valorPago` quando paga/conciliada, senão
   `Math.max(valorDevido - valorPago, 0)`) quanto as `cells` renderizadas (o mesmo array de 6 células de hoje,
   incluindo `formatCurrency(valor)` na última). Declarar `const totalValor = bodyRows.reduce((acc, r) => acc +
   r.valor, 0)` logo depois — o Total soma exatamente o mesmo `valor` já usado para renderizar cada linha,
   nunca uma segunda derivação (Don't Hand-Roll, RESEARCH.md).
   b. No `autoTable(doc, {...})`, adicionar `theme: "plain"` explícito (era implícito antes). Trocar `body:
   linhas.map(...)` por `body: bodyRows.map((r) => r.cells)`. Adicionar `foot: [[{ content: "Total", colSpan: 5
   }, { content: formatCurrency(totalValor), styles: { halign: "right" } }]]` — o `halign` fica direto no
   `CellDef.styles` da célula de valor do `foot`, nunca dependendo de `columnStyles` (que não alcança a seção
   `foot`, Pitfall 2). Adicionar `showFoot: "lastPage"` explícito — nunca deixar no default da biblioteca, que
   repetiria o Total em cada página de um export com 2+ páginas (Pitfall 3). Manter `showHead: "everyPage"`
   como está.
   c. Em `styles`, manter `fontSize: 9`/`textColor: foreground`/`lineColor: border` e acrescentar `lineWidth: {
   top: 0, right: 0, bottom: 0.75, left: 0 }` (linha horizontal sutil só embaixo de cada célula — nunca `top`
   junto com `bottom`, dobraria a linha renderizada em cada fronteira) e `cellPadding: 5`.
   d. Trocar `headStyles: { fontStyle: "bold", fillColor: [255, 255, 255], textColor: foreground }` (fundo
   branco fixo) por `fillColor: headerFill` (fundo cinza claro, D-05). Adicionar `footStyles: { fontStyle:
   "bold", fillColor: headerFill, textColor: foreground }` (novo — a linha de Total usa o mesmo fundo do
   cabeçalho, negrito, D-06).
   e. Manter `alternateRowStyles: { fillColor: rowShade }` (zebra, já existia, valor da constante agora é
   cinza) e `columnStyles: { 5: { halign: "right" } }` (índice de Valor inalterado nesta task, D-02).

4. O ramo de lista vazia (linhas 165-173) e o rodapé (linhas 223-246) não mudam de estrutura — só herdam a nova
cor de `muted` automaticamente via o passo 2.
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && f=src/components/reports/relatorio-financeiro-pdf.ts && grep -q 'orientation: "landscape"' "$f" && grep -q "foreground: \[number, number, number\] = \[38, 38, 38\]" "$f" && grep -q "headerFill: \[number, number, number\] = \[242, 242, 242\]" "$f" && grep -q "border: \[number, number, number\] = \[217, 217, 217\]" "$f" && grep -q "muted: \[number, number, number\] = \[107, 107, 107\]" "$f" && grep -q "rowShade: \[number, number, number\] = \[247, 247, 247\]" "$f" && grep -q "const bodyRows = linhas.map" "$f" && grep -q "const totalValor = bodyRows.reduce" "$f" && grep -q 'theme: "plain"' "$f" && grep -q "body: bodyRows.map((r) => r.cells)" "$f" && grep -q 'content: "Total", colSpan: 5' "$f" && grep -q 'content: formatCurrency(totalValor), styles: { halign: "right" }' "$f" && grep -q 'showFoot: "lastPage"' "$f" && grep -q 'lineWidth: { top: 0, right: 0, bottom: 0.75, left: 0 }' "$f" && grep -q "headStyles: { fontStyle: \"bold\", fillColor: headerFill, textColor: foreground }" "$f" && grep -q "footStyles: { fontStyle: \"bold\", fillColor: headerFill, textColor: foreground }" "$f" && grep -q "alternateRowStyles: { fillColor: rowShade }" "$f" && grep -q "columnStyles: { 5: { halign: \"right\" } }" "$f" && test -z "$(git status --porcelain -- ../supabase/migrations 2>/dev/null)" && npx tsc --noEmit && npm run lint && npm run build && echo TASK1_TRACER_OK</automated>
  </verify>

  <acceptance_criteria>
    - `new jsPDF(...)` usa `orientation: "landscape"`; as 5 constantes de cor têm os valores novos
      (`headerFill` é nova)
    - A lista usa `theme: "plain"`, `bodyRows`/`totalValor` computados uma única vez, `foot` com `colSpan: 5` +
      célula de valor com `halign: "right"` direto no `CellDef.styles`, `showFoot: "lastPage"` explícito
    - `styles.lineWidth` tem só `bottom: 0.75` diferente de zero (nunca `top` junto)
    - `headStyles`/`footStyles` usam `fillColor: headerFill`, `fontStyle: "bold"`
    - Colunas da lista inalteradas (6 células, mesma ordem de hoje)
    - Zero arquivo novo em `supabase/migrations/`
    - `npx tsc --noEmit`, `npm run lint` e `npm run build` (de dentro de `web/`) não reportam erro novo
  </acceptance_criteria>

  <reversibility rating="reversible">Redesenho puramente visual/client-side de um módulo já em produção —
  troca de constantes de cor, opções de `jspdf-autotable` já suportadas pela versão instalada, sem nenhuma
  migração, escrita no banco ou Server Action nova. Reverter é restaurar o arquivo.</reversibility>

  <done>`relatorio-financeiro-pdf.ts` exporta um PDF A4 paisagem, paleta cinza inteira, lista com linha
  horizontal sutil + zebra (sem grade vertical) e uma linha "Total" em negrito no fim, somando a coluna Valor
  das linhas listadas, sem repetir em páginas seguintes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Espelhar o novo contrato em reconciliacao-pdf.ts + mudança de colunas corrigida (D-02) + atualizar 10-UI-SPEC.md</name>

  <files>web/src/components/reports/reconciliacao-pdf.ts, .planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md</files>

  <read_first>
    - `web/src/components/reports/reconciliacao-pdf.ts` (arquivo inteiro, 203 linhas) — `LinhaListaPDF`
      (linhas 20-32, já declara `cards.inquilino: string | null`, hoje não lido pelo `.map`), construtor
      (linha 57), constantes de cor (linhas 66-69), Blocos 1-2 (linhas 95-139, `theme:"grid"`, mesmos
      identificadores de cor da Task 1), lista atual (linhas 154-176, `head` de 5 colunas, célula "Contrato"
      lendo `l.cards?.endereco`), rodapé (linhas 178-198)
    - `.planning/phases/21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros/21-PATTERNS.md` seção
      "`web/src/components/reports/reconciliacao-pdf.ts`" (linhas 150-229) — baseline atual de 5 colunas (linha
      178-188) vs. alvo corrigido de 6 colunas (linha 190-225), com a consequência de deslocamento de índice
      explicada linha 227 (Valor 3→4, `colSpan` do Total 3→4)
    - `.planning/phases/21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros/21-CONTEXT.md` D-02
      (parágrafo "Correção pós-pesquisa") — a decisão operativa: `reconciliacao-pdf.ts` GANHA a mudança de
      colunas nesta fase, `relatorio-financeiro-pdf.ts` não muda de colunas
    - `.planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md` linhas 159-196 (seção completa "## PDF
      Export Layout Contract" a atualizar in-place no passo 3 desta task)
  </read_first>

  <action>
1. No construtor `new jsPDF({ orientation: "portrait", unit: "pt", format: "a4" })` (linha 57), trocar
`"portrait"` por `"landscape"` — mesma mudança da Task 1. Trocar as quatro constantes de cor (linhas 66-69)
pelos mesmos cinco valores da Task 1 (`foreground = [38,38,38]`, `headerFill = [242,242,242]` — nova,
`border = [217,217,217]`, `muted = [107,107,107]`, `rowShade = [247,247,247]`). Blocos 1-2 (linhas 95-139) e o
rodapé (linhas 178-198) não precisam de nenhuma outra mudança — herdam a cor nova pelos mesmos identificadores,
igual à Task 1.

2. Redesenhar a `autoTable` da lista (linhas 154-176, dentro do `else` do `if (linhas.length === 0)`) com DUAS
mudanças simultâneas — o mesmo tratamento visual da Task 1, MAIS a mudança de colunas corrigida (D-02):
   a. Declarar `const totalValor = linhas.reduce((acc, l) => acc + l.valor, 0)` antes do `autoTable` (este
   arquivo não precisa de um `bodyRows` intermediário como a Task 1 — `l.valor` já é um número plano por linha,
   sem derivação situação-dependente).
   b. Trocar `head: [["Data", "Contrato", "Tipo", "Valor", "Observação"]]` (5 colunas) por `head: [["Data",
   "Contrato", "Inquilino", "Tipo", "Valor", "Observação"]]` (6 colunas — "Inquilino" inserida logo depois de
   "Contrato").
   c. No `body: linhas.map((l) => [...])`, trocar a célula "Contrato" de `` `#${l.cards?.numero ?? 0}
   ${l.cards?.endereco ?? ""}` `` para `` `#${l.cards?.numero ?? 0} ${l.cards?.proprietario ?? ""}` `` (mesmo
   formato `#numero texto`, só troca qual campo de `cards` alimenta o texto) e inserir uma nova célula logo
   depois: `l.cards?.inquilino ?? ""`. O array de 5 células vira um array de 6 — `Data`, `Contrato`,
   `Inquilino`, `Tipo`, `Valor`, `Observação`, nessa ordem. O campo `endereco` continua declarado em
   `LinhaListaPDF.cards` (não remover do tipo) — só a célula desta lista deixa de lê-lo.
   d. Adicionar `theme: "plain"`. Adicionar `foot: [[{ content: "Total", colSpan: 4 }, { content:
   formatCurrency(totalValor), styles: { halign: "right" } }, ""]]` — `colSpan: 4` cobre as 4 primeiras colunas
   (Data/Contrato/Inquilino/Tipo), a célula de valor vem em seguida com `halign: "right"` direto no `styles`
   (nunca via `columnStyles`), e uma célula vazia final fecha a coluna Observação — 3 células de `foot`
   somando exatamente as 6 colunas do `head` (2 do primeiro objeto por causa do `colSpan:4` + 1 + 1 = 6).
   Adicionar `showFoot: "lastPage"` explícito. Manter `showHead: "everyPage"`.
   e. Em `styles`, manter `fontSize: 9`/`textColor: foreground`/`lineColor: border` e acrescentar `lineWidth: {
   top: 0, right: 0, bottom: 0.75, left: 0 }` e `cellPadding: 5` (idêntico à Task 1).
   f. Trocar `headStyles: { fontStyle: "bold", fillColor: [255, 255, 255], textColor: foreground }` por
   `fillColor: headerFill`. Adicionar `footStyles: { fontStyle: "bold", fillColor: headerFill, textColor:
   foreground }`.
   g. Manter `alternateRowStyles: { fillColor: rowShade }`. Trocar `columnStyles: { 3: { halign: "right" } }`
   (Valor no índice 3, layout de 5 colunas) por `columnStyles: { 4: { halign: "right" } }` (Valor desloca para
   o índice 4 porque "Inquilino" foi inserida no índice 2 — PATTERNS.md linha 227, o detalhe mais propenso a
   erro desta task).

3. Atualizar `.planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md` § "PDF Export Layout Contract"
(linhas 159-196) in-place (recomendação da própria 21-RESEARCH.md, Open Question 1), agora que a forma final
dos dois módulos é conhecida. Abrir a seção com uma frase deixando explícito que o contrato agora governa os
DOIS módulos (`relatorio-financeiro-pdf.ts` e `reconciliacao-pdf.ts`, espelhados desde a Phase 19, redesenhados
na Phase 21). No item "1. Page format", trocar "A4, portrait" por "A4, paisagem (landscape)" e atualizar a nota
sobre número de colunas para citar as 6 colunas de cada módulo (incluindo a nova "Inquilino" de
`reconciliacao-pdf.ts`). No item "2. Header block", trocar o hex do título de `#18341c` para `#262626` e o hex
de borda da tabela de filtros de `#dbeed4` para `#d9d9d9`. No item "4. List", substituir a descrição de estilo
de tabela: a lista agora usa `theme:"plain"` com regra horizontal só embaixo de cada célula (`lineWidth`
bottom-only, `#d9d9d9`) e SEM borda vertical entre colunas, zebra branco/`#f2f2f2` já não é mais opcional (era
"optional but not required" antes — agora travada, D-04/D-05), e o cabeçalho da tabela ganha fundo `#f2f2f2`
com texto negrito (era fundo branco antes). Logo depois, acrescentar um parágrafo novo descrevendo a linha de
Total (nova, Phase 21, D-06): última linha da lista, negrito, mesmo fundo `#f2f2f2` do cabeçalho (via
`foot`/`footStyles` do `jspdf-autotable`), somando a coluna Valor de todas as linhas listadas — sempre
`showFoot:"lastPage"`, nunca o default da biblioteca que repetiria o total em cada página de um export com 2+
páginas; zero linhas mantém o estado vazio existente (sem tabela, sem Total). No item "6. Footer", trocar o hex
mudo de `#5c7060` para `#6b6b6b`. Ao final da seção (depois da tabela de tipografia existente, que não muda de
tamanhos/pesos), acrescentar uma tabela nova "8. Color palette" com as 5 linhas Foreground/Header+Total
fill/Border/Muted/Zebra e seus hex (`#262626`/`#f2f2f2`/`#d9d9d9`/`#6b6b6b`/`#f7f7f7`), fechando com a frase
"Nenhuma cor viva (verde/vermelho/azul saturado) aparece em nenhum dos dois PDFs (D-03)".
  </action>

  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && f=src/components/reports/reconciliacao-pdf.ts && grep -q 'orientation: "landscape"' "$f" && grep -q "headerFill: \[number, number, number\] = \[242, 242, 242\]" "$f" && grep -q "const totalValor = linhas.reduce" "$f" && grep -q '"Data", "Contrato", "Inquilino", "Tipo", "Valor", "Observação"' "$f" && grep -q "l.cards?.proprietario ?? \"\"" "$f" && grep -q "l.cards?.inquilino ?? \"\"" "$f" && ! grep -q "l.cards?.endereco" "$f" && grep -q 'theme: "plain"' "$f" && grep -q 'content: "Total", colSpan: 4' "$f" && grep -q 'content: formatCurrency(totalValor), styles: { halign: "right" }' "$f" && grep -q 'showFoot: "lastPage"' "$f" && grep -q 'lineWidth: { top: 0, right: 0, bottom: 0.75, left: 0 }' "$f" && grep -q "footStyles: { fontStyle: \"bold\", fillColor: headerFill, textColor: foreground }" "$f" && grep -q "columnStyles: { 4: { halign: \"right\" } }" "$f" && d="C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.planning/phases/10-relat-rio-financeiro-dedicado/10-UI-SPEC.md" && grep -q "paisagem" "$d" && grep -q "#262626" "$d" && grep -q "#f2f2f2" "$d" && grep -q "#d9d9d9" "$d" && grep -q "#6b6b6b" "$d" && grep -q "showFoot" "$d" && grep -q "reconciliacao-pdf.ts" "$d" && test -z "$(git status --porcelain -- ../supabase/migrations 2>/dev/null)" && npx tsc --noEmit && npm run lint && npm run build && echo TASK2_OK</automated>
    <human-check>
      <test>Baixar os dois PDFs — "Exportar PDF" em `/relatorios/financeiro` (Relatório Financeiro dedicado) e
      em `/relatorios/imobiliaria` (Dinheiro da imobiliária) — e abrir os dois arquivos. Confirmar em ambos: A4
      paisagem; paleta cinza inteira (texto quase preto, cabeçalho/Total com fundo cinza claro, bordas cinza,
      nenhuma cor viva — verde/vermelho/azul — em lugar nenhum); na lista, linha horizontal sutil entre as
      linhas sem nenhuma borda vertical, e zebra branco/cinza claro alternando; a última linha da lista é
      "Total", em negrito, mesmo fundo cinza do cabeçalho, com o valor batendo com a soma manual das linhas
      visíveis (considerando o filtro aplicado no momento da exportação). Se algum dos dois exports tiver
      linhas suficientes para gerar 2+ páginas, confirmar que "Total" aparece só na última página, não repetido
      em cada uma. No PDF de Dinheiro da imobiliária especificamente, confirmar a nova coluna "Inquilino" (logo
      depois de "Contrato") e que "Contrato" mostra o proprietário do contrato, não mais o endereço do imóvel.
      Os três blocos estruturais (título+timestamp, filtros aplicados, totais/resumo) devem continuar
      presentes nos dois PDFs, só recoloridos.</test>
      <expected>Os dois PDFs abrem em paisagem, com a paleta cinza nova; lista com linha sutil + zebra, sem
      grade vertical; linha "Total" em negrito somando corretamente, presente só na última página de um export
      multi-página; Dinheiro da imobiliária com as colunas Contrato(proprietário)/Inquilino corretas; os três
      blocos estruturais existentes continuam presentes nos dois.</expected>
      <why_human>A estrutura visual real do PDF (orientação, paleta, zebra, negrito, comportamento em múltiplas
      páginas) só é verificável abrindo os arquivos gerados de verdade — grep/tsc/build confirmam a forma do
      código-fonte, não o resultado visual renderizado pela biblioteca em tempo de execução.</why_human>
    </human-check>
  </verify>

  <acceptance_criteria>
    - `reconciliacao-pdf.ts` usa `orientation: "landscape"` e as mesmas 5 constantes de cor da Task 1
    - `head` tem 6 colunas (Data/Contrato/Inquilino/Tipo/Valor/Observação); célula "Contrato" lê
      `proprietario` (não mais `endereco`); nova célula "Inquilino" lê `l.cards?.inquilino`
    - `foot` tem `colSpan: 4` + célula de valor com `halign:"right"` direto no `styles` + célula vazia final (3
      células de `foot`, cobrindo as 6 colunas); `columnStyles` aponta para o índice 4 (Valor)
    - `showFoot: "lastPage"` explícito; `theme:"plain"` + `lineWidth` bottom-only na lista
    - `10-UI-SPEC.md` § "PDF Export Layout Contract" reflete paisagem, os 5 hex cinza, o novo estilo de lista
      (sem grade vertical, zebra travada) e a linha de Total — cobrindo os dois módulos
    - Zero arquivo novo em `supabase/migrations/`
    - `npx tsc --noEmit`, `npm run lint` e `npm run build` (de dentro de `web/`) não reportam erro novo
  </acceptance_criteria>

  <reversibility rating="reversible">Mesmo tipo de mudança da Task 1 (redesenho visual client-side) mais uma
  troca de coluna JSX aditiva sobre campos já buscados desde a Phase 19 (`proprietario`/`inquilino`) — nenhuma
  migração, nenhuma Server Action nova. A atualização de `10-UI-SPEC.md` é só documentação. Reverter é
  restaurar os dois arquivos tocados.</reversibility>

  <done>`reconciliacao-pdf.ts` exporta um PDF A4 paisagem, mesma paleta cinza e estilo de lista/Total da Task
  1, com 6 colunas (Contrato mostrando proprietário, nova coluna Inquilino); `10-UI-SPEC.md` documenta o
  contrato novo cobrindo os dois módulos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `linhas`/`categorias`/`totais` (já filtrados e autorizados, em memória no cliente) → `jspdf-autotable` (renderização client-side) | nenhum dado novo cruza nenhuma fronteira nesta fase — os dois módulos só recebem arrays que o componente chamador já buscou/filtrou/autorizou antes desta fase (RLS + Server Actions, inalterados) |
| `reconciliacao-pdf.ts`: `l.cards?.proprietario`/`l.cards?.inquilino` (já buscados desde a Phase 19) → célula "Contrato"/"Inquilino" renderizada | troca de qual campo já autorizado é lido — nenhum campo novo passa a ser buscado do servidor nesta fase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | soma do Total (`bodyRows.reduce`/`linhas.reduce`) | low | accept | aritmética client-side pura sobre dado já em memória, já autorizado e já renderizado na tela — nenhuma nova superfície de entrada, nenhuma escrita |
| T-21-02 | Information Disclosure | célula "Contrato" de `reconciliacao-pdf.ts` passa a mostrar proprietário em vez de endereço | low | accept | `proprietario` já é lido e renderizado por esta mesma sessão desde a Phase 20 (tela `dinheiro-imobiliaria-view.tsx`) e já aparece no bloco de filtros deste mesmo PDF — só muda qual campo já autorizado aparece nesta célula específica |
| T-21-03 | Tampering | constantes de cor/orientação hardcoded | low | accept | literais de compilação, nenhuma entrada de usuário alcança esses valores |
| T-21-SC | Tampering | instalação de pacote npm/pip/cargo | low | accept | zero pacote novo nesta fase — `jspdf`/`jspdf-autotable` já são dependências de produção auditadas desde a Phase 10; Package Legitimacy Audit não aplicável (confirmado em 21-RESEARCH.md) |
</threat_model>

<verification>
1. `grep`/`npx tsc --noEmit`/`npm run lint`/`npm run build` (ambas as tasks) confirmam a forma do código: as 5
   constantes de cor novas nos dois módulos, `theme:"plain"`+`lineWidth` bottom-only+zebra na lista, `foot`/
   `footStyles`/`showFoot:"lastPage"` explícito (nunca o default), `halign:"right"` direto na célula de valor
   do `foot` (nunca via `columnStyles`), e — só em `reconciliacao-pdf.ts` — o `head` de 6 colunas com
   Contrato(proprietário)/Inquilino nos índices corretos e `columnStyles`/`colSpan` deslocados. Também
   confirmam zero arquivo novo em `supabase/migrations/` e que `10-UI-SPEC.md` foi atualizado com os hex/termos
   corretos
2. `<human-check>` consolidado no fim da Task 2 cobre o que grep/tsc/build não provam sozinhos: o resultado
   visual real dos dois PDFs abertos (paisagem, paleta, zebra, negrito), o comportamento do Total numa exportação
   multi-página, e as novas colunas de `reconciliacao-pdf.ts` renderizadas
</verification>

<success_criteria>
- Os dois PDFs exportam em A4 paisagem, paleta cinza inteira (sem cor viva), com os três blocos estruturais
  existentes mantidos, só recoloridos (D-01/D-03/D-07, PDFMODELO-01/04)
- A lista principal dos dois PDFs usa linha horizontal sutil (sem grade vertical) + zebra (D-04, PDFMODELO-02)
- Uma linha "Total" nova, em negrito com fundo cinza igual ao cabeçalho, soma a coluna Valor das linhas
  listadas (já filtradas) e nunca se repete em páginas seguintes de um export multi-página (D-06, PDFMODELO-03)
- `reconciliacao-pdf.ts` ganha a coluna "Inquilino" e troca a célula "Contrato" de endereço para proprietário;
  `relatorio-financeiro-pdf.ts` mantém suas colunas inalteradas (D-02 corrigido, PDFMODELO-05)
- `10-UI-SPEC.md` § "PDF Export Layout Contract" documenta o contrato novo, cobrindo os dois módulos, sem ficar
  desatualizado em relação ao código
- Zero migração de banco, zero pacote novo, zero Server Action nova
</success_criteria>

<output>
Create `.planning/phases/21-redesenho-do-modelo-de-pdf-dos-relat-rios-financeiros/21-01-SUMMARY.md` when done
</output>
