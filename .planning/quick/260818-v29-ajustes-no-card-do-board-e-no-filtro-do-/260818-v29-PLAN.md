---
task: 260818-v29-ajustes-no-card-do-board-e-no-filtro-do-board
type: execute
autonomous: false
files_modified:
  - web/src/components/kanban/card-item.tsx
  - web/src/components/kanban/card-detail-dialog.tsx
  - web/src/components/financeiro/id-pill.tsx
  - web/src/components/search-field.tsx
  - web/src/components/kanban/board.tsx
  - web/src/components/kanban/column.tsx

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A pílula Ativo/Inativo não aparece mais na face sempre-visível do card do Board — só dentro do diálogo de detalhes (aberto ao clicar no card)"
    - "Dentro do diálogo de detalhes, o toggle Ativo/Inativo continua com escrita otimista imediata (onToggleAtivo direto), sem virar campo do formulário salvo pelo botão 'Salvar'"
    - "A pílula #N continua no canto inferior direito da face sempre-visível do card, porém com peso visual mais discreto que antes — não compete com o valor do aluguel"
    - "A mesma IdPill usada na tabela do Financeiro (parcelas-table.tsx) mantém a aparência atual — a variante discreta é exclusiva do card do Board"
    - "Confirmar a busca do Board com Enter rola a tela até o primeiro card que bate com o termo digitado, na ordem em que aparece nas colunas"
    - "Enter na busca sem termo digitado, ou sem nenhum resultado, não rola a tela nem lança erro"
    - "A busca dos Relatórios (reports-view.tsx) não muda de comportamento — SearchField ali não recebe a nova prop de Enter"
  artifacts:
    - web/src/components/kanban/card-item.tsx
    - web/src/components/kanban/card-detail-dialog.tsx
    - web/src/components/financeiro/id-pill.tsx
    - web/src/components/search-field.tsx
    - web/src/components/kanban/board.tsx
    - web/src/components/kanban/column.tsx
  key_links:
    - "card-item.tsx -> CardDetailDialog (onToggleAtivo threaded como prop nova) -> board.tsx handleToggleAtivo (escrita otimista já existente, inalterada)"
    - "SearchField (onSubmit novo, opcional) -> board.tsx handleSearchSubmit -> Map<id, HTMLDivElement> preenchido via registerRef em card-item.tsx/column.tsx -> scrollIntoView"
---

<objective>
Três ajustes pontuais no Board, pedidos pelo usuário após uso em produção: (1) mover a pílula Ativo/Inativo do card para dentro do diálogo de detalhes, tirando-a da face sempre-visível; (2) deixar a pílula #N (que continua na face sempre-visível) visualmente mais discreta, sem competir com o valor do aluguel; (3) fazer o Enter na busca do Board rolar a tela até o card correspondente, em vez de só destacar sem navegar.

Purpose: o card do Board hoje mostra informação demais na face sempre-visível (endereço, inquilino, valor, ID, status) — tirar o status daí deixa a leitura rápida mais limpa, e a pílula de ID mais discreta evita que ela dispute atenção com o valor. A busca com Enter fecha um gap de usabilidade: hoje ela só realça, sem levar o usuário até o card.
Output: card do Board com face sempre-visível mais enxuta (ID discreto, sem toggle) e diálogo de detalhes com o toggle; busca do Board navegando até o resultado ao confirmar com Enter.
</objective>

<execution_context>
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/victo/OneDrive/Desktop/kanban para aluguel/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@web/src/lib/kanban/types.ts
@web/src/lib/kanban/search.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mover o toggle Ativo/Inativo para o diálogo de detalhes; tornar a pílula #N discreta na face do card</name>
  <files>web/src/components/kanban/card-item.tsx, web/src/components/kanban/card-detail-dialog.tsx, web/src/components/financeiro/id-pill.tsx</files>
  <action>
Em web/src/components/financeiro/id-pill.tsx: adicionar uma prop opcional variant ("default" | "subtle", default "default"), sem tocar em numero nem no aria-label. Importar cn de @/lib/utils e trocar a string de classe fixa por cn(...), mantendo "inline-flex h-5 items-center rounded-full px-2 text-xs tabular-nums" para as duas variantes (sem min-w-, restrição já estabelecida na Phase 6.1). A variante "default" mantém exatamente as classes atuais (bg-secondary/25 text-secondary-foreground font-semibold) — parcelas-table.tsx não muda de aparência, porque esse call site não passa a prop nova. A variante "subtle" usa bg-muted text-muted-foreground font-medium — reaproveita os mesmos tokens já usados no estado "Inativo" do próprio toggle e no texto de endereço/inquilino do card, sem cor ou espaçamento novo.

Em web/src/components/kanban/card-item.tsx: remover o botão de toggle Ativo/Inativo (hoje dentro da div que agrupa pílula+toggle, com onPointerDown/onMouseDown/onClick fazendo stopPropagation e chamando onToggleAtivo com o valor invertido de card.ativo) — essa lógica migra inteira para card-detail-dialog.tsx. A linha inferior do card passa a ter só o valor do aluguel à esquerda e o IdPill com variant="subtle" à direita (a div que só existia para agrupar pílula+toggle pode ser removida, já que sobra um único elemento). Passar onToggleAtivo como prop nova para o CardDetailDialog, junto das props que ele já recebe (card, open, onOpenChange, onSave) — a prop onToggleAtivo que CardItem já recebe do componente pai não muda de assinatura, só passa a ser repassada também para o diálogo.

Em web/src/components/kanban/card-detail-dialog.tsx: adicionar onToggleAtivo (mesma assinatura de card-item.tsx) às props do componente. Renderizar o toggle logo abaixo do DialogHeader e antes do form de edição — fora do form, para não virar campo salvo junto com Proprietário/Endereço/etc pelo botão "Salvar" (o toggle preserva a escrita otimista imediata que já tem hoje, chamando onToggleAtivo direto no clique, igual ao padrão de handleToggleAtivo/persistOrRevert em board.tsx, que não muda neste plano). Reaproveitar o markup do botão removido de card-item.tsx (mesmas classes condicionais por card.ativo, mesmo texto "Ativo"/"Inativo", mesmo aria-label condicional usando card.endereco) dentro de uma linha com um rótulo curto tipo "Situação" ao lado — sem stopPropagation/onPointerDown/onMouseDown (esses existiam só para o toggle não abrir o próprio diálogo por bubbling dentro do card arrastável; dentro do diálogo isso não se aplica). O onClick chama onToggleAtivo com o id do card e o valor invertido de card.ativo, direto. Como card é prop (não estado local do diálogo), o toggle reflete card.ativo atualizado assim que board.tsx re-renderiza a lista após a escrita otimista — nenhum estado novo precisa ser criado no diálogo para isso.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && CI="web/src/components/kanban/card-item.tsx" && CD="web/src/components/kanban/card-detail-dialog.tsx" && IP="web/src/components/financeiro/id-pill.tsx" && ! grep -q '!card.ativo' "$CI" && grep -q 'variant="subtle"' "$CI" && grep -q 'onToggleAtivo' "$CD" && grep -q '!card.ativo' "$CD" && grep -q 'variant' "$IP" && echo TASK1_OK</automated>
  </verify>
  <done>npm run lint e npm run build passam; card-item.tsx não referencia mais !card.ativo (toggle removido) e usa variant="subtle" no IdPill; card-detail-dialog.tsx recebe onToggleAtivo e chama !card.ativo no próprio onClick do toggle; id-pill.tsx expõe a prop variant; parcelas-table.tsx segue sem alteração de aparência.</done>
</task>

<task type="auto">
  <name>Task 2: Enter na busca do Board navega até o card correspondente</name>
  <files>web/src/components/search-field.tsx, web/src/components/kanban/board.tsx, web/src/components/kanban/column.tsx, web/src/components/kanban/card-item.tsx</files>
  <action>
Em web/src/components/search-field.tsx: adicionar prop opcional onSubmit (função sem argumentos). No onKeyDown já existente (que hoje só trata Escape), adicionar um branch para event.key === "Enter": só chama event.preventDefault() e onSubmit() quando a prop onSubmit foi passada — se onSubmit for undefined (caso do uso em reports-view.tsx, que não vai passar essa prop), o branch não faz nada, preservando o comportamento atual da busca dos Relatórios sem nenhuma mudança.

Em web/src/components/kanban/card-item.tsx: adicionar prop opcional registerRef, do tipo (id: string, el: HTMLDivElement | null) => void. O ref do div raiz do card hoje é só setNodeRef (vindo de useSortable) — trocar por um callback combinado que chama setNodeRef(el) e, se registerRef foi passado, registerRef(card.id, el) também (useCallback com deps [setNodeRef, registerRef, card.id]). Isso dá a board.tsx uma forma de guardar o nó DOM de cada card sem interferir no drag-and-drop do dnd-kit, que continua dependendo de setNodeRef normalmente.

Em web/src/components/kanban/column.tsx: adicionar a mesma prop opcional registerRef às props de Column e repassar para cada CardItem renderizado no map de column.cards.

Em web/src/components/kanban/board.tsx: criar um React.useRef guardando um Map<string, HTMLDivElement> (persiste entre renders, não causa re-render) e uma função estável (React.useCallback, deps vazias) registerCardRef(id, el) que faz map.set(id, el) quando el existe e map.delete(id) quando el é null (cleanup do React ao desmontar um card). Passar registerRef={registerCardRef} para o Column dentro do SortableContext — mas NÃO passar essa prop para o CardItem renderizado dentro do DragOverlay (é uma cópia visual solta durante o arraste; registrar o ref dela sobrescreveria a referência do card real). Implementar handleSearchSubmit: se !searching ou matchedIds.size === 0, não faz nada (Enter com campo vazio combina com todo card via matchingIds, então sem essa guarda rolaria para o primeiro card do board mesmo sem busca ativa — bug a evitar); senão, pega o primeiro id de matchedIds (a ordem de iteração de um Set é a ordem de inserção, e matchingIds insere na mesma ordem em que columns/column.cards são percorridos e renderizados, ou seja, a mesma ordem visual) e chama scrollIntoView({ behavior: "smooth", block: "center" }) no elemento correspondente do Map, se existir. Passar onSubmit={handleSearchSubmit} para o SearchField já renderizado no topo do board.
  </action>
  <verify>
    <automated>cd "C:/Users/victo/OneDrive/Desktop/kanban para aluguel/web" && npm run lint && npm run build && cd .. && grep -q 'onSubmit' web/src/components/search-field.tsx && grep -q 'scrollIntoView' web/src/components/kanban/board.tsx && grep -q 'registerRef' web/src/components/kanban/column.tsx && grep -q 'registerRef' web/src/components/kanban/card-item.tsx && ! grep -q 'onSubmit' web/src/components/reports/reports-view.tsx && echo TASK2_OK</automated>
  </verify>
  <done>npm run lint e npm run build passam; SearchField aceita onSubmit opcional que só dispara com Enter; board.tsx implementa handleSearchSubmit com a guarda de busca vazia/zero resultados e rola até o primeiro match via scrollIntoView; reports-view.tsx não referencia onSubmit (comportamento inalterado).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Confirmar os três ajustes no navegador</name>
  <precondition>Tasks 1 e 2 commitadas; npm run lint e npm run build passam.</precondition>
  <what-built>
    (1) O toggle Ativo/Inativo saiu da face sempre-visível do card e passou a viver dentro do diálogo de detalhes. (2) A pílula #N continua na face do card, agora com peso visual mais discreto. (3) Confirmar a busca do Board com Enter rola a tela até o primeiro card correspondente.
  </what-built>
  <how-to-verify>
    1. Abra o Board. Olhe a face de qualquer card: confirme que não há mais o botão "Ativo"/"Inativo" ali — só endereço, proprietário, inquilino (se houver), valor e a pílula #N no canto inferior direito, visivelmente mais discreta que o valor (que continua sendo o elemento de maior destaque da linha).
    2. Clique no card para abrir o diálogo de detalhes: confirme que o toggle Ativo/Inativo aparece lá dentro, clique nele e confirme que o status muda na hora (sem precisar clicar em "Salvar"). Feche o diálogo e reabra: confirme que o novo status persistiu. Recarregue a página inteira e confirme de novo.
    3. Clique em "Salvar" no diálogo depois de editar algum campo (ex: telefone) sem mexer no toggle: confirme que o status Ativo/Inativo não muda sozinho por causa do salvamento do formulário.
    4. Na busca do Board, digite um termo que bate com exatamente um card e aperte Enter: confirme que a tela rola suavemente até aquele card, centralizando-o na tela.
    5. Digite um termo que bate com vários cards e aperte Enter: confirme que rola até o primeiro deles na ordem em que aparecem nas colunas (de cima para baixo, da coluna mais à esquerda para a mais à direita).
    6. Apague a busca (campo vazio) e aperte Enter: confirme que nada acontece (sem rolagem, sem erro no console). Digite um termo que não bate com nenhum card e aperte Enter: confirme que também não acontece nada além da mensagem "Nenhum imóvel encontrado." já existente.
    7. Confirme que arrastar cards entre colunas, excluir um card e a página de Relatórios (busca lá) continuam funcionando exatamente como antes.
  </how-to-verify>
  <resume-signal>Descreva o resultado dos 7 passos, ou aponte o que divergiu.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Card do Board -> diálogo de detalhes | Nenhuma fronteira nova: o toggle Ativo/Inativo só muda de local visual, continua chamando o mesmo `onToggleAtivo`/`setCardAtivo` (Server Action) já auditado nas Phases 5/6 |
| Campo de busca -> `scrollIntoView` | Puramente client-side, sobre nós DOM já renderizados a partir de dados que o board já carregou; nenhum dado novo cruza do cliente para o servidor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick29-01 | Tampering | Toggle Ativo/Inativo movido para dentro do form de edição por engano | medium | mitigate | Task 1 exige explicitamente que o toggle fique fora do `<form onSubmit={handleSubmit}>`, chamando `onToggleAtivo` direto no `onClick` — verificado no passo 3 do checkpoint humano |
| T-quick29-02 | Denial of Service | `scrollIntoView` chamado sobre `undefined` (card não registrado no Map, ex: fora da tela por virtualização futura ou id inexistente) | low | mitigate | Chamada sempre com optional chaining (`?.scrollIntoView`); ausência no Map é um no-op, não uma exceção |
| T-quick29-03 | Information Disclosure | Nenhuma — nenhum dado novo é exposto; ambas as mudanças reorganizam informação que já estava visível/acessível ao usuário autenticado | low | accept | N/A |

**Supply chain:** Nenhum pacote novo instalado neste plano.
</threat_model>

<verification>
1. npm run lint && npm run build passam após cada task
2. Toggle Ativo/Inativo não aparece mais na face sempre-visível do card; aparece no diálogo de detalhes com escrita otimista intacta
3. Pílula #N do card do Board usa variant="subtle"; a da tabela do Financeiro não muda
4. Enter na busca do Board rola até o primeiro card correspondente; campo vazio ou zero resultados não fazem nada
5. Busca dos Relatórios inalterada
6. Checkpoint humano (Task 3) confirma os 7 passos em produção
</verification>

<success_criteria>
- Face sempre-visível do card do Board mostra só endereço/proprietário/inquilino/valor/pílula #N discreta
- Diálogo de detalhes do card ganha o toggle Ativo/Inativo, com o mesmo comportamento de escrita otimista de antes
- Confirmar a busca do Board com Enter direciona o usuário até o card correspondente
</success_criteria>

<output>
Create `.planning/quick/260818-v29-ajustes-no-card-do-board-e-no-filtro-do-/260818-v29-SUMMARY.md` when done
</output>
